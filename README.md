# ThreeNative

ThreeNative is a small, vanilla-Three.js-first game framework for WebGPU games. The
framework owns bootstrap, lifecycle, input, physics bindings, UI state, and playtest
hooks. Generated `src/render/` code owns the look.

## Benchmark status

**VOID — the apparatus is ready, but no authoritative provider usage event or blind
human score was available for the required three-repeat experiment.** The dated result
file [records the void](docs/benchmark/RESULTS-2026-08-02.md) without inventing quality
or cost data.

## Static comparison

The table below is generated by `pnpm tsx scripts/count-loc.ts`. CI runs the same
classifier in `--check` mode, so hand-edited numbers fail the benchmark job.

<!-- benchmark:loc:start -->
**LOC measurement:** Raw LOC is the physical source count. Normalised LOC is counted after both arms are formatted in memory with this repository's Biome configuration. Framework plumbing is **51.4%** of the control's — the plumbing column is the addressable surface named in `CHARTER.md` §3, because the game column is the model's work in both arms. The total ratio is **93.0%**. This table is a **regression ratchet** against frozen hand-written source, not the win condition; the win condition is `pnpm sweep:pair`, agent against agent. See `docs/benchmark/PROTOCOL.md`.

| Arm | File | Raw LOC | Normalised LOC | Plumbing LOC | Game LOC | Vanilla wins? |
|---|---|---:|---:|---:|---:|---|
| Vanilla | `examples/abyss-vanilla/src/main.js` | 410 | 473 | 138 | 335 | — |
| ThreeNative | `examples/abyss-framework/src/main.tsx` | 69 | 69 | 13 | 56 | — |
| ThreeNative | `examples/abyss-framework/src/render/lighting.ts` | 10 | 10 | 2 | 8 | — |
| ThreeNative | `examples/abyss-framework/src/render/postprocessing.ts` | 9 | 9 | 4 | 5 | — |
| ThreeNative | `examples/abyss-framework/src/scenes/Abyss.ts` | 352 | 352 | 52 | 300 | — |
| **Vanilla** | **control total** | **410** | **473** | **138** | **335** | — |
| **ThreeNative** | **framework total** | **440** | **440** | **71** | **369** | — |

**Static result:** framework wins. Vanilla wins are shown per summary row; the framework example remains the independently playable proof scene until the manual Abyss parity run is completed.
<!-- benchmark:loc:end -->

## Local commands

```sh
pnpm install --frozen-lockfile
pnpm dev
pnpm test
pnpm test:browser
pnpm tsx scripts/arm-census.ts
```

The census command writes dated, four-class arm evidence to
`docs/verification/arm-census-<date>.md` and reconciles every class with the normalized LOC
reported by `scripts/count-loc.ts`.

The runnable examples are `examples/abyss-vanilla/` (frozen control) and
`examples/abyss-framework/` (framework Abyss port and physics proof scene).

## Docs

[`CHARTER.md`](docs/architecture/CHARTER.md) is the only binding document, and
[`docs/README.md`](docs/README.md) maps everything else — PRDs, verification, strategy,
architecture, product — labelling which parts are proposals. Start with
[`docs/strategy/CONFLICTS.md`](docs/strategy/CONFLICTS.md): the eight places where the
strategy contradicts `CHARTER.md`, and what to do about each.
