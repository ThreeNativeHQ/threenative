{
  "schemaVersion": 1,
  "name": "native-smoke-multitouch-one-pointer-negative",
  "target": "web",
  "subject": "multitouch-player",
  "viewport": { "width": 640, "height": 360 },
  "warmupFrames": 2,
  "steps": [
    {
      "label": "airborne-two-pointers",
      "pointers": [{ "id": 7, "x": 0.2, "y": 0.8 }],
      "holdFrames": 10,
      "release": true
    }
  ],
  "artifacts": { "screenshots": false },
  "assert": {
    "diagnostics": {
      "noRuntimeDiagnostics": false,
      "runtimeDiagnosticsOptOutReason": "Headless Linux Chromium emits the known WebGPU popErrorScope diagnostic; the negative control is assertion-driven."
    },
    "resources": [
      { "id": "GameState", "path": "maxPointers", "gte": 2 },
      { "id": "GameState", "path": "movedWithTwoPointers", "equals": true },
      { "id": "GameState", "path": "leftGroundWithTwoPointers", "equals": true }
    ]
  }
}
