{
  "schemaVersion": 1,
  "name": "native-physics-collision-mask",
  "target": "web",
  "subject": "dynamicBox",
  "viewport": { "width": 640, "height": 360 },
  "warmupFrames": 0,
  "steps": [{ "waitFrames": 180, "label": "fell-through" }],
  "artifacts": { "screenshots": false },
  "assert": {
    "movement": { "entity": "dynamicBox", "minDistance": 5 },
    "resources": [
      {
        "id": "GameState",
        "path": "parity.collisionEventSet",
        "equals": [],
        "allowTrivial": "The collision mask intentionally excludes this pair, so an empty collision-event set is the held result this mask scenario verifies."
      },
      {
        "id": "GameState",
        "path": "parity.control",
        "equals": "masked",
        "changed": false,
        "allowTrivial": "The mask is configured before the run and never changes; this row verifies the native and browser mask label remains the declared value."
      }
    ]
  }
}
