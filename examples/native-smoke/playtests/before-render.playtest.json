{
  "schemaVersion": 1,
  "name": "before-render-once-per-world-draw",
  "viewport": {
    "width": 640,
    "height": 360
  },
  "warmupFrames": 0,
  "steps": [
    {
      "waitTicks": 120,
      "screenshot": "prepared-frame"
    }
  ],
  "assert": {
    "diagnostics": {
      "runtimeReady": true,
      "noConsoleErrors": true,
      "noRuntimeDiagnostics": true
    },
    "resources": [
      {
        "id": "state",
        "path": "preparedFrames",
        "changed": true,
        "gte": 2,
        "allowTrivial": "Startup world draws run before the scenario samples, so changed:true is the progress proof; the minimum only has to hold once warmup has dispatched, and may already hold."
      },
      {
        "id": "state",
        "path": "preparationErrors",
        "equals": 0,
        "allowTrivial": "Every actual world draw checks exactly one preparation and the last fixed-update tick; zero accumulated errors is the invariant."
      }
    ]
  },
  "artifacts": {
    "console": true,
    "screenshots": true
  }
}
