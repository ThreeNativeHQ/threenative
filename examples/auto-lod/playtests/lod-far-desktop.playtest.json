{
  "name": "lod-far-desktop",
  "schemaVersion": 1,
  "subject": "the native host's far camera submits at most half the model's authored triangles",
  "target": "desktop",
  "viewport": {
    "width": 1280,
    "height": 720
  },
  "warmupFrames": 90,
  "artifacts": {
    "screenshots": false,
    "console": true
  },
  "steps": [
    {
      "kind": "wait",
      "label": "settled",
      "waitTicks": 120,
      "release": true
    }
  ],
  "assert": {
    "resources": [
      {
        "allowTrivial": "The scene starts on the far route and holds it, so the invariant is satisfied before the run; the matching near scenario drives the value from 368 to 8192 to prove the selection follows the camera.",
        "id": "GameState",
        "lte": 4096,
        "path": "triangles"
      }
    ],
    "sceneNodes": [
      {
        "select": {
          "nameContains": "hull-mesh"
        },
        "minCount": 1,
        "visible": true
      }
    ]
  }
}
