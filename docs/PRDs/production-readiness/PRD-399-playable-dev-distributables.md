# PRD-399 — Playable dev distributables with responsive React UI

**Status:** IN PROGRESS
**Complexity:** 9 → HIGH; risk override: release integrity and cross-platform execution.
**Owner:** Codex, thread 01a0c7c0-0940-75b1-80f4-7da03413d298
**Depends on:** Existing packaging and playtest mechanisms from PRD-365/366; UI work from PRD-393/398 is evaluated before reuse.

## Context

The requested outcome is a smooth development release flow: simple commands produce conventional, playable distributables on every supported platform. React WebUI must ship whenever selected, be the default, and visibly track gameplay in real time on native. Building a binary or asserting a bridge message alone does not prove these outcomes.

The initial audit found Windows release output is a ZIP rather than an installer, the config resolver defaults UI to `native`, macOS signing writes its integrity manifest after sealing the bundle, and main's latest native-release run (35586254701) fails in `Require the triggering CI completion` before building any platform. Historical successes do not qualify this candidate.

Task checkout: `/home/joao/projects/threenative/threenative-engine/.worktrees/dev-release`, branch `fix/dev-release`, base `215de303dd2ef919519c355c67db5994da5f412e` (`origin/develop`). Owner above; cleanup pending delivery. The primary checkout has unrelated active changes and is preserved.

Draft [PR #279](https://github.com/ThreeNativeHQ/threenative/pull/279) targets develop. The release run above was triggered by successful **scheduled** CI (35581076683), while its gate only accepts main **push** CI; it is not evidence of a broken platform compile. Host discovery: Linux browser tooling and Android API 35/16 KB AVDs are installed, no Android device is currently attached, no self-hosted repository runners were returned, and no signing-related repository secrets were listed. iOS, macOS and Windows require their native hosts. No credential values were read.

## Solution

Fix the shared engine/config/packaging mechanisms and reuse the existing `threenative build`, playtest runner, consumer qualification and release workflows. No new CLI command or distribution framework. Final proof must follow a single frozen candidate through package hashes, installation/extraction and real gameplay, including the React HUD, pointer input, restart, assets and offline native launch. Native UI proof measures state-to-visible-update latency and update cadence during continuous changes, rather than treating "overlay attached" as responsive UI.

Use local browser/Linux/Android lanes first and native Windows/macOS/iOS CI hosts for host-specific checks. Simulator evidence is labelled as such; iOS acceptance is the final simulator app running its gameplay and React UI scenario on hosted macOS CI. Call it a simulator build; do not claim physical-device installation or Developer ID/App Store trust without separate evidence. Publishing a public package or release is a separate final approval after a concrete candidate is qualified.

## Integration Ledger

| Capability | Consumer path | Reuse / disposition | Evidence |
| --- | --- | --- | --- |
| Default React WebUI | generated project → config resolver → UI bundle → target packager → native WebView | existing UiLayer and platform overlays | Phase 1 |
| Responsive native UI | gameplay frame → existing UI state bridge → React commit → platform presentation | inspect existing per-frame and snapshot work before changing | Phase 2 |
| Installable desktop game | `threenative build --target desktop --mode release` → desktop packager → installed app | extend existing container packaging | Phase 3 |
| Final-build verification | target build → package identity → install/extract → existing playtest | existing cross-target consumer scenario | Phase 4 |
| Repeatable dev release | documented build commands / CI completion → release qualification | existing release workflows, exact-candidate checks | Phase 5 |

## Execution Phases

### Phase 1 — Default UI reaches every build

**Status:** PARTIAL
**Files:** config resolver, scaffold configs, build/packager tests and affected docs.

- [x] [local; agent] Omitted UI selection resolves to React WebUI; explicit native remains supported, including the intentionally UI-free minimal template. Regression covers actual config loading. Evidence: two config-loader failures (`native` instead of `web`) before correction; config/scaffold/build plus core regressions now **240 passed**. Scaffold hashes regenerated from actual `createProject` output after the shared UI guidance and minimal override changed.
- [ ] [local/shared; agent and native CI] Every selected WebUI build carries HTML, JavaScript, CSS and assets into its final package; a missing bundle fails with an actionable error. Partial: the no-config CLI regression builds real UI output and checks nonempty HTML-linked JavaScript/CSS plus `--ui` forwarding for desktop, Android and iOS. All 43 consumer/template tests pass; independent review passes. The packagers in this regression are stubs, so final-platform inclusion remains open. A later iOS audit found two real integration defects: the low-level config normalizer discarded `ui`, and the application never called its existing WebView attach function. Packaging and startup regressions reproduce both. The fix retains and validates the renderer, writes `TNUIRenderer`, attaches to SDL's UIKit view before game evaluation, and fails if selected UI is missing. Asset resolution now confines decoded paths and symlinks to the UI directory and rejects foreign origins. The iOS playtest captures the launched PID rather than the app folder name and retains ordinary script errors. Packaging/startup and console regressions fail before the fixes and pass afterward; 77 focused native tests pass with one Apple-only Foundation check skipped on Linux, and all 11 iOS driver/device-runner tests pass. The workflow builds a starter React consumer from installed tarballs and joins a decoded full-simulator screenshot state ID to the app's actual publication log. Its scenario passes against a sealed Linux build; its pixel-check block passes against real Linux compositor output and rejects a missing publication. The native desktop GPU screenshot excludes the separate overlay, so it is not used as React pixel evidence. The shared fixture uses a shaded sphere to satisfy the existing nonblank-capture guard; the rebuilt release still measures 59.95 Hz and p95 27 ms with 25/26 ms idle wakes (`ui-cadence-ios-fixture-final/`). Independent review found and fixed Objective-C retained-object leaks. Apple-host compilation/execution remains pending; one screenshot will establish visible state, not touch or cadence.
- [ ] [local; agent] Generated project commands and help match the supported build paths without undocumented setup or silent target downgrades. Partial: all ten templates now route `pnpm test` through `pnpm build:web` and Vite's production preview. Ten command regressions failed before the change; all 109 scaffold/playtest-contract tests pass afterward, with hashes recomputed from actual scaffolds. The installed starter's exact new `pnpm test` command exits 0: 24 production scenarios and 130 assertions pass. The web CLI also now launches the installed Vite JavaScript entry through Node, avoiding Windows `.cmd` execution without a shell. Its regression uses no shell shim and preserves an output path containing spaces and `&`; it fails before the change and passes afterward. All 62 build/consumer/template tests pass. Actual Windows execution and remaining target commands are pending.

### Phase 2 — Native React UI visibly tracks live gameplay

**Status:** PARTIAL
**Files:** existing UI bridge/state and native overlay implementations; existing UI verification scenarios.

- [x] [local; agent] Continuous game state reaches React on each rendered game frame without a fixed 100 ms publishing throttle; idle UI does not accumulate a message backlog. Regression tests exercise the real owner path. Evidence: `game.spec.ts` drives `defineGame`, actual `Scene.render` writes and `subscribeUiState`; default delivery red, explicit interval green, then both green. Independent review found and fixed a one-frame delay for render-hook writes; flush now follows those hooks. The final sustained test drives 64 callbacks, requires at least eight actual world renders, and passes both default and explicit 100 ms cases. The native read-only probe exposed another owner-path defect: without an authored `game.ui` access, no bridge receiver existed. Startup now connects automatically and failed startup closes the channel; both host and in-process rejection regressions fail before and pass after cleanup. The owner publishes its already-coalesced snapshot synchronously, avoiding an extra host microtask boundary. All 65 game/state/bridge tests pass. The packaged Linux React probe records approximately 60 receipts and commits per second, p95 render-state-to-receipt and render-state-to-React-commit of 1 ms, and zero state messages during two-second idle periods. This closes state delivery, not visible presentation.
- [x] [local; agent] Linux final build proves visible UI animation/state changes and pointer input, with measured presentation cadence and state-to-paint latency; no attachment-only substitute. The old GTK window path delivered React commits in about 1 ms but intermittently presented only 30 updates/s with visible p95 up to 92 ms. A second GTK pump did not reliably fix that and was reverted. Reused the existing offscreen WebKit worker/latest-frame mailbox/compositor from `09bab3b29`, resize from `feee5699c`, and cadence source at `0ca55a30f`, without unrelated decoder changes or ordinal-based latency claims. The port's calibrated 240 FPS OS capture matches exact React state IDs to their game-render publication timestamps: 1373 visible states, zero invalid captures, p50 27 ms, p95 28 ms, max 40 ms, idle wake 27/28 ms (`ui-sequence-native-1790068326380/`). Capture delay is included, not subtracted. The repeat after all input fixes passes with 1379 visible states, zero invalid captures, p50 25 ms, p95 28 ms, max 42 ms and idle wake 25/26 ms (`ui-sequence-native-1790069547177/`). The rebuilt, sealed starter relocated into `native UI game/consumer` passes pause/resume/movement over 402 frames with zero diagnostics (`linux-native-ui-final/`). Review-found blockers are fixed: guarded non-Linux ABI, bounded startup cancellation, completed/cancelled pointer gestures and native GDK text editing. `pnpm test:ui-native` runs a real WebKit control fixture covering click focus, shifted text, French layout, repeats, Backspace/Delete, preventDefault and focus release; it passes. Real OS clicks/typing into a packaged React input also pass, including gameplay W held across focus transfer and clicking back to the game (`/tmp/threenative-prd399-ui-os-final.log`). Compiler regressions cover pointer ownership and cancelled key ownership through repeat/release; all 23 focused host/build/packaging checks pass. Independent read-only review passes. Full `pnpm test`, `pnpm typecheck`, `pnpm lint` and document checks pass after the port. The permanent `pnpm native:verify:ui --project <installed game> --runtime <native host>` command now builds the React fixture through the installed release CLI, relocates and integrity-checks the archive, captures exact visible IDs and retains its archive, screenshot, raw observations and executable/runtime hashes. The first run passes at p95 27 ms, 60.05 visible updates/s and two 26 ms idle wakes; the repeat after artifact-safety and cleanup fixes passes at p95 27 ms, exactly 60 visible updates/s, max 42 ms, zero dropped/invalid/stale observations and idle wakes 26/27 ms (`artifacts/prd399/ui-cadence-command-3/`). Actual capture is 240.73 Hz on Linux/X11 with NVIDIA RTX 2080; this is not a physical monitor refresh measurement. Five analyzer/CLI tests pass, including a sustained 30 Hz false-positive control and a regression that refuses reuse of prior passing artifacts. The archive remains and the owned temporary project is removed after success. Independent read-only review passes. This capture backend currently qualifies Linux only. This remains checkpoint evidence, not the final immutable cross-platform cohort.
- [ ] [shared; native Windows/macOS/Android/iOS hosts] The same UI verification runs per platform and records cadence, latency, dropped/stale updates and screenshots. At a stable 60 FPS workload, target p95 state-to-visible-update latency is at most 50 ms with no sustained 10/15/30 Hz UI cap; report actual device/refresh rate and failures.

Android diagnostic checkpoint: the same React fixture builds as a signed release APK through the installed CLI, selects WebUI without an explicit UI setting, and passes all eight native-library 16 KB checks. APK SHA-256: `d27cbb907281748930c541a42a4404f4c6b4f1bcac9c26c6cc8dea92021a3730`. On API 36 / 16 KB x86_64 emulator 37.1.11, the existing playtest helpers present 640×360 at density 160; Android reports 60 Hz and Vulkan names NVIDIA RTX 2080. The captured pixels are joined to actual frame presentation timestamps from [AOSP Winscope v2 metadata](https://android.googlesource.com/platform/frameworks/av/+/b675cea85b508d7f60f1344e5a24c3db7a5b8d0f/cmds/screenrecord/screenrecord.cpp), including its realtime offset. Frame counts and normalized decoded PTS must match metadata within 1 ms; observed maximum error is 0.145 ms. The repeat (`artifacts/prd399/android-ui-cadence-3/`) measures p95 46.43 ms, 58.42 observed UI IDs/s (minimum 56 per active second), source 59.84 Hz, idle wakes 40.52/40.69 ms and zero invalid/stale/unmatched IDs. The first recording remains a failed cadence observation: one second captured 48/59 IDs, despite p95 49.12 ms. Jittery 60 Hz recording cannot attribute every missed ID to UI loss. A real release APK deliberately limited to 30 visible UI updates/s is rejected by the same capture path: source 59.89 Hz, observed 29.95 Hz, `VISIBLE_RATE` failure (`android-ui-cadence-30hz/`). Independent review confirms timestamp attribution for captured composition; this is SurfaceFlinger virtual-display evidence, not physical scanout. The permanent Android command is implemented below; reliable Android latency, repeated final-cohort proof and other platform cadence remain open.


Android command checkpoint: `pnpm native:verify:ui --target android --project <installed game> --device <serial>` now builds through the installed release CLI and captures the signed, installed APK; `--apk <packaged cadence fixture>` repeats that exact artifact. The verifier checks the installed APK SHA-256, decoded-frame/metadata alignment, exact state IDs, sustained visible cadence and both idle wake-ups. A different signed game APK is refused even when the cadence fixture is already installed. Seven analyzer/command regressions pass, including malformed timestamps, 30 Hz presentation and stale artifact refusal. The actual build path exposed system bars clipping the barcode when the desktop fixture's windowed setting was reused; Android now requests fullscreen while desktop remains windowed. Real Android recording cancellation returns nonzero, leaves no passing result, restores 1080×2400/density 420 and stops its fixture/recorder. Review found a late Linux cancellation gap after the last capture check; a post-cleanup failure guard fixes it, and a real signal during final host shutdown is rejected. Normal Linux reuse still passes at p95 29 ms, source/visible 60 Hz and idle wakes 25/26 ms (`ui-cadence-android-refactor/`). The permanent command also rejects the real 30 Hz APK (p95 53.59 ms); the low-latency synthetic control independently verifies sustained-rate refusal. Independent final source review passes.

**Android latency is not qualified.** The permanent baseline run fails at p95 54.74 ms (`android-ui-command-1/`). A diagnostic release APK logs React layout commits for every tenth state: publication-to-commit p95 is 2 ms over 136 matched IDs, while commit-to-captured-composition p95 is 47.60 ms (`android-ui-commit-trace/`). Removing the forced WebView texture layer passes two captures at 43.68/45.99 ms but the full project-build run still fails at 50.50 ms; adding immediate invalidation worsens p95 to 65.28 ms. Both experimental host edits are reverted. The 50 ms bound is unchanged and every failed run retains its raw observations without a passing result. A six-second Perfetto diagnostic records SurfaceFlinger timelines and React log timestamps, but kernel ftrace parsing reports `FTRACE_STATUS_PARTIAL_PAGE_READ`; inspecting its unaffected timeline records is diagnostic only, not complete pipeline evidence. A subsequent trace omits the faulty kernel source and parses normally: WebView activation follows the first draw, so the changed tree waits for the next draw. Android application frame completion is not display presentation; joining its display token to the SurfaceFlinger display frame puts one sampled commit at approximately 52 ms, so no assumed recording delay is subtracted. Chromium WebView 133's named presentation callback is draw feedback, not a physical presentation fence.

A paired diagnostic draws the identical source state ID into one native CanvasLayer draw and the React overlay (`android-ui-paired-1/`). All 1617 pixel pairs decode; during 1318 active frames, React trails by one ID in 923 frames and two IDs in 225. Native absolute p95 is 31.86 ms; React is 54.31 ms. For 1250 IDs observed in both outputs, the first-visible difference is p95 33.74 ms; this intersection excludes unmatched observations. Source-age maxima across the intentional two-second idle are not rendering latency. A real signed control displays the previous state in React: lag increases from one/two IDs to two/three at median/p95, native remains 31.50 ms and React fails at 67.66 ms (`android-ui-paired-delay/`). This establishes visible UI/world lag and validates its detection; it does not qualify physical scanout or establish a production fix. The paired A/B with Chromium tracing rejects that proposed mechanism: forced-layer versus default-layer activation-to-submission medians are 15.93/16.07 ms (31 sampled commits each), UI lag remains one/two IDs at median/p95, and absolute UI p95 remains failing at 53.53/52.83 ms (`android-ui-paired-hardware-trace/`, `android-ui-paired-default-trace/`). Removing the layer does not remove the downstream frame wait. These APKs enable debugging solely for traces; production Java and external fixture sources are restored. No latency threshold, game presentation timing or host implementation is changed by this diagnostic checkpoint.

Android frame-phase diagnostic: 31 exact visible IDs from the default-layer trace correlate commit-to-next-Blink-frame delay with source-to-visible latency (r=0.903, slope 0.995 ms/ms); 30 IDs from the hardware-layer trace give r=0.876. A temporary 60 Hz native pacer waits for Android Choreographer while preserving the React fixture and 50 ms bound. Its exact signed APK (`61fc0de7925471430867e1a4038f12410e51b32f25c8e330a35a8fea2b9eabf0`) passes three captures at p95 48.85/49.11/48.77 ms, source 59.68–59.89 Hz, observed UI 55.32–56.32 Hz and no pacing timeouts (`android-ui-vsync-1/`, `-2/`, `-3/`). The retained older baseline passes one capture at 42.57 ms; rebuilding the unchanged baseline from the same current inputs fails at 51.53 ms (`android-ui-vsync-current-baseline/`). This supports further phase-pacing work but does not qualify a production implementation: the probe only handles 60 Hz, the margin is narrow, and refresh-rate/lifecycle/native-world checks are unrun. Both temporary native/Java edits are restored; the diagnostic patch and APK are retained, and the failed baseline has no passing result.

2026-09-23 real Pixel 8 latency (Phase 2 remains open): first measurements on a REAL Pixel 8 (shiba, Android, Wi-Fi adb); every earlier Android number was an emulator. Production APK, 60 FPS fixture. Phone forced to 60 Hz display: p95 55.35 ms (n=1304), split pub→commit p95 5 ms / commit→visible p95 53.7 ms; Perfetto: WebView activation misses the current functor sync (+1 frame) and the app VRI layer runs QueuedBuffer 2–3 (SF presents ~55 Hz against a 60 Hz app). Four host experiments, all reverted: no forced WebView HW layer p95 55.0/55.2/56.0; deliver in next doFrame ~72 (+1 frame); latest-wins coalescing gated on postVisualStateCallback 55.9/56.0/55.6; coalesced flush inside the current doFrame 55.8/55.5/55.9. Phone at its DEFAULT 120 Hz display (the 60 FPS workload unchanged), with the capture band widened to 55–125 Hz: 3/3 PASS — p50/p95/max 38.58/41.17/57.07, 38.58/41.17/54.00, 38.66/41.73/57.05 ms; capture 88.9/88.2/88.8 Hz, interval p95 18.4/18.9/18.0 ms. Conditions, stated plainly: phone unplugged, battery 30→22 %, thermal status 0 (below device-preflight.mjs's 50 % physical-qualification floor, which this harness does not call). Artifacts retained locally, not committed: `/home/joao/projects/threenative/threenative-engine/.worktrees/prd399-latency/artifacts/prd399-latency-screening/{band125-cap1,band125-cap2,band125-cap3,e1-*,e2-*,e3-*,e4-*,perfetto-baseline,analysis}`. Verdict: on a 120 Hz panel the latency bound is met in screening only -- the run sits below the 50 % physical-qualification floor, so it is not qualification evidence and a charged, preflighted rerun is still owed; a 60 Hz-locked panel misses by ~5 ms and stays open.

### Phase 3 — Conventional, integrity-checked desktop distributions

**Status:** IN PROGRESS
**Files:** existing desktop container/signing adapters, shipped build command and platform tests.

macOS signing decision, independently reviewed: sign staged dependencies first, hash their final bytes and the resources, write the internal manifest, then sign the outer application once. The main executable uses an explicit Apple-signature integrity record because its signature seals the manifest; a whole-file hash inside that manifest creates a cycle. Resolution must verify the actual application signature and reject signature records on other payloads/platforms. CI ad-hoc signing, relocation and tamper checks remain required.

- [ ] [local/shared; agent and Windows CI] Windows release creates a standard installer with game identity, shortcuts, uninstall support and complete runtime/UI dependencies; install, launch and uninstall execute on Windows. Partial: the missing-installer regression fails before the change and passes afterward. The existing release command now prepares a portable ZIP and NSIS setup before publishing either, with rollback preserving both previous outputs on failure. Setup uses per-user identity, explicit owned-file deletion, registered-location protection and WebView2 detection/bootstrapper verification when UI is selected. Requested signing covers the game, embedded uninstaller and setup; a failed uninstaller signing command rejects compilation. NSIS 3.11 / Wine 11.17 executes the permanent opt-in lifecycle test successfully: literal dollar/define-like names, occupied-directory refusal, empty-directory install, competing-location refusal, upgrade removal of obsolete files, unrelated-file preservation, uninstall and registry cleanup. A real signing helper writes its invocation marker and exits 17; the compiler refuses the build. All four installer tests pass in this local integration run. Focused packaging tests and all 125 CI structure/dependency checks pass. `pnpm native:verify:windows:installer --installer <setup.exe> --project <game> --scenario <scenario>` installs the exact final setup, resolves its payload, drives the existing playtest and uninstalls it; `--require-signed` requires and verifies all three signatures. The Windows workflow now provisions NSIS, builds with its disposable CI certificate and runs this command against the real React pause/resume/movement scenario. Actual Windows locked-file behavior, Microsoft bootstrapper execution, signatures and installed gameplay are pending hosted execution; Wine mechanics cannot close those claims. Full local `pnpm test` passes (1333 native tests, 58 skipped; 29 ABI tests; 5499 workspace tests, eight skipped), along with typecheck, lint (776 warnings), docs and budgets. The packed runtime contains the exact compiler, template and verifier; its verifier reaches the explicit Windows-host guard when invoked on Linux. Independent review found and fixed both uninstaller-signing exit handling and competing-install identity; signed CI qualification now refuses an unsigned manifest. The implementation reuses the release packager, NSIS and the playtest runner; its new code covers installer ownership, WebView prerequisites, signing and their executable checks. Hosted Windows run 35724841899 / job 106736668476 builds the runtime and passes starter HUD/input, but its installer lifecycle suite fails to import before running any test. The exact failure reproduces locally with Windows CRLF source: Vite SSR leaves an obsolete shebang after injected imports in the library-only `desktop-distribution.mjs`. Removing that shebang fixes the LF/CRLF transform-and-parse regression; all four non-integration installer tests pass. That syntax correction is now confirmed on Windows: job 106755648219 passes all five installer tests, including actual locked-file refusal. Setup/game/uninstaller signatures verify and installed gameplay passes, as recorded in Phase 4. The final verifier exposes a stricter-than-contract empty-directory check after owned-file removal; its correction is recorded there. Microsoft bootstrapper execution on a host without WebView2 remains unrun.
- [ ] [shared; macOS CI] macOS produces a relocatable `.app` distribution, validates bundle identity/resources and signing order, launches after extraction and carries the React HUD. Report Developer ID trust and notarization as unverified unless CI has the credentials to test them. Partial: the signing-order and required JIT-entitlement regressions fail before correction. Nested code is now signed and hashed before the manifest, then the outer app is signed without recursively rewriting dependencies. The executable has an explicit signature record; resolution checks the sealed manifest location, inventory, hashes and strict app signature, and refuses unsupported records or unavailable verification. Final payloads are rechecked after stapling. All 121 packaging tests pass, with the real macOS test skipped on Linux; independent review passes. The macOS workflow now runs real signed extraction/tampering and MAP_JIT checks, then packages its playable React starter with ad-hoc signing. CI merge candidate `aafb2214f84f8efa2faf3e2191e583e0f9ebdcca` (parents `f5f50c3f8` and `215de303d`) passes the macOS job (run 35705022714, job 106671902830). Its artifact manifest is signed darwin-arm64. Ten finalization tests pass, including actual MAP_JIT allocation and executable/dependency/UI/manifest tamper refusals. The relocated signed starter renders 300 frames with 20,802 colors and expected asset pixels, and its overlay attaches. Interactive pause/resume/movement passed on the pre-release executable, not yet on that relocated signed app. macOS x64 remains unrun; ad-hoc signing does not prove Developer ID trust/notarization.
- [ ] [local/shared; agent and architecture hosts] Linux produces a relocatable archive/application with launch metadata, complete required payload and explicit system prerequisites; extraction and launch execute on each advertised architecture. Partial: the native CI `starter-linux` job is now a matrix with a `linux-arm64` leg on GitHub's hosted `ubuntu-24.04-arm` runner that builds, packs, scaffolds, relocates and launches the release archive; the leg is unrun as of this commit. Linux arm64 has no V8 or Dawn prebuilt (kuoruan/libv8 ships Linux x64 only; Dawn's `ubuntu-latest` asset is x64) and a source V8 build is disallowed in CI, so the arm64 host builds with QuickJS over wgpu-native (`wgpu-linux-aarch64`, already in the dependency lock); skia/swc/libuv/quiche/webp publish no arm64 Linux archive, so those optional features stay disabled.

### Phase 4 — Test the exact final artifacts

**Status:** PARTIAL
**Files:** existing consumer qualification/release jobs and playtest scenarios, only where coverage is missing.

- [x] [local; agent] Web production output is served without a dev server and passes gameplay/HUD/input/restart assertions in a real browser. Checkpoint `7e50c5256`: packed cohort installed normally outside the repository, `pnpm build:web`, then production-readiness and pause scenarios against `vite preview`; exit 0, seven assertions passed, zero diagnostics, NVIDIA Turing WebGPU adapter. Captured React score/lives/position HUD and pause/restart controls are visible. Local artifacts: `artifacts/prd399/web/`; command log `/tmp/threenative-prd399-web-playtest.log`. Final candidate must rerun after later changes.
- [ ] [local/shared; target hosts] Linux x64/arm64 final distributions pass the consumer scenario from a relocated path containing spaces, outside the source tree. Partial: Linux x64 `pnpm build:desktop --mode release` produced an integrity-checked archive with `ui/index.html`; extraction to `/home/joao/projects/threenative/sandbox/prd399/relocated game/consumer` and its executable passed five consumer assertions over 941 frames on RTX 2080/Vulkan. The runtime-requested capture omits the separate overlay; OS capture and visible UI proof remain pending. ARM64 is unrun. The `starter-linux` CI matrix now runs the same relocated release-container verifier on `ubuntu-24.04-arm` and records the bound WebGPU adapter (`[WebGPU] Adapter/Vendor/Backend`) into `release-container-linux-arm64/adapter.txt`, so a run can state "lavapipe software Vulkan" from evidence rather than assumption; the arm64 leg is unrun and this item stays open.
- [x] [shared; Windows CI] The installed Windows x64 game passes the consumer scenario from its installed location. Exact merge candidate `100435fe08cafdd37d243caf677f79b0076fe89a` (PR head `215615f58`), run 35730530361 / job 106755648219: the final signed installer runs, all three signatures verify, and the installed React pause/resume/movement scenario passes 402 frames with 2.00001 m movement and zero diagnostics. Retained setup SHA-256 `4c8b53f0250187e1a9eeadf1ffb15dbfc1a9e88ca905135e6f3968bf166f5930` (`artifacts/prd399/windows-final-215615/`). This closes installed gameplay, not UI cadence or public trust. The broader verifier still fails after successful uninstall because it expects an empty install directory and finds `game`; every manifest-owned payload and ownership marker has already been checked absent. Exact remaining contents were not retained by the workflow, so WebView2 profile data is only a hypothesis. The verifier now keeps every owned-payload/manifest/marker absence check, inventories remaining paths without following links, and retains and reports runtime-created data instead of asserting that the directory is empty. A regression preserves profile data and an external linked sentinel while rejecting owned remainders. All six installer tests pass with real NSIS 3.11 / Wine 11.17, including the new check after actual uninstall; independent read-only review passes. The corrected verifier now passes on Windows for exact merge candidate `28e4a98226c793c8c6cd972855a11904fecacbec` (PR head `86dc61a33`), run 35737602263 / job 106780764017: all six installer lifecycle tests and signed installed gameplay/uninstall pass. The retained inventory identifies 228 WebView2 profile/parent paths, confirming the previous hypothesis without deleting that data. Retained signed setup SHA-256 `5ae938130bed8788b92641b1fc9b5f9d3077f78b201353312c027d9e4170f5fb` (`artifacts/prd399/windows-final-86dc/`). Final immutable-cohort rerun remains required.
- [ ] [shared; macOS CI] macOS arm64/x64 final applications pass the consumer scenario after extraction. The desktop workflow now resolves the relocated container with the installed integrity verifier and runs the installed playtest CLI against its exact executable, using the same React pause/resume/movement scenario as Windows. A regression executes the workflow block with paths containing spaces and verifies that playtest failure propagates; all 44 native workflow tests and 125 CI structure/dependency tests pass. Local execution caught relative scenario paths being resolved beneath `--project`; scenario and artifact paths are now absolute. The actual block, with only its artifact output redirected, passes on the existing sealed Linux starter over 402 frames with zero diagnostics. This proves command wiring only; Apple-host execution remains pending. Independent review passes. Full local `pnpm test` passes (1344 runtime-native tests, 57 skipped; 29 ABI tests; 5501 workspace tests, eight skipped), along with typecheck, lint, docs and budgets. The first full run lost an unrelated lock-test temporary directory (`ENOENT`); its focused rerun and the fresh complete suite pass without changing lock code. The deletion cause is unconfirmed.
- [ ] [local; Android emulator/device] Exact signed Android APK installs and plays, the release AAB validates, and the chosen signing/application identity is recorded. 16 KB compatibility and React UI are covered. Observed default-path failure: installed `pnpm build:android` exits 1 because the published v0.3.2 `prebuilt-lock.json` omits `android-arm64-v8a-v8-snapshot`. The current release producer stages both ABI snapshots; the old published cohort cannot qualify this source. The pinned V8 source recipe now builds both ABIs. The source-backed consumer debug APK builds, installs on the API 36 x86_64 16 KB emulator, and contains real React HTML/JS/CSS. All eight native libraries and archive offsets pass the 16 KB check; the running guest reports page size 16384. Normal launch visibly renders the world and HUD. The first input playtest fails its capture guard: only the HUD is visible after the scripted run, although recorded simulation samples show movement and startup completion. A wait-only scenario fails identically before input, and a two-second capture delay does not help. A native GPU capture is black as well, which rules against the React WebView merely covering a correctly rendered world. Fresh WebView caches and an Android guest reboot do not fix the black output. The identical APK renders correctly on two consecutive API 35 emulator launches. The API 36 / 16 KB lane also fails with SwiftShader: one launch is black and another has visibly corrupted geometry, so neither qualifies. Both used emulator 36.6.11. A separately installed, checksum-verified stable emulator 37.1.11 renders the same unmodified APK correctly on four consecutive launches; first and fourth captures show the complete world and React HUD. This isolates a host-emulator-version dependency, not a source fix. Both the debug APK and the consumer-key-signed release APK pass the full 402-frame pause/resume/movement scenario twice on emulator 37.1.11, with approximately 2 m movement and no diagnostics. The playtest runner now clears logcat after its intentional old-process shutdown; a regression confirms that launch errors still fail. Signing passwords use child-process environment variables, never literal command arguments. Strict AAB verification rejects unsigned, partially unsigned and expired-certificate archives while allowing a valid self-signed Android certificate; real JDK regressions pass. The release AAB passes bundletool validation, but its derived universal APK fails 16 KB archive alignment (libSDL3.so at offset 0x1f7000). AGP 8.2.2 emits no 16 KB bundle configuration. AGP 8.11.1 / Gradle 8.13 fixes the generated APK: the new `pnpm native:verify:android:artifact <apk|aab> [--bundletool <jar>]` command rejects the old AAB and passes all eight libraries in the new one. Wrapper/distribution hashes match Gradle published checksums. The updated runtime tarball was installed in the consumer before rebuilding; the source override selects the Gradle checkout, not the installed packaging script. Both the rebuilt signed APK and consumer-key-signed AAB-derived universal APK pass the 402-frame UI/input scenario twice on API 36 / 16 KB emulator 37.1.11 with zero diagnostics. All outputs contain the React HTML, JS, CSS and manifest. Checkpoint SHA-256: APK `4ee6b3f177b44f29cf86dc86b4e359ddb0b2346d757cad2a2710c5289e072fea`; AAB `bdc39eb44fa728b273545e7c57968f272a2d500cca72e9beef598d41fae19d2c`; derived APK `cf8f416fde89c34370c9fadb2bf86ce6b9af61558a1258afbc234f863a3f9ef7`. Identity is `com.threenative.consumer`, signed by the session-local qualification key (certificate SHA-256 `1198c4cd5c3ca1594676bf9553d8fbaa87062a36395645658d0cabab49119415`), not a public store publishing identity. Independent review found and fixed a CLI-test URL/path conversion that could falsely pass on Windows. Installed-package execution exposed a symlinked-entry guard that silently skipped the new verifier; a directory-junction regression fails before the realpath fix and passes afterward. The actual command from the installed final tarball passes all eight AAB-derived libraries. Full `pnpm test` passes (1329 runtime-native tests, 57 skipped; 29 ABI tests; 5499 workspace tests, eight skipped), as do `pnpm typecheck`, `pnpm lint` and `pnpm check:docs`. Native coverage was remeasured for the CI-consumed report; `pnpm budgets` passes. The Gradle-only lock change invalidated local acquisition receipts; every referenced component and payload was compared unchanged against the prior committed lock before updating those local receipts. Native UI cadence on Android, arm64 execution and a complete new prebuilt cohort remain pending; these are checkpoint artifacts, not the final immutable release.
- [ ] [shared; iOS simulator CI] The final simulator app boots and plays the consumer scenario with the selected React UI on a hosted macOS runner. Report simulator coverage as simulator coverage. CI merge candidate `aafb2214f84f8efa2faf3e2191e583e0f9ebdcca` compiles on the macOS CI host, but its simulator verification fails because `TN_NATIVE_WORKER_PROOF_PASS` is absent (run 35705022714, job 106671902942). The worker acknowledges at frame 19 and computation is dispatched at frame 21. Failure is logged about 3.85 seconds later while rendering reaches 242 presents, but review found that the old log follows blocking worker termination, so it does not timestamp deadline expiry. Null GPU handles appear after the proof exception. Added direct worker compute-entry/completion/result-posted timestamps, a dispatch timestamp, and failure logging before termination; all bounds, iterations and checksum assertions remain unchanged. The deadline-order regression fails before the log move, then all four native-smoke tests, bundle verification and example typecheck pass. The real Linux desktop-core gate passes 300 frames: compute takes 25 ms, checksum 2110598008, four frames advance and no callback arrives after termination. Full `pnpm test`, `pnpm typecheck`, `pnpm lint` (772 existing warnings), document checks and independent read-only review pass for this diagnostic change. A direct desktop override of the browser-oriented two-tick worker scenario asserts before completion and fails; it is not passing evidence. The next Apple-host run passes on exact merge candidate `3526ccb7eadfd21b1f0bd97bd019ef42dd4f365b` (parents `5e7f2cfc6` and `215de303d`): run 35713566348, job 106700841615. JavaScriptCore computation takes 1096 ms, delivers checksum 2110598008, advances 72 frames and reports zero callbacks after termination. Simulator runtime, consumer handoff and physics checks pass with unchanged deadline/iterations. The previous intermittent timeout is not claimed fixed by diagnostic logging; repeat platform qualification remains required, as does final CI simulator UI cadence. Partial (2026-09-23): the simulator app boots and passes its smoke and consumer scenarios once Apple-subsystem `E` records and non-failure `F` records classify as logs. The React overlay now attaches, loads (`TN_UI_OVERLAY:{"loaded":true}`), runs and receives state (`TN_UI_PAGE` ready and first-state) after three host fixes: the `tnHost` handler is registered before `WKWebView` init (the view copies its configuration), UI resources return HTTP 200 with `Access-Control-Allow-Origin` (Vite emits `crossorigin` module scripts), and the overlay is pinned with constraints. The hosted `simctl` capture still shows no overlay pixels and the overlay frame stays `{874, 402}` on a portrait capture of the landscape-configured starter, so the pixel proof is unqualified; it runs only in the `ios_only` dispatch until it passes.

### Phase 5 — Release commands, qualification and delivery

**Status:** PARTIAL
**Files:** existing release workflows/commands and their regression tests; relevant user-facing instructions.

- [ ] [local/shared; agent and CI] Repair demonstrated release-flow failures while preserving candidate identity, prerequisite coverage and fail-closed publication. Execute local regressions and observe the corresponding real workflow. Partial: `gates` skips CI completions whose originating event is not `push`; its exact-main-push verdict and all tag checks remain intact. Schedule/manual/PR regressions fail before the fix; 58 release refusal/staging tests pass afterward. Independent review passes the trigger and template-command changes. Hosted run 35730529704 reaches Android release compilation successfully, then fails staging because AGP 8.11.1 moved stripped native libraries under `stripReleaseDebugSymbols/out/lib`. Both workflow engine stages and the local release assembler still read the older directory. Updated those existing paths; executable staging regressions first select deliberately stale old-layout bytes, then pass using current task output for both ABIs. The focused staging and CI structure/dependency suite passes all 130 tests. The actual workflow V8 staging block and local assembler each copy all 11 rebuilt Android payloads with SHA-256 equality to the current AGP outputs (`android-staging-current/`). Independent source review passes. Full `pnpm test` passes (1341 runtime-native tests, 58 skipped; 29 ABI tests; 5501 workspace tests, eight skipped), as do typecheck, lint, documentation and budgets. Hosted staging must rerun; no new release cohort is claimed. Concurrent remote staging commits through `0227b34b8` are merged without rewriting history. Their Gradle normalization hook fails the supported `-PthreenativeAbis=x86_64` build by requiring both ABIs; the actual Gradle task reproduces that failure and passes after choosing the existing consumer-path correction. The extra copy hook and its conflicting static path test are omitted from the merge result. All 130 focused staging/CI checks pass again; production code remains identical to the fully tested local checkpoint.
- [ ] [local/shared; agent and CI] One clean immutable candidate owns every final package hash and its programmatic verification; all required repository checks pass, with no stale or mixed-build evidence.
- [ ] [local; agent] Publish concise executable build/test instructions, obtain an independent review, keep one draft PR against develop, and retire this checkout after authorized integration/completion with its needed artifacts preserved. Partial: runtime README documents Windows installer verification, Android APK/AAB validation and the Linux visible React cadence command with prerequisites, thresholds and retained artifacts. PR #279 remains a draft against develop; cross-platform completion and checkout retirement remain open.

## Acceptance and remaining limits

2026-09-22 Apple follow-up: dedicated run 35743318277 compiles the iOS host at `f442273d6`, including the new overlay wiring. Its native-only smoke then fails because the newly accurate PID log capture exposes UIKit AssetManager error 260: `TNLaunchBackground` references a missing `Assets.car`. The raw host plist and unbranded low-level packager both named that uncompiled color. The fix removes the raw-host reference, emits named launch resources only when the packager compiles their catalog, and omits an unobserved background color from the package report. Both regressions fail before correction. Independent review also catches the branded path naming `TNLaunchBackground` while compiling `LaunchBackground.colorset`; a regression reads the staged plist name and opens that exact compiler-input colorset, reproducing the missing asset. The name now matches, and color channels use normalized numeric values from [Apple’s asset-catalog format](https://developer.apple.com/library/archive/documentation/Xcode/Reference/xcode_ref-Asset_Catalog_Format/Named_Color.html) instead of bare hex strings. A second review pass closes the last named-catalog path: `renderIosInfoPlist` wrote `CFBundleIconName` whenever `app.icons.ios` existed, so an accepted empty `app.icons.ios` (`config.ts` allows it; `build.ts` resolves it to no icon) named `AppIcon` with no compiled `Assets.car`, the same AssetManager 260 class; a focused regression fails first and the key is now emitted only when the same icon expression `stageIosIcon` compiles. Verification of this dirty correction at `f442273d6`+uncommitted: iOS focused tests 37 passed/0 failed (`/tmp/prd399-ios-finish.M9MMhz/focused-tests-green.log`, exit 0); `pnpm typecheck` exit 0; `pnpm lint` exit 0 (780 warnings); `pnpm test` 5501 passed/8 skipped across 453 files (exit 0); `pnpm budgets` exit 0; doc-evidence specs 31 passed (exit 0). No error filter is added. The simulator must rerun before the new React pixel check can execute; actual actool compilation, the native host build, raw-host smoke and React pixel execution remain unrun here.

2026-09-22 iOS diagnostic-severity correction: hosted run 35748912544 / job 106817682859 failed `verify-ios-simulator` at `69774cbdb` with `TN_PLAYTEST_CONSOLE_ERROR` and `TN_PLAYTEST_RUNTIME_DIAGNOSTIC`. The sole captured type-`error` entry was a real Default (`Df`) Apple platform record -- `[com.apple.UIKit:AssetManager] ... Error Domain=NSCocoaErrorDomain Code=260 ... couldn't find Assets.car` -- that `ios.ts` promoted only because the message text contains "Error Domain"; genuine `E`/`F` records with no error keyword sat in the same log classified as `log`. The driver now parses the compact record (timestamp + recognised severity + real `[com.apple.<subsystem>:<category>]`): `E`/`F` classify error even without error keywords, a recognised non-error level is `log` unless an explicit app/JS marker (`[error]`, FATAL, GPUValidationError) is present, and any unrecognised format keeps the existing textual scan. The native JS console marker is confirmed as the `[error]`/`[log]` prefix (JSC `consoleFn`, `jsc_engine.mm:1076`); no error filter was added and no OS or application error is suppressed. Focused local evidence: `packages/playtest/__tests__/ios-driver.spec.ts` red before the fix (1 failed/4 passed, exactly the Df promoted to error and the E/F demoted to log), green after (5/5); `ios-driver` + `ios-device-playtest` 12/12 pass; `pnpm --filter @threenative/playtest typecheck` exit 0. Native iOS simulator verification remains open until `native-platforms.yml` reruns at the new committed SHA; the newly severity-aware classification will also surface genuine `E`/`F` os_log records that were previously silent, which that Apple run must account for.

2026-09-22 iOS classifier review correction: the compact-record match now requires the real prefix -- timestamp, recognised severity, `process[pid:thread]`, then the `[com.apple.<subsystem>:<category>]` field immediately after it -- so an Apple-looking field quoted inside a non-Apple message no longer selects severity-aware handling (focused regression: a custom-subsystem Default record embedding `[com.apple.UIKit:AssetManager]` plus Error text stays `error`; the package suite is red 1/5 against the prior pattern and green 5/5 after; `ios-driver` + `ios-device-playtest` 12/12; `pnpm --filter @threenative/playtest typecheck` exit 0; Biome on the two touched files 0 errors). On the retained f442 capture (273 records) the strict parser exposes 7 genuine `E`/`F` records previously classified `log` -- one `F` [com.apple.runtime-issues] `UIScene` lifecycle Fault (`ios/Info.plist` carries no `UIApplicationSceneManifest`; SDL3 `3.2.30`), one `E` [com.apple.app_launch_measurement] unsent CA event, one `E` [com.apple.CFBundle:plugin] factory notice, and four `E` [com.apple.coreaudio:AQ] LoudnessManager records (the simulator has no hardware acoustic ID) -- while the AssetManager `Df` record correctly returns to `log`. No blanket suppression: the native rerun (still pending) must account for all seven, and the UIScene Fault is a real forward-compat launch requirement (Apple TN3187: required from the release after iOS 26). Run 35748912544 failed and is not retroactively passed.

2026-09-22 iOS WebUI wiring checkpoint: full `pnpm test` passes (1348 runtime-native tests, 58 skipped; 29 ABI tests; 5501 workspace tests, eight skipped), with full typecheck, lint (780 warnings), documentation checks and budgets passing. Native coverage was remeasured successfully for the existing CI-consumed record. The CI structure check initially caught the new ninth shared scaffold caller; its expected inventory is updated and all 125 checks pass. Independent read-only review passes after the MRC cleanup. Local state-scenario and exact compositor-pixel checks pass; actual iOS compilation, visible React execution, input and cadence remain unverified.

2026-09-22 Windows preservation checkpoint: full `pnpm test` passes (1342 runtime-native tests, 58 skipped; 29 ABI tests; 5501 workspace tests, eight skipped), with typecheck, lint (779 warnings), docs and budgets passing. Six real NSIS/Wine installer checks pass, and the packed runtime contains byte-identical verifier/helper sources (tarball SHA-256 `28cdeb7af8e56b7a8a92b26b0963045e3fb298a239def08ef0d232a9ed3c2ca1`). Independent review passes. The first full run was interrupted by SIGTERM after native checks; gate resume refused a missing verified lease, so a fresh complete run was used. No code was changed to address that interruption. The owned Android emulator is shut down and its temporary image released. The subsequent hosted Windows cleanup passes as recorded in Phase 4; the Android source producer also passes in run 35737602263.

2026-09-22 Android command / Windows CRLF checkpoint: full `pnpm test` passes (1341 runtime-native tests, 58 skipped; 29 ABI tests; 5499 workspace tests, eight skipped). Typecheck, lint (779 warnings), documentation checks (2196 links) and budgets pass. The native source coverage report remains current because this checkpoint changes command/analysis code, not the native implementation. Independent read-only review passes after the late-cancellation guard. Android latency and the hosted Windows installer remain unqualified for the reasons above.

2026-09-22 visible UI command checkpoint: full `pnpm test` passes (1338 runtime-native tests, 58 skipped; 29 ABI tests; 5499 workspace tests, eight skipped). Typecheck, lint (777 warnings), documentation checks and budgets pass. The documented `--executable` rerun against the same sealed container also passes at p95 28 ms, 60 visible updates/s and idle wakes 27/28 ms. The first full suite identified unregistered temporary-directory creators; the test now uses the existing cleanup helper and the runner registers its success-cleanup/failure-retention policy, and the complete rerun passes. Native coverage was remeasured for the existing CI-consumed report. Its first run timed out in the unchanged worker-production target; that target passed in isolation in 0.94 seconds and the complete coverage rerun passed, without changing its bounds or implementation. No worker-timeout fix is claimed.

2026-09-22 Android presentation-pacing review correction: the production display-synchronized pacer's review findings are fixed. The registered executable regression (`threenative-presentation-pacing-test`, pass line `PRESENTATION_PACING_OK`, 28 assertions over 10 cases) fed frame timestamps as if they were milliseconds while `notePresentationFrame` takes nanoseconds, so every display target was unreachable and the 83 ms fallback made the checks pass; units are now explicit and display release is bounded at 50 ms, below that fallback. `paceToDisplayFrame` also returned false on timeout while leaving the stale frame and target, so every later present re-waited the full timeout; it now uses one absolute deadline (spurious or early callbacks cannot extend the budget) and drops the schedule and frame on timeout, retaining software pacing until a fresh callback re-arms it. The lost-signal regression fails before the fix (next present took 83 ms) and passes after (16 ms); `maxFps 0` stays uncapped, cap-below-refresh arithmetic (30 on 60 Hz waits two display frames) and foreground single-registration are unchanged. JNI identity is verified against the packaged class: `com.threenative.runtime.MystralActivity` resolves `Java_com_threenative_runtime_MystralActivity_nativeOnPresentation*`, present in both packaged ABIs. A now-superseded diagnostic APK was built from this earlier source (before the deadline-carry correction below) through the installed release CLI (`THREENATIVE_RUNTIME_SOURCE=packages/runtime-native --allow-source-build`): APK SHA-256 `d0d296cb480c936e4e7c86d022b07ebbe40b0c4dc366801ff6e824404e5f676f`, x86_64 `libmystral-runtime.so` `10f9b124f0823c304ceb9b45ef3c294eee57e52d695c7fa328042e5dc1c6638d`, arm64 `bf2aa7bf500c1cac659fb8db7af1869902310c8bc07c2a47836a659822914523`, marker `TN_PRESENTATION_PACING` present. Post-fix captures on the owned 60 Hz API 36 / 16 KB emulator 37.1.11: `android-pacing-review-2/` p95 50.09 ms, source 59.89 Hz, visible 56.16 Hz; `android-pacing-review-6/` p95 59.44 ms, source 59.32 Hz, visible 55.21 Hz; three further recordings were rejected by the unchanged SAMPLING gate under host contention. Android latency still fails the unchanged 50 ms bound, matching the earlier production distribution (one of eleven prototype captures passed at p95 47.45 ms); no threshold, cadence/drop/idle requirement, capture-delay subtraction or best-run selection was changed, so **Android latency remains open**. A 120-capable panel/uncapped lane is unavailable on this host and stays unverified; on-device below-refresh cap and native-world fps/input/pause-resume remain unrun in this correction.

2026-09-22 Android presentation-pacing deadline-carry correction (supersedes the diagnostic APK `d0d296cb...` above): review found `paceToDisplayFrame` still advanced the display target from the first frame it observed rather than the deadline it was actually scheduled for, so a carried fractional remainder could release up to a display interval late. The eligible path now falls through to one shared end advancement (`target += interval`) with the existing no-burst recovery, carrying the remainder. The registered executable regression is now 30 assertions over 10 cases, including the cap-50 carried-remainder case that fails 1/30 on the pre-correction code and passes 30/30 (`PRESENTATION_PACING_OK`) after (`/tmp/prd399-bounded-pacing-checkpoint.txt`); lifecycle, absolute timeout, stale-drop and `maxFps 0` are unchanged. The `d0d296cb...` APK predates this correction and is retained only as a diagnostic artifact -- at that checkpoint no APK or latency capture had yet been produced from the corrected source, so the 50.09 ms / 59.44 ms failures and the three SAMPLING rejections stood unchanged and **Android latency remained open**; the hardware-backend checkpoint below supersedes that statement with fresh captures from the corrected source. Runtime correctness improved; no performance pass is claimed.

2026-09-22 Android hardware-backend latency checkpoint: the earlier 47–69 ms Android captures were all software-rendered. The owned emulator (`threenative_ps16k`, port 5580) had been launched `-gpu auto` with no `DISPLAY`, and `emulator.log:7` records `Your GPU drivers may have a bug. Switching to software rendering.` (`vulkan_mode_selected:lavapipe gles_mode_selected:swangle`). It was stopped cleanly (`adb -s emulator-5580 emu kill`) and relaunched on the same AVD/port with `-gpu host -memory 4096 -cores 4 -read-only` and `DISPLAY=:0`; the guest then reports SurfaceFlinger GLES `Android Emulator OpenGL ES Translator (NVIDIA GeForce RTX 2080/PCIe/SSE2)`, `vkjson` `deviceName NVIDIA GeForce RTX 2080 / driverName NVIDIA / deviceType 2.0`, and `dumpsys gpu cpuVulkanInUse 0`. A fresh signed release APK was built from HEAD `b7ee0f2a0` through the installed CLI with `THREENATIVE_RUNTIME_SOURCE` selecting this checkout (`--allow-source-build`); APK SHA-256 `1f9b1bed7783bd9db18aee5e94a358dce46ccd3c9f8f2bf144b3b1741a174eb6`, x86_64 `libmystral-runtime.so` `1e958351a665bd9b60100b55e325ca09474e04e8748eae7fe37df2c2f4f9faec`, carry marker `TN_PRESENTATION_PACING` present. Two independent captures on that same APK (60 Hz panel, adapter NVIDIA RTX 2080) both fail the unchanged 50 ms bound: `/tmp/prd399-android-hardware-proof.MVa5JL/cap1` p50 47.63 / p95 64.52 / max 93.44 ms, capture 59.61 Hz, source 59.79 Hz (57–60), visible 55.74 Hz, 77 unobserved, 0 invalid, idle 43.28/48.55 ms; `/tmp/prd399-android-hardware-proof.MVa5JL/cap2` p50 47.35 / p95 65.08 / max 81.11 ms, capture 59.47 Hz, source 59.84 Hz, visible 56.95 Hz, 55 unobserved, 0 invalid, idle 47.50/49.31 ms. Both pass every other gate (sampling, source rate, visible rate, idle, 1 ms timestamp alignment) and fail the same latency cause, so no third capture was run. A foreground/background/resume lifecycle smoke on that APK (tap + DPAD_CENTER + swipe with no crash) retains one PID (14422) across pause/resume with SDL `onStop`/`onStart`/`onResume`, `maxFps:60` re-applied, post-resume 59.1–60.35 fps; input dispatch/no-crash is lifecycle smoke only, not asserted functional input. GPU-backed runs still fail latency: p50 ≈ 47 ms is stable, p95 64.52/65.08 ms both exceed the 50 ms bound, and **Android latency remains open**. Measurement is screenrecord Winscope-v2 virtual-display composition; physical scanout is unmeasured. `perf --logcat` fails with `TN_PERF_MARKER_MALFORMED`: retained `TN_FRAME_BUDGET`/`TN_HOST_GAP` messages are truncated (subsequent inspection measured 1023-byte messages). The truncation owner remains unidentified; only surviving fields were inspected, not a successfully parsed performance report. On-device below-refresh 30 cap and `maxFps 0` were not run (no existing device command; only the verified `threenative-presentation-pacing-test` regression covers them); 120 Hz remains unverified.

2026-09-22 Linux UI port checkpoint: full `pnpm test` passes (1326 runtime-native tests, 57 skipped; 29 ABI tests; 5498 workspace tests, eight skipped). Full `pnpm typecheck`, `pnpm lint` (772 existing warnings) and `pnpm check:docs` pass. `pnpm test:ui-native` passes the actual WebKit input fixture on an isolated Xvfb; all 23 focused packaging/host checks pass. The source host rebuild, relocated starter input scenario, real OS React input fixture and repeat calibrated pixel-ID measurement all pass. Native Windows/macOS/iOS results below describe the earlier CI merge candidate, not this Linux port.

2026-09-22 Android test wiring checkpoint: the native contract inventory now includes the presentation-pacing executable (44 targets). The metadata JVM probe supplies the Android Choreographer and lifecycle API stubs and runs the actual production `MystralActivity.onResume`; only the unavailable JNI start/stop declarations are replaced in its temporary compiled fixture, with exactly-once match checks. The original metadata and frame-rate reapply assertions remain intact. Executing production `onResume` first reproduced `UnsatisfiedLinkError: nativeOnPresentationFramesStarted`; the isolated JNI stub then passed the same probe. Sequential checks, with recorded exit statuses, pass: 46 focused packaging/contract tests, full `pnpm typecheck`, full `pnpm lint` (780 warnings), and full `pnpm test`, including 5505 workspace tests (five skipped). Logs: `/tmp/prd399-muse-p6mhina6/review-gates/`. This test wiring does not close any remaining platform or latency acceptance item.

2026-09-22 committed checkpoint `f5f50c3f8`, after startup/publication, portable Vite and macOS signing fixes (before the subsequent Linux UI port): full `pnpm test` passes, including 1323 runtime-native tests (57 skipped), its 29 ABI tests and 5498 workspace tests (eight skipped). The native package's physics parity and package validation commands also pass. Full `pnpm lint` passes with 772 existing warnings; `pnpm check:docs` validates 2196 links. Workflow structure/dependency tests pass all 125 cases. A concurrent typecheck briefly saw declarations removed by the test command's rebuild; the sequential full `pnpm typecheck` rerun passes. The V8 and QuickJS hosts and required native regression executables were built locally before the full unit run. Independent read-only reviews pass the default UI, frame publishing/startup cleanup, portable Vite and macOS signing changes. These checks do not qualify unexecuted platform artifacts: macOS real signing/JIT and the signed starter workflow remain pending, alongside the other open phase boxes.

Installed checkpoint provenance: JavaScript/package source `7e50c52560344c58054e959565cfaef124b663db`; tarballs under `artifacts/prd399/cohort`, installed consumer `/home/joao/projects/threenative/sandbox/prd399/consumer`. Normal installation downloaded the pinned v0.3.2 Linux x64 prebuilt (runtime SHA-256 `7d48d511b6605519eec2f20874249144260b0e921988acb629c2360c7f188fbb`, tools `078ab5977492409618808179a39039d3317bdd7c3c9d361900b1ec55a1a8a178`). This checkpoint proves that published consumer path; it is not a rebuilt native runtime at the candidate SHA and does not close immutable final-cohort acceptance.

2026-09-22 iOS CI follow-up (Phase 4 remains open): iOS alone selects pinned SDL 3.4.16, including its `SDLUIKitSceneDelegate`; desktop and Android retain SDL 3.2.30. The lock, downloader, CMake and simulator verifier use the same iOS source. The packager and verifier require the scene-manifest fields and reject comment-spoofed or legacy hosts before staging. The spoof regression fails with the old guard and passes with the fix. Focused checks pass 46/46, native SBOM passes 4/4, and `verify-ios-simulator --check` passes. The new lock digest invalidated 12 local receipts; they were refreshed through verified pinned downloads. During that work, an unsupported downloader `--help` invocation deleted the local QuickJS cache; its pinned archive restored it, and a read-only help guard now has a sandboxed regression. Apple CI compilation, simulator launch, React pixels and the earlier OS diagnostics still need verification. Physical-device signing and playability are outside this PRD's CI acceptance.

2026-09-22 Android diagnostic fix (Phase 2 remains open): AOSP `__android_log_print` clipped preformatted lines at 1023 bytes; an API 36 emulator probe confirmed a 1650-byte JSON message was truncated by print and preserved by `__android_log_write`. Four existing emitters now use write while retaining tags and severity. A 300-frame `TN_HOST_GAP` samples array exceeds logcat's roughly 4 KB entry cap, so Android emits the complete aggregates; desktop retains the raw samples used by the networking proof (12/12). A signed release APK rebuilt through the installed CLI from HEAD `9c15a2842` plus this source diff (`9a2ef26d…`, installed hash matched) passes `perf --logcat` with 24 complete frame-budget windows and parseable host-gap aggregates. Focused dependency/help and download-retry tests pass 14/14; strict perf parsing passes 34/34. `--help` and `-h` run under denied filesystem writes and network access in the regression, so a future help-path failure cannot remove real dependency caches. The pacing regression now asserts the production path returned for each display, lost-signal, pause and restart case, replacing two scheduler-sensitive 60 ms wall-clock checks that failed on macOS CI; the focused Linux executable passes. Full typecheck, lint, test (5502 passed, eight skipped), refreshed native coverage (42 targets, 79.13% overall) and budgets pass on this combined diff. Android visible latency remains above the unchanged 50 ms p95 target; diagnostic parsing and pacing-test correction do not close that gate.

2026-09-22 latency follow-up: A signed Android APK built from `8785472c3` plus removal of the WebView's forced hardware layer still failed the unchanged pixel verifier at p95 65.08 ms on the same GPU-backed emulator. The experimental source change was reverted. The assumption that this explicit layer causes the visible delay is unsupported; the platform/render-to-composition path needs a different measured explanation before another fix.

2026-09-22 Android V8 import checkpoint: CI `native-platforms / Android V8 source payload` failed at the top-level pngjs import in `download-deps` when run install-free. This commit moves the PNG require into `assertIosIconSource` (via `node:module` `createRequire`), keeps `SDL3_IOS_VERSION` single-sourced from `package-ios.mjs` and its sync validation; a bare temp copy reproduces `ERR_MODULE_NOT_FOUND` before and reports `IMPORT_OK` after, and the existing ios-packaging checks pass 20/20. Full V8 payload/CI and the other PRD gates remain open.

Every phase box is required acceptance evidence; none is satisfied by a plan, a mocked host, a different source revision or historical evidence. Windows signing and CI macOS ad-hoc signing require their executable checks. Report Developer ID trust, notarization and physical iOS playability as unverified unless independently tested; they are outside this PRD’s CI acceptance. Report exact supported OS/architecture, simulator/device, trust and prerequisite limits. This task does not promise the absence of every possible defect; it must demonstrate a playable final artifact and responsive selected UI on every claimed platform.
