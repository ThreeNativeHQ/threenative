# PRD-048 verification evidence

Date: 2026-08-08
Status: Phases 0–2 and 5 complete; Phase 3 local installer/packed-consumer controls pass.
The first release run executed but failed before publication; its Android SDL-Java fix passes
locally, while real assets, registry packages, the corrected release rerun, and the executed
clean consumer remain open. Android source/device and packed no-toolchain assembly pass
locally; the released-consumer emulator run remains open.

## First tag-gated release attempt — 2026-08-09

[GitHub run 31314171195](https://github.com/jonit-dev/threenative/actions/runs/31314171195)
executed tag `runtime-native-v0.1.8` at `7c20861`. Linux, macOS arm64, and Windows x64 each
built, ran, captured, and staged a desktop runtime. Android built native physics and both
runtime ABIs, then failed at `compileReleaseJavaWithJavac`: the clean `--android` dependency
set downloaded SDL's AAR but not the SDL source tree containing `org.libsdl.app.SDLActivity`.
No release or checksum lock was published.

Commit `d7e5cc5` prepares `0.1.9` by adding `sdl3` to the clean Android dependency set and
deduplicating the combined `--all` list. The exact missing boundary was re-proven locally:

```text
isolated download-deps.mjs --only sdl3                 PASS
SDLActivity.java present; 8 MiB SDLThread patch        PASS
:app:compileReleaseJavaWithJavac --rerun-tasks
  --no-build-cache                                      PASS — 16 tasks executed
assembleRelease, arm64-v8a + x86_64                    PASS
```

This closes the local defect, not the release criterion. Publication, checksum assets, and
the toolchain-free released consumer stay open until a corrected version tag runs end to end.

## Phase 0 — inherited distribution deletion

The six Mystral demo-distribution scripts were deleted: 1,159 lines total. The live
`packages/runtime-native/scripts/bundle.mjs` remains present and is still used by the Android
proof.

```text
build-production.sh   276 lines deleted
bundle-examples.ts    113 lines deleted
package-app.sh        418 lines deleted
prebundle.ts          151 lines deleted
set-version.mjs        78 lines deleted
test-examples.sh      123 lines deleted
```

## Native regression proofs

```text
pnpm --filter @threenative/runtime-native native:build  PASS
node packages/runtime-native/scripts/build-android-first-proof.mjs  PASS
```

The Android proof rebuilt the catalog Three 0.185.1 import-free bundle, preserved the ready,
first-frame and 300-frame markers, and reproduced output SHA-256
`a7c721841124ff570c1392cf5e3922acbd1cf3bca2efc7d781ccdd2a003185d6`.

## Repository gate

`TMPDIR` was redirected to a disposable ext4 directory under `/home/joao` because the
machine's `/tmp` tmpfs was full. This changes scratch placement only; test behavior is
unchanged. The disposable directory was removed after the run.

```text
pnpm typecheck   PASS
pnpm lint        PASS — 345 files
pnpm test        PASS — final root run: 150 files, 1,160 tests
pnpm budgets     PASS — 6 framework packages, 3 examples,
                  4,276/15,000 framework LOC,
                  48,266/50,000 native runtime LOC (historical 2026-08-08 gate),
                  largest template 1,200 LOC
pnpm sync:agents --check  PASS — 11 source mirrors
```

## Phase 1 — web target parity

`create-threenative` now ships the existing-package `threenative` bin. `build` is the only
implemented top-level command; `--target` remains a flag. A packed consumer install was used,
not workspace resolution. Each direct Vite output was moved outside the project before the
CLI run so Tailwind could not scan a prior output tree.

```text
minimal     vite build vs threenative build --target web  BYTE_IDENTICAL
starter     vite build vs threenative build --target web  BYTE_IDENTICAL
platformer  vite build vs threenative build --target web  BYTE_IDENTICAL
packages/create-threenative/__tests__/build.spec.ts         4 PASS
pnpm --filter create-threenative typecheck                  PASS
pnpm --filter create-threenative build                      PASS (publint included)
```

No playtest scenario or template render source was changed. A repeatable packed-package gate
then scaffolded, installed and ran each template's committed suite:

```text
pnpm test:templates  PASS
minimal              1 scenario
starter             10 scenarios
platformer          14 scenarios
```

## Phase 2 — Linux desktop artifact from source

A scaffolded minimal project with a native-safe Three.js scene was bundled through the CLI
and compiled by the source-built Linux runtime. The output was a 112 MB executable ELF. It
ran under Xvfb on the real Linux host, emitted all lifecycle markers, rendered exactly 300
frames, and wrote a nonblank 1280×720 screenshot.

```text
TN_NATIVE_SMOKE_READY:webgpu
TN_NATIVE_SMOKE_FIRST_FRAME
TN_NATIVE_SMOKE_300_FRAMES:300
Rendered 300 frames in 8546ms
Screenshot middle pixel BGRA: 255 170 68 255
packages/runtime-native/artifacts/distribution-linux-2026-08-08.png  1280x720 PNG
```

This is Phase 2's source-machine proof, not the Phase 3 clean-machine proof. macOS and
Windows remain OPEN.

## Phase 3 — prebuilt distribution controls

The installer selects an explicit supported `platform-arch`, requires HTTPS, downloads to
memory, verifies SHA-256 before writing, and fails closed when the platform, manifest,
release entry, fetch, or checksum is invalid. The actual archive was packed and inspected:
it contains the distribution scripts and exact Android wrapper/project files, but no C++
source tree, native build input, or built Android output.

```text
packages/runtime-native/tests/distribution.test.mjs  9 PASS
unsupported aix-ppc64                                rejected with aix-ppc64
unbuilt darwin-x64 and linux-arm64                   rejected with their platform keys
corrupted linux-x64 payload                          checksum failure; output absent
packed required dependency + corrupt payload         pnpm install failed closed
packed pnpm consumer + local verified payload        install lifecycle ran; payload installed
remote checksum-lock bootstrap                       verified before runtime fetch
Android five-asset transaction                       missing/corrupt set writes nothing
pnpm pack --json                                     actual archive has no C++ runtime source
```

All generated templates explicitly allow the optional runtime lifecycle through pnpm 10's
`onlyBuiltDependencies`; the packed-consumer test proves the hook actually executes.
`native-release.yml` now prepares the positive lane: three real-runner builds precede asset
and checksum publication, then a fresh Linux job uses local package tarballs with native
toolchain commands masked to build, launch 300 frames, and inspect the screenshot. That job
is a tarball contract proof, not the literal npm consumer gate.

The real positive gate is still **OPEN**. GitHub has no releases, and `origin/main` is 123
commits behind this work and has neither native workflow. No committed checksum lock exists,
correctly. npm has no `create-threenative`, runtime-native, core, physics, or playtest package.
The registry's unrelated predecessor UI owns `0.1.0` through `0.1.11`, so the current UI
package now reserves `0.1.12`; starter and platformer must move their immutable `0.1.0` pin
before publication. The concurrent PRD-049 templates also pin
`threenative-sculpt-mcp@0.1.0`, which is absent from npm. This machine has no npm credentials
(`npm whoami` returns `ENEEDAUTH`). Required external sequence:

1. Push the native commits/workflows, then push the version-matched runtime tag.
2. Let the three runner builds and post-publish tarball consumer pass; retain logs/screenshots.
3. With npm organization access, publish the aligned template dependency set, runtime-native,
   then `create-threenative`.
4. On a fresh toolchain-free machine, run literal `pnpm create threenative`, desktop build,
   and 300-frame launch. Only that closes acceptance criterion 2.

## Registry package readiness — 2026-08-09

Core, physics, playtest, UI, and create-threenative build and pass `publint`. Each package
also passes `npm publish --dry-run --access public` from its own package directory. The packed
manifests contain no `private: true` or `catalog:` values; pnpm resolves workspace and catalog
protocols to registry versions. Every explicit export and binary points at a packed file.

```text
@threenative/core@0.1.0       9 files,  24,682-byte tarball
@threenative/physics@0.1.0   15 files,  49,133-byte tarball
@threenative/playtest@0.1.0  12 files, 157,638-byte tarball
@threenative/ui@0.1.12        3 files,   2,299-byte tarball
create-threenative@0.1.0    123 files,  54,694-byte tarball
publication manifest test     3 PASS
physics backend controls      9 PASS
packed web export consumer     PASS
packed native condition        PASS
packed create CLI scaffold     PASS — minimal, no install
```

No publish was attempted. Authentication, ownership of the `@threenative` scope, the UI pin,
the sculpt MCP package, runtime assets, and runtime-native publication remain external gates.
The sculpt package does have an identified clean local source at
`/home/joao/projects/threenative-sculpt-mcp` commit `d0479de`; its typecheck and 27 tests pass,
and its packed manifest carries Apache-2.0 `LICENSE` and `NOTICE`. It has no Git remote, so the
safe resolution is to create and push its dedicated repository, verify that exact SHA, then
publish `0.1.0`. If that cannot happen before this release, remove the complete PRD-049 sculpt
integration from the templates; retaining an `E404` dependency is not a releasable middle state.

Apple remains separately OPEN: neither Apple workflow has run, and the release lane has no
Developer ID certificate, notarization credentials, or signing/notarization step. Its macOS
binary is therefore runner evidence only, not a signed distribution claim. The iOS simulator
lane also cannot run until the unpushed workflow reaches a macOS runner with Xcode.

## Phase 4 — Android handoff; iOS OPEN

The CLI bundled the normal `@threenative/physics` entry with the `threenative-native`
condition. The source uses `rapier`, `RigidBody3D`, `Area3D`, and `CollisionShape3D`; bundle
inspection found no WebAssembly, Rapier WASM or Recast marker. Gradle built both Android
ABIs, and the explicit SDK `adb` installed and cold-launched the APK on `emulator-5554`.

```text
Gradle :app:assembleDebug                         BUILD SUCCESSFUL
adb install -r dist-native/minimal.apk           Success
am start -W com.mystral.engine/.MystralActivity  Status: ok; LaunchState: COLD
TN_NATIVE_SMOKE_READY:webgpu
TN_NATIVE_SMOKE_FIRST_FRAME
TN_NATIVE_SMOKE_300_FRAMES:300
```

The PRD-046 device scenario proves the normal public API's resting height, movement,
collision, and collision-mask behavior; wrong-height, wrong-gravity and wrong-mask variants
all fail. This is a source-workspace handoff, not a prebuilt packed-consumer proof. iOS and
physical Android hardware remain OPEN.

The packed no-toolchain handoff is now locally proven with the actual packed runtime tarball.
A local checksum server supplied the current stripped runtime + SDL libraries for both ABIs
and SDL's Java AAR. The Android SDK view contained no `ndk/` or `cmake/`; PATH shims for
CMake, Ninja, C/C++, NDK and Rust wrote no invocation log. Gradle selected the prebuilt lane
and produced the APK in 6 seconds. Its contents were inspected directly:

```text
lib/arm64-v8a/libSDL3.so                 2,071,384 bytes
lib/arm64-v8a/libmystral-runtime.so     20,207,232 bytes
lib/x86_64/libSDL3.so                    2,239,184 bytes
lib/x86_64/libmystral-runtime.so        21,787,664 bytes
native toolchain invocation log          absent
```

Both runtime libraries export `tn_physics_step`. The release workflow checks that symbol
before publishing and creates checksum entries for all five Android payloads. Its fresh
packed consumer exposes an SDK without NDK/CMake, builds normal, masked, and wrong-gravity
APKs, and prepares emulator outcomes for normal pass, wrong-height fail, mask-against-normal
fail, masked pass, and wrong-gravity fail. **That workflow/device evidence remains OPEN until
the release lane actually runs.** Physical Android hardware also remains OPEN.

The source-emulator gate also now fails closed on the APK asset itself. Generated game
assets moved from `src/main/assets` into Gradle's modeled
`build/generated/threenative/assets` root, and the verifier extracts
`assets/scripts/main.js` from the assembled APK before installation. A stale packaged asset
is rejected when its SHA-256 differs from the generated metadata. On 2026-08-09 the gate
passed twice on `emulator-5556`; the second build had `buildAndroidFirstProofBundle` and
`mergeDebugAssets` both `UP-TO-DATE`, while generated and packaged bytes still matched:

```text
bundle SHA-256  d515e339730d1a8c46d2e9b96f111b07bac2a924053fadf76303b5d7bdf38856
APK asset SHA-256
                d515e339730d1a8c46d2e9b96f111b07bac2a924053fadf76303b5d7bdf38856
300-frame marker, clean logs, screenshot, 3-second liveness             PASS
```

This repairs the source-build cache contract only. The released, checksum-locked consumer
and physical Android hardware evidence remain open. The shared `package-android.mjs` path
was also assembled with the import-free native-smoke bundle; its input and extracted APK
asset both had SHA-256
`49887ed8a4a8b6cffaf57ba26bf9154510263eff56c151a8219e73f6e080dfad`.

The second-wave targeted gate was rerun after adding that contract:

```text
@threenative/runtime-native test  PASS — 16 files, 55 passed, 30 skipped
pnpm typecheck                    PASS
native-release.yml parse         PASS — 7 jobs, all mobile lock/control keys present
pnpm budgets                      PASS — 51,756/50,000 native LOC review trigger reported (historical 2026-08-09 gate)
pnpm lint                         PASS — 370 files
pnpm test                         PASS — root Vitest 1,230/1,230 and 1,228/1,228
pnpm sync:agents --check          PASS — 11 source mirrors
```

The earlier full repository gate above and this second-wave gate are historical evidence from
2026-08-08/09. The current 2026-08-10 measurement is 61,617/50,000 native LOC; see the
[native LOC verification](native-loc-trigger-2026-08-10.md) for the command output, attribution,
and area table.

## Phase 5 — template surface and WASM honesty

All templates expose web, desktop, Android, and iOS build scripts. Their agent docs state
that normal physics selects native Rapier on Android, while published mobile consumption is
blocked on real prebuilt runtime assets; platformer is additionally blocked by Recast WASM.
Linux evidence is from a source machine, not a clean consumer. macOS, Windows, iOS, and
physical hardware are OPEN.
