# Genre sweep ledger — platformer — framework arm — 2026-08-16

Genre: platformer
Round: 9
Brief SHA-256: ca159854f59e5be00ee7306aed862335bfc9dec410a1c4ef91bed27d5505f0ef
Arm: framework
Proof result: 1/2
Proof SHA-256: 7cf3f69edef397bebc0e9eae66794cfb5b1a6306cc97ef9a161a6bc7d1eba26b
Template: starter
Archive: docs/benchmark/sweeps/platformer-2026-08-16
Framework version: 0.1.0
User source LOC: 2071
Source files: 20
Framework files: 8
Three-only files: 9
Reach rate: 0.4
Used exports: Area3D, AudioBus, CharacterBody3D, CollisionShape3D, DebugOverlay, GameCanvas, ICtx, IGame, IPhysicsContext, RigidBody3D, Scene, SceneFrame, acceptHotUpdate, defineGame, playtest, rapier, replay, useGameState
Unused exports: $, A, ANDROID_TRANSPORT_CAPABILITIES, AdbAndroidDriver, AnimationPlayer, Area3D, AreaEvent, AreaHandler, B, C, CanvasLayer, CharacterBody3D, CollisionShape3D, CollisionShapeKind, D, DeviceBridgeTransport, DeviceMailboxTransport, E, F, G, GPUParticles3D, H, I, IAndroidDriver, IAndroidDriverOptions, IAndroidPlaytestDependencies, IAndroidPointer, IAndroidPointerInjection, IArea3DOptions, IAreaContact, IAudioBusOptions, IAudioPlayOptions, IBridgeTransport, ICharacterBody3DOptions, ICollisionShapeHandle, IDeviceBridgeInstallation, IDeviceMailbox, IDeviceMailboxPaths, IDevicePlaytestDriver, IDevicePlaytestTarget, IDevicePlaytestTransport, IGameObservationContribution, IGameObservationSampleRequest, IGamePlatformSource, IGamePluginHooks, IGamePluginRuntime, IHotDiagnostics, IIntersectPointOptions, IIntersectRayOptions, IIntersectShapeOptions, IIosCommandOptions, IIosDriverOptions, IIosPlaytestDependencies, INavigationAgent3DOptions, INavigationContext, INavigationObstacle3DOptions, INavigationRegion3DOptions, IPathFollow3DOptions, IPathFollow3DProjection, IPathFollow3DSample, IPhysicsBodyCreateOptions, IPhysicsBodyHandle, IPhysicsCharacterOptions, IPhysicsColliderHandle, IPhysicsContext, IPhysicsHandle, IPhysicsInputSnapshot, IPhysicsOptions, IPhysicsPointQuery, IPhysicsQueryHit, IPhysicsRayHit, IPhysicsRayQuery, IPhysicsRotation, IPhysicsRuntimeSimulation, IPhysicsShapeDescriptor, IPhysicsShapeQuery, IPhysicsSimulation, IPhysicsVector3, IPhysicsWorldHandle, IPlaytestAssertionResult, IPlaytestBridgeClient, IPlaytestBridgeDescription, IPlaytestBridgeV1, IPlaytestCaptureProvenance, IPlaytestContactObservation, IPlaytestDeviceRequest, IPlaytestDeviceResponse, IPlaytestDiagnostic, IPlaytestDiagnosticsPolicy, IPlaytestFlagHelp, IPlaytestFramebufferCoverageAssertion, IPlaytestFramebufferCoverageObservation, IPlaytestGameplayObservation, IPlaytestObservationSnapshot, IPlaytestObservations, IPlaytestOptions, IPlaytestPathAssertion, IPlaytestPerformanceAssertion, IPlaytestPerformanceObservation, IPlaytestPointer, IPlaytestProtocolDiagnostic, IPlaytestReport, IPlaytestRuntimeDiagnosticsSample, IPlaytestSampleRequest, IPlaytestScenario, IPlaytestServerConfig, IPlaytestSetupRequest, IPlaytestSignalAssertion, IPlaytestStep, IPlaytestWorldObservation, IPlaytestWorldRuntimeAssertion, IPlaytestWorldRuntimeObservation, IPointHit, IRandom, IRawInputPointer, IRayHit, IRaycastOptions, IRenderAdvisorExamplePaths, IRenderAdvisorInput, IRenderAdvisorObservedInput, IRenderAdvisorObservedPass, IRenderAdvisorRecommendation, IRenderAdvisorReport, IRenderAdvisorSceneCollapseAggregate, IReplayRecording, IReplayRecordingSample, IRigidBody3DOptions, IRunIosCommand, IScenePickerOptions, IShapeHit, IStandalonePlaytestConfig, IStandalonePlaytestReport, IThreeNativeConfig, IThreePlaytestBridgeInstallation, IThreePlaytestBridgeOptions, IThreePlaytestEntity, IThreePlaytestPhysics, IThreePlaytestPhysicsBody, IThreePlaytestResources, IosTransportKind, J, JsonValue, K, L, M, MAX_PHYSICS_QUERY_RESULTS, N, NavigationAgent3D, NavigationAgentEvent, NavigationAgentHandler, NavigationObstacle3D, NavigationPlugin, NavigationRegion3D, O, P, PHYSICS_COLLISION_EVENT_STRIDE, PHYSICS_TRANSFORM_STRIDE, PLAYTEST_ASSERTION_REGISTRY, PLAYTEST_ATTACH_TIMEOUT_MS, PLAYTEST_BRIDGE_GLOBAL, PLAYTEST_CAPABILITY_REGISTRY, PLAYTEST_FLAGS, PLAYTEST_PHYSICS_BODY_LIMIT, PLAYTEST_PHYSICS_SAMPLE_LIMIT, PLAYTEST_PROTOCOL_LIMITS, PLAYTEST_PROTOCOL_VERSION, PLAYTEST_RUNNER_EXPECTED_GLOBAL, PathFollow3D, PhysicsBody3D, PhysicsDirectSpaceState3D, PhysicsPlugin, PhysicsQueryVector3, PhysicsShapeKind, PlaytestBridgeError, PlaytestCliUsageError, PlaytestScenarioError, PlaytestVec3, PlaywrightTransport, Q, R, Recording, ReplayPointer, RigidBody3D, RigidBodyType, S, STANDALONE_PLAYTEST_OBSERVATION_FIELDS, ScenePicker, ScheduleHandle, Scheduler, T, ThreeNativeOrientation, ThreePlaytestPhysicsRecorder, U, V, W, X, XcrunIosDriver, Y, Z, _, a, a0, a1, a2, a3, adviseThreeRenderWorkload, androidMailboxPaths, androidTouchBatches, assertJsonSafe, assertPortableState, b, boundedTeardownStep, buildReport, c, captureVisualSurface, connectDevicePlaytestBridge, connectPlaytestBridge, connectPlaytestBridgeTransport, createRandom, createReplayDriver, d, deviceMailboxPaths, discoverAdb, e, evaluateRichPlaytestAssertions, f, failedDiagnosticsAssertion, formatUsage, g, h, i, initStandalonePlaytest, installThreePlaytestBridge, interactionGroups, invalidScenario, j, jsonByteLength, k, l, loadPlaytestScenario, m, missingPlaytestCapabilities, n, o, openPageAndConnectBridge, p, pageLifecycleDiagnostic, parseAndroidConsole, parseLaunchedPid, parseReplayRecording, parseStandalonePlaytestArgs, playtestDiagnostic, playtestStepDrivesMovement, playtestStepHoldTicks, playtestStepWaitTicks, preflightDisplay, q, r, rapier, readPlaytestEndpoint, recast, recordToScenario, rejectUnknownKeys, requireAssertions, requiredPlaytestCapabilities, resolveDiagnosticsPolicy, rotatedTouchPosition, runAndroidPlaytest, runDevicePlaytest, runIosPlaytest, runStandalonePlaytest, s, t, u, unknownPlaytestCapabilities, v, validateDeviceEndpoint, version, w, writeCaptureProvenance, x, y, z
Measurement command: `pnpm sweep:measure docs/benchmark/sweeps/platformer-2026-08-16`
First game-code tool call: Tool call 16 of ~139 wrote `src/render/palette.ts`. The 15 before it read the brief, looked at the reference, scaffolded, and booted the project.
Visual result: LOSS — blind instrument score 3.0 against the other arm's 3.5, chosen against at high confidence. The critic named the reason: neutral grey cliffs and a distance fogged to near-white, against a reference of warm-toned brown rock. Evidence: `docs/benchmark/rounds/round-9/judge.json`.

The paired comparison, the column verdicts and the dispositions are in
`docs/verification/round-9-2026-08-15.md`. This file records what this arm built and what got in
its way.

## Playtest proof

Sealed proof 1/2 scenarios, 9/10 assertion rows. The only failure is `state.goalReached`, which the other arm also failed, so it cancels in the comparison. Both `diagnostics` rows passed with 0 console errors, 0 network errors and 0 runtime diagnostics. Captured headed on a real adapter — `nvidia / turing`, read by field name rather than through `JSON.stringify`, which returns `{}` for `GPUAdapterInfo`.

## Friction ledger

| API or surface | What blocked the build | Workaround | Evidence |
| --- | --- | --- | --- |
| `SceneCollapse` startup pass | Merged 2,642 source meshes into 26 and dropped every shadow: the scene rendered 2 casters while `renderer.shadowMap.enabled` read `true`. No API to opt out, exclude an object, or preserve the flags. | A `ctx.startup.whenReady()` traverse re-setting `castShadow` and `receiveShadow` on every mesh but the sky dome; casters went 2 to 28. | Confirmed in `packages/core/src/collapse.ts`: the merged meshes were built without either flag and the sources removed. Fixed this round, with `packages/core/__tests__/collapse.spec.ts` failing when the fix is reverted. |
| playtest `visibility` assertion | A registered entity whose `mesh` is a `Group` of body parts failed `TN_PLAYTEST_VISIBILITY_FAILED` in 5 of 9 scenarios while `movement` passed and the character was on screen at 20+ px. | `player.mesh` replaced with an invisible `Mesh` proxy at the capsule centre, the character rig parented under it. | Single-variable test: the hot-reload scenario went FAIL to PASS on that change alone. Deferred to round 10. |
| `templates/starter/src/render/sky.ts` | The dome is radius 90 and the fog far plane 80, so the whole dome sat past it and rendered as one flat fog-coloured wash; the authored vertex gradient never reached the screen. | `fog: false` on the dome material. | Read directly in the template. Fixed this round across all seven templates, with a `create-threenative` test that fails when the flag is removed. |
| `templates/starter` `test` script | All ten scenarios target `pnpm dev`, and seven failed `TN_PLAYTEST_PAGE_NAVIGATED` there while passing unchanged against `vite build` plus `vite preview`. | All scenario runs moved to build plus preview on a separate port. | Every `--server-command` in the template script is `pnpm dev`. Not changed this round: unreproduced on a quiet machine, and round 8 was corrected for promoting exactly that. |
| `threenative-playtest` default | Without `--headed` the run served WebGPU from SwiftShader even with the `webgpu` recipe: `capture.json` recorded `architecture: "swiftshader"` and the run produced 43 spurious console errors and 43 runtime diagnostics, with no warning in CLI output. | Always `--headed` under `sh scripts/xvfb.sh`. | The same scenario went FAIL to PASS with zero diagnostics. Deferred to round 10. |
| scaffolded `AGENTS.md` | Instructs `xvfb-run`, which fabricates exit 1 on a successful command on this host, and lists Chromium flags without `--enable-features=Vulkan` — documenting the SwiftShader configuration while its own text says a run that does not name its adapter is not evidence. | Ignored both; used `scripts/xvfb.sh` and added the flag. | Deferred to round 10. |
| `ICharacterBody3DOptions.gravity` | `gravity: 24` accelerated the character upward to `y=5.1, grounded:false`, indistinguishable from a collider bug on screen. The value is added to `velocity.y`, so down is negative, and the type is a bare `readonly gravity?: number`. | `const GRAVITY = -24`, found by dumping the entity registry from the page. | Deferred to round 10 as a doc-comment change. |
| `CollisionShape3D.capsule` | The body origin is the capsule centre, so a character modelled standing on its own origin floated exactly `halfHeight + radius` above every surface. | The visual rig is a child offset by `-(0.26 + 0.28)`. | Deferred to round 10 as a doc-comment change. |
| `IInputAction.down` | Overloaded: for `jump: { down: ["Space"] }` it means the keys that press the action; for a vector action it means the down direction. No discriminator in the type, and the template relies on an undeclared default `move` binding so there is no worked example. | Explicit `move: { up/down/left/right }`; the side effect is that `pressed("move")` is true whenever ArrowDown is held. | Deferred to round 10. |
| `TN_PLAYTEST_CONSOLE_ERROR` message | Says to open `console.json` in the artifact directory; with `--artifacts <dir>` that directory held only `after.png` and `capture.json`. The runner names an artifact it never writes. | Errors reconstructed from `capture.json`. | Deferred to round 10. |
| `templates/starter/playtests/seed.playtest.json` | Asserts `levelX` equals `-0.6056551518850029`, a starter internal, and is wired into the template's `pnpm test`; it cannot survive the user writing their own level. | Deleted, along with `restart`, `monitoring` and `look`, and `respawn` rewritten against the new level. | The literal is in the shipped file. Deferred to round 10 with the coverage question stated. |
