# PRD-132 — publint in every package gate

2026-08-17. Acceptance was run after `pnpm install --frozen-lockfile` and a clean package
build. All six non-private package manifests now run `publint --strict` from `scripts.test`.

## 1. Workspace package tests

Command:

```text
pnpm -r --if-present run test
```

Result: exit `0`. The relevant real output included a strict publint run for every package and
135 test files with 1,194 tests passing:

```text
Scope: 11 of 12 workspace projects
packages/playtest test: Running publint v0.3.22 for @threenative/playtest...
packages/playtest test: All good!
packages/core test: Running publint v0.3.22 for @threenative/core...
packages/core test: All good!
packages/runtime-native test: Running publint v0.3.22 for @threenative/runtime-native...
packages/runtime-native test: All good!
packages/physics test: Running publint v0.3.22 for @threenative/physics...
packages/physics test: All good!
packages/ui test: Running publint v0.3.22 for @threenative/ui...
packages/ui test: All good!
packages/create-threenative test: Running publint v0.3.22 for create-threenative...
packages/create-threenative test: All good!
packages/playtest test: Test Files  135 passed (135)
packages/playtest test: Tests  1194 passed (1194)
packages/playtest test: no orphans
```

`runtime-native` declares `@threenative/playtest` as a dev dependency because its existing
production-profile tests import playtest's built files. That makes pnpm run playtest's test
before runtime-native's test and removes the clean-worktree race observed on the first attempt.

## 2. Guard passes

Command:

```text
pnpm vitest run scripts/__tests__/publint-gate.spec.ts
```

Result: exit `0`.

```text
✓ scripts/__tests__/publint-gate.spec.ts (2 tests) 12ms
Test Files  1 passed (1)
Tests  2 passed (2)
```

## 3. Guard negative control

`publint` was temporarily removed from `packages/core/package.json` `scripts.test`, then the
guard was rerun.

Command:

```text
pnpm vitest run scripts/__tests__/publint-gate.spec.ts
```

Required red result: exit `1`, naming `@threenative/core`.

```text
 RUN  v4.1.10 /home/joao/projects/threenative/threenative-engine/.worktrees/prd-132-publint-r6

 ❯ scripts/__tests__/publint-gate.spec.ts (2 tests | 1 failed) 16ms
     × requires every non-private package test script to run publint
     ✓ requires an executable publint command rather than a text mention

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  scripts/__tests__/publint-gate.spec.ts > publint package gate > requires every non-private package test script to run publint
AssertionError: Packages missing publint in scripts.test: @threenative/core: expected [ '@threenative/core' ] to deeply equal []
Test Files  1 failed (1)
Tests  1 failed | 1 passed (2)
```

The manifest was restored immediately, and the same guard then returned exit `0`:

```text
✓ scripts/__tests__/publint-gate.spec.ts (2 tests) 12ms
Test Files  1 passed (1)
Tests  2 passed (2)
```

The restored manifest was checked again and the guard returned exit `0` with both tests passing.

## 4. Root test gate

Command:

```text
pnpm test
```

Result: exit `0`. The package gate output again included strict publint success for all six;
the final root Vitest summary was:

```text
Running publint v0.3.22 for @threenative/runtime-native...
All good!
Running publint v0.3.22 for @threenative/physics...
All good!
Running publint v0.3.22 for @threenative/ui...
All good!
Running publint v0.3.22 for create-threenative...
All good!
Test Files  135 passed (135)
Tests  1194 passed (1194)
no orphans
```

## Findings ledger

`publint --strict` emitted no diagnostics after each package build. No findings required a
fix or acceptance exception.

| Package | Strict result |
| --- | --- |
| `@threenative/core` | no diagnostics |
| `create-threenative` | no diagnostics |
| `@threenative/physics` | no diagnostics |
| `@threenative/ui` | no diagnostics |
| `@threenative/playtest` | no diagnostics |
| `@threenative/runtime-native` | no diagnostics |

## Additional checks

- `pnpm typecheck`: exit `0`.
- `pnpm exec biome check scripts/__tests__/publint-gate.spec.ts`: exit `0`.
- `pnpm lint`: exit `1` on pre-existing cognitive-complexity diagnostics across unrelated
  examples, core, and scripts files (202 warnings and Biome's diagnostic-count error). The new
  guard has no full-lint diagnostic; unrelated files were not changed.
