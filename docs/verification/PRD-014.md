# PRD-014 verification

Date: 2026-08-04

Status: AUTOMATED GATES PASS; browser scenarios remain unverified until the downstream
starter wiring is complete.

## Green checks

| Check | Result |
| --- | --- |
| `pnpm typecheck` | PASS — root, workspace packages, and `examples/abyss-framework` |
| `pnpm lint` | PASS — Biome checked 138 files |
| `pnpm test` | PASS — 93 files, 455 tests; package builds and publint passed |
| `pnpm budgets` | PASS — 7 packages, 2,524 framework LOC, 6 PRD files |
| `pnpm tsx scripts/count-loc.ts` | PASS — command completed |
| `pnpm vitest run scripts/__tests__/sync-agent-docs.spec.ts` | PASS — generated core mirror is synchronized |
| `git diff --check` | PASS |
| Closed-list check | PASS — `grep -c "AudioBus\|ctx.random\|ctx.after" packages/core/AGENTS.md` returned `3` |
| Capability parity | PASS — core playtest integration asserts `runtime.audio` and `runtime.world` are advertised with payloads |
| New scenario JSON | PASS — both starter scenarios parse through `loadPlaytestScenario` |

## Declared red controls

Each mutation was restored immediately after the failing focused test.

| Control | Observed failure |
| --- | --- |
| Wall-clock `after()` | `schedule.spec.ts`: callback fired on the first tick; expected `0`, received `1` |
| Skip scheduler clear on `goto` | `schedule.spec.ts`: leaked callbacks produced `3`, expected `0` |
| Tick while paused | `schedule.spec.ts`: position changed from `0.09999999999999999` to `1.100000000000001` |
| Accept non-finite delay | `schedule.spec.ts`: expected `after(NaN)` to throw, but it did not |
| Resolve tween every tick | `schedule.spec.ts`: promise resolved after the first partial tick |
| Play audio immediately | `audio.spec.ts`: queued voices were `0`, expected `1` |
| Skip voice stop | `audio.spec.ts`: voices remained `2`, expected `0` |
| Skip listener reparent | `audio.spec.ts`: listener parent was `null`, expected the camera |
| Ignore null audio buffer | `audio.spec.ts`: expected a throw, but playback was accepted |
| Advertise without audio capability payload | `playtest.spec.ts`: expected `runtime.audio`, but the capability was absent |
| Reseed from `Math.random` | `random.spec.ts`: first seeded draw differed between identical seeds |
| Fabricate an unconfigured seed | `random.spec.ts`: reported `0`, expected `null` |
| Allow `range(a, b)` where `b <= a` | `random.spec.ts`: expected a throw, but no error occurred |
| Return `undefined` from empty `pick()` | `random.spec.ts`: expected a throw, but no error occurred |

## Unverified environment checks

- Browser execution of `pause.playtest.json` and `seed.playtest.json` is pending until the
  starter pause/menu wiring and seeded starter scene are complete in the downstream lanes.
