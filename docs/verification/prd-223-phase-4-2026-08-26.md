# PRD-223 Phase 4 — `runScript` extraction

**Date:** 2026-08-26

**Host:** Linux x86_64, NVIDIA GeForce RTX 2080, Vulkan

**Result:** PASS

## Red-green characterization

Before extraction, the new source-ordering test failed both assertions:

```text
runScript call sequence: [] instead of the five required steps
setupHeadlessEnvironment must be defined
```

After extraction, `runScript` calls these phases exactly once and in this order:

1. `setupHeadlessEnvironment`
2. `createConfiguredRuntime`
3. `attachUiOverlayIfConfigured`
4. `wirePlaytestMailboxBridge`
5. `driveMainLoop`

The deliberate reorder control moved overlay attachment before runtime construction. The ordering
test failed with the changed sequence and passed again after restoration.

## Bounds and lifetime preservation

`runScript` is 25 lines. The five named steps are 10, 35, 25, 21, and 5 lines respectively. The
file is 1,666 lines, below the existing 1,800-line package contract.

The pure extraction preserves screenshot `_exit`, Android CPU-profile dumping, recorder-before-runtime
destruction, encoder-thread joining, debug-server shutdown, and mailbox-before-game-load ordering.
The overlay helper accepts the live runtime to encode its construction dependency in the call.

## Runtime negative control

The screenshot branch was temporarily limited to one frame while the gate still required 300. The
runtime exited successfully and the analyzer failed before pixel acceptance with:

```text
missing exact 300-frame completion
presented 1 times for 300 frames; expected exactly one present per frame
```

This proves the gate observes the frame-loop step rather than accepting a leftover screenshot.

## Green verification

Both native CLI configurations compiled:

- `build/tn-linux` with video disabled
- `build/tn-linux-contracts-video` with video enabled

The complete runtime-native Vitest suite passed:

```text
Test Files  77 passed (77)
Tests       549 passed | 34 skipped (583)
```

The full desktop command passed with `SDL_AUDIODRIVER=dummy` because this host's ALSA device reports
`Host is down`:

```sh
SDL_AUDIODRIVER=dummy pnpm native:verify:desktop
```

Observed results:

```text
desktop core gate passed: 300 frames, 1280x720
desktop physics playtest proof passed: 14 assertions
native contract lane passed: 22 of 22 targets
```

The desktop log names `NVIDIA GeForce RTX 2080` and the Vulkan backend; it is not SwiftShader. It
contains `TN_NATIVE_SMOKE_READY:webgpu`, `TN_NATIVE_SMOKE_FIRST_FRAME`, `Rendered 300 frames in
10857ms`, and `TN_PRESENTS:300`.

The inspected 1280×720 screenshot is non-blank and contains the expected blue/orange scene plus
the magenta canvas-layer overlay. Its recorded SHA-256 is
`7d02387b8e2e797342d78b7cdb1a8ac0403e3157f3d7fc8611c93fc435a63034`.

## Platform scope

Linux desktop executed. Windows, macOS, Android, and iOS were not executed in this phase and receive
no result from this record.
