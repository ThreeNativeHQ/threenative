# PRD-315 handoff — 2026-09-01

Session ran out of context mid-execution of
`docs/PRDs/authoring/PRD-315-wildwood-loads-fast-and-its-animals-belong-in-the-valley.md`.
The owner gave a free pass to deviate from the PRD's phase script; the goals stand. This file is
the resume point for the next agent (main lane) and for the virtual-shadow-map lane.


## Landed on `main` (engine repo)

| Commit | What | Proof |
|---|---|---|
| `ea1b83a0` | `packages/assets/src/gltf-io.ts`: one codec-aware glTF reader shared by the health report and the model pass. Fixes the production failure `TN_ASSETS_MODEL_UNREADABLE … "meshopt.decoder"` that killed Wildwood's initial compile. | `health.spec` "should measure a Meshopt-compressed model", `watch.spec` "byte-identical under models: none" — red on old health.ts, green now |
| `1330230d` | `raw-unreal` and `ueformat` package `test` scripts run Vitest (21 + 37 tests) before build/publint. | Negative control: winding flip disabled → both package commands red |
| `61c1c22c` | `assets.models.sharedImages: true` — each distinct embedded image written once under `public/shared/images/<key>.<codec>.<ext>`, GLB references it by relative uri (`packages/assets/src/passes/shared-images.ts`). Store keys on **source** bytes + settings; in-memory within a build, on disk across builds. Plus skip-before-apply: an input whose digest + pass configuration matches the previous manifest entry is reused without running passes. Undeclared-output guard exempts previous-receipt paths. | `shared-images.spec` (5), `compile.spec` "compile cache"; `packages/assets` 151/151 three consecutive runs |
| `da4648c1` | `assets.models.sharedImages` carried through the CLI validator seam (`packages/create-threenative/src/config.ts`, core `IThreeNativeModelsConfig`). | `config.spec` two new tests, red on unknown key → green |

Not yet run on main: `pnpm typecheck && pnpm lint && pnpm test` as a whole (only package-scoped
suites and biome on changed files). **Run the full gate before pushing.**

## Wildwood sandbox (`/home/joao/projects/threenative/sandbox/wildwood`, uncommitted)

- `tools/measure-startup.mjs` — cold-start measurement (5 cold contexts, headed WebGPU under
  `tools/capture-lock.sh`): markers, bytes, first-frame classification. Borrows `pngjs` from the
  installed playtest package. Baseline recorded in `artifacts/startup-baseline-lowtier.log` and
  `artifacts/startup/baseline-lowtier.json`:

  | run | valley built | startup ready | first non-white frame | total MB |
  |---|---:|---:|---:|---:|
  | 1 | 9540 | 26994 | 533 | 1113.9 |
  | 2 | 21872 | 45562 | 379 | 1113.9 |
  | 3 | 10483 | 28111 | 662 | 1113.9 |
  | 4 | 9339 | 26975 | 555 | 1113.9 |
  | 5 | 9287 | 26765 | 688 | 1113.9 |

  Adapter `nvidia | turing`. Served bytes were 4x source (stale `models: "none"` output from an
  older build). `TN_STARTUP_WARMUP` times out at 16.5 s with `compiled: 0` — readiness waits on a
  whole-scene `compileAsync` that never finishes inside the 15 s budget (see "engine follow-ups").
- `tools/compile-assets.mjs` — one-shot asset compile (bundles `threenative.config.ts` with
  esbuild, calls `compileAssets`).
- `package.json` repointed to fresh tarballs in `sandbox/.packages/`:
  `threenative-assets-0.3.0-shared-71f50dbd4e3c.tgz`, `threenative-core-0.3.0-shared-c6331c0b6aae.tgz`,
  `create-threenative-0.2.3-shared-e0e503bbee1b.tgz`. Installed with `npx pnpm@10.25.0 install`
  (system pnpm is 11.x; the sandbox was installed with 10.25).
- `threenative.config.ts` now `models: { sharedImages: true, textures: { overrides: [{ codec: "uastc", slot: "baseColorTexture" }] } }`
  and `textures: { overrides: [{ codec: "none", glob: "landscape/grass_bush_atlas_alpha.jpg" }] }`
  (that atlas is 1024x205, not a multiple of 4; unused by the game).
- `public/` stale bake output deleted (3.1 GB of hashed files; only the receipt-listed 41 and the
  hashed HDR were git-tracked). `.gitignore` only ignores top-level hashed files — extend it to
  `public/**/*.[hash].*`.
- `.mcp.json`, `screenshots/live/*` were already modified before this session; `playtests/pond.playtest.json` untracked before this session.

### Blocker to resolve first (main lane)

The fresh compile fails in the **packaged** assets dist but not from engine source:

```text
TN_ASSETS_PASS_FAILED: pass 'model' failed for 'fab/1ac647da-…/Materials/UnrealMaterialLibrary.glb':
Cannot read properties of null (reading 'getRoot')
  at listTextureSlots (@gltf-transform+functions@4.4.2 …)
  at textureBindings (assets dist)
```

Diagnosis so far: the sandbox holds **two** glTF-Transform versions (`4.4.2` via Wildwood's
`@gltf-transform/cli` devDependency, `4.5.0` pulled by the assets tarball's range), and
`functions@4.4.2` is being paired with a `core@4.5.0` document. The engine catalog pins 4.4.2.
Check `packages/assets/package.json` publishes exact pins (or that pnpm pack resolves `catalog:`
to `4.4.2`, not a caret), reinstall, and re-run `node tools/compile-assets.mjs` (expect several
minutes: 43 distinct images, ~25 s per 4-image model set measured before sharing; second run is
seconds). Then re-run `tools/capture-lock.sh node tools/measure-startup.mjs --runs 5 --label fresh --query "?lowtier"`.

### Then, in value order (main lane)

1. **Startup tiers** in `src/scenes/Valley.ts`: create the loading screen at the top of `load()`
   (ctx.canvasLayer is available there; the loop renders while held), load ground maps + stone
   maps + proof asset + a near-camera species subset as the critical tier, then flora niches,
   animals (parallel unique GLB loads in `spawnWildwoodAnimals.ts`), HDRI after `enter` under a
   scene generation token. `createFoliage` sows all niches at once — split it per niche.
   Gate: `playtests/startup.playtest.json` (diagnostics, resources on `valleyReady`, performance).
2. **Engine: startup warmup** (`packages/core/src/warmup.ts`, `game.ts` `startupCompile`): the
   "scene" granularity compiles the whole scene in one `compileAsync` and abandons it at 15 s.
   Measure per-object granularity with slices on the fresh build; report `TN_STARTUP_WARMUP`
   honestly; consider making `ctx.startup.progress` reflect asset/compile progress instead of the
   binary 0→1 (`ctx.assets` has no counters today — add `requested/settled`).
3. **Engine: playtest startup timing — DONE on `main` at `9802e46b`** (`92aa1c1a` re-pins the scaffold hashes). Core stamps `ctx.startup.timeline` (`loadStartedMs`, `enteredMs`, `compileSettledMs`, `readyMs`) and the bridge publishes it as `observations.startup.timeline`; scenarios assert `startup: { maxEnteredMs, maxCompileSettledMs, maxReadyMs }`, failing closed on a missing milestone. Wildwood needs the repacked `@threenative/core` and `@threenative/playtest` tarballs from this `main` for `playtests/startup.playtest.json` to use it; the PRD's "controllable valley in 2.5 s p95" becomes `startup: { maxEnteredMs: 2500 }`. Original note follows.
   There was no `startup` assertion kind. Add wall-clock
   fields (bridge install → scene entered → ready) to `IPlaytestStartupObservation` in
   `packages/playtest/src/protocol.ts` + core `playtest.ts`, and a `startup: { maxReadyMs }`
   assertion in `assertion-schema.ts` so startup is an observation, not a console anecdote.
4. **Animals**: run `sandbox/wildwood/tools/measure-animals.mjs`-style audit on the served
   `ue/Models/SK_*.glb` (importer report says version 45; source repo at
   `/home/joao/projects/threenative/threenative-asset-mcp` has ~1300 lines of *uncommitted* WIP
   modified today at 13:27–13:40 by another lane — do not stash or touch it). Decide whether
   `stripJunkTriangles` in `Animal.ts` is deletable by measuring triangle/material-group identity
   on the fresh pack. Measure model-forward vs displacement (`yawOffset` is 0 for all six, unproven).
   Then `rapier()` + `recast()` in `game.ts`, bake a land-only `NavigationRegion3D` excluding
   `LAKE` and `POND` (from `src/render/terrain.ts`), drive wander/flee via `NavigationAgent3D`
   (`getNextPathPosition`; the agent does not move the object itself).
5. HUD field map (`src/ui/Hud.tsx`, `style.css` palette is cyan/monospace), close-shadow lighting
   (consume the VSM lane's `VirtualShadowNode` once merged), 1024 m terrain via `TerrainTiles`
   only if the 190 m valley is healthy, marketplace props only through the A/B gate.
6. Engine-level follow-ups noticed: KTX2 UASTC output has no zstd supercompression (check
   `ktx2-encoder` options; 2K UASTC is ~5.3 MB per map, transfer-heavy); GLTFLoader gives every
   model its own GPU texture even when the file is shared — a URL-keyed texture cache in
   `ctx.assets` would cut VRAM 8x for the pines.

Verification records to write: `docs/verification/PRD-315-phase0.md` (baseline table above,
versions: engine `b14b27b9`→`da4648c1`, game `146e172`, asset MCP installed 0.7.0 / report 45),
and one file per later gate. Move the PRD to `docs/PRDs/done/` only when §6 holds.

## Virtual shadow map lane (subagent `vsm-lane-2`)

Worktree: `/home/joao/projects/threenative/threenative-engine/.claude/worktrees/agent-a5fb0a722c6a37f09`,
branch `worktree-agent-a5fb0a722c6a37f09`. Prototype from `/home/joao/Downloads/threenative-virtual-shadow-map.zip`
(WebGL2, three r160; owner's `comparison.png` showed blocky/offset shadows).

- Done there: commit `976293fa` "fix(vsm-prototype): right-handed light basis; every page rendered
  mirrored along u" — Part 1 (prototype diagnosis and fix).
- In progress: `packages/core/src/render/virtual-shadow-pages.ts` (renderer-independent page keys,
  pool/LRU, clipmap snapping, demand, invalidation) then `VirtualShadowNode extends ShadowBaseNode`
  (`three/webgpu`, TSL; precedent `examples/jsm/csm/CSMShadowNode.js`) so a game writes
  `light.shadow.shadowNode = new VirtualShadowNode(light, options)`. Mechanism only; stats getter;
  `TN_VIRTUAL_SHADOW` marker; `@situation` JSDoc for the capability manifest; templates AGENTS.md
  entry; headed-WebGPU proof (sphere over plane vs stock `ShadowNode`, changed-pixel ratio);
  `pnpm typecheck && pnpm lint`.
- The agent was asked to commit as it goes and to append its own status below when it stops. If
  it is gone, resume from the worktree's `git log` and `git status`; merge to `main` only after the
  gates above and a review against `packages/core/AGENTS.md` ("anything a screenshot shows" must
  not enter core — a shadow *mechanism* with all parameters from the light and options is the
  argued exception; state that argument in the commit).

### VSM lane status (appended by the lane)

**2026-09-01, written by the coordinating session (the subagent hit its session limit).**
Branch `worktree-agent-a5fb0a722c6a37f09`, worktree `.claude/worktrees/agent-a5fb0a722c6a37f09`:

- `cc5ac647` wip: the renderer-independent page module and its spec (28 tests), preserved.
- `835df973` `VirtualShadowNode extends ShadowBaseNode` in `packages/core/src/render/virtual-shadow.ts`:
  camera-centred clip levels, each a stock `ShadowNode` over a placeholder light, snapped to its
  own texel grid (DirectionalClipmap with a page per texel), cached until the window moves or a
  tracked caster changes inside it, selected per fragment by light-space distance. Exported from
  core's index with `@situation` JSDoc; starter `AGENTS.md` section "One shadow for a big outdoor
  level"; constraints guard lists the two files as mechanism. Spec: 7 tests, node env.
- `3f81df7b` fix + proof: a level is requested only after `updateBefore` placed it (the first cut
  rendered a blank map on frame one and three's per-frame guard kept it). Proof harness
  `packages/core/scripts/vsm-proof` on `nvidia | turing`: under/beside luminance stock 39/102,
  virtual 40/102, changed-pixel ratio 0.0002; six level renders in twelve frames, rest cached.
- **Landed on `main`** at `c19bba6c` (six commits cherry-picked; the lane's worktree carried an
  older lineage, so its branch was not merged as-is): `72101f16` prototype fix, `2856ff27` page
  module, `fd7263e3` VirtualShadowNode, `290594a3` first-frame fix + WebGPU proof, two format
  commits, then the manifest regeneration and scaffold re-pin on main. The Wildwood lane must
  rebase `codex/prd315-20260901` on this `main` before merging; no file overlaps.
- Gates on the branch as of `f63501b0`: `pnpm exec vitest run packages/core` 89 files, 905/905;
  `biome check --diagnostic-level=error .` clean; `tsc -p packages/core` clean. Full `pnpm test`
  on the branch started by the coordinator; merge to `main` after it is green.
- Root `pnpm exec vitest run` on the branch at `a0dde636`: 3214 passed, 4 skipped; `scaffold.spec`
  byte-stability re-pinned for the manifest growth (same as the ueformat re-pin);
  `verify-golden-path` and `generated-shooter-input` pass in isolation (load flakes);
  `scripts/__tests__/quality-json.spec.ts` fails on `main` as well — seven new `as unknown as`
  casts in `packages/core/src/water-surface.ts` and `wave-field.ts` from the earlier water work,
  not this branch. Root `pnpm test` aborts earlier in `runtime-native` tests that need the
  compiled host, which the worktree lacks.
- Not done: the sparse page atlas (page pool and demand pass are ported and tested but unused —
  a page render re-traverses the scene, so levels were chosen first); native conformance case;
  full `pnpm test` on the branch (core build there fails only on the missing engine-mcp dist);
  Wildwood consumption in `src/render/lighting.ts`.
- Usage: `sun.shadow.shadowNode = new VirtualShadowNode(sun, { clipExtents: [12, 40, 120] })`;
  `trackCaster(animal.object)` for movers; `TN_VIRTUAL_SHADOW` reports rendered vs cached.

## Coordinator notes

- 2026-09-01 late afternoon: the handed-off lane (`codex/prd315-20260901`) fixed the packaged-dist
  blocker (`1240d7a5 isolate packed gltf-transform graph`) and Wildwood's fresh bake completed:
  `public/shared` 86 MB, `public/fab` 76 MB, manifest present, against 1.1 GB served before. No
  further commits on that branch since; the Wildwood tree has 14 uncommitted changes. A fresh
  cold-start measurement (`tools/measure-startup.mjs --label fresh-lowtier`) was started by the
  coordinator; its table belongs in `docs/verification/PRD-315-phase0.md` next to the baseline.
- Watch item: the lane's standalone texture cap (`cb4853c0`) must not downscale the four ground
  normal maps below what the close camera needs; check the sizes in the manifest before judging
  the look.
- 2026-09-01 ~16:45, **course correction for the Wildwood lane**: `threenative.config.ts` now
  declares `assets.textures.maxSize`, which the lane's branch validates in
  `packages/create-threenative/src/config.ts` and types in `packages/core/src/config.ts` — but the
  sandbox still installs the coordinator's `create-threenative-0.2.3-shared-e0e503bbee1b.tgz` and
  `threenative-core-0.3.0-shared-c6331c0b6aae.tgz`, so `pnpm dev` dies at
  `TN_CONFIG_UNKNOWN_KEY: assets.textures.maxSize is not recognised` and the fresh startup
  measurement could not run. Fix on that lane: `pnpm --filter create-threenative build` and
  `pnpm --filter @threenative/core build` on the branch, `pnpm pack` both into
  `sandbox/.packages/`, rename with a content hash, repoint `package.json`, `npx pnpm@10.25.0
  install`, then `tools/capture-lock.sh node tools/measure-startup.mjs --runs 5 --label fresh-lowtier
  --query "?lowtier"`. The same seam bit twice today; the config.spec seam test on that branch
  should also exercise `textures.maxSize` end to end through `compileAssets`.
- The lane's five commits (`1240d7a5`…`1a1c5157`) are assets-only plus the two config files, all
  with tests; nothing conflicts with the VSM branch. Merge order when both are gated: assets lane
  first, VSM branch second.
- 2026-09-01 ~17:40: `main` now carries VirtualShadowNode (`c19bba6c`) and the startup
  observation (`9802e46b`, `92aa1c1a`). The Wildwood lane's last commit is still
  `1a1c5157`/`fix(assets): declare all shared image candidates` at 16:44 and its sandbox still
  cannot start (see the config-key correction above); if it stays idle, the next agent should take
  the Wildwood lane over: rebase its branch on `main`, repack core, assets, playtest and
  create-threenative from the rebased tree, reinstall, run `tools/compile-assets.mjs` (cache
  makes it seconds), measure, then the startup tiers.

