# PRD-359 task 8b — recall and core instructions — 2026-09-06

This is implementation evidence for the recall corpus and core authoring instructions. The row
remains pending an independent read-only review checkpoint.

## Caller and source

- `scripts/fixtures/capability-recall/corpus.json:502-525` separates the positive transport query
  from authoritative replication and client prediction.
- `scripts/fixtures/capability-recall/budget.json:115-130` pins the 60-row corpus and two
  game-owned networking rows.
- `scripts/__tests__/capability-recall.spec.ts:58-72` is the named ownership regression.
- `packages/core/AGENTS.md:22-32` documents the shipped subpath, defaults, overrides, delivery
  guarantees, reference server and unsupported native status; `packages/core/CLAUDE.md:24-34` is
  its generated mirror.

## Red

Commands:

```sh
pnpm caps:recall --json
pnpm exec vitest run scripts/__tests__/capability-recall.spec.ts
```

The pre-repair recall gate exited `1` (`/tmp/prd359-red-capability-recall-gate.json`): its pinned
not-owned mapping still expected `request.multiplayer=networked-multiplayer`, and the two new
rows were absent. The stale ownership assertion was also collected in the focused test run
(`/tmp/prd359-red-capability-recall.spec.log`).

## Green

Commands:

```sh
pnpm exec vitest run scripts/__tests__/capability-recall.spec.ts packages/engine-mcp/__tests__/search.spec.ts
pnpm caps:recall
pnpm sync:agents
```

All exited `0`.

- The focused suites passed 62/62 tests (`/tmp/prd359-green-capability-recall-focused.log`).
- Recall passed with 60 rows, 53/60 distinct-symbol recall, 58/60 actionable rows, 0 unresolved
  results and 16 reject hits (`/tmp/prd359-green-capability-recall-gate.log`).
- `pnpm sync:agents` regenerated the core `CLAUDE.md` mirror; the source/mirror check is included
  in the repository gate.

The two out-of-scope queries remain explicit not-owned rows; the positive transport query recalls
`connect` and is no longer answered by the old blanket multiplayer guidance.
