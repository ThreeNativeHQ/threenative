# PRD-359 Task 3a quiche artifact preflight — 2026-09-05

The pinned release is `quiche-0.24.6-3` from
`https://github.com/mystralengine/library-builder/releases`. The six requested non-iOS
download URLs generated by `packages/runtime-native/scripts/download-deps.mjs` all returned
HTTP 302 from GitHub; followed downloads completed with curl exit 0. The GitHub release API
returned HTTP 200 and supplied the digests below. Local `sha256sum` matched every digest.

| Lane | Exact asset URL suffix | SHA-256 | ABI/layout checked |
| --- | --- | --- | --- |
| Windows x64 | `quiche-win-x64.zip` | `8177451669222e4c22839e6194842004fe17c56dc6a6a62d106b1d6f7a7c54a1` | `quiche.lib`; first member x86-64 COFF |
| Linux x64 | `quiche-linux-x64.zip` | `f8d7114f5344c47b9c94df2db946751d5545c46efa13939755a106bfc0455a04` | `libquiche.a`; first member ELF x86-64 |
| macOS arm64 | `quiche-mac-arm64.zip` | `bfb75867965a3f13a740bd3ce886b424023a778abda4462779953bd7da24ff4b` | `libquiche.a`; first member Mach-O arm64 |
| macOS x64 | `quiche-mac-x86_64.zip` | `570053e970802de5dbb4fd66bc605b6da789c34033d1199f6f85661a72a70da0` | `libquiche.a`; first member Mach-O x86_64 |
| Android arm64 | `quiche-android-arm64.zip` | `6e7958d93cc718fe42db93435c5e07b7a44dc202f1a9a166b5e112f0bdf48b34` | `libquiche.a`; first member ELF AArch64 |
| Android x86_64 | `quiche-android-x64.zip` | `9203da07df5f0d6a3d53273d374c9c095f8769b34a0d56ef356dcaa286fab27a` | `libquiche.a`; first member ELF x86-64 |

Every archive contains exactly `include/quiche.h` plus the platform library expected by
`CMakeLists.txt`. The release digests and bytes are proven; downloader checksum enforcement is
merely unconfigured: the `quiche` and `quiche-android` entries have no `sha256`, so
`downloadFile()` does not verify them. `install-prebuilt.mjs` covers runtime release artifacts,
not quiche.

Matrix gaps remain: macOS x64 release/preset execution is unresolved even though its quiche
archive exists; Android still needs both physical arm64 and x86_64 emulator qualification. iOS
remains owner-deferred: device arm64 and x86_64 simulator assets are published, while arm64
simulator is absent as documented. No non-iOS requested quiche asset is missing.

Evidence commands were bounded HEAD/API requests, six direct GETs into
`/tmp/networking-359-artifact-preflight`, `sha256sum`, `unzip -Z1`, `llvm-ar`, and `file`; no
downloader, build, publish, or source mutation ran.

The coordinator independently ran `sha256sum /tmp/networking-359-artifact-preflight/quiche-*.zip` (exit 0); all six digests matched the table. No platform execution is inferred from archive availability or object-file inspection.
