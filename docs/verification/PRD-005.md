# PRD-005 verification — 2026-08-02

## Implemented gates

| Gate | Result | Evidence |
|---|---|---|
| Framework Abyss port builds | PASS | `pnpm --filter abyss-framework build` |
| Framework TypeScript | PASS | `pnpm --filter abyss-framework typecheck` |
| Framework interaction scenario | PASS | `play.playtest.ts`: start, ArrowRight + Space, score `1` |
| Vanilla control browser smoke | PASS | `pnpm test:browser`: 1 passed, non-black frame |
| LOC classifier fixture | PASS | `count-loc.spec.ts`: 3 tests |
| README freshness | PASS | `pnpm tsx scripts/count-loc.ts --check` |
| Blind identifier removal and shuffle | PASS | `score-blind.spec.ts`: 3 tests |
| Prompt mismatch void | PASS | `validatePromptHash("sealed", "edited")` returns `void` |

The CI `benchmark` job calls the classifier, builds both examples, runs the harness
tests, runs the frozen vanilla browser check, and runs the framework playtest.

## Full local gates

| Command | Result |
|---|---|
| `pnpm install --frozen-lockfile` | PASS |
| `pnpm lint` | PASS, 105 files |
| `pnpm typecheck` | PASS |
| `pnpm test` | PASS, 38 files / 80 tests |
| `pnpm build` | PASS |
| `pnpm test:browser` | PASS, 1 test |
| `pnpm exec playwright test --config=benchmark.playwright.config.ts` | PASS, 1 test |
| `pnpm budgets` | PASS, 7 packages / 1,427 framework LOC / 6 PRDs |

The browser output contains the known Dawn/WebGPU backend messages already allowed by
the frozen control (`Instance dropped in popErrorScope` and the 720-byte mapped buffer
message). They do not fail either browser assertion.

## Static comparison

`README.md` is generated from the explicit framework port manifest:

- Vanilla frozen control: 410 physical lines, 138 plumbing / 272 game.
- Framework Abyss port: 412 physical lines, 87 plumbing / 325 game.
- Static result: vanilla wins the total-line row.

## Actual experiment result

`docs/benchmark/RESULTS-2026-08-02.md` records `VOID`. No external model run supplied
the six authoritative final usage events, and no blind human scoring session occurred.
The local browser checks are apparatus verification only, not fabricated benchmark
repeats. The prompt hash recorded in the result is:

`366bb64f3e7464e4a6615840366a8d2b225a19bb5fe2e112d596215b7f1e3ef2`

Because the result is void rather than a quality or cost loss, no §12 abandon action is
claimed.
