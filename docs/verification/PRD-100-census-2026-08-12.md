# PRD-100 Phase 0 census — 2026-08-12

This is the unmodified-tree measurement for PRD-100. The source PRD was supplied in the
repository main worktree and was copied into this lane before implementation evidence was
recorded.

## Constructor-threaded callback census

Command:

```sh
rg -n --glob '*.{ts,tsx}' 'onCollect|onStomp|onTouch|#releaseAsset' \
  packages/create-threenative/templates examples/abyss-framework/src
```

Raw output:

```text
examples/abyss-framework/src/scenes/TerrainProbe.ts:27:  readonly #releaseAsset: () => boolean;
examples/abyss-framework/src/scenes/TerrainProbe.ts:34:    this.#releaseAsset = () => ctx.assets.release("model", this.#assetPath);
examples/abyss-framework/src/scenes/TerrainProbe.ts:83:    this.#releaseAsset();
packages/create-threenative/templates/platformer/src/entities/Pickup.ts:17:  constructor(ctx: GameCtx, at: Vector3, onCollect: () => void) {
packages/create-threenative/templates/platformer/src/entities/Pickup.ts:40:      onCollect();
packages/create-threenative/templates/platformer/src/entities/Patrol.ts:26:    onStomp: () => void,
packages/create-threenative/templates/platformer/src/entities/Patrol.ts:27:    onTouch: (fromX: number) => void,
packages/create-threenative/templates/platformer/src/entities/Patrol.ts:55:        onStomp();
packages/create-threenative/templates/platformer/src/entities/Patrol.ts:56:      } else onTouch(this.mesh.position.x);
```

The constructor-threaded gameplay callback count is three parameters in two entities, all in
the platformer template: `Pickup.onCollect`, `Patrol.onStomp`, and `Patrol.onTouch`.
`TerrainProbe.#releaseAsset` is an entity-owned cleanup handle, not a callback threaded through
a constructor. The platformer also has the only hand-written `runtime.events` bridge in the
census. That is one distinct template, not the three distinct games or templates required by
the Signal ship bar.

## Deferred-removal census

Command:

```sh
rg -n --glob '*.{ts,tsx}' \
  'const (pickups|patrols|pearls|hunters)|removeCollected|removeDefeated|splice\(' \
  packages/create-threenative/templates examples/abyss-framework/src
```

Raw output:

```text
packages/create-threenative/templates/platformer/src/playtest-events.ts:14:  return pendingEvents.splice(0);
examples/abyss-framework/src/scenes/Abyss.ts:174:    const pearls: Pearl[] = [];
examples/abyss-framework/src/scenes/Abyss.ts:183:    const hunters: Hunter[] = [];
packages/create-threenative/templates/platformer/src/scenes/Level.ts:71:    const pickups: Array<{ id: string; value: Pickup }> = [];
packages/create-threenative/templates/platformer/src/scenes/Level.ts:72:    const patrols: Array<{ id: string; value: Patrol }> = [];
packages/create-threenative/templates/platformer/src/scenes/Level.ts:85:    const removeCollected = (): void => {
packages/create-threenative/templates/platformer/src/scenes/Level.ts:90:          pickups.splice(index, 1);
packages/create-threenative/templates/platformer/src/scenes/Level.ts:94:    const removeDefeated = (): void => {
packages/create-threenative/templates/platformer/src/scenes/Level.ts:99:          patrols.splice(index, 1);
packages/create-threenative/templates/platformer/src/scenes/Level.ts:150:      removeCollected();
packages/create-threenative/templates/platformer/src/scenes/Level.ts:151:      removeDefeated();
```

The two independent deferred-removal loops are `removeCollected` and `removeDefeated` in
the platformer level. The Abyss arrays are unrelated scene-local render/gameplay cleanup and
do not remove registered entities through `Registry`.

## Hand-written group-query census

Command:

```sh
rg -n --glob '*.{ts,tsx}' 'filter\(|find\(|some\(|every\(' \
  packages/create-threenative/templates examples/abyss-framework/src
rg -n --glob '*.{ts,tsx}' \
  'filter\([^\n]*(tag|kind|type)|find\([^\n]*(tag|kind|type)|tags?\s*===|kind\s*===' \
  packages/create-threenative/templates examples/abyss-framework/src
```

Raw output:

```text
examples/abyss-framework/src/replay-proof.ts:34:    position.some((value) => typeof value !== "number" || !Number.isFinite(value)) ||
packages/create-threenative/templates/starter/src/scenes/Play.ts:117:      entityCount: Object.keys(ctx.entities.snapshot()).filter((id) => id !== "hud").length,
packages/create-threenative/templates/starter/src/scenes/Play.ts:129:      entityCount: Object.keys(ctx.entities.snapshot()).filter((id) => id !== "hud").length,
packages/create-threenative/templates/starter/src/scenes/Play.ts:175:        entityCount: Object.keys(frameCtx.entities.snapshot()).filter((id) => id !== "hud").length,
packages/create-threenative/templates/platformer/src/render/touch-controls.ts:102:    const left = [...pointers.values()].find((pointer) =>
packages/create-threenative/templates/platformer/src/render/touch-controls.ts:177:    return [...pointers.values()].some(
packages/create-threenative/templates/platformer/src/render/postprocessing.ts:15:  if (renderer.kind !== "webgpu") return;
packages/create-threenative/templates/starter/src/render/postprocessing.ts:15:  if (renderer.kind !== "webgpu") return;
```

There are zero hand-written group queries outside the Phase 2 per-type arrays. The starter
filters exclude the HUD by registry id, and the touch-control queries inspect pointers, not
entities. Groups therefore fail the predeclared `>=3` bar and are KILL under rule 1; Phase 3
is deleted and no `Registry.group()` API is added.

## Baseline gates on the unmodified tree

### `pnpm test:templates`

Raw output:

```text
> threenative@0.1.0 test:templates
> xvfb-run -a -s '-screen 0 1600x900x24' tsx scripts/verify-template-playtests.ts

Progress: resolved 201, reused 150, downloaded 7, added 154, done
.../@threenative/runtime-native install: Prebuilt release manifest fetch failed for 'linux-x64': HTTP 404.
.../@threenative/runtime-native install: Failed

> minimal@ pretest
> playwright install chromium

> minimal@ test
> vite build && threenative-playtest --scenario playtests/play.playtest.json --url http://127.0.0.1:4173 --server-command "pnpm dev --host 127.0.0.1 --port 4173 --strictPort"

vite v8.2.0 building client environment for production...
✓ built in 2.54s
{
  "diagnostics": [
    {
      "code": "TN_PLAYTEST_RUNNER_FAILED",
      "message": "page.evaluate: Execution context was destroyed, most likely because of a navigation.",
      "severity": "error"
    }
  ],
  "pass": false
}
Error: pnpm test exited 2.
Node.js v20.19.6
 ELIFECYCLE  Command failed with exit code 1.
```

Exit: `1`. The failure is in the pre-existing scaffold runner/environment path after the
minimal template build; it is retained here as the required baseline, not treated as a
feature result.

### `pnpm test:playtest`

Raw output:

```text
> threenative@0.1.0 test:playtest
> pnpm --filter @threenative/playtest build && xvfb-run ... node packages/playtest/dist/runner/cli.js examples/abyss-framework/playtest/moves.json ...

ESM Build success in 2268ms
DTS Build success in 16904ms
Running publint v0.3.22 for @threenative/playtest...
All good!
{
  "artifactDirectory": ".../artifacts/playtest",
  "diagnostics": [
    {
      "code": "TN_PLAYTEST_BRIDGE_MISSING",
      "message": "Scenario requires semantic capabilities but '__THREENATIVE_PLAYTEST_BRIDGE__' is not installed.",
      "severity": "error"
    }
  ],
  "pass": false,
  "scenario": "framework-movement",
  "target": "web",
  "url": "http://127.0.0.1:5180"
}
 ELIFECYCLE  Command failed with exit code 2.
```

Exit: `2`. The existing abyss-framework scenario is missing its semantic bridge before this
PRD's changes.

### Baseline generated-source measurement

```text
$ pnpm tsx scripts/count-loc.ts
suggested framework normalised baseline: 432 (current baseline 441)
platformer template LOC: 1486
```

## Phase 0 verdict

| Candidate | Measurement | Verdict |
|---|---:|---|
| Signal | 3 callback parameters plus 1 event bridge, all in the platformer template | KILL; the `>=3` distinct-game/template bar was not met |
| `queueFree` | 2 independent deferred-removal loops | SHIP |
| Groups | 0 hand-written group queries outside those arrays | KILL; Phase 3 deleted |
