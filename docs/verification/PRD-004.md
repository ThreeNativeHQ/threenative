# PRD-004 verification evidence

This file records local verification for `docs/PRDs/PRD-004-scaffold.md`.

| Gate | Result | Negative control observed red? |
|---|---|---|
| CLI templates | PASS — `minimal` and `starter` accept target/template/no-install flags and write the §9b tree | missing-template and non-empty-target assertions fail; no destructive overwrite is allowed |
| generated render ownership | PASS — lighting, post, and materials are ordinary generated Three.js with zero `@threenative/*` imports | source assertion would fail on a framework import |
| UI state bridge | PASS — `useGameState` selector tests cover 600 writes and unselected-key stability; React receives the throttled core store | removing selector usage changes the render-count assertion |
| generated playtest | PASS — template scenario contains movement, score, and diagnostics assertions; player calls the real input map | removing `ctx.input.vector("move")` fails the scenario-source assertion |
| local install/build | PASS — packed local core, physics, and UI archives install into a fresh starter; `pnpm build` completes | unpublished registry dependencies caused a red smoke before peer/archive wiring was fixed |
| headless scaffold boot | PASS — generated starter canvas boots, HUD changes `0` to `1`, player moves from `x=-2.00` to `x=4.10`, and page errors remain empty | broken movement or pickup wiring leaves the score at `0` |
| package budgets | PASS — UI and CLI remain within the repository caps | `pnpm budgets` is the enforcing gate |

## Regular gates

```text
pnpm install --frozen-lockfile  PASS — all 8 workspace projects resolve from the lockfile
pnpm typecheck                  PASS — root, core, create-threenative, playtest, physics, and ui
pnpm lint                       PASS — Biome checked 95 files
pnpm test                       PASS — 36 files, 74 tests
pnpm build                      PASS — Vite, tsup, and publint; abyss-framework builds
pnpm test:browser               PASS — frozen abyss-vanilla control, 1 test
pnpm budgets                    PASS — 7 packages, 1422 framework LOC, 6 PRD files
```

## Integration proof

```text
caller census: create-threenative CLI is wired by .github/workflows/ci.yml scaffold-smoke
caller census: generated Play.ts calls setupLighting, setupPost, and createMaterials
caller census: generated App.tsx mounts GameCanvas and Hud reads useGameState
ownership proof: src/render contains no @threenative imports
visual checkpoint: generated starter is a playable dark scene with lit floor, player, pickup, and HUD
```

The browser environment used WebGL2 fallback, producing only expected adapter and
Rapier initialization warnings. The explicit SVG favicon prevents the browser’s
default `/favicon.ico` request from appearing as an application error.
