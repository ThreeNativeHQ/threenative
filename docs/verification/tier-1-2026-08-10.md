<!-- schemaVersion: 1 -->

# Tier 1 reliability ledger — 2026-08-10

- PRD: `PRD-064`
- Status: **Tier 1 not reached**
- Host: Linux x86_64; desktop runtime `packages/runtime-native/build/tn-linux/mystral`
- Android device: `emulator-5554`, booted and online through `adb`

Tier 1's proposed license is one portable build that runs on browser WebGPU, desktop
Linux/macOS/Windows, and the Android emulator, with iOS builds and packages. This ledger does
not activate that license: iOS and physical hardware were not executed, Android was non-green,
and the Phase 4 performance gate was not reached.

## Target results

| Target | Command | Pass | Fail | Blocked | Exit | Outcome |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| Browser | `pnpm parity -- --target web` | 67 | 0 | 0 | 0 | Executed and green; the multitouch row passed. |
| Desktop Linux | `TN_RUNTIME=packages/runtime-native/build/tn-linux/mystral pnpm parity -- --target desktop --reference artifacts/conformance/web --out .runtime/prd064/desktop-full` | 65 | 1 | 1 | 1 | Executed; the camera-parented overlay failed GPU validation, and desktop multitouch is explicitly excluded in the registry. |
| Android emulator | `pnpm parity -- --target android --device emulator-5554 --reference artifacts/conformance/web --out .runtime/prd064/android-full-2` | 27 | 40 | 0 | 1 | Executed over all 67 rows with a real pass/fail split; non-green, with no device-blocked rows. |

The desktop `blocked: 1` cell is not silent: `90-multitouch-input` carries the
`desktop-multitouch-input` exclusion, owner `PRD-064`, and a reason in
`packages/runtime-native/conformance/registry.json`. The desktop failure was
`25-camera-parented-overlay` with GPU validation errors caused by mismatched depth and color
attachment sizes after the renderer resize. The Android multitouch row failed on its merits;
the emulator was executed, not counted as blocked.

## Controls

| ID | Control | Result | Exit | Evidence |
| --- | --- | --- | ---: | --- |
| `phase-1-drop-pointer` | Drop one pointer from the browser multitouch proof. | Observed red: `multitouch proof timed out`. | 1 | `TN_MULTITOUCH_DROP_POINTER=1 TN_MULTITOUCH_TIMEOUT_MS=1000 TN_BROWSER_TIMEOUT_MS=10000 xvfb-run -a -s '-screen 0 1600x900x24' node packages/runtime-native/conformance/run-conformance.mjs --target web --only-tests 90-multitouch-input --out .runtime/prd064/negative-multitouch-final` |
| `phase-2-excluded-pass` | Claim the excluded desktop multitouch row as a pass. | Observed red: `90-multitouch-input: desktop-multitouch-input is excluded for target desktop`. | 1 | `node --input-type=module -e 'import { readFileSync } from "node:fs"; import { validateReport } from "./packages/runtime-native/conformance/run-conformance.mjs"; const registry=JSON.parse(readFileSync("./packages/runtime-native/conformance/registry.json","utf8")); const results=registry.tests.map(({id})=>({id,status:"blocked"})); const row=results.find(({id})=>id==="90-multitouch-input"); Object.assign(row,{status:"pass",browser:{completed:true,uniform:false},native:{completed:true,uniform:false},metrics:{pixelMismatchRatio:0,perceptualDeltaE:0},gpuValidationErrors:[]}); const report={schemaVersion:"0.2.0",registrySchemaVersion:registry.schemaVersion,threeVersion:registry.threeVersion,mode:"execution",target:"desktop",summary:{pass:1,fail:0,blocked:results.length-1,planned:0,validated:0},results}; const errors=validateReport(report,registry); if(errors.length===0) throw new Error("negative control unexpectedly passed"); console.error(errors.join("\\n")); process.exitCode=1;'` with a synthetic pass report; the runner validator rejected it. |
| `phase-3-missing-device` | Run Android with no listed AVD. | Observed red: `TN_PARITY_ANDROID_DEVICE_BLOCKED: Android device emulator-prd064-missing was not listed by adb devices.` | 1 | `pnpm parity -- --target android --device emulator-prd064-missing --only-tests 90-multitouch-input --out .runtime/prd064/negative-android-device` |
| `phase-4-slow-web` | Slow the web render path and require `TN_PROD_PERFORMANCE_BUDGET`. | **UNVERIFIED**; the production profile entry point is absent. | 254 | `pnpm profile:production` → `ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL Command "profile:production" not found` |
| `phase-4-slow-native` | Slow only native and require distinct process/artifact identities. | **UNVERIFIED**; the production profile entry point is absent. | 254 | Same `pnpm profile:production` setup failure; no parity performance claim recorded. |
| `phase-4-startup` | Delay the first non-blank frame and require `TN_PROD_STARTUP_BUDGET`. | **UNVERIFIED**; the production profile entry point is absent. | 254 | Same `pnpm profile:production` setup failure; no cold-start claim recorded. |

The browser positive control passed `90-multitouch-input` after the input fix, and the full
browser matrix was `67/0/0`. The desktop overlay positive outcome was not reached because its
capture still failed GPU validation. Android completed a 67-row run, but its positive matrix was
`27/40/0`, so no Tier 1 performance or release claim is made.

## Gates

| Gate | Exit | Result |
| --- | ---: | --- |
| `pnpm typecheck` | 0 | Passed after building the existing `@threenative/physics` and `@threenative/ui` workspace outputs required by the fresh checkout. |
| `pnpm lint` | 0 | Passed; Biome checked 394 files. |
| `pnpm test` | 0 | Passed; root Vitest 84 files / 629 tests, runtime-native 36 files / 187 passed / 30 skipped, physics parity Vitest 24 passed, and Rust parity 1 passed. |
| `pnpm budgets` | 0 | Passed hard invariants; reported the existing native runtime review trigger at 61,868 / 50,000 LOC and 5,980 / 15,000 framework LOC. |

## Ledger schema check

The schema check was run after this file was written:

```sh
pnpm exec vitest run --config packages/runtime-native/vitest.config.ts --dir packages/runtime-native packages/runtime-native/tests/tier-1-ledger.test.mjs
```

Observed output: `Test Files 1 passed (1); Tests 2 passed (2); exit 0`.

## Verdict

**Tier 1 not reached.** The browser multitouch contract is green, desktop multitouch is
dispositioned rather than silently blocked, and Android is now an executed 67-row result. The
desktop overlay failure, Android merit failures, and three unverified Phase 4 controls remain
open. This document makes no mobile-readiness claim.
