# PRD-247 repair verification — 2026-08-28

**Status: PASS for the repaired web and unit paths.** Native execution is unverified: this lane
has no attached device, and the generated-project native lane cannot find a Linux x64 prebuilt
runtime.

## Red controls

Each control was run with the defect present, then the source was restored before the green run.

1. The original overhead billboard consumer used `lockAxis: "y"`. The focused probe exited `1`:

   ```text
   {"dot":0.004999937501171852,"front":[...],"expected":[...]}
   Error: overhead nameplate is edge-on
   ```

   The same camera/rotated-parent case passes after the nameplate uses the unrestricted billboard.

2. Removing `for (const billboard of billboards) billboard.update();` from the shooter consumer
   made `packages/create-threenative/__tests__/shooter-per-item.spec.ts` exit `1` with `1 failed,
   2 passed`. The restored source passes with `3 passed`.

3. Removing `composeCameraShake(camera, shakeOffset);` made the same focused suite exit `1` with
   the structural assertion failing and the shaken camera retaining zero position delta. The
   restored source passes with the full position and rotation assertion.

4. The old runtime-cost test has no draw-call or frame-time comparison. This limitation probe
   exited `1`:

   ```text
   Error: runtime-cost test does not compare draw calls or frame time against the PRD parent
   ```

   The replacement evidence is the parent/current packaged-starter comparison below. Before the
   billboard module-load allocations were removed, that comparison also exited `1`:

   ```text
   {"currentLength":3665138,"parentLength":3665111,"firstDifference":1251335}
   Error: normalized starter app bundle differs
   ```

## Green unit and package evidence

```text
pnpm exec vitest run packages/core/__tests__/billboard.spec.ts packages/create-threenative/__tests__/shooter-per-item.spec.ts
Test Files  2 passed (2)
Tests       8 passed (8)
exit 0
```

The final focused gate expanded to six files (billboard, camera shake, sprite animator, shooter
consumer, runtime-cost, and scaffold) and exited `0` with `56 passed`. Before refreshing the
intentional scaffold hashes for the generated capability-reference change, that gate exited `1`
at `scaffold.spec.ts:308` with the old action-rpg hash expected and the new hash received; the
hash refresh is the only scaffold-fixture change.

The focused assertion covers an overhead camera and rotated parent, the unrestricted shooter
consumer and its update caller, plus both position and rotation shake composition. `pnpm build`
also exited `0` and regenerated the capability manifests/reference after the public example
changed to the unrestricted billboard form.

## Generated shooter playtest

Command:

```text
node packages/playtest/dist/runner/cli.js \
  /home/joao/.cache/tn-prd247-repair-trash-20260828/tn-prd247-shooter-w5dhIu/shooter/playtests/per-item.playtest.json \
  --project /home/joao/.cache/tn-prd247-repair-trash-20260828/tn-prd247-shooter-w5dhIu/shooter \
  --url http://127.0.0.1:5191 \
  --artifacts /home/joao/.cache/tn-prd247-repair-trash-20260828/tn-prd247-shooter-w5dhIu/per-item-final-green \
  --browser-recipe webgpu --headed \
  --server-command 'pnpm dev --host 127.0.0.1 --port 5191 --strictPort'
```

Result: exit `0`, WebGPU adapter `nvidia/turing`, no console or network errors,
`nameplateFacingCamera=1` at `sprite-baseline`, `cameraShakes=1` at `hit`, pickup frame changes,
hit signal, movement, and visibility all passed. The inspected non-blank capture is:

`/home/joao/.cache/tn-prd247-repair-trash-20260828/tn-prd247-shooter-w5dhIu/per-item-final-green/after.png`

The capture shows the arena and readable `TARGET` nameplates. The generated-project removal
control exited `1` after deleting only the billboard update loop; the decisive result was:

```text
resource.state.nameplateFacingCamera.atSteps: expected 1, value 0, pass false
cameraShakes, pickupFrameChanges, hit, movement, visibility: pass
```

The temporary loop was restored after the control.

Native probes were explicit: `adb devices` exited `0` with no devices listed, and generated
`pnpm build:desktop` exited `1` after producing the web/native bundles because the package lacked
`prebuilt/linux-x64/threenative-runtime`.

## Unused-starter cost comparison

The parent was commit `7ac47850f56352e198d6f36438193d1849966ed4`; the current tree was compared
against it. Both fresh packaged `starter` projects built with `pnpm build` and exited `0`. The
comparison measured the production application JavaScript after replacing only content-hash asset
names, plus the shared `three.core` asset byte-for-byte:

```json
{
  "currentApp": "index-ENmOe0QT.js",
  "parentApp": "index-D42snZhI.js",
  "currentAppBytes": 3665225,
  "parentAppBytes": 3665225,
  "normalizedApplicationBytesIdentical": true,
  "sharedThreeCoreBytesIdentical": true
}
```

The paired WebGPU scene checks reported `7 draw calls` and `1,301 triangles` for both parent and
current starters on the NVIDIA adapter. `profile:starter` sampled a 3-second RAF window after
warm-up with vsync and frame-rate limiting disabled:

```text
parent:  frames 1865, mean 1.608847 ms, p50 1.300000 ms, p95 2.000000 ms, p99 2.800000 ms, max 97.800000 ms, slow 8, exit 0
current: frames 1893, mean 1.585103 ms, p50 1.300000 ms, p95 1.900000 ms, p99 3.000000 ms, max 78.400000 ms, slow 8, exit 0
```

These frame-time numbers are runtime samples, not a claim of byte-identical timing. The evidence
supports identical unused application bytes and observed draw-call counts; frame-time equality is
not asserted because the browser scheduler and GPU load vary between runs. The existing
`template-runtime-cost.spec.ts` remains a vector-allocation sentinel and is not presented as
draw-call or frame-time evidence.

## Required gate record

```text
pnpm typecheck  -> exit 0
pnpm budgets    -> exit 0
changed-file biome check -> exit 0
git diff HEAD^ HEAD --check -> exit 0
```

For completeness, the repository-wide commands were also run. `pnpm lint` exited `1` on existing
unrelated cognitive-complexity diagnostics in `examples/abyss-framework`, `examples/engine-load-test`,
`examples/native-smoke`, `packages/assets`, and existing core tests; the changed-file Biome command
above exited `0`. `pnpm test` exited `1` in its pre-test documentation check on these existing
broken links: `docs/PRDs/done/PRD-228-the-pixel-budget-is-the-engines.md` and the two PRD-232
references to it. Neither failure is in the PRD-247 repair files.

`pnpm sync:agents` was not needed because no `AGENTS.md` file changed. Native remains unverified
for the limitation stated at the top of this record.
