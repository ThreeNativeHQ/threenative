# PRD-359 task 5a evidence

Date: 2026-09-06  
Branch: `networking-359`  
Base commit: `4dc62f08` (task 5a changes are the pending working-tree snapshot)

## Scope

Task 5a wires the example's build-time networking configuration to the extracted
`createNetworkingGame` caller. The default has no networking configuration, so the
existing smoke test remains offline. An enabled configuration contains only public
endpoints, room/player identifiers and no credential; the runtime obtains the staged
grant from `networking-session.json`, exchanges it at the configured issuer, and passes
the returned credential to `@threenative/core/net`.

## Red / diagnosis

The negative build used an HTTP endpoint in an otherwise valid enabled configuration:

```text
Error: THREENATIVE_NETWORKING_CONFIG: endpoint must be an HTTPS URL without credentials or a fragment
```

Vite failed while loading `vite.config.ts` and exited 1. The temporary invalid fixture
was removed after the run.

## Green checks

Default, disabled package test:

```text
pnpm --filter threenative-native-smoke test
exit 0
dist/native-smoke.js  7,050.57 kB
Verified native-smoke.js (7050571 bytes), one file with no imports
```

Enabled configuration build:

```text
THREENATIVE_NETWORKING_CONFIG=/home/joao/projects/threenative/threenative-engine/.worktrees/networking-359/examples/native-smoke/networking.config.example.json \
  pnpm --filter threenative-native-smoke build
exit 0
dist/native-smoke.js  7,050.71 kB
```

The enabled bundle contains the configured `https://localhost:4433/game` endpoint,
`https://localhost:8443/token` issuer and `networking-proof` room. It contains no
`__TN_NETWORKING_CONFIG__` placeholder and no credential. Its SHA-256 is
`861f9bee11a13f02d96dbdf5370c7f4e113dfb383be873dba9ea576e70d03b13`.

The enabled bundle also passes the existing native-bundle gate:

```text
node examples/native-smoke/scripts/verify-bundle.mjs
exit 0
Verified native-smoke.js (7050712 bytes), one file with no imports
```

Source checks:

```text
pnpm --filter threenative-native-smoke typecheck
exit 0
git diff --check
exit 0
```

## Caller and limits

`examples/native-smoke/src/game.ts:260` constructs the injected networking game;
`examples/native-smoke/src/game.ts:322`, `:485` and `:522` call `enter`, `update` and
`exit` on the existing scene lifecycle; `:600` routes the existing `restart` UI intent
to `retry`. `examples/native-smoke/src/networking-game.ts:124` reads the staged grant,
`:127` exchanges it with the issuer, and `:178` calls the shared transport at the
configured `/game` endpoint. No credential is stored in the example configuration.

The live two-client gameplay, peer observations, server simulation and adverse lifecycle
proof remain Task 5b/7a work; this row proves the build-time opt-in and its fail-closed
operator boundary only.
