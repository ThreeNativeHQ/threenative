# PRD-035 verification

Status: **implementation delivered; isolated Brave/WebGPU consumer gate passes; full suite/manual release evidence pending**.

Commits merged into `docs/opportunity-areas-prds`:

- `3b27b8a` — hot-reload lifecycle, state carrier, template wiring, diagnostics, and gates.
- `90baf3a` — read-only review repairs: helper self-registration and starter restart reset.

## Green gates

| Gate | Result |
|---|---|
| `pnpm typecheck` | PASS — root and all workspace/example typechecks |
| `pnpm lint` | PASS — Biome checked 314 files |
| `pnpm test` | PASS — 135 files, 941 tests; package builds, publint, and no orphans |
| `pnpm budgets` | PASS — 7 packages, 3,939 framework LOC, 6 PRD files, largest template 1,200 LOC |
| `pnpm sync:agents --check` | PASS — 10 mirrors |
| `pnpm tsx scripts/count-loc.ts --check` | PASS |
| `pnpm visuals` | PASS — minimal, starter, and platformer structure/parity floor |

## Browser evidence

The real scaffolded starter run performed ten source edits and passed the state-preservation
and leak assertions: reload count 10, one navigation entry, one canvas, flat scene/entity/
physics counts, zero resting audio voices, stable player position, and heap threshold.

The same checked-in test now passes on an isolated Brave 150.1.92.143 WebGPU runner using
real X11 display authorization: `1 passed (21.3s)`. It observed ten real HMR writes, one
navigation entry, flat scene/entity/physics counts, stable score and player position, zero
resting audio voices, heap within threshold, and zero console/page errors. The test waits
for the pickup instead of assuming a fixed frame count, which keeps the consumer assertion
valid on the host's low-FPS WebGPU path.

The prescribed full `pnpm test:browser` command remains unrun because an unrelated existing
server owns port 4173. Headless Chromium still emits WebGPU backend errors before the first
edit, so it is not used as the supported-browser result.

## Review and remaining evidence

Linchpin read-only review requested two code changes; both were repaired in `90baf3a` and
verified with 38 focused tests plus a second real Vite run. The review's remaining items are
evidence gaps, not claimed passes: jump-height/fall-rate measurement, malformed-state browser
overlay/full-reload behavior, observed-red source-revert controls, and the full browser suite.

The PRD stays in `docs/PRDs/` and the roadmap remains open until the full browser suite,
manual checkpoints, and observed-red controls are complete.
