# PRD-002 verification evidence

This file records local verification for `docs/PRDs/PRD-002-core.md`.

| Gate | Result | Negative control observed red? |
|---|---|---|
| fixed loop: 60 updates/s | PASS — virtual 1-second clock produces exactly 60 updates | covered by the loop regression assertion; no source mutation run |
| fixed loop: catch-up clamp | PASS — a 10-second stall produces exactly 5 updates | covered by the loop regression assertion; no source mutation run |
| renderer: WebGL2 fallback | PASS — absent `navigator.gpu` returns the injected WebGL2 renderer and resizes it | covered by the fallback assertion; no source mutation run |
| input: keydown → vector | PASS — synthetic `KeyA` produces `(-1, 0)` | covered by the input regression assertion; no source mutation run |
| input: blur clears | PASS — synthetic blur returns the move vector to `(0, 0)` | covered by the input regression assertion; no source mutation run |
| assets: cache + load order | PASS — repeated model requests share one promise; `enter` follows awaited `load` | covered by the asset/load-order assertions; no source mutation run |
| state: throttle + latest value | PASS — 600 writes notify 10 times in one second and publish `599` | covered by the state regression assertions; no source mutation run |
| core visual constraint | PASS — source grep is empty; core source is 662 lines | covered by `constraints.spec.ts` |

## Regular gates

```text
pnpm install --frozen-lockfile  PASS — all 5 workspace projects resolve from the lockfile
pnpm typecheck                  PASS — root, core, and playtest checks
pnpm lint                       PASS — Biome checked 34 files
pnpm test                       PASS — 13 files, 34 tests
pnpm -r build                   PASS — Vite, tsup, and publint; abyss-framework builds
pnpm test:browser               PASS — frozen abyss-vanilla control, 1 test
pnpm budgets                    PASS — 4 packages, 662 framework LOC, 6 PRD files
```

## Browser integration proof

The framework example was served with Vite and opened in Chromium using the same
WebGPU launch flags as the control. The smoke check observed one visible canvas and
the HUD score changing from its initial value to `1` after 1.5 seconds. The only page
error was Chromium/Dawn's known `Instance dropped in popErrorScope` diagnostic.

```text
caller census: defineGame in examples/abyss-framework/src/main.ts
caller census: InputMap, createRenderer, and createGameStore are wired through core/game.ts
visual grep: no matches in packages/core/src
```
