# PRD-006 verification — 2026-08-02

## Entity registry gates

| Gate | Result | Evidence |
|---|---|---|
| Resolve by name | PASS | `entities.spec.ts`: `get("player")` returns the same object |
| Duplicate name throws | PASS | second `add("player", ...)` throws |
| `debug()` beats `autoFields` | PASS | snapshot matches the explicit debug object exactly |
| Mesh does not explode snapshot | PASS | ≤24 fields and no nested `geometry` |
| Vector `.toArray()` support | PASS | `Vector3` becomes `[1, 2, 3]` |
| Scene exit clears | PASS | captured registry becomes `{}` after `game.stop()` |
| Overlay rows | PASS | 2 entities × 2 fields render as 4 body rows |
| Overlay polling | PASS | fake-timer test stays ≤11 polls per second |
| Core ECS vocabulary grep | PASS | no `archetype`, query, component, or system identifiers |
| Production strip | PASS | framework `dist/` has no `DebugOverlay` or `__THREENATIVE__` |
| Browser state bridge | PASS | Playwright reads `player.hull`, `position`, and `score` |

## Full gates

| Command | Result |
|---|---|
| `pnpm lint` | PASS, 112 files |
| `pnpm typecheck` | PASS |
| `pnpm test` | PASS, 42 files / 100 tests |
| `pnpm build` | PASS |
| `pnpm test:browser` | PASS, frozen vanilla control |
| `pnpm exec playwright test --config=benchmark.playwright.config.ts` | PASS, framework state/overlay/playtest |
| `pnpm budgets` | PASS, 7 packages / 1,598 framework LOC / 6 PRDs |

The browser output retains the pre-existing Dawn/WebGPU backend messages, which are
explicitly filtered by the control and framework playtests; the interaction assertions
remain green.
