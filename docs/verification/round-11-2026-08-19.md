# Improvement round ledger — round 11 — paired template visual AB — 2026-08-19

Round: 11
Date: 2026-08-19
Framework commit: before requested `937085e1` (unavailable after the repository history rewrite); after `709ac6bb002e40f55ac9a500b39dab7d85b928b7` (`HEAD`)
Framework version: 0.1.0
Genres: template-visual
Budget: one paired seven-template capture, one shuffled duplicate-calibrated bundle, and three model raters
Stop condition met: none
Next action: keep the five dispositions below as the round result; repair only the two re-opened gaps in a later round and do not convert this model score into the required human baseline claim

This is a template-only before/after run, not a framework-versus-vanilla game pair. The round-ledger
parser requires two condition arms named `framework` and `vanilla`, so those names below mean
before and after respectively. No functional or authoring-cost conclusion is drawn from the
schema-only column row.

## Arms

| Genre | Arm | Archive | Brief SHA-256 | Proof SHA-256 | Proof passed/total | Instrument visual | User LOC | Source files | Reach rate |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| template-visual | framework | docs/verification/visuals/ab-2026-08-19/before | abf9eb1ebfdd8ae86ab67682754826d6fe383b0e367e9b6f21854e6207e01aed | abf9eb1ebfdd8ae86ab67682754826d6fe383b0e367e9b6f21854e6207e01aed | 7/7 | unmeasured | n/a | 7 | n/a |
| template-visual | vanilla | docs/verification/visuals/ab-2026-08-19/after | abf9eb1ebfdd8ae86ab67682754826d6fe383b0e367e9b6f21854e6207e01aed | abf9eb1ebfdd8ae86ab67682754826d6fe383b0e367e9b6f21854e6207e01aed | 7/7 | unmeasured | n/a | 7 | n/a |

The paired bundle is `docs/verification/visuals/ab-2026-08-19/blind/bundle.json`, with 16 samples:
seven before frames, seven after frames, and two duplicate pairs. The prompt hash above is the
bundle's `promptSha256`; it is used in both condition rows because this run has no sealed gameplay
brief or proof arm.

The requested before object `937085e1` cannot be resolved locally: `git cat-file -t 937085e1`
returned `fatal: Not a valid object name 937085e1`, `git fetch --no-tags origin 937085e1` returned
`fatal: couldn't find remote ref 937085e1`, and the GitHub commit lookup returned HTTP 422
`No commit found for SHA: 937085e1`. The reachable pre-round-10 source-equivalent is
`250d5b2511b9f25b849441c3284bd2657bc7f844`, the parent of the round-10 source commit
`acabc39d9ef9f543b132b8780489084f737eafc7`; the before PNGs were freshly recaptured from that
detached worktree with the same harness. The old templates needed a temporary, capture-only
`@threenative/studio` `0.1.0` to `0.2.0` registry compatibility substitution; no template source
was changed in this lane.

Both conditions were captured headed under Xvfb with
`--ozone-platform=x11 --enable-unsafe-webgpu --disable-gpu-sandbox --ignore-gpu-blocklist
--enable-features=Vulkan`. The after capture command was `sh scripts/xvfb.sh pnpm visuals:baseline`
from detached `HEAD`; the before capture used `sh scripts/xvfb.sh pnpm exec tsx scripts/template-baseline.ts`
from the detached source-equivalent because that old commit did not yet carry the package script.
Each produced seven non-blank 1280x720 PNGs.

## Column verdicts

| Genre | Functional | Visual | Cost | Verdict |
| --- | --- | --- | --- | --- |
| template-visual | tie | tie | tie | schema-only row; this AB has no functional or cost column |

## Gap list

| # | Genre | Column | What vanilla did better | Evidence | Smallest next action |
| --- | --- | --- | --- | --- | --- |
| 1 | template-visual | Visual and native parity | The shooter improved above resolution, but the platformer movement was within the instrument's error bar after Shape A removed its geometry HUD | `score.json`: shooter +2 WIN, platformer −1 INDETERMINATE; `packages/create-threenative/templates/platformer/AGENTS.md:118-122` documents the native HUD loss | Decide whether a portable native HUD is needed; do not silently restore a second web HUD |
| 2 | template-visual | Visual | Neither defense +1 nor racing −1 exceeded the measured resolution | `score.json`: both rows INDETERMINATE; `after/defense.png` and `after/racing.png` are the paired frames | Re-open only with a new paired measurement or a clearly visible defect |
| 3 | template-visual | Visual | Platformer moved −1, exactly at the resolution, so the fog change is not measurable here | `score.json`: platformer 3 → 2, INDETERMINATE; `after/platformer.png` is the frame | Re-measure with a higher-resolution instrument before changing the fog |
| 4 | template-visual | Visual | Starter lost two points, above resolution | `score.json`: starter 4 → 2, LOSS; `after/starter.png`; `packages/create-threenative/templates/starter/src/scenes/Play.ts:66-69` still creates `sculptureMesh` | Leave open for the next round; no repair in this measurement lane |
| 5 | template-visual | Visual and correctness | Minimal stayed at 2, which the instrument cannot resolve, while its source still advertises `ITEMS` | `score.json`: minimal 2 → 2, INDETERMINATE; `after/minimal.png`; `packages/create-threenative/templates/minimal/src/scenes/Play.ts:26-29` still calls `createHud` | Leave the HUD/scene contradiction open for a later template change |

## Dispositions

| Gap # | Disposition | Measurement disposition | 20-line verdict | Named live caller | PRD | Reason if rejected |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | user space | INDETERMINATE | shooter closed above resolution; platformer unresolved | `templates/shooter`, `templates/platformer` | none | Source Shape A is present, but the platformer's −1 is at MDE and its desktop/native HUD cost is real |
| 2 | user space | INDETERMINATE | unresolved | `templates/defense`, `templates/racing` | none | Both deltas are at or under MDE; source presence of a sky helper is not treated as proof of a visible result |
| 3 | user space | INDETERMINATE | unresolved | `templates/platformer` | none | The platformer delta is exactly −MDE; the frame cannot distinguish a fog improvement from rater variance |
| 4 | user space | RE-OPENED | re-opened | `templates/starter` | none | The −2 LOSS is resolvable and the after frame still has the torus-knot source caller |
| 5 | user space | RE-OPENED | re-opened | `templates/minimal` | none | The score is indeterminate, but the after frame and source still show the `ITEMS` HUD contradiction |

### Gap 1 native HUD fact

Shape A was taken in the source before this round: `packages/create-threenative/templates/platformer/src/scenes/Level.ts`
has no `createHud` call, while `packages/create-threenative/templates/platformer/src/ui/App.tsx:12`
renders the React `<Hud game={game} />`. Therefore the platformer claims desktop but has **no HUD on
native desktop** after gap 1; the template's own `AGENTS.md:118-122` states that fact and tells a
user to add a native instanced-geometry HUD if needed.

Visual MDE: 1

The three raters were fresh read-only model raters, not human participants. The six duplicate
observations across the three raters measured MDE 1. This run makes no claim about the human blind
session required by `docs/product/VISUAL-BASELINE.md`.

## Visual deltas

| Template | Before | After | Δ | Verdict |
| --- | --- | --- | --- | --- |
| action-rpg | 4 | 3 | −1 | INDETERMINATE |
| defense | 2 | 3 | +1 | INDETERMINATE |
| minimal | 2 | 2 | 0 | INDETERMINATE |
| platformer | 3 | 2 | −1 | INDETERMINATE |
| racing | 3 | 2 | −1 | INDETERMINATE |
| shooter | 2 | 4 | +2 | WIN |
| starter | 4 | 2 | −2 | LOSS |

The measured aggregate is over the two resolvable rows only: mean after **3.00**, with **1 of 2**
counted rows at the 4/5 floor. Five rows are excluded as `INDETERMINATE`; this is a model score,
not a seven-template human quality-floor result.

## Negative controls (observed red)

| Check | Deliberate failure | Observed result |
| --- | --- | --- |
| Adapter identity | Omit `--enable-features=Vulkan` from the headed WebGPU probe | Exit 1; `adapter.info` reported `vendor=google`, `architecture=swiftshader`, so the run was rejected |
| Duplicate calibration | Build the same seven-template bundle with `--duplicates 0 --raters 1`, then score it | Exit 2; `TN_VISUAL_AB_NO_DUPLICATE_PAIR: the bundle carries no duplicate pair, so its resolution cannot be measured and no score may be reported.` |
| Delta resolution | In a synthetic ledger, change `defense` from `INDETERMINATE` to `WIN` while keeping Δ `+1` and MDE `1` | Exit 1; the validator rejects a sub-MDE delta claimed as a result |
| Gap coverage | In a synthetic copy, remove one of the five disposition rows | Exit 1; the validator rejects the ledger because every gap must have exactly one disposition |
| Template source firewall | Compare the pre-round-10 source to `HEAD` as a deliberately broad control | Exit 1 with historical template diffs; the scoped current-worktree check is separately green and no template source is changed by this lane |
| Bundle fail-closed path | Run the no-verdict bundle before supplying rater files | Exit 2; `TN_VISUAL_AB_NO_VERDICT` requires the requested three verdict files before scoring |

## Gates

| Gate | Command | Result | Evidence |
| --- | --- | --- | --- |
| Typecheck | `pnpm typecheck` | pass | exit 0 after building the local core, playtest, physics, and ui package artifacts required by the fresh worktree |
| Lint | `pnpm lint` | pass | exit 0; 235 pre-existing complexity warnings |
| Test | `pnpm test` | pass | exit 0; 161 JavaScript test files and 1,491 tests passed, native runtime 48 files with 318 passed and 37 skipped |
| Budgets | `pnpm budgets` | pass | exit 0; review triggers reported 15,025/15,000 framework LOC and 78,289/50,000 native LOC |

## Firewall attestation

| Rule | Held? | Evidence |
| --- | --- | --- |
| Before and after captured in separate detached worktrees | yes | before source-equivalent at `250d5b25`; after at `709ac6bb`; each produced seven PNGs |
| Raters did not see the reveal before scoring | yes | verdict files were supplied only after the blind bundle and reveal were inspected for evidence collection |
| Adapter gate held | yes | both headed probes read `vendor=nvidia`, `architecture=turing`; the omitted-Vulkan negative probe read SwiftShader and was rejected |
| No template source was written in this lane | yes | `git diff -- packages/create-threenative/templates` is empty; only the docs file and AB artifacts are in scope |

## Notes

The bundle and score are the durable evidence: `blind/bundle.json`, `reveal.json`, the three rater
files, and `score.json` under `docs/verification/visuals/ab-2026-08-19/`. The score command exits 1
because the resolvable starter row is a LOSS; that is an observed measurement, not a harness error.

The exact requested before commit remains unresolved after the capture attempt. The fallback is
labelled rather than silently renamed, so a later owner can replace the before set if the rewritten
commit is recovered. No acceptance claim is made that the fallback is byte-identical to `937085e1`.
