<!-- schemaVersion: 1 -->

# Tier 1 — the desktop lane, measured with provenance — 2026-08-15

PRD-076 Phases 1 and 2, desktop half. This is the **third** desktop measurement, and the first
whose numbers can be traced back to the report they came from. It supersedes neither predecessor
by assertion — it reproduces one of them and fails to reproduce the other, and says which.

**It covers the desktop lane only.** The browser lane was not re-run and the Android emulator lane
was not run at all. No mobile-readiness, physical-device or iOS claim is made, and **Tier 1 is not
claimed reached** by this file.

## Target results

| Target | Command | Pass | Fail | Blocked | Exit | Outcome |
| --- | --- | --- | --- | --- | --- | --- |
| Desktop Linux | `node packages/runtime-native/conformance/run-conformance.mjs --target desktop --out /home/joao/projects/threejs-webgpu/packages/runtime-native/.runtime/prd076-desktop-2026-08-15/report.json` | 66 | 0 | 1 | 2 | one declared exclusion, no failures |

Exit `2` is correct and is not a regression: `reportExitCode` returns `2` whenever
`blocked > 0`, and one row is excluded by the registry. **The desktop lane cannot exit `0` while
that exclusion stands**, which is the whole subject of PRD-077.

```console
$ TN_RUNTIME=$PWD/packages/runtime-native/build/tn-linux/mystral \
    sh scripts/xvfb.sh node packages/runtime-native/conformance/run-conformance.mjs \
    --target desktop --out …/prd076-desktop-2026-08-15/report.json
{
  "wrote": ".../prd076-desktop-2026-08-15/report.json",
  "target": "desktop",
  "mode": "execution",
  "summary": { "pass": 66, "fail": 0, "blocked": 1, "planned": 0, "validated": 0 }
}
```

## Provenance — the thing neither predecessor has

`REPORT_SCHEMA_VERSION` `0.3.0`.

| Field | Value |
|---|---|
| commit | `0358c025` |
| dirty | **`true`** |
| runtime SHA-256 | `c7fb272289b05aa7…` |
| reference-set SHA-256 | `25599d6ea462ea32…` |
| blocked row | `90-multitouch-input`, `TN_PARITY_ROW_EXCLUDED: desktop-multitouch-input` |

**`dirty: true` is a real limitation and is recorded rather than tidied away.** Another agent works
in this checkout concurrently and had uncommitted edits to `packages/core`, `packages/physics`,
`packages/playtest` and several templates while this ran. None of them is in the desktop
conformance path, and the runtime binary is hashed and predates them — but the tree was not clean
and a run that says otherwise would be lying about what it measured.

## What this settles, and what it does not

### The r2 desktop summary reproduces exactly

`parity-2026-08-10-r2.md` records `66 / 0 / 1`. This run records `66 / 0 / 1`, with the same single
blocked row. **On the numbers, r2 is right.**

### The r2 desktop exit cell is still impossible, and this run is why it matters

r2 records exit **`0`** for a `66 / 0 / 1` summary. This run produced the same summary and exits
`2`, because that is what the rule computes:

```console
$ pnpm parity:ledger docs/verification/parity-2026-08-10-r2.md
FAIL docs/verification/parity-2026-08-10-r2.md
  Desktop Linux / Exit: records exit 0, but a summary of 66 pass / 0 fail / 1 blocked exits 2.
  The runner cannot emit the recorded value.
```

Phase 0 adjudicated that cell from the code. This is the same verdict from a live run: the summary
was transcribed correctly and the exit code beside it was not. **One hand-typed cell in an
otherwise accurate ledger** — which is the argument for generating them.

### The tier-1 desktop row does not reproduce

`tier-1-2026-08-10.md` records `65 / 1 / 1` with `25-camera-parented-overlay` failing on GPU
validation, *mismatched depth and colour attachment sizes after resize*. That row passes here with
zero GPU validation errors, and it is the resize path that ran:

```console
$ node -e "<read the report>"
overlay: pass | gpuValidationErrors: 0
```

The scene resizes the real renderer per viewport —
`conformance/scenes/shared/camera-parented-overlay.js:55` calls `renderer.setSize` inside the
viewport loop, and the observation comes from the drawing buffer, so removing the resize fails the
row rather than passing it. The failing condition was exercised.

**This does not mean `tier-1-2026-08-10.md` was wrong when it was written.** It means the defect it
recorded does not reproduce at `0358c025`, five days and a great many commits later. The tier-1
report that would settle which no longer exists — that is Phase 0's finding and it has not changed.
Either it was fixed in passing, or it was environment-specific. **This run cannot tell those apart
and does not guess.**

### Therefore PRD-076 Phase 1 has nothing to fix

Phase 1's outcome was *"`25-camera-parented-overlay` passes on desktop after a mid-run viewport
resize, with no scene-side annotation."* That is already true, measured, with the scene file
unedited. `packages/core/src/renderer.ts` and `viewport.ts` are untouched by this PRD: **a fix
authored against a defect that does not reproduce would be a change with no observed red behind
it**, which is the thing this repository refuses everywhere else.

## What was not measured

- **The browser lane was not re-run.** Both predecessors agree on it (`67 / 0 / 0`, exit `0`) and
  nothing here re-checks that agreement.
- **The Android emulator lane was attempted and could not be measured.** See below. The `67/0/0`
  against `27/40/0` disagreement is **still open**.
- **The reference-set hash behind either 2026-08-10 run is still unrecoverable.** No report written
  before 2026-08-15 recorded one. This run records `25599d6e…`; the comparison it would enable does
  not exist yet.
- **Tier 1 is not reached and is not claimed.** One lane of three, with one declared exclusion,
  cannot reach it.

## The Android lane — attempted, and it stops before it measures anything

PRD-076 Phase 0 recorded the Android lane as unreachable because `adb` listed a physical Pixel 8
and no emulator. That is no longer true: no physical device is attached, four AVDs exist, and
`threenative_api35` boots.

```console
$ emulator -avd threenative_api35 -no-window -no-audio -no-boot-anim -gpu swiftshader_indirect &
$ adb wait-for-device && adb shell getprop sys.boot_completed
1
$ adb shell getprop ro.build.version.sdk
35
```

**With `ANDROID_SDK_ROOT` and `ANDROID_HOME` both set**, one row was run:

```console
$ node packages/runtime-native/conformance/run-conformance.mjs --target android \
    --only-tests 01-basic-cube --out …/prd076-android-probe/report.json
{ "summary": { "pass": 0, "fail": 1, "blocked": 66 } }

$ node -e "<read the report>"
error: "TN_ANDROID_DISPLAY_ORIENTATION: captured 720x1280 but the lane requires 1280x720;
        the display was still rotating."
```

Retried with `TN_ANDROID_SETTLE_MS=8000` and `TN_ANDROID_ROTATION_TIMEOUT_MS=30000`, after forcing
`accelerometer_rotation=0` and `user_rotation=1`: **the same error, verbatim.**

The cause is the AVD's panel, and it is the hazard `android-touch.mjs` already documents for
touch coordinates, arriving in the capture path:

```console
$ adb shell wm size
Physical size: 1080x2400
$ adb exec-out screencap -p > screen.png     # PNG header
screencap: 1080 x 2400
$ adb shell settings get system user_rotation
0
$ adb shell dumpsys display | grep mViewports
… orientation=0, logicalFrame=Rect(0, 0 - 1080, 2400), deviceWidth=1080, deviceHeight=2400
```

`wm size 1280x720` letterboxes a landscape *logical display* into a portrait panel; it does not
rotate the framebuffer. `waitForAndroidDisplaySize` is satisfied because the override reads back,
and the capture that follows is still portrait. `user_rotation=1` does not stick — it reads back
`0`, reset by the activity requesting its own orientation.

**Three things this establishes, and one it does not.**

1. The Android lane **cannot be measured on `threenative_api35` as configured**. It fails at the
   first row, before any scene is compared, so nothing about parity is learned from it.
2. **Phase 0's `ANDROID_SDK_ROOT`/`ANDROID_HOME` hypothesis is not the blocker here.** Both were
   set and the lane still failed, for a different and specifically named reason. That hypothesis
   is not refuted for the 2026-08-10 run — it is simply not what stops the lane today.
3. The failure is an **environment mismatch, not a merit failure**, and it is the shape a
   40-row delta would take: every row that compares a capture against a 1280x720 reference fails
   identically, and none of them is about the framework.
4. It does **not** establish that this is what produced `27/40/0` on 2026-08-10. That run's report
   no longer exists. This is a plausible mechanism with the same signature, and it is recorded as
   a hypothesis with evidence rather than as the answer.

**What would settle it:** an AVD whose panel is natively 1280x720 landscape, or a lane that
captures the logical display rather than the panel. Both are PRD-076 Phase 2 work and neither was
attempted here.
