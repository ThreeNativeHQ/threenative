# PRD-134 documentation link gate

Executed 2026-08-17 in the `prd-134-doc-links` worktree.

The checker now removes fenced code blocks and inline backtick spans before scanning. The root
`test` script runs `check:docs` first; the duplicate standalone CI step was removed. Five stale
links to the archived PRD-129 were also corrected after the parser exposed them.

## Acceptance evidence

### 1. `pnpm check:docs`

Result: exit `0`. The final command output is recorded below after this evidence file was staged,
so the tracked-file count includes this document.

```text
(node:3785028) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.

> threenative@0.1.0 check:docs /home/joao/projects/threenative/threenative-engine/.worktrees/prd-134-doc-links
> tsx scripts/check-doc-links.ts

(node:3785167) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
Checked 512 relative documentation links across 392 Markdown files.
(node:3785185) Warning: The 'NO_COLOR' env is ignored due to the 'FORCE_COLOR' env being set.
```

Verified count: 392 Markdown files and 512 relative documentation links.

### 2. `pnpm vitest run scripts/__tests__/check-doc-links.spec.ts`

Result: exit `0`; 9 tests passed, including fenced-code, tilde-fence, inline-code, escaped
backtick prose, code-span closing, and malformed prose coverage.

### 3. Negative control

The temporary link `docs/does-not-exist.md` was added to
`docs/PRDs/batch-26-08-16/README.md`, then `pnpm check:docs` was run and returned exit `1`:

```text
Broken documentation links:
docs/PRDs/batch-26-08-16/README.md -> docs/does-not-exist.md
```

The temporary link was removed before delivery.

### 4. `pnpm test`

Result: exit `0`. Its output began with `pnpm run check:docs` and the successful link count above;
the complete repository run finished with 133 test files and 1,194 tests passed.
