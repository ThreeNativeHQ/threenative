# Round 12 closed — 2026-08-22

`pnpm round:next` reported "close round 12 — All evidence and dispositions are recorded; close
the round." The ledger (`docs/verification/round-12-2026-08-20.md`) is complete: all five gaps
dispositioned (three user-space carryovers, two closed by the round's own repairs), every gate
pass recorded, negative controls observed red. Nothing in the ledger names an open PRD.

## Stale gate record

`pnpm gate:status` showed RED only because HEAD moved after the unit phase succeeded
(`worktree HEAD changed from ae61bcc4 to 1cf091d8`, `exitCode: 0`, `state: "succeeded"`). The
record is advisory local state; the next long gate overwrites `artifacts/gates/status.json`.
Cleared by rerunning the suite at HEAD — see baseline below.

## Baseline at batch start (attribution record)

The batch README requires trusted measurements before defect work. Baseline attribution for
2026-08-22, taken before any batch lane touched source:

| Gate | Result | Attribution |
| --- | --- | --- |
| `pnpm typecheck` | pass | clean |
| `pnpm lint` | 7 errors | all seven are formatter complaints in **uncommitted WIP of a concurrent session** (`packages/create-threenative/templates/*/package.json`, edited 13:11–13:15, not part of this batch) |
| `pnpm test` | 3 failing files | see below |

Test failures, one commit-introduced, two concurrent-session:

1. `scripts/__tests__/primary-docs.spec.ts` — create-threenative manifest bumped to 0.2.3 in the
   same uncommitted WIP; README row not yet updated there. Resolves when that session lands.
2. `scripts/__tests__/sync-agent-docs.spec.ts` — mirror drift inside
   `.claude/worktrees/asset-pipeline/`, a live sibling lane's in-flight checkout (its own commits,
   not this tree's). Transient; not this batch's state to repair.
3. `packages/physics/__tests__/documentation.spec.ts` — **committed defect**: d2ca4f0f deleted
   `docs/PRDs/native/README.md`, which this gate hard-reads. Fixed at e5a2c21b (pin repointed to
   `docs/architecture/NATIVE-RUNTIME.md §0b`; red was ENOENT at 13:28, green 3/3 after).

Also fixed en route to a runnable suite: three dead links in `docs/README.md` left by d2ca4f0f
blocked `check:docs`, the first phase of every `pnpm test` (12893986).

## Round 13 opening state

Round 12's carried gaps (platformer native HUD decision; defense/racing re-measure; platformer fog
re-measure) remain user-space decisions above this batch's cut. The improvement loop's next
framework-vs-vanilla round resumes after this defect batch closes; `pnpm round:next` will compute
it against whichever ledger is newest.
