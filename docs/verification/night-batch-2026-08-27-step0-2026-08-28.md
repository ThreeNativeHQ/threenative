# Night batch 2026-08-27 — Step 0 record (2026-08-28)

Executed at batch open, on `main` at `a53cd0ed` (clean, ahead 28 of origin). Agent worktree
mtimes all 2026-08-23/24 — no live sibling lanes; tree attributed to this batch.

## Gates on HEAD

`pnpm typecheck && pnpm lint && pnpm test` — **exit 0** (run started 2026-08-28 ~01:00 on the
clean post-abort tree; first attempt failed exit 1 solely from in-progress merge conflict
markers during the trial merge below, re-run clean and green).

## Unmerged lane 1 — `prd-223-runtime-native-complexity-reduction`: PARKED, not merged

The batch README's "looks merge-complete" was right in content and wrong in mechanism. Findings:

- Its content was **squash-landed** at `84640c6a` ("chore: land
  prd-223-runtime-native-complexity-reduction (squash)"), which is an ancestor of `main`.
- The PRD already lives in `docs/PRDs/done/PRD-223-runtime-native-complexity-reduction.md`.
- `git diff main <branch> -- packages/runtime-native/src packages/runtime-native/include` is
  **deletions only** (253 lines across `bindings.cpp`, `bindings_state.h`) — the branch has
  nothing main lacks; main is strictly ahead (overnight bindings work it predates).
- A trial merge conflicted in 5 files, all of them main's newer versions winning (e.g. main's
  `verify-native-contracts.mjs` carries the `threenative-render-pass-class-table-test` row the
  overnight canary repair added; the branch's copy predates it). Merging would have resurrected
  stale files.

Disposition: tip `52dc59ec2db2d99d588d9f34453373ebe6a521c1` archived at
`refs/archive/branches-2026-08-27/prd-223-runtime-native-complexity-reduction`, branch deleted.
No merge commit exists.

## Unmerged lane 2 — `linchpin/loading-screen-dismissed-before-scene-ready`: PARKED, not merged

- Its fix landed on `main` as `b6d3a9bf` ("fix(core): keep loading screen until scene is ready"),
  desktop-proven per the loop log (`b6d3a9bf`/`ca419748`); `packages/core/src/startup-readiness.ts`
  exists on main.
- The branch predates the overnight re-stamp `6f709f06` exactly as the README predicted:
  `git diff main <branch> -- packages/core packages/create-threenative` is 623 insertions /
  **3,591 deletions** across 48 files — main is strictly ahead (its 19 insertions are fragments
  of pre-PRD-189/190 projection code main has since rewritten).

Disposition: tip `bd28c7335383eb757795ebaff2a9b3e78b667131` archived at
`refs/archive/branches-2026-08-27/linchpin-loading-screen-dismissed-before-scene-ready`, branch
deleted. The README's standing rule stays live: reconcile loading scope against `6f709f06`
before anyone edits loading code again. No lane tonight edits loading scope; Lane C item 2 is a
capture, not a code change.

## Device lane preamble (Lane C)

- `adb connect 192.168.1.192:5555` (Wi-Fi) → **`failed to connect … Connection refused`** —
  third refused attempt on record.
- USB Pixel 8 (`37251FDJH0037Z`, product `shiba`) answers `adb devices` as `device`. Lane C
  runs on the USB serial; records name the physical Pixel 8 lane, never an emulator number.
