# PRD-041 verification — DRY the sweep corpus

**State: DONE.** Implementation completed 2026-08-08 on `docs/opportunity-areas-prds`.

## Delivered

- Added Godot-shaped `Area3D.monitoring`, defaulting to `true` and clearing contact state
  when disabled so re-enabling cannot emit a stale exit.
- Added public `Game.goto(name)` and a frame re-entry guard so navigation reconstructs the
  scene and does not continue the old frame against the new scene.
- Updated the starter template with keyboard and React restart paths, pickup monitoring,
  `ctx.tween`, `ctx.after`, and registry-backed restart proof.
- Added the starter monitoring and restart playtest scenarios and documented the `ctx`
  surface in starter/minimal `AGENTS.md` and generated `CLAUDE.md` mirrors.

## Evidence

| Check | Result |
|---|---|
| Focused tests | PASS — 35 tests across core `Game`, physics `Area3D`, and template contracts. |
| `pnpm typecheck` | PASS — root and all workspace consumers. |
| `pnpm lint` | PASS — Biome checked 310 files. |
| `pnpm test` | PASS after the repair — 132 test files, 899 tests; the pinned Vite binary was supplied through a temporary lane-local symlink because the lane install had no `.bin/vite`, then the symlink was removed. |
| `pnpm budgets` | PASS — 7 packages, 3,733 framework LOC, 7 PRD files, largest template 1,199 LOC. |
| `pnpm sync:agents` | PASS — 10 mirrors, 0 written. |
| Scenario validation | PASS — JSON parse and `loadPlaytestScenario` validation for both new scenarios. |
| Real browser scenarios | UNVERIFIED — Chromium exited `SIGTRAP` after `crashpad ... setsockopt: Operation not permitted` in the sandbox, including `xvfb-run`, WebGPU flags, and `--disable-crash-reporter`. |
| n≥10 adoption rerun | EVIDENCE GAP — the PRD's later corpus measurement was not run in this implementation lane. |

## Review and commits

The read-only Linchpin review returned `REQUEST_CHANGES`: the restart scenario compared
score `0 → 0` with `changed: true` and did not assert entity-count restoration. The manager
repaired both defects in the same lane; no second reviewer was launched per the one-review
repair rule.

- `85171cb` — integrated `3439be6`, initial implementation
- `2450673` — integrated `1f07d81`, restart-proof repair
