<!-- schemaVersion: 1 -->

# Cross-platform parity ledger — 2026-08-10 rerun 2

> **SUPERSEDED on the desktop lane, 2026-08-15, by
> [`tier-1-2026-08-15.md`](tier-1-2026-08-15.md). One cell below is wrong.**
>
> The desktop `Exit` cell records **`0`** for a summary of `66 pass / 0 fail / 1 blocked`.
> `reportExitCode` returns `2` whenever `blocked > 0` and did so at `c8cdc18`, the commit that
> landed this file, so the runner could not have emitted `0`. `pnpm parity:ledger` reports it.
>
> **The summary itself reproduces exactly.** A 2026-08-15 desktop run measured `66 / 0 / 1` with
> the same single excluded row. This was an accurate ledger with one hand-typed derived cell that
> drifted from its source — which is the argument for generating them rather than typing them.
>
> The browser lane is **not** superseded; it was not re-run. **The Android lane's `67 / 0 / 0`
> was reproduced on 2026-08-16** — see [`tier-1-2026-08-15.md`](tier-1-2026-08-15.md). On both
> summaries, this ledger was right and only its desktop exit cell was not.

> **Android cell CONFIRMED, 2026-08-19, by
> [`android-parity-2026-08-19.md`](android-parity-2026-08-19.md).** Two full emulator lanes at
> `1b7a02d4` each measured `66 / 1 / 0`: 66 rows passed real pixel comparison and the 67th,
> `25-camera-parented-overlay`, never produced a frame to compare — a marker timeout, not a pixel
> mismatch ([PRD-166](../PRDs/done/PRD-166-camera-parented-overlay-never-marks-on-android.md)). No row
> contradicts this ledger's `67 / 0 / 0`, and the contradicting `27 / 40 / 0` in
> [`tier-1-2026-08-10.md`](tier-1-2026-08-10.md) is retired on its Android row. **This ledger's
> Android summary stands.** Scoped to Android; the desktop exit-cell correction above is unchanged.
>
> Superseded note, 2026-08-19, kept for the record: an earlier run recorded `0 / 67 / 0`, exit `1`,
> but stopped at the foreground-capture guard before pixel comparison. That guard has since been
> attributed to a harness defect — API 35 stopped printing `mCurrentFocus` under the dump it read —
> and repaired.

- Related PRDs: `PRD-054`, `PRD-055`
- Host: Linux x86_64; desktop runtime `packages/runtime-native/build/tn-linux/mystral`
- Android device: `emulator-5554`, booted and online through `adb`

This ledger records the browser, desktop, and Android parity rerun after the canvas-resize,
Android display-letterboxing, and landscape-display fixes. It does not claim mobile readiness.

## Target results

| Target | Command | Pass | Fail | Blocked | Exit | Outcome |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| Browser | `pnpm parity -- --target web` | 67 | 0 | 0 | 0 | Executed and green; the multitouch row passed. |
| Desktop Linux | `TN_RUNTIME=$PWD/packages/runtime-native/build/tn-linux/mystral pnpm parity -- --target desktop --reference artifacts/conformance/web --out .runtime/parity-desktop3` | 66 | 0 | 1 | 0 | Executed and green for runnable rows; `90-multitouch-input` is explicitly blocked by the desktop registry. |
| Android emulator | `ANDROID_SDK_ROOT=/home/joao/Android/Sdk ANDROID_HOME=/home/joao/Android/Sdk pnpm parity -- --target android --device emulator-5554 --reference artifacts/conformance/web --out .runtime/parity-android3` | 67 | 0 | 0 | 0 | Executed and green; the Android multitouch supplemental proof passed. |

The desktop command expands `$PWD` to an absolute `TN_RUNTIME` path. A relative
`TN_RUNTIME` silently produces 66 spawn failures with `exitCode: null` and empty output.

The desktop blocked row is `90-multitouch-input`; the registry reason is that the desktop lane
has no native multitouch injector. The desktop `25-camera-parented-overlay` row passed. The
Android report also records `25-camera-parented-overlay` and `90-multitouch-input` as passing,
and `supplemental.androidMultitouch.status` is `pass`. No row failed, so no orientation-guard
retry was run.

## Gates

| Gate | Exit | Result |
| --- | ---: | --- |
| `pnpm typecheck` | 0 | Passed. |
| `pnpm lint` | 0 | Passed; Biome checked 394 files. |
| `pnpm test` | 0 | Passed; 629 + 196 passed/30 skipped + 24 + 1 Rust. |
| `pnpm budgets` | 0 | Passed; the native LOC review trigger is reported at 62,224 / 50,000. |

## Verdict

The measured result is **desktop-ready and Android-emulator plumbing-ready**. iOS and physical
hardware were not executed here. This ledger makes no mobile-readiness claim and does not mark
PRD-054 or PRD-055 done.
