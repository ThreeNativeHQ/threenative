# PRD-040 verification — collision layers and masks

**State: DONE.** Completed 2026-08-08 on `docs/opportunity-areas-prds`.

## Delivered

- Added unsigned `interactionGroups(layer, mask)` and exported it from `@threenative/physics`.
- Added `collisionLayer` / `collisionMask` to `RigidBody3D`, `Area3D`, and `CharacterBody3D`.
- Renamed the platformer one-way option to `oneWayLayers` and removed the packed-value heuristic.
- Added asymmetric Area-mask filtering and preserved caller-owned Rapier groups when the new
  options are omitted.
- Added the fresh scaffold `platformer-collision-layers` scenario for debris filtering,
  player pickup, and one-way landing.

## Evidence

| Check | Result |
|---|---|
| Tracked source shift census | PASS — `git grep '<< 16' -- packages templates` returns only `packages/physics/src/collision.ts`. |
| Focused physics suite | PASS — 27 targeted tests, including sign, malformed input, determinism, asymmetric Area masks, disjoint bodies, one-way behavior, and all three group-preservation cases. |
| `pnpm typecheck` | PASS — root plus all 7 workspace consumers. |
| `pnpm lint` | PASS — Biome checked 308 files with no diagnostics. |
| `pnpm test` | PASS on the repair rerun — 132 files, 890 tests. The repository lacks a root `node_modules/.bin/vite`; the existing pinned Vite binary was supplied through a temporary symlink for the scaffold test and removed afterward. |
| `pnpm budgets` | PASS — 7 packages, 3,712 framework LOC, 8 PRD files, largest template 1,199 LOC. |
| `pnpm sync:agents` | PASS — 10 mirrors, 0 written. |
| Fresh consumer build/typecheck | PASS — Vite production build and TypeScript check for the generated platformer. |
| Real consumer scenario | PASS — `debris-filtered=0`, `player-pickup=1`, `peakRise=1.988`, one `collected` signal, `+Y movement delta=2.082`, and zero console/network/runtime diagnostics. |

Scenario: `platformer-collision-layers` against the generated production build under
`xvfb-run` with `--browser-recipe webgpu --headed`; the report ended with `"pass": true`.

## Review and commits

The read-only Linchpin review returned `REQUEST_CHANGES` with two defects: Area queries used
Rapier's bilateral group filter for an asymmetric mask contract, and constructors overwrote
caller-owned groups. Both were repaired in the same lane in `127b1f7`; the full gates and
consumer scenario were rerun. No second reviewer was launched, per the one-review repair rule.

- `67a70d2` — collision-layer implementation and scenario
- `2ea1d8b` — review repairs preserving groups and Area-mask direction
