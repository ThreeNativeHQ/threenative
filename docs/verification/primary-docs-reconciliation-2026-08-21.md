# Primary docs reconciliation — 2026-08-21 (PRD P2-4)

Lane: technical-debt-p2-2026-08-21 / P2-4. Executor: agent session, main tree.
Source of truth used throughout: the executable CLIs (`packages/create-threenative/src/`),
the package manifests (`packages/*/package.json`, templates included), and the shipped
`.mcp.json` surface written by installing core (landed in 74e5213b moments before this lane
started; this lane builds on it and does not restate or revert it).

## Command inventory (derived from executables)

| Binary | Surface | Derived from |
| --- | --- | --- |
| `threenative` | `build` (`--target web\|desktop\|android\|ios`) | `src/commands.ts` (`threenativeCommands`), consumed by `cliHelp()`; advertised stdout pinned by `__tests__/cli.spec.ts` |
| `threenative` | `doctor [--text]` | same |
| `create-threenative` | default scaffold `<directory>`, subcommand `inspect <file.glb> [--json]`; flags `--template`, `--no-install`, `--help`, `--json`, plus `--core-package`/`--physics-package`/`--playtest-package`/`--runtime-native-package`/`--ui-package`/`--engine-mcp-package`/`--cli-package` | `cliHelp()` / `parseArgs()` in `src/index.ts` |
| do **not** exist as CLI commands | `dev`, `test`, `ship` (v1 vocabulary; the executable rejects them, exit 1) | `__tests__/cli.spec.ts` rejection loop |

`pnpm dev` inside a generated project is a package script, never a CLI command — stated
explicitly in `packages/create-threenative/AGENTS.md`.

## Package inventory (derived from manifests)

Published (`publishConfig.access: public`) workspace packages, README must list each at its
manifest version:

| Package | Version |
| --- | --- |
| `@threenative/core` | 0.2.0 |
| `create-threenative` | 0.2.2 |
| `@threenative/physics` | 0.2.1 |
| `@threenative/playtest` | 0.2.0 |
| `@threenative/runtime-native` | 0.2.0 |
| `@threenative/ui` | 0.2.1 |
| `threenative-engine-mcp` | 0.2.0 |

External pinned package a doc may name: `threenative-asset-mcp` 0.4.0 (template dependency).
MCP server names shipped into every generated project `.mcp.json` (from
`templates/starter/.mcp.json`): `threenative-assets`, `threenative-sculpt`,
`threenative-engine`. Engine MCP tools (derived from `packages/engine-mcp/src/index.ts`):
`engine_search_capabilities`, `engine_capability_detail`.

## Changed docs

- `README.md` — removed the `@threenative/studio` Packages-table row (no such package ships);
  added `threenative-engine-mcp` 0.2.0 row. All seven remaining rows now carry manifest
  versions, enforced by the new spec.
- `docs/architecture/AGENT-INTERFACE.md` — status line now states two CLI commands; the
  "what exists now" block describes the shipped engine MCP route (server, both tool names,
  the three `.mcp.json` servers via core shims) instead of claiming "**No ThreeNative MCP
  server exists**"; deleted references to the removed `scripts/studio-probe.ts` /
  `studio-inspect.ts` / `studio-loop.ts`; the proposal table no longer cites the nonexistent
  `threenative ship`.
- `packages/create-threenative/AGENTS.md` — CLI surface section rewritten: two `threenative`
  commands ever, scaffolder usage and flags, `pnpm dev` disclaimed as a package script; asset
  MCP paragraph updated to the core-shim `.mcp.json` delivery from 74e5213b.
- `AGENTS.md` (root) — rule 6 added ("Primary docs follow the executables", naming the new
  spec); capability-route sentence names `threenative-engine-mcp` as the server behind the two
  tools.
- Mirrors regenerated by `pnpm sync:agents` (root + create-threenative `CLAUDE.md`; 16 pairs
  checked).
- New enforcement: `scripts/__tests__/primary-docs.spec.ts` (4 semantic checks deriving
  package/command/tool inventories from the executable surfaces);
  `scripts/__tests__/sync-agent-docs.spec.ts` gains a test pinning the primary instruction
  pairs byte-generated from their AGENTS sources;
  `packages/create-threenative/__tests__/cli.spec.ts` derives the documented command set from
  the executable's advertised stdout; `packages/create-threenative/src/commands.ts` is new and
  single-sources the command list for help text and checks alike;
  `scripts/sync-agent-docs.ts` exports `readSharedFragments` (was module-private).

Docs checks continue to skip evidence archives (sync skips `docs/benchmark` sweep archives;
link check skips `docs/benchmark/sweeps/`) while scanning every live product doc.

## Gates

| Gate | Command | Result |
| --- | --- | --- |
| Focused new specs | `pnpm exec vitest run scripts/__tests__/primary-docs.spec.ts packages/create-threenative/__tests__/cli.spec.ts scripts/__tests__/sync-agent-docs.spec.ts` | exit 0 — 15 passed (4 + 3 + 8) |
| Mirror regen | `pnpm sync:agents` | exit 0 — "agent docs synced: 16 mirrors, 2 written" |
| Mirror check | `pnpm sync:agents --check` | exit 0 — "agent docs in sync: 16 CLAUDE.md mirrors" |
| Doc links | `pnpm check:docs` | exit 0 — "Checked 721 relative documentation links across 495 Markdown files." |
| Typecheck | `pnpm typecheck` | exit 0 |
| Lint | `pnpm lint` | exit 0 (235 repo-wide warnings, pre-existing, none fatal) |
| Full suite | `pnpm test` | exit 1 — fails at the first build step, outside this lane: capability-manifest generation rejects the concurrent P2-3 lane's in-flight playtest exports (see below) |
| Root vitest (run directly because the shared build step above blocks everything) | `pnpm exec vitest run` | exit 1 — 1536/1537 passed; sole failure is the same out-of-scope cause |

Out-of-scope red, reported not fixed (it lives in `packages/playtest/src/index.ts`, a tree this
lane is forbidden to touch; owned by the P2-3 decompose lane):

```
Capability manifest generation failed:
public exports without @situation tags: @threenative/playtest:evaluateRichPlaytestAssertions (@threenative/playtest), @threenative/playtest:resolveDiagnosticsPolicy (@threenative/playtest)
```

The same error is the sole root-vitest failure
(`scripts/__tests__/capability-manifest.spec.ts > proves the committed manifest contains the
navigation regression entry`, throwing from `build-capability-manifest.ts:449`).

## Observed-red negative controls

### Control 1 — stale primary-doc claim (initial state, before any doc fix)

The spec was written first and run against the untouched stale tree. Exit 1, all four checks
red, each naming its real stale claim:

```
 FAIL  scripts/__tests__/primary-docs.spec.ts > ... > should name only packages that a shipped manifest declares
AssertionError: README.md names packages no shipped manifest declares: expected [ '@threenative/studio' ] to deeply equal []

 FAIL  ... > should list every published package in the README at its manifest version
AssertionError: README omits the shipped package threenative-engine-mcp: expected false to be true

 FAIL  ... > should advertise only commands the executable CLIs dispatch
AssertionError: docs/architecture/AGENT-INTERFACE.md advertises a threenative command the CLI does not dispatch: expected [ 'ship' ] to deeply equal []

 FAIL  ... > should keep the engine capability route discoverable
AssertionError: AGENTS.md never names the shipped engine MCP server: expected false to be true

 Test Files  1 failed (1)
      Tests  4 failed (4)
EXIT=1
```

### Control 1a — post-fix mutation: add a nonexistent package claim

Re-added `| `@threenative/studio` | `0.2.0` | Local browser Studio for project work |` to the
README Packages table, restored afterwards:

```
AssertionError: README.md names packages no shipped manifest declares: expected [ '@threenative/studio' ] to deeply equal []
EXIT=1        (restored -> EXIT=0)
```

### Control 1b — post-fix mutation: remove the engine MCP claim

`s/threenative-engine-mcp/threenative-capability-server/g` over AGENT-INTERFACE.md, restored
afterwards — both directions fire (phantom token AND required-presence):

```
AssertionError: docs/architecture/AGENT-INTERFACE.md names packages no shipped manifest declares: expected [ 'threenative-capability-server' ] to deeply equal []
AssertionError: docs/architecture/AGENT-INTERFACE.md never names the shipped engine MCP server: expected false to be true
EXIT=1        (restored -> EXIT=0)
```

### Control 2 — mirror-only edit vs `pnpm sync:agents --check`

Appended a comment line to the root `CLAUDE.md` mirror only, then regenerated:

```
agent docs out of sync with shared fragments or AGENTS.md:
  CLAUDE.md
Run: pnpm sync:agents
EXIT=1
-> pnpm sync:agents && pnpm sync:agents --check
agent docs in sync: 16 CLAUDE.md mirrors
RESTORED_EXIT=0
```

## What was not done

- No commit was made (coordinator commits).
- `pnpm test` full-suite green is impossible while the P2-3 lane's `@situation` tags are
  mid-flight; rerun the full gate once that lane lands its exports.
