# PRD-042 verification — playtest operator ergonomics

**State: DONE.** Completed 2026-08-07 on `docs/opportunity-areas-prds`.

## Delivered

- Added `--help` usage generated from the same `PLAYTEST_FLAGS` registry used by parsing.
- Added cause-specific CLI diagnostics, absolute scenario paths, and exit-code routing:
  `0` pass, `1` assertion failure, `2` before assertions.
- Added signal teardown, headless Linux/WebGPU preflight guidance, current `xvfb-run` and
  `--browser-recipe webgpu` documentation, and a fail-closed orphan-process gate.
- Repaired the independent review findings in the same Linchpin lane: setup failures now
  return exit 2, missing values cannot swallow the next flag, and unknown flags fail usage.

## Evidence

| Check | Result |
|---|---|
| Clean-lane `pnpm --filter @threenative/playtest test` | PASS — build/publint, 128 files, 866 tests, orphan gate `no orphans` |
| Main `pnpm typecheck` | PASS — root, 7 workspace projects, framework example |
| Main Vitest corpus excluding the unrelated dirty proof-set file | PASS — 130 files, 877 tests |
| `pnpm budgets` | PASS — 7 packages, 3,668 framework LOC, 9 live PRD files before this move |
| `pnpm sync:agents` | PASS — 60 mirrors, 0 written |
| Independent review | `REQUEST_CHANGES`; three defects repaired and manager-verified in-lane; no second review launched per Linchpin protocol |

## Repository gate notes

- `pnpm test` reached 130 files / 882 tests and had one failure in
  `scripts/__tests__/proof-set.spec.ts:141`: the user-dirty
  `docs/benchmark/genres/topdown-action/proof/topdown-action-pointer.playtest.json`
  currently contains `shots` but not the required `reload` resource. The file was left
  untouched.
- `pnpm lint` reported 10 formatter-only errors: 9 critic JSON files inside existing
  `.worktrees/` and the same user-dirty topdown proof JSON. Those files were left untouched.

## Commits

- `683061f` — requested PRD source
- `685333f` — operator diagnostics implementation
- `e10b06c` — review repairs and final orphan-gate script
