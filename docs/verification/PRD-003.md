# PRD-003 verification evidence

This file records local verification for `docs/PRDs/PRD-003-physics.md`.

| Gate | Result | Negative control observed red? |
|---|---|---|
| rigid body fall | PASS — a 5-unit dynamic drop moves downward by 4.5–5.2 units in 60 steps | covered by the rigid-body regression assertion; no source mutation run |
| floor fidelity | PASS — a 4-unit crate rests at `y=2.1 ± 0.05` on a fixed floor | covered by the floor regression assertion; no source mutation run |
| fixed-step ownership | PASS — the plugin invokes the raw Rapier world exactly 60 times for 60 fixed updates | covered by the plugin tick-count assertion; no source mutation run |
| area events | PASS — dynamic bodies and characters enter once, remain once, and exit once | covered by area enter/exit assertions; no source mutation run |
| character controller | PASS — the character climbs the low step and rejects the 60° slope | covered by character step/slope assertions; no source mutation run |
| determinism | PASS — 30fps and 144fps simulations finish within `0.01` position delta | covered by the determinism assertion; no source mutation run |
| raw escape hatches | PASS — `RigidBody3D.body`, `RigidBody3D.mesh`, and `ctx.physics.world` are public | covered by compile-time API usage and runtime plugin tests |
| physics visual constraint | PASS — physics source is 469 lines and contains no material, light, post-processing, shader, WebGPU, or WebGL references | covered by source grep |
| 20-line user measurements | PASS — every physics wrapper call site in the framework example is 1–12 lines | measured in `examples/abyss-framework/src` |

## Regular gates

```text
pnpm install --frozen-lockfile  PASS — all 6 workspace projects resolve from the lockfile
pnpm typecheck                  PASS — root, core, playtest, physics, and framework references
pnpm lint                       PASS — Biome checked 50 files
pnpm test                       PASS — 25 files, 55 tests; physics contributes 5 files, 9 tests
pnpm test:browser               PASS — frozen abyss-vanilla control, 1 test in 2.2s
pnpm build                      PASS — Vite, tsup, and publint; abyss-framework builds
pnpm budgets                    PASS — 5 packages, 1183 framework LOC, 2367 markdown lines, 6 PRD files
```

## Browser integration proof

The framework example was served from the primary checkout with Vite and opened in
Chromium. Holding `ArrowRight` for 3 seconds changed the visible HUD from `Score: 0`
to `Score: 1` and moved the player from `-2` to `3.65`, proving that the character
crosses the pickup area and scores exactly once. The browser fell back to WebGL2 in
the available environment; the app remained interactive. The only console error was
the optional missing `/favicon.ico` request.

```text
caller census: rapier() is installed in examples/abyss-framework/src/main.ts
caller census: RigidBody3D, CharacterBody3D, Area3D, and CollisionShape3D are used in the framework example
raw escape: ctx.physics.world is exercised by the plugin tick-count test
visual grep: no matches in packages/physics/src
```
