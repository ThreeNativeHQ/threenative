# PRD-034 verification — 2026-08-08

**State: DONE.** The navigation implementation, focused tests, scaffold build, headed
consumer scenarios and repository-wide gates pass. A fresh read-only review returned
`REQUEST_CHANGES`; the findings were repaired in this lane and manager-verified. Per
Linchpin's repair protocol, no second reviewer was launched.

## Gate results

| Gate | Result | Evidence |
|---|---|---|
| Navigation unit tests | PASS | 3 files, 13 tests: region 4, agent 6, obstacle 3 |
| Typecheck | PASS | `pnpm typecheck` completed across 7 workspace projects |
| Changed-file Biome | PASS | Physics navigation, template, and scaffold files checked |
| Scaffolded platformer build | PASS | Vite emitted the Recast WASM compatibility artifact |
| Minimal bundle isolation | PASS | No recast artifact or `recast-navigation` text in `dist/` |
| Budget | PASS | 7 packages, 3668 framework LOC, 8 PRD files, largest template 1200 LOC |
| Sync check | PASS | `agent docs in sync: 10 CLAUDE.md mirrors` |
| Full lint | PASS | 305 files checked; the three pre-existing critic reports were formatter-only housekeeping in `eaa6257` |
| Full test | PASS | 119 files, 810 tests |

## Browser consumer evidence

Commands were run against a freshly scaffolded platformer under `xvfb-run`, using the real
Vite build and headed WebGPU recipe.

`platformer-chase` (fresh scaffold, port 4176):

```text
diagnostics: PASS — consoleErrors=0, networkErrors=0, runtimeDiagnostics=0
movement.pathLength: PASS — pathLength=11.054116317956037, minimum=9
movement.reachesPosition: PASS — closestDistance=0.6107503170005638, maximum=1.2
pass: true
```

`platformer-avoidance` (same fresh scaffold, port 4175):

```text
diagnostics: PASS — consoleErrors=0, networkErrors=0, runtimeDiagnostics=0
components.chaser.navigationFinished: PASS — after=true
components.chaser.avoidance.navigationFinished: PASS — after=true
components.chaser.avoidance.separation: PASS — after=1.0987744581203862, minimum=0.56
movement.pathLength: PASS — pathLength=12.667855151626402, minimum=9
movement.reachesPosition: PASS — closestDistance=0.6104379880822677, maximum=1.2
pass: true
```

The browser emitted only the existing duplicate-Three warning, React DevTools information,
and Recast's deprecation warning; no page errors or console errors were reported. The player
remained stationary within the chase run, and chasers were excluded from player collision
response while retaining Recast crowd avoidance. A headed screenshot was captured at
`/tmp/prd034-browser.vfRANX/game4/artifacts/playtest/after.png` and inspected for the
platform silhouette, HUD, lighting, contact shadows and enemy presentation.

## Independent review and repair

| Review finding | Repair | Manager verification |
|---|---|---|
| Avoidance scenario asserted only one path and arrival. | Added both `navigationFinished` component assertions and a non-trivial separation observation. | Fresh `platformer-avoidance` run passed with separation `1.0987744581203862`. |
| Reachability accepted distant or vertically mismatched targets. | Added horizontal and vertical target matching for same-poly and partial paths, with unit regressions. | Focused navigation tests passed; far and vertical targets are red when mutated. |
| `NavigationRegion3D.enabled` was not consumed. | Added an accessor-backed flag and fail-closed agent checks, with disabled-region coverage. | Focused navigation tests passed. |
| Agents could be constructed without a baked region. | Constructor now throws when no region exists; empty/disabled coverage added. | Focused navigation tests passed. |
| Evidence gap: no pre-existing consumer mutation was captured for the plugin-removal control. | Kept the gap explicit in the ledger; no unsupported claim was added. | Accepted as documented evidence gap under Linchpin's same-lane repair/no-second-review protocol. |

The browser run also exposed and fixed a consumer-level issue not present in the review: the
idle player could be displaced by chaser physics. Chaser colliders now exclude the player
collision group while remaining crowd agents; the fresh chase run proves the player stayed at
the spawn position.

## Observed-red controls

| Control | Exact mutation/run | Observed red result |
|---|---|---|
| Blocker geometry | Remove `blocker` from `NavigationRegion3D.meshes` in a copied scaffold, then run `chase.playtest.json` | Exit 1; path length `3.920573937418335`; closest distance `4.328102891881244` |
| Agent cursor | Replace `this.agent.getNextPathPosition()` with `this.mesh.position` in a copied scaffold, then run `chase.playtest.json` | Exit 1; path length `0`; closest distance `7.5` |
| Plugin registration | Remove `recast()` from the copied platformer plugin list, then run `chase.playtest.json` | Exit 2; `TN_PLAYTEST_RUNNER_FAILED`, `Cannot advance a stopped loop` after the scene navigation-context failure |
| Bundle isolation | Add a live `recast()` plugin import to a copied minimal scaffold, then run Vite build | Build succeeds but emits `recast-navigation.wasm-compat-BWLobtMG.js` and one `recast-navigation` content hit |
| Crowd avoidance | Run `navigation-obstacle.spec.ts` with `avoidanceEnabled: false` | The negative assertion expects separation `< 0.56`; the positive threshold is `>= 0.56`, so the mutated behavior is red |

## Caller census

- `recast()` — `packages/create-threenative/templates/platformer/src/main.ts:22`.
- `NavigationRegion3D` — `packages/create-threenative/templates/platformer/src/scenes/Level.ts:61`.
- `NavigationAgent3D` — `packages/create-threenative/templates/platformer/src/entities/Chaser.ts:31`.
- `NavigationObstacle3D` — `packages/create-threenative/templates/platformer/src/scenes/Level.ts:84`.
- `@threenative/physics/navigation` — platformer imports in `main.ts`, `Level.ts` and
  `Chaser.ts`; the root physics entry remains free of a runtime Recast import.

## Remaining delivery work

1. Merge the committed PRD lane into the current branch.
