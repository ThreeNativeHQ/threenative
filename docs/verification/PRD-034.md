# PRD-034 verification — 2026-08-08

**State: REVIEW PENDING.** The navigation implementation, focused tests, scaffold build,
headed consumer scenarios and repository-wide gates pass. The PRD remains live until the
independent review is recorded.

## Gate results

| Gate | Result | Evidence |
|---|---|---|
| Navigation unit tests | PASS | 3 files, 12 tests: region 4, agent 5, obstacle 3 |
| Typecheck | PASS | `pnpm typecheck` completed across 7 workspace projects |
| Changed-file Biome | PASS | Physics navigation, template, and scaffold files checked |
| Scaffolded platformer build | PASS | Vite emitted the Recast WASM compatibility artifact |
| Minimal bundle isolation | PASS | No recast artifact or `recast-navigation` text in `dist/` |
| Budget | PASS | 7 packages, 3628 framework LOC, 9 PRD files, largest template 1200 LOC |
| Sync check | PASS | `agent docs in sync: 10 CLAUDE.md mirrors` |
| Full lint | PASS | 305 files checked; the three pre-existing critic reports were formatter-only housekeeping in `eaa6257` |
| Full test | PASS | 119 files, 809 tests |

## Browser consumer evidence

Commands were run against a freshly scaffolded platformer under `xvfb-run`, using the real
Vite build and headed WebGPU recipe.

`platformer-chase`:

```text
diagnostics: PASS — consoleErrors=0, networkErrors=0, runtimeDiagnostics=0
movement.pathLength: PASS — pathLength=11.054116317956037, minimum=9
movement.reachesPosition: PASS — closestDistance=0.6107503170005638, maximum=1.2
pass: true
```

`platformer-avoidance`:

```text
diagnostics: PASS — consoleErrors=0, networkErrors=0, runtimeDiagnostics=0
movement.pathLength: PASS — pathLength=12.635748404681202, minimum=9
movement.reachesPosition: PASS — closestDistance=0.6104960670831304, maximum=1.2
pass: true
```

The browser emitted only the existing duplicate-Three warning, React DevTools information,
and Recast's deprecation warning; no page errors or console errors were reported. A headed
screenshot was captured at `/tmp/prd-034-platformer-final.png` and inspected for the
platform silhouette, HUD, lighting, contact shadows and enemy presentation.

## Observed-red controls

| Control | Exact mutation/run | Observed red result |
|---|---|---|
| Blocker geometry | Remove `blocker` from `NavigationRegion3D.meshes` in a copied scaffold, then run `chase.playtest.json` | Exit 1; path length `3.920573937418335`; closest distance `4.328102891881244` |
| Agent cursor | Replace `this.agent.getNextPathPosition()` with `this.mesh.position` in a copied scaffold, then run `chase.playtest.json` | Exit 1; path length `0`; closest distance `7.5` |
| Plugin registration | Remove `recast()` from the copied platformer plugin list, then run `chase.playtest.json` | Exit 2; `TN_PLAYTEST_RUNNER_FAILED`, `Cannot advance a stopped loop` after the scene navigation-context failure |
| Bundle isolation | Add a live `recast()` plugin import to a copied minimal scaffold, then run Vite build | Build succeeds but emits `recast-navigation.wasm-compat-BWLobtMG.js` and one `recast-navigation` content hit |
| Crowd avoidance | Run `navigation-obstacle.spec.ts` with `avoidanceEnabled: false` | The negative assertion expects separation `< 0.56`; the positive threshold is `>= 0.56`, so the mutated behavior is red |

## Caller census

- `recast()` — `packages/create-threenative/templates/platformer/src/main.ts:22`.
- `NavigationRegion3D` — `packages/create-threenative/templates/platformer/src/scenes/Level.ts:61`.
- `NavigationAgent3D` — `packages/create-threenative/templates/platformer/src/entities/Chaser.ts:31`.
- `NavigationObstacle3D` — `packages/create-threenative/templates/platformer/src/scenes/Level.ts:84`.
- `@threenative/physics/navigation` — platformer imports in `main.ts`, `Level.ts` and
  `Chaser.ts`; the root physics entry remains free of a runtime Recast import.

## Remaining delivery work

1. Commit the lane, run the fresh read-only Sol review, and merge only after the review and
   required gates are recorded.
