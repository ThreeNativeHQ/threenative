# PRD-210 — the native host survives backgrounding, memory pressure, and its own crashes

**Date:** 2026-08-23 (session of 2026-08-24)
**Branch:** `worktree-agent-a60b0b3f74d66bb64`
**Spec:** [`../PRDs/batch-2026-08-23-mobile-stability/PRD-210-native-host-survives-backgrounding-and-crashes.md`](../PRDs/batch-2026-08-23-mobile-stability/PRD-210-native-host-survives-backgrounding-and-crashes.md)
**Bugs owned:** [4 and 9](../bugs/mobile-stability-2026-08-23.md)

**What this record claims:** the Linux desktop host, and — from 21:57 onward, after the PRD-214
lane released the device — the **physical Pixel 8** (`192.168.1.192:5555`, shiba, Android 17,
Wi-Fi ADB, discharging throughout). Desktop and device results are in separate sections and neither
borrows from the other. The Android **emulator was not used at all**; no result here is an emulator
result. iOS is not claimed anywhere.

The device rungs settled the tombstone question and then found a defect in this change that the
desktop lane could not see: **resuming from the background presents nothing.** That is filed as
[`../bugs/resume-presents-nothing-2026-08-23.md`](../bugs/resume-presents-nothing-2026-08-23.md)
and it blocks the pause half of this PRD.

Machine: Linux 7.1.4 x86_64, GCC 16.1.1, CMake 4.4.2, Dawn + V8 13.1.201.22, preset `tn-linux`.

---

## What landed

| Phase | Change | Files |
| --- | --- | --- |
| 1 | Android never installs crash-signal handlers, so debuggerd keeps writing tombstones | `include/mystral/platform/crash_policy.h`, `src/platform/crash_handlers.cpp`, `src/runtime.cpp` |
| 2 | A NULL handle from wgpu logs the operation and throws to JS instead of reaching the FFI | `include/mystral/webgpu/checked_handle.h`, `src/webgpu/checked_handle.cpp`, `src/webgpu/bindings.cpp` |
| 3 | `SDL_AddEventWatch` lifecycle, a paused loop, a host-side AudioContext registry, `TN_LIFECYCLE` markers | `include/mystral/platform/lifecycle.h`, `src/platform/lifecycle.cpp`, `src/runtime.cpp`, `src/audio/audio_context.cpp` |
| 4 | `display.backgroundMode` plumbed to both hosts, default `"pause"` | `packages/core/src/config.ts`, `scripts/package-android.mjs`, `MystralActivity.java`, `src/platform/android_main.cpp`, `src/cli/main.cpp` |

Three new executable proofs run in one display-free lane, wired into `native:verify:desktop`:

```sh
node packages/runtime-native/scripts/verify-desktop-stability.mjs
```

---

## Red controls, observed

### The default gate at HEAD

The three guard suites were run with the new sources moved out of the tree, so the assertions saw
exactly what `01ec0658` ships.

```
 ❯ tests/crash-handler-policy.test.mjs (4 tests | 4 failed) 16ms
   × the crash-handler decision is one pure function, not a scattered ifdef 8ms
   × Android installs no crash handler, so debuggerd keeps writing tombstones 1ms
   × the runtime no longer installs signal handlers itself 4ms
   × the crash-policy proof is built and run by a lane that needs no display 3ms
 ❯ tests/lifecycle-pause.test.mjs (7 tests | 7 failed) 18ms
   × the pause hook is an SDL event watch, not a pump case 6ms
   × the paused loop runs no JavaScript and presents no frame 4ms
   × audio is suspended through a host-side registry, not left to JavaScript 3ms
   × the markers report what happened, in both modes 0ms
   × a terminal event is terminal whatever backgroundMode says 0ms
   × display.backgroundMode is plumbed from the config to both hosts 1ms
   × the lifecycle proof is built and run by a lane that needs no display 2ms
 ❯ tests/wgpu-null-handle.test.mjs (5 tests | 5 failed) 32ms
   × the checked-handle helper logs the operation and throws, and never silently continues 9ms
   × every ranked create/begin/finish site is checked before its handle is used 6ms
   × host-side paths with no JS frame skip the work instead of carrying the NULL 5ms
   × queue.submit no longer drops a NULL command buffer on the floor 7ms
   × the NULL-handle proof is built and run by a lane that needs no display 3ms
 Test Files  3 failed (3)
      Tests  16 failed (16)
```

Two of those reds name the defect directly:

```
AssertionError: runtime.cpp must not install a handler for SIGABRT; the policy owns that decision
```

After the change, the same three files:

```
 Test Files  3 passed (3)
      Tests  16 passed (16)
```

### A NULL handle into wgpu's FFI, executed

`tests/wgpu_null_handle_test.cpp` forks a child that makes the pre-fix call — a NULL encoder into
the real `wgpuCommandEncoderBeginRenderPass` — and the parent reports how the child died. This is
the fault shape behind the six unnamed `SIGNALED status=11` exits, reproduced in a contained
process on this machine:

```
RED (negative control): a NULL encoder into wgpuCommandEncoderBeginRenderPass killed the child
with Segmentation fault (signal 11) — no JS frame, no named operation
```

The same NULL through the checked path, driven from JavaScript so it runs where the migrated call
sites run:

```
[WebGPU] TN_WGPU_NULL_HANDLE: device.createCommandEncoder returned no handle (label=frame); refusing to pass NULL to wgpu
PASS a NULL handle throws into V8 rather than returning undefined and letting the caller continue
PASS the thrown message names the operation, not just an address
PASS the thrown message carries the TN_WGPU_NULL_HANDLE marker a logcat filter finds
PASS the thrown message carries the arguments the call was made with
PASS a non-NULL handle passes the check and throws nothing
GREEN on V8: TN_WGPU_NULL_HANDLE: device.createCommandEncoder returned no handle (label=frame); refusing to pass NULL to wgpu
native wgpu NULL-handle contract passed
```

QuickJS and JavaScriptCore report `SKIP … not compiled into this build` — a skip, not a pass. The
desktop preset carries V8 alone; `verify-desktop-audio.mjs --dual` is the existing lane that
configures a V8+QuickJS build, and running these proofs there is queued below.

### The signal disposition, observed

`tests/crash_handler_policy_test.cpp` stands a `sigaction`/`SA_SIGINFO` handler in for debuggerd —
the same shape the zygote installs — applies each policy, then reads the disposition back. The
third case in every group is the negative control: it is what HEAD did on *every* platform, and it
visibly takes the disposition away.

```
PASS Android with no MYSTRAL_SHOW_CRASH_DIALOG leaves the handlers to the platform
PASS Android with MYSTRAL_SHOW_CRASH_DIALOG=1 still leaves the handlers to the platform
PASS desktop with no MYSTRAL_SHOW_CRASH_DIALOG suppresses its crash dialog
PASS desktop with MYSTRAL_SHOW_CRASH_DIALOG=1 installs nothing
[Mystral] Crash handlers left to the platform; debuggerd owns the tombstone
PASS the Android policy leaves SIGSEGV chained to debuggerd's stand-in
PASS MYSTRAL_SHOW_CRASH_DIALOG=1 leaves SIGSEGV chained too
PASS negative control: the desktop policy replaces SIGSEGV, which on Android is how the tombstone was lost
… SIGABRT, SIGBUS, SIGILL identical …
compiled for desktop; resolved policy SuppressDialog
PASS the live resolver is the same pure decision, read from the real environment
PASS no signal was raised while proving this
native crash-handler policy contract passed
```

**This proves the mechanism, not the tombstone.** A tombstone in `dumpsys dropbox` can only come
from the phone. That is criterion 1 in the open list below.

### The lifecycle transition table and the watch

`tests/lifecycle_policy_test.cpp` drives the real table with real SDL constants, then pushes an
event through SDL itself so the watch is exercised on SDL's own send path — the property that
makes a watch work on Android where a poll cannot.

```
PASS SDL_EVENT_WILL_ENTER_BACKGROUND maps to paused
PASS SDL_EVENT_DID_ENTER_BACKGROUND maps to paused
PASS SDL_EVENT_WINDOW_MINIMIZED maps to paused
PASS SDL_EVENT_WINDOW_HIDDEN maps to paused
PASS SDL_EVENT_WILL_ENTER_FOREGROUND maps to resumed
PASS SDL_EVENT_DID_ENTER_FOREGROUND maps to resumed
PASS SDL_EVENT_WINDOW_RESTORED maps to resumed
PASS SDL_EVENT_WINDOW_SHOWN maps to resumed
PASS SDL_EVENT_TERMINATING maps to terminating
PASS SDL_EVENT_WINDOW_DESTROYED maps to terminating
PASS SDL_EVENT_WINDOW_FOCUS_LOST maps to observed
PASS SDL_EVENT_WINDOW_OCCLUDED maps to observed
PASS SDL_EVENT_KEY_DOWN maps to none
PASS SDL_EVENT_MOUSE_MOTION maps to none
PASS the loop starts unpaused
PASS backgrounding pauses the loop
PASS the pause is reported as a TN_LIFECYCLE marker
PASS the pause marker says the pause was applied
PASS foregrounding resumes the loop
PASS the resume is reported as a TN_LIFECYCLE marker
PASS losing focus does not pause the loop
PASS losing focus is still recorded
PASS backgroundMode=continue keeps the loop running
PASS the marker names the mode that executed
PASS the marker says the pause was not applied
PASS a terminal event stops the loop even under backgroundMode=continue
PASS the terminal event is reported
PASS "pause" parses
PASS "continue" parses
PASS an unrecognized backgroundMode is rejected rather than guessed
PASS a missing backgroundMode is rejected rather than guessed
[Audio] AudioContext created (sample rate: 44100 Hz)
[Audio] AudioContext resumed
PASS a constructed AudioContext registers itself with the host
[Audio] AudioContext suspended by the host lifecycle
PASS backgrounding suspends every live AudioContext
PASS the host records that it owns the suspension
[Audio] AudioContext resumed
PASS foregrounding resumes the contexts the host suspended
PASS resuming does not un-suspend a context the game suspended itself
PASS a destroyed AudioContext leaves the registry
PASS the SDL event watch installs
PASS installing twice is idempotent
PASS an event pushed through SDL reaches the watch synchronously, before any poll
native lifecycle policy contract passed
```

**This proves the state machine, not the battery.** "Screen-off stops presenting within ~1 s"
requires the phone.

### The catch-up clamp, asserted rather than rebuilt

PRD-210 says to assert `FixedStepLoop`'s existing clamp, not to write a second one.
`packages/core/__tests__/loop.spec.ts` now covers the resume shape: ten minutes of elapsed time
produces five updates, and a clock that moved backwards produces none and does not poison the
frames after it.

Mutation control — removing the elapsed clamp at `packages/core/src/loop.ts:111`
(`const elapsed = Math.max(0, …)` → `const elapsed = …`):

```
     ✓ should clamp catch-up to 5 steps after a long stall 1ms
     × should absorb a backgrounded app's time jump without a burst of updates 8ms
AssertionError: expected 0 to be greater than 0
      Tests  1 failed | 17 passed (18)
```

Restored:

```
 ✓ packages/core/__tests__/loop.spec.ts (18 tests) 22ms
      Tests  18 passed (18)
```

---

## Gates

| Gate | Result |
| --- | --- |
| `pnpm typecheck` | **pass** (all packages and examples `Done`) |
| `pnpm lint` | **pass**, exit 0 (291 pre-existing warnings, no errors) |
| `pnpm check:docs` | **pass** — 768 links across 608 files |
| `pnpm build` | **pass** |
| `pnpm -r run test` (package phase) | **red on `@threenative/playtest` only**, see below |
| `vitest run` (unit phase) | 1892/1893 pass; the one failure is a 5 s timeout in `scripts/__tests__/check-capability-docs.spec.ts` under load, **green in 3.96 s when run alone** |
| `pnpm budgets` | **pass** (framework LOC trigger reports 18 390/15 000, informational) |
| `pnpm census` | run in this commit: total 81 502 → 83 447, 7 cells updated |
| `pnpm native:build` | **pass**, `[382/382] Linking CXX executable mystral` |
| `node scripts/verify-desktop-stability.mjs` | **pass**, all three contracts |

### The `@threenative/playtest` red is a concurrent-lane artifact, not this change

`__tests__/orphan-cleanup.sh` flags any process whose command line contains
`playwright_chromiumdev_profile-` and whose PID was not in its baseline. Three agent lanes were
running suites in this checkout during this session, and the flagged processes carried other runs'
profile directories (`/tmp/threenative-suite.mKZBeK/…`, `/tmp/playwright_chromiumdev_profile-x2rkaB`)
— six `/tmp/threenative-suite.*` directories and 14 Chromium shells existed machine-wide at the
time. Run on its own, the same gate passes:

```
> @threenative/playtest@0.3.0 test
> bash __tests__/orphan-cleanup.sh && publint --strict

no orphans
All good!
```

Nothing in this change starts a browser. Recorded as an environment observation, not as a green
for a run that went red.

---

## Physical Pixel 8 — executed 2026-08-23, 21:57–22:10

Device: Pixel 8 (shiba), Android 17 `CP2A.260705.006`, arm64, V8, Wi-Fi ADB at
`192.168.1.192:5555`, **discharging** for every rung. Condition at handoff from the PRD-214 lane:
38 %, 37.5 °C. It was left idle through the Android build and cooled to 34.1 °C, and every rung ran
at **Thermal Status 0 (NONE)** with the battery between 36 % and 35 %. Thermal was not a confound
for these rungs in any case: they assert whether a crash record exists, not how long a frame takes.

Subject: `examples/native-smoke` in the first-proof APK (`com.threenative.game`), built from this
branch. **The three engine blockers named for the sandbox game were never reached** — the KTX2/WASM
build failure, the OGG startup failure and the stale asset manifest are all properties of a game
that loads assets, and this host-level lane loads none. No local workaround was needed and nothing
was moved aside.

The host's three new behaviours are live on the phone:

```
I MystralRuntime: backgroundMode=pause
I MystralStdio: [Mystral] Lifecycle watch installed; backgroundMode=pause
I MystralStdio: [Mystral] Crash handlers left to the platform; debuggerd owns the tombstone
```

### The control is one binary, not two builds

Rung 2 needs the pre-fix behaviour running against the same fault, and `01ec0658` carries no
deliberate-crash trigger, so a build-to-build comparison would have differed in two ways at once.
`debug.threenative.prefix_handlers=1` reinstates the pre-fix handlers verbatim inside the same APK.
Same binary, same game, same fault, same device, one variable.

### Rung 1 — a crash after startup leaves a symbolized tombstone. PASS.

`debug.threenative.deliberate_crash=240`, handlers left to the platform. `dumpsys dropbox --print
data_app_native_crash` gained a new entry, symbolized, naming the exact call path:

```
signal 11 (SIGSEGV), code 1 (SEGV_MAPERR), fault addr 0x0000000000000000 (read)
Cause: null pointer dereference
backtrace:
      #00 pc 0000000000625434  ...libmystral-runtime.so (mystral::platform::crashDeliberately()+60)
      #01 pc 00000000005c9224  ...libmystral-runtime.so (mystral::RuntimeImpl::pollEvents()+620)
      #02 pc 00000000005c8dd4  ...libmystral-runtime.so (mystral::RuntimeImpl::run()+196)
      #03 pc 00000000006681e8  ...libmystral-runtime.so (SDL_main+3996)
      #04 pc 0000000000951e38  ...libmystral-runtime.so (Java_org_libsdl_app_SDLActivity_nativeRunMain+604)
```

`dumpsys activity exit-info` recorded it as a crash, not a bare signal:

```
timestamp=2026-08-23 21:57:41.670 pid=31199 ... reason=5 (APP CRASH(NATIVE)) subreason=0 status=11
```

Battery 36 %, 32.2 °C, Thermal Status 0.

### Rung 2 — the pre-fix control reproduces the unnamed signature exactly. PASS.

Same fault, `debug.threenative.prefix_handlers=1`. The process died in 5 s and dropbox did **not**
gain an entry — the newest is still rung 1's 21:57:41:

```
--- dropbox entries BEFORE launch          --- dropbox entries AFTER
2026-08-21 20:14:08 data_app_native_crash  2026-08-21 20:14:08 data_app_native_crash
2026-08-23 21:15:57 data_app_native_crash  2026-08-23 21:15:57 data_app_native_crash
2026-08-23 21:57:41 data_app_native_crash  2026-08-23 21:57:41 data_app_native_crash
```

and exit-info recorded the 2026-08-23 signature, character for character:

```
timestamp=2026-08-23 22:04:52.973 pid=32331 ... reason=2 (SIGNALED) subreason=0 (UNKNOWN) status=11
```

`reason=2 (SIGNALED) ... status=11` with no tombstone is what the bug doc recorded six times.
Removing the handler install turns the same fault into `reason=5 (APP CRASH(NATIVE))` with a
symbolized backtrace. **Bug 4's no-tombstone half is settled.**

Battery 35 %, 32.1 °C, Thermal Status 0.

#### Two void attempts, recorded rather than stitched

Rung 2 was run three times. The first two are void and no part of them is used: `am start` was
issued while the phone's screen had gone off, and the host parked in
`Waiting for valid ANativeWindow...` without ever running a frame, so the fault never fired. The
third attempt woke the screen first. The startup hang is filed as a separate finding in the bug
report; it reproduces on the control path too, so it is not caused by this change.

### Rung 3 — screen-off stops presenting. PASS, with a caveat on the marker.

Screen on, presenting at 60 Hz; `KEYCODE_SLEEP`; 15 s. One further 60-frame tick was emitted and
then presenting stopped — against bug 9's recorded baseline of a full 60 s of continued presenting.
Tick granularity is 60 frames, so this bounds the pause at about a second but does not measure it;
"within ~1 s" is consistent with the data and is not independently established by it.

The caveat is real and worth carrying: **no `TN_LIFECYCLE` marker is observable while the app is
paused.** All of them — including `{"event":"paused","applied":true}` — arrive in one burst at the
instant of resume. The reason is structural and is the same fact that made a watch necessary: the
watch sets the flag synchronously, but the thread that writes markers is then parked inside SDL's
`Android_WaitLifecycleEvent`, so it cannot write anything until SDL lets it go. The pause is
applied correctly; only its reporting is deferred. A gate that watches for a live "paused" marker
on Android will wait forever.

### Rung 4 — resume. FAIL, and it is this change's own defect.

Waking resumed the loop, and the loop presents nothing:

```
# before backgrounding
TN_PRESENTS_TICK:{"frames":720,"presents":720,...}
# 12 s off, 12 s back
TN_PRESENTS_TICK:{"frames":10080,"presents":781,...}
TN_PRESENTS_TICK:{"frames":10740,"presents":781,...}
```

`frames` runs away at roughly 600/s while `presents` stays frozen. A `screencap` after resume is
uniformly black; one taken before it shows the scene. Android destroyed the `ANativeWindow` on
background and the `WGPUSurface` still points at it.

PRD-210 asked for exactly this and the implementation did not carry it: *"Surface revalidated like
startup does."* Filed as
[`../bugs/resume-presents-nothing-2026-08-23.md`](../bugs/resume-presents-nothing-2026-08-23.md).
**The pause half of this PRD is not shippable until that lands**, and it is already on `main` as
`5989e4a2`, so it needs either the surface fix or a default of `"continue"` in the meantime.

Battery 35 %, 32.1 °C, Thermal Status 0 at the end. Both debug properties were reset to `0` and the
app force-stopped; the screen-off timeout was read (1 800 000 ms), briefly changed, and restored to
1 800 000 ms.

## Acceptance criteria still open on the physical Pixel 8

The device was leased to PRD-214 for this whole session. None of the following was run, simulated,
or inferred. Emulator results, if any are taken later, must be named as the emulator.

| # | Criterion | What must run on the phone | Roughly |
| --- | --- | --- | --- |
| ~~1~~ | ~~A crash after startup leaves a symbolized tombstone~~ | **DONE — rung 1 above** | — |
| ~~1b~~ | ~~Negative control for #1~~ | **DONE — rung 2 above** | — |
| ~~2~~ | ~~Screen-off stops presenting~~ | **DONE — rung 3 above**, with the marker caveat recorded | — |
| 2a | Audio is suspended on background, on device | the smoke scene plays nothing, so the registry had no live context to suspend and `[Audio] … suspended by the host lifecycle` never had cause to fire. Needs a game with sound | 15 min |
| 2b | Resume recovers | **FAILED — rung 4 above.** Re-run after the surface fix lands | 15 min |
| 2c | Deleting the BACKGROUND case re-reddens rung 3 | rebuild with the `Pause` case removed | 20 min |
| 3 | `backgroundMode:"continue"` overrides pausing while markers keep flowing | repackage with `display.backgroundMode: "continue"`, repeat rung 3, expect ticks to continue and `"applied":false` | 20 min |
| 4 | Ten relaunch-over-pressure cycles produce zero reds or named crashes | `am start` → hold → `force-stop` → immediate relaunch ×10, GL mtrack/rss per cycle, dropbox after each | 60–90 min |
| 5 | Desktop minimize behaves per the same contract | a desktop session with a window manager — the headless Xvfb lane here has nothing that can minimize a window | 15 min |
| ~~6~~ | The wgpu-native backend's NULL behaviour | partially answered: the checked-handle path is compiled and live in the APK that produced rung 1, and no `TN_WGPU_NULL_HANDLE` fired in normal operation. A *forced* NULL on wgpu-native is still unexecuted | 20 min |

Also queued, not device-bound but not run here:

- `node scripts/verify-desktop-audio.mjs --dual` then the stability lane against that build, so the
  NULL-handle contract is proven on QuickJS (the documented Android rollback engine) rather than
  reported `SKIP`.
- `node conformance/run-conformance.mjs` — PRD-210's verification strategy asks for it after the
  bindings change. Not run here; it needs a display lane this session did not open.

**iOS is not claimed at all**, by the batch's own exclusion: no physical lane exists.

## Out of scope, restated

This change makes the *next* crash nameable. It does not identify the mid-run fault behind the
2026-08-23 sextet, and nothing here should be read as having caught it.
