# Improvement round ledger — round 4 — 2026-08-10

Round: 4
Date: 2026-08-10
Framework commit: 5f5b17b (pre-round working tree)
Framework version: 0.1.0
Genres: physics-puzzle
Budget: one completed round 4 granted by the owner on 2026-08-10; no second genre or rerun
Stop condition met: kill switch
Next action: Reopen Phase 2 win criteria; do not run a fifth genre or rerun either arm

Functional-column numbers recorded before the PRD-113 sealed proof contract are not comparable
with numbers recorded after its revised proof hash.

## Arms

One row per genre per arm. Both arms share one brief hash and one sealed proof hash.

| Genre | Arm | Archive | Brief SHA-256 | Proof SHA-256 | Proof passed/total | Instrument visual | User LOC | Source files | Reach rate |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| physics-puzzle | framework | docs/benchmark/sweeps/physics-puzzle-2026-08-11 | bdfb940a2ec2e0ecf6dffa8f360f0ee3e39884cf60b50dd4db1b21626af36e20 | c241ea5e4120afd4a50325a5b9ee0606e81e1b9d8539896f2b6e9f6b8f85da0d | 0/1 | 4 | 700 | 14 | 0.35714285714285715 |
| physics-puzzle | vanilla | docs/benchmark/sweeps/physics-puzzle-2026-08-11-2 | bdfb940a2ec2e0ecf6dffa8f360f0ee3e39884cf60b50dd4db1b21626af36e20 | c241ea5e4120afd4a50325a5b9ee0606e81e1b9d8539896f2b6e9f6b8f85da0d | 0/1 | 3 | 352 | 2 | n/a |

## Column verdicts

Framework wins a column when it is at least as good as the vanilla arm. Reach is context, not a gate.

| Genre | Functional | Visual | Cost | Verdict |
| --- | --- | --- | --- | --- |
| physics-puzzle | tie | win | loss | Framework ties functionally, wins visual, and loses fair authored cost by 2 LOC; the Phase 2 capability gate remains not green. |

## Gap list

Vanilla won the fair authored-cost column by two lines after the firewall repair; the gap is recorded below and is not reopened in this defect-only handoff.

| # | Genre | Column | What vanilla did better | Evidence | Smallest change that would close it |
| --- | --- | --- | --- | --- | --- |
| 1 | physics-puzzle | cost | Vanilla authored 352 LOC; the repaired framework arm authored 354 LOC above its starter. | `pnpm sweep:pair` reports a +2 framework-minus-vanilla authored-LOC delta; final physical source totals are 700 and 352 LOC respectively. | Remove or simplify two framework-authored lines without weakening the public-input firewall or causal replay assertion. |

## Dispositions

The single vanilla-winning column is the two-line fair authored-cost gap recorded above; it remains user-space work.

| Gap # | Disposition | 20-line verdict | Named live caller | PRD | Reason if rejected |
| --- | --- | --- | --- | --- | --- |
| 1 | rejected | Two lines are below the 20-line threshold. | framework physics-puzzle `Play` source | none | Further cost tuning is outside this repair and must not weaken the firewall or replay proof. |

## Deletions this round

The persistent-export census produced no deletion decision attributable to this paired proof. The prior census remains the evidence record.

| Export | Rounds unreached | Deleted? | Evidence |
| --- | --- | --- | --- |
| None | None | no — no new deletion candidate was earned by this round | docs/verification/deletions-2026-08-10.md |

## Gates

| Gate | Command | Result |
| --- | --- | --- |
| Typecheck | pnpm typecheck | pass |
| Lint | pnpm lint | fail |
| Test | pnpm test | pass |
| Budgets | pnpm budgets | pass |

## Firewall attestation

| Rule | Held? | Evidence |
| --- | --- | --- |
| Arms built in separate contexts | yes | The repaired framework arm was rebuilt in /tmp/round4-framework-builder and the vanilla arm in /tmp/round4-vanilla-builder from the shared brief/reference inputs. |
| Neither builder saw the sealed proofs | yes | Neither builder contained a proof contract or reveal during typecheck/build; the proof was supplied only when sweep:proof ran after each archive build. The framework package CLI was unavailable, so its Vite build fallback is recorded below. |
| Judge was fresh, read-only, blind to arm | yes | The fresh bundle and critic input contained only sample-01 and sample-02; reveal stayed outside docs/verification/blind-physics-puzzle-round-4 and was read/copied only after sweep:judge passed. |
| Repaired source contains no sealed input sequence | yes | Play.ts has no ArrowRight, 72-tick constant, or derived fixed replay sequence; it consumes only the public `ctx.input.vector("move")` input and gates snapshots on that live replay input. |

## Notes

- Before this ledger existed, `pnpm round:next` observed the historical stop `round 3 / Stop condition recorded: budget`. This ledger records the owner grant and the completed round, so the round-4 result is the latest evidence rather than a rewrite of round 3.
- The shared brief hash is `bdfb940a2ec2e0ecf6dffa8f360f0ee3e39884cf60b50dd4db1b21626af36e20`. The repaired causal-replay proof hash is `c241ea5e4120afd4a50325a5b9ee0606e81e1b9d8539896f2b6e9f6b8f85da0d`.
- The no-op negative control was built from `/tmp/round4-noop-physics` with `pnpm exec tsc --noEmit -p /tmp/round4-noop-physics/tsconfig.json` and `pnpm exec vite build`. From the repository root, `pnpm sweep:proof /tmp/round4-noop-physics` exited 1 and reported 0/1 after evaluating all 10 sealed assertion IDs: `world.seed` passed; eight physics/gameplay assertions failed, and the separate `diagnostics` assertion failed. The committed runner report is [the no-op artifact](round-4-2026-08-10-artifacts/no-op/proof-artifacts/0/runner-report.json); its proof result is [no-op proof.json](round-4-2026-08-10-artifacts/no-op/proof.json).
- Framework and vanilla proof commands exited 1 with 0/1 and the actual `TN_PLAYTEST_CAPABILITY_MISSING` error for `runtime.physics`. The archived runner reports are [framework](../benchmark/sweeps/physics-puzzle-2026-08-11/proof-artifacts/0/runner-report.json) and [vanilla](../benchmark/sweeps/physics-puzzle-2026-08-11-2/proof-artifacts/0/runner-report.json).
- The real-arm preflight reports have empty `assertions` because the fail-closed runner did not evaluate assertions; no assertion IDs or results were synthesized. `pnpm sweep:pair docs/benchmark/sweeps/physics-puzzle-2026-08-11 docs/benchmark/sweeps/physics-puzzle-2026-08-11-2` passed with framework 0/1 and vanilla 0/1, fair authored cost 354 versus 352 LOC, delta +2 LOC and -728 bytes; final physical source totals are 700 versus 352 LOC.
- The sealed proof includes contacts, settled bodies, fixed seed, movement, replay state, goal state, and diagnostics. Its `replayMatches` assertion is false at `settled` and true at `replay-sequence`, so removing the replay input step cannot produce a passing proof. The second contact is named `mission` so the pair instrument has distinct result IDs for the solid-body and goal contacts.
- The framework arm uses a dynamic `RigidBody3D` for the solid-body push path and an `Area3D` only for the goal trigger; the vanilla arm uses a dynamic non-sensor Rapier collider for the solid-body collision and a sensor only for the goal trigger. Both replay checks compare deterministic physics snapshots rather than assigning `replayMatches` at contact time.
- Both rebuilt pages were captured at 1280x720 with Playwright after the proof preflight stopped; each had one canvas, the framework had no browser/page errors, and vanilla had only its expected favicon 404. The anonymous bundle is `docs/verification/blind-physics-puzzle-round-4`; its critic input was `docs/verification/physics-puzzle-round-4-critic.json`, judge output is `docs/verification/blind-physics-puzzle-round-4/judge.json`, and reveal is `docs/verification/physics-puzzle-round-4-reveal.json`.
- Blind polish was scored before reveal. Sample-01 scored 2.4 average and sample-02 scored 2.8; the reveal maps sample-01 to vanilla and sample-02 to framework. The critic input contains no arm identifier.
- The prior firewall and blind-score attestations, proof artifacts, critic, judge, and reveal were invalidated and moved to `/tmp/round4-superseded-evidence-r2` before this rebuild. They are superseded evidence, not current attestations.
- The framework archive typechecked, but its archived package snapshot had no `threenative` CLI (`threenative: command not found`); the installed Vite build completed and is the rebuilt web output used for capture. Vanilla `pnpm typecheck && pnpm build` passed.
- Final declared gates: `pnpm typecheck` exit 0; `pnpm lint` exit 1 on 16 pre-existing `packages/core` findings in `collapse.spec.ts`, `collapse.ts`, and `game.ts`; `pnpm test` exit 0 with 85 test files and 645 tests passed; and `pnpm budgets` exit 0 with the existing 64,214-line native review trigger. The in-scope package change only adds the optional `physicsDebugSeries` observation so an explicitly empty no-op provider reaches assertion evaluation; it does not make an empty series pass a physics assertion.
- Framework ties functionally because both arms recorded the same fail-closed 0/1 result, wins the measured visual column, and loses the corrected fair authored-cost column by 2 LOC. It does not demonstrate a capability the vanilla arm cannot match because neither arm reached assertion evaluation; the kill switch therefore records Phase 2 as still not green and prohibits a fifth genre or arm rerun.
