# Improvement round ledger — round `222` — `2026-08-25`

Round: `222`
Date: `2026-08-25`
Framework commit: `2c17f3f7`
Framework version: `0.1.0` (workspace root; runtime-native `0.3.0` unreleased)
Genres: `n/a — this round executes PRD-222 (per-platform performance targets), not an arm-vs-arm sweep`
Budget: `one session, device-bound: the Pixel 8 battery floor (~4–6 rungs per prior sessions) and its cooling cadence are the real budget; no token/time cap stated`
Stop condition met: `none yet`
Next action: `PRD-222 Phase 0 — drive Chrome 151 on the Pixel 8 against the fps-framework (bayview) dev server over adb reverse, capture TN_FRAME_BUDGET windows, then the native APK arm in the same thermal window; compute parity into docs/verification/prd-222-2026-08-25.md before any engine code`

A blank, `TBD`, or `<placeholder>` value is a failed round, not a round with a gap. Use
`unmeasured` where a step genuinely did not run, and say why in the notes.

## Arms

This round is not a framework/vanilla sweep — `/goal` binds the self-improve loop to
PRD-222, whose arms are **platform lanes of one game** (bayview), compared as a parity
ratio on one physical phone. The two "arms" below are those lanes.

| Genre | Arm | Archive | Brief SHA-256 | Proof SHA-256 | Proof passed/total | Instrument visual | User LOC | Source files | Reach rate |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| bayview / browser Android Chrome | `web-on-pixel` | pending (`../sandbox/fps-framework`, hand-driven lane) | unmeasured | unmeasured | unmeasured | unmeasured | unmeasured | unmeasured | n/a |
| bayview / native Android APK | `native-on-pixel` | pending (`com.threenative.bayview`) | unmeasured | unmeasured | unmeasured | unmeasured | unmeasured | unmeasured | n/a |

## Column verdicts

| Genre | Functional | Visual | Cost | Verdict |
| --- | --- | --- | --- | --- |
| bayview parity (Tier 2) | unmeasured | unmeasured | unmeasured | unmeasured |

## Gap list

| # | Genre | Column | What vanilla did better | Evidence | Smallest change that would close it |
| --- | --- | --- | --- | --- | --- |
| None | None | None | None | None | None |

## Dispositions

| Gap # | Disposition | 20-line verdict | Named live caller | PRD | Reason if rejected |
| --- | --- | --- | --- | --- | --- |
| None | None | None | None | None | None |

## Deletions this round

| Export | Rounds unreached | Deleted? | Evidence |
| --- | --- | --- | --- |
| None | 0 | no — no completed prior round to compare | unmeasured |

## Gates

| Gate | Command | Result |
| --- | --- | --- |
| Device preflight | `node packages/runtime-native/scripts/device-preflight.mjs 192.168.1.192:5555` | pass (exit 0) — battery 33.6 °C / 58 % / discharging, thermal status 0 at charter time |
| Typecheck | `pnpm typecheck` | not yet run this round |
| Lint | `pnpm lint` | not yet run this round |
| Test | `pnpm test` | not yet run this round |

## Firewall attestation

Not an arm-vs-arm build round; the firewall rows do not apply as written. What applies
instead is PRD-222's own comparison validity rule:

| Rule | Held? | Evidence |
| --- | --- | --- |
| Parity halves from the same device, same thermal window, same scene build | pending | both runs taken back-to-back on serial 192.168.1.192 with temperatures recorded per half |
| Neither builder judges | yes | no builder agents used; measurement only |
| Sealed scenarios untouched | yes | playtests read-only this phase |

## Notes

- `pnpm round:next` currently fails on the previous newest ledger (`round-196-published-install.md`,
  missing `## Notes`) — that file belongs to another lane and was left untouched; this ledger
  supersedes it as newest by name sort.
- Physics SIGSEGV guard active for every native launch: check `pidof` ~45 s after launch, treat a
  dead process as a relaunch (`docs/bugs/physics-simulation-callback-segv-flaky-2026-08-25.md`).
- Prior-session quirks carried forward: cool to ≤31.5 °C before preflight-sensitive runs; battery
  floor bites after ~4–6 rungs; vite dev server needs `--host 127.0.0.1`.
- The Android-browser playtest lane (PRD-222 ledger row 1) does not exist yet; Phase 0 uses the
  PRD's sanctioned fallback, a hand-driven Chrome run, recorded as such.
