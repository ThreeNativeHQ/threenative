# PRD-239 verification

Continuation commit under test: `1b41811618bb22b29aaa78ec8d6a515ad1ea57fc` (native wheel sign
repair and continuation evidence). Parent-round commit referenced by the earlier gates:
`d26fde5e4b556c7b0cfed475b6b6bed1856a089d` (feature commit
`a386e649898345ccfc08f856a2c523f9cd1e240f`, manager formatting/evidence commits, and the parent
repair; its defective vertical sign is not the continuation result).

## Manager gates

| Gate | Result | Command and observed output |
| --- | --- | --- |
| Typecheck | PASS | `pnpm typecheck` — exit 0; recursive scope `16 of 17` workspace projects completed, including `packages/core`, `packages/create-threenative`, `packages/playtest`, and `examples/abyss-framework`. |
| Budgets and generated references | PASS | `pnpm budgets` — exit 0; `workspace package derivation ok: 8 packages`; `version pins ok`; `native shim contract passed`; `core boundary and scaffold hygiene passed`; `capability docs: 57 public class/function exports carry complete doc tags`; `ctx-surface supersession table in sync`; `capability reference in sync`; `examples: no superseded constructs across 52 files`. LOC review triggers were reported, not failures. |
| Focused framework tests | PASS | `pnpm exec vitest run packages/core/__tests__/input.spec.ts packages/create-threenative/__tests__/scaffold.spec.ts packages/create-threenative/__tests__/template.spec.ts packages/playtest/__tests__/click-step.spec.ts packages/playtest/__tests__/scenario.spec.ts` — exit 0; 5 files, 160 tests passed. |
| Native contract tests | PASS | `pnpm --dir packages/runtime-native exec vitest run --config vitest.config.ts tests/input-multitouch.test.mjs tests/runtime-next-contract.test.mjs` — exit 0; 2 files, 39 passed, 2 skipped. |
| Changed-file formatting | PASS | `pnpm exec biome format --write packages/core/src/input.ts` followed by `git diff --check` — exit 0; the formatting-only commit is `e2f9cca8`. |
| Abyss consumer playtest | PASS | `sh scripts/xvfb.sh node packages/playtest/dist/runner/cli.js examples/abyss-framework/playtests/zoom-input.playtest.json --url http://127.0.0.1:5181 --server-command "pnpm --filter abyss-framework dev --host 127.0.0.1 --port 5181 --strictPort" --browser-recipe webgpu --headed` — exit 0; NVIDIA WebGPU adapter, camera separation `5950`, limit `5975`, diagnostics empty. |
| Generated starter zoom playtests | PASS | `sh scripts/xvfb.sh node node_modules/@threenative/playtest/dist/runner/cli.js --scenario playtests/zoom-pinch.playtest.json --scenario playtests/zoom-wheel.playtest.json --browser-recipe webgpu --headed --server-command 'pnpm dev --host 127.0.0.1 --port $PORT --strictPort'` from the generated starter — exit 0; both scenarios passed on NVIDIA WebGPU. Pinch separation was `9.11044920495327` within `9.2`; wheel run also passed, with screenshots captured. |

## Repair round — lane-239

The manager review requested one scoped repair round. The controls below were run before and after
each repair in this worktree. The final repair commit is the commit containing this section.

### Red and green controls

1. **Browser wheel transport emits one sample.** The pre-repair fake browser observed the synthetic
   second dispatch:

```text
$ env TMPDIR=/home/joao/t239 pnpm exec vitest run packages/playtest/__tests__/click-step.spec.ts -t 'wheel input delivers exactly one browser input sample'
AssertionError: expected [ { deltaY: -160 } ] to have a length of +0 but got 1
Test Files 1 failed (1)
Tests 1 failed | 10 skipped (11)
exit_code=1
```

   A real Abyss run without the frame barrier also failed because the fixed-step sample raced the
   browser event: `TN_PLAYTEST_CAMERA_ASSERTION_FAILED`, `separation: 6000`, `within: 5975`,
   `pass: false`, exit 1. After removing the synthetic `dispatchEvent` and waiting for the one
   Playwright event, the focused control was green:

```text
$ env TMPDIR=/home/joao/t239 pnpm exec vitest run packages/playtest/__tests__/click-step.spec.ts -t 'wheel input delivers exactly one browser input sample'
Test Files 1 passed (1)
Tests 1 passed | 10 skipped (11)
exit_code=0
```

2. **Native wheel steps fail closed.** Before the native-runner guard, all three targets reached
   the bridge path instead of reporting unsupported input:

```text
$ env TMPDIR=/home/joao/t239 pnpm exec vitest run packages/playtest/__tests__/click-step.spec.ts -t 'native (android|desktop|ios) wheel input fails closed before startup'
AssertionError: expected [ { …(5) } ] to deep equally contain ObjectContaining{…}
"code": "TN_PLAYTEST_BRIDGE_MISSING"
Test Files 1 failed (1)
Tests 3 failed | 8 skipped (11)
exit_code=1
```

   The repaired runners reject wheel steps before `prepare` or `start`:

```text
$ env TMPDIR=/home/joao/t239 pnpm exec vitest run packages/playtest/__tests__/click-step.spec.ts -t 'native (android|desktop|ios) wheel input fails closed before startup'
Test Files 1 passed (1)
Tests 3 passed | 8 skipped (11)
exit_code=0
```

   The browser schema control also passed: `packages/playtest/__tests__/scenario.spec.ts -t
   'browser wheel step'` — `Test Files 1 passed (1)`, `Tests 1 passed | 46 skipped (47)`, exit 0.

3. **Native conformance uses the host source contract.** Removing the scene's native listener made
   the source control fail:

```text
$ env TMPDIR=/home/joao/t239 pnpm --dir packages/runtime-native exec vitest run --config vitest.config.ts tests/input-multitouch.test.mjs -t 'native scroll conformance records the host source contract'
AssertionError: The input did not match the regular expression /addEventListener\(['"]wheel['"]/u.
Test Files 1 failed (1)
Tests 1 failed | 11 skipped (12)
exit_code=1
```

   Removing `processMouseWheel(event.wheel);` from the SDL event switch independently failed with
   `AssertionError: The input did not match the regular expression /processMouseWheel\(event\.wheel\)/u.`
   The restored source-contract control passed:

```text
$ env TMPDIR=/home/joao/t239 pnpm --dir packages/runtime-native exec vitest run --config vitest.config.ts tests/input-multitouch.test.mjs -t 'native scroll conformance records the host source contract'
Test Files 1 passed (1)
Tests 1 passed | 11 skipped (12)
exit_code=0
```

   The registry is now `planned` with `availability: source-contract-only`; no native runtime
   scroll result is claimed.

4. **Browser and native wheel signs agree.** Temporarily flipping the core mapping produced the
   opposite-sign regression:

```text
$ env TMPDIR=/home/joao/t239 pnpm exec vitest run packages/core/__tests__/input.spec.ts -t 'DOM deltaY'
AssertionError: expected -0.3 to be close to 0.3, received difference is 0.6, but expected 0.005
Test Files 1 failed (1)
Tests 2 failed | 40 skipped (42)
exit_code=1
```

   Temporarily restoring the old native negative scale failed the SDL source control with
   `AssertionError: The input did not match the regular expression /data\.deltaY = event\.y \* 120\.0/u.`
   With the mapping restored, the green controls were:

```text
$ env TMPDIR=/home/joao/t239 pnpm exec vitest run packages/core/__tests__/input.spec.ts -t 'DOM deltaY'
Test Files 1 passed (1)
Tests 2 passed | 40 skipped (42)
exit_code=0

$ env TMPDIR=/home/joao/t239 pnpm --dir packages/runtime-native exec vitest run --config vitest.config.ts tests/input-multitouch.test.mjs -t 'native scroll preserves the SDL to DOM wheel sign'
Test Files 1 passed (1)
Tests 1 passed | 11 skipped (12)
exit_code=0
```

   The contract is now `deltaY < 0` toward the user → positive zoom intent and `deltaY > 0`
   away from the user → negative intent. SDL horizontal wheel values remain scaled positively to
   DOM pixels; vertical SDL values are inverted into DOM `deltaY`. `window.cpp` still routes
   `SDL_EVENT_MOUSE_WHEEL` through `processMouseWheel` and the native DOM dispatch callback.

5. **Capability examples compile once.** The pre-repair manifest contained two declarations:

```text
$ env TMPDIR=/home/joao/t239 pnpm exec vitest run packages/engine-mcp/__tests__/search.spec.ts -t 'compilable example for the plain-language zoom capability'
AssertionError: expected [ 'const game', 'const game' ] to have a length of 1 but got 2
Test Files 1 failed (1)
Tests 1 failed | 12 skipped (13)
exit_code=1
```

   After fixing the source annotation and regenerating both manifests and the capability reference:

```text
$ env TMPDIR=/home/joao/t239 pnpm exec vitest run packages/engine-mcp/__tests__/search.spec.ts -t 'compilable example for the plain-language zoom capability'
Test Files 1 passed (1)
Tests 1 passed | 12 skipped (13)
exit_code=0
```

### Repair verification

| Gate | Result | Exact command/result |
| --- | --- | --- |
| Focused changed tests | PASS | Core, engine MCP, playtest, schema, template, and scaffold suites — exit 0; 6 files, 177 tests passed. |
| Native focused tests | PASS | `pnpm --dir packages/runtime-native exec vitest run --config vitest.config.ts tests/input-multitouch.test.mjs` — exit 0; 12 passed. |
| Playtest build | PASS | `pnpm --filter @threenative/playtest build` — exit 0. |
| Typecheck | PASS | `env TMPDIR=/home/joao/t239 pnpm typecheck` — exit 0; recursive `16 of 17` projects completed. |
| Budgets and census | PASS | `env TMPDIR=/home/joao/t239 pnpm census` — exit 0; `env TMPDIR=/home/joao/t239 pnpm budgets` — exit 0; native total `107309` and generated references in sync. |
| Changed-file Biome | PASS | `pnpm exec biome check` on the changed TypeScript/JavaScript files — exit 0; 31 existing warnings, no errors. |
| Generated agent mirrors | PASS | `pnpm sync:agents` — exit 0; `16 mirrors, 0 written`. |
| Full build | PASS | `env TMPDIR=/home/joao/t239 pnpm build` — exit 0. |

### Web playtests and captures

- Real Abyss scenario: `sh scripts/xvfb.sh node packages/playtest/dist/runner/cli.js examples/abyss-framework/playtests/zoom-input.playtest.json --url http://127.0.0.1:5181 --server-command "pnpm --filter abyss-framework dev --host 127.0.0.1 --port 5181 --strictPort" --browser-recipe webgpu --headed` — exit 0; NVIDIA WebGPU, camera `6000 → 5950`, separation `5950 <= 5975`, diagnostics empty.
- Abyss before/after capture rerun with the same steps and `screenshots: before-after`: exit 0; `changedPixelRatio: 0.9260134548611111`; inspected [`before.png`](../../artifacts/playtest/before.png) and [`after.png`](../../artifacts/playtest/after.png), showing the non-blank menu and rendered Abyss scene.
- Generated starter: `pnpm sandbox --genre platformer --template starter --name lane239-starter --out /home/joao/t239/sandbox` followed by both `zoom-pinch.playtest.json` and `zoom-wheel.playtest.json` with `--browser-recipe webgpu` — sandbox and playtests exited 0; both reports passed on the NVIDIA Turing WebGPU adapter. Pinch artifacts are 1280×720, 64,130-byte before and 133,133-byte after; wheel artifacts are 1280×720, 64,057-byte before and 133,388-byte after. Inspected all four: each before capture is the starter menu and each after capture is the rendered platform scene with player, HUD, and flag; console captures had no errors.

### Native limitation

`adb devices -l` exited 0 with only `List of devices attached`; `xcrun` is unavailable. The generated
starter sandbox also reported no prebuilt `runtime-native@0.3.0` release for `linux-x64` (GitHub
404) and continued without the native runtime. Native wheel delivery therefore remains unverified;
the source-contract tests above are the only native evidence, and the conformance registry makes no
runtime-native scroll claim.

## Visual inspection

Inspected non-blank WebGPU captures from the generated starter:

- `/tmp/threenative-starter-4w3pNt/starter/artifacts/playtest/20-playtests-zoom-pinch.playtest.json/before.png`
- `/tmp/threenative-starter-4w3pNt/starter/artifacts/playtest/20-playtests-zoom-pinch.playtest.json/after.png`
- `/tmp/threenative-starter-4w3pNt/starter/artifacts/playtest/21-playtests-zoom-wheel.playtest.json/before.png`
- `/tmp/threenative-starter-4w3pNt/starter/artifacts/playtest/21-playtests-zoom-wheel.playtest.json/after.png`

The before captures show the starter menu; the after captures show the rendered platform scene,
player, HUD, and flag. The playtest reports recorded `changedPixelRatio: 0.9544748263888889`
for the wheel run and no console or network errors.

## Worker verification and negative controls

The worker's committed verification reported `pnpm build`, `pnpm typecheck`, `pnpm budgets`, and
`pnpm quality` passing; full Vitest passed with 240 files and 2,361 tests. Removal controls were
red for the scroll binding, wheel listener, pinch derivation, native host source, and
`springArm.dolly()` consumer. These controls cover the browser/native transport, schema, and
consumer wiring rather than only the presence of exported symbols.

The full repository gates were not green in this lane for pre-existing reasons: `pnpm lint`
reported 6 existing cognitive-complexity errors and 447 warnings; `pnpm test` stopped on stale
PRD documentation links; and `pnpm test:templates` reported the existing shooter
`TN_CAPTURE_BLANK` visual failure. Android/iOS are unverified because no native binary or attached
device was available.

## Linchpin helper

`sh /home/joao/.codex/plugins/cache/linchpin/linchpin/0.10.0/scripts/linchpin.sh gate \
/home/joao/projects/threenative/threenative-engine/docs/PRDs/feature-mining/PRD-239-camera-intent-is-one-portable-gesture-stream.md \
/home/joao/projects/threenative/threenative-engine/.linchpin/lane-239-gates.md`

The helper exited 1 with:

```text
ERROR: missing Negative Controls: docs/PRDs/feature-mining/PRD-239-camera-intent-is-one-portable-gesture-stream.md
ERROR: missing Acceptance Criteria: docs/PRDs/feature-mining/PRD-239-camera-intent-is-one-portable-gesture-stream.md
ERROR: missing Checkpoint Protocol: docs/PRDs/feature-mining/PRD-239-camera-intent-is-one-portable-gesture-stream.md
ERROR: one or more Files (N) lists are malformed: docs/PRDs/feature-mining/PRD-239-camera-intent-is-one-portable-gesture-stream.md
ERROR: ledger row lacks a real caller or negative control: docs/PRDs/feature-mining/PRD-239-camera-intent-is-one-portable-gesture-stream.md
ERROR: Negative Controls must have one exact command/result field per gate: docs/PRDs/feature-mining/PRD-239-camera-intent-is-one-portable-gesture-stream.md
```

That source PRD was not rewritten during implementation.

## Continuation round — lane-239-sign-continuation

This continuation closes the native vertical-sign defect left by the parent repair and adds the
missing negative/green evidence for the generated starter's scroll binding. The native runtime
remains unexecuted; the registry row stays `availability: source-contract-only` and `status:
planned`.

### Required negative controls — red before restoration

1. **Native SDL-to-DOM wheel sign.** After changing the native test to evaluate the production
   `data.deltaY` assignment in both directions, the existing pass-through conversion failed:

```text
$ pnpm --dir packages/runtime-native exec vitest run --config vitest.config.ts tests/input-multitouch.test.mjs -t 'native scroll preserves the SDL to DOM wheel sign'
AssertionError: Expected values to be strictly equal:

120 !== -120

Tests 1 failed | 11 skipped (12)
exit_code=1
```

2. **Generated starter scroll binding.** With `scroll: true` temporarily removed from the
   generated starter's `zoom` binding, the exact playtest control reached the camera assertion and
   failed with no camera movement:

```text
$ sh scripts/xvfb.sh node node_modules/@threenative/playtest/dist/runner/cli.js --scenario playtests/zoom-wheel.playtest.json --browser-recipe webgpu
"code": "TN_PLAYTEST_CAMERA_ASSERTION_FAILED"
"distance": 0,
"expectMoved": false,
"pass": false,
exit_code=1
```

The same red run also reported the expected headless-environment diagnostics (`TN_PLAYTEST_SOFTWARE_ADAPTER`
and `TN_CAPTURE_BLANK`); those were not used as the behavior result. The scratch binding was restored
immediately after this control.

### Restored sign and binding evidence

- Native `processMouseWheel` now maps SDL `y = 1` to DOM `deltaY = -120` and SDL `y = -1` to
  DOM `deltaY = 120`. The behavioral regression evaluates that assignment and retains the source
  contract proving `SDL_EVENT_MOUSE_WHEEL` → `processMouseWheel` → `dispatchWheelEvent` → the
  conformance scene's real `wheel` listener.
- Browser `InputMap` coverage remains explicit: negative DOM `deltaY` produces positive zoom intent,
  and positive DOM `deltaY` produces negative zoom intent. The generated starter keeps
  `zoom: { gamepadAxes: [3], pinch: true, scroll: true }`.

```text
$ pnpm --dir packages/runtime-native exec vitest run --config vitest.config.ts tests/input-multitouch.test.mjs
Test Files 1 passed (1)
Tests 12 passed (12)
exit_code=0

$ pnpm exec vitest run packages/core/__tests__/input.spec.ts
Test Files 1 passed (1)
Tests 42 passed (42)
exit_code=0
```

### Restored WebGPU playtest

From `/home/joao/t239/sandbox/lane239-starter`, the restored binding passed the real headed WebGPU
scenario:

```text
$ sh /home/joao/projects/threenative/threenative-engine/.worktrees/feature-mining-239-sign-continuation-20260828/scripts/xvfb.sh node /home/joao/projects/threenative/threenative-engine/.worktrees/feature-mining-239-sign-continuation-20260828/packages/playtest/dist/runner/cli.js --scenario playtests/zoom-wheel.playtest.json --browser-recipe webgpu --headed --server-command 'pnpm dev --host 127.0.0.1 --port $PORT --strictPort' --project .
"diagnostics": [],
"separation": 9.11044920495327,
"within": 9.2,
"changedPixelRatio": 0.9544943576388889,
"vendor": "nvidia",
"architecture": "turing",
"pass": true,
exit_code=0
```

Inspected `/home/joao/t239/sandbox/lane239-starter/artifacts/playtest/before.png` and
`after.png`: both are nonblank; the before capture is the starter menu and the after capture is the
rendered platform scene with player, HUD, and flag. `adb devices -l` still reports no attached
device and `xcrun` is unavailable, so no native runtime result is claimed.
