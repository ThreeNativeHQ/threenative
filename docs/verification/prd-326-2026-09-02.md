# PRD-326 verification — high-churn paths fail before regressions ship

Run: 2026-09-02

Baseline SHA for every phase: `5879799d54a873d65245abb7f038b47b4e35594b`

This record keeps one evidence file for the seven PRD phases so the verification tree remains
under its tracked-file budget. Each phase records its green command, collected result, negative
controls, restoration, and executed targets.

## Phase 1 — startup, batch verdicts, and startup waits

Status: PASS for the targeted behavioral gate.

### Green run

```text
pnpm exec vitest run packages/core/__tests__/game.spec.ts packages/playtest/__tests__/cli-usage.spec.ts packages/playtest/__tests__/runner.spec.ts packages/playtest/__tests__/device-playtest.spec.ts
```

Exit code: `0`

Collected result: 4 files, 129 tests passed.

The captured game trace proves the opaque loader renders before the first world frame, fixed-step
compute is absent before `startup.ready`, and compute occurs on the first ready frame. The browser
and Android runner tests sample only after the advertised startup phase. Batch tests preserve
assertion exit codes `1`, framebuffer-window exit `2`, and capture-lock timeout exit `75`.

### Negative controls

- Inverted the startup compute guard; `pnpm exec vitest run packages/core/__tests__/game.spec.ts -t 'keeps startup compute behind the first ready world'` exited `1`: `firstCompute=1` and `firstWorld=2` violated the strict ordering assertion.
- Replaced batch worst-of aggregation with the last report code; `pnpm exec vitest run packages/playtest/__tests__/cli-usage.spec.ts -t 'batch CLI verdict preserves the worst assertion exit code'` exited `1`: expected `1`, received `0` for `[fail, pass]`.
- Removed the browser startup-wait call; `pnpm exec vitest run packages/playtest/__tests__/runner.spec.ts -t 'browser runner samples only after the advertised startup phase is ready'` exited `1`: the report had no `startup.phase=ready` observation.
- Removed the Android startup-wait call; `pnpm exec vitest run packages/playtest/__tests__/device-playtest.spec.ts -t 'Android runner samples only after the advertised startup phase is ready'` exited `1`: observed phases were only `collapsing`, not terminal `ready`.

Each mutation was restored before the green run.

Targets: Linux Node/Vitest core runtime, browser fixture runner, and Android runner transport
simulation. No physical Android device or iOS target was executed in this phase.

## Phase 2 — renderer retargeting and GPU timing

Status: PASS for the targeted behavioral gate.

### Green run

```text
pnpm exec vitest run packages/core/__tests__/renderer.spec.ts packages/core/__tests__/gpu-timestamp-resolve-cadence.spec.ts packages/core/__tests__/game-frame-budget-surface.spec.ts
```

Exit code: `0`

Collected result: 3 files, 18 tests passed.

The renderer test retargets an explicit `worldPass` from scene A to scene B and rejects ambiguous
output graphs. The frame-budget surface receives a positive GPU timestamp and keeps rendering
when timestamp resolution rejects; the cadence test resolves GPU timestamps per frame.

### Negative controls

- Made `setOutputPipelineRoot` a no-op; `pnpm exec vitest run packages/core/__tests__/renderer.spec.ts -t 'retargets the explicit world pass'` exited `1`: the pass still referenced scene A instead of scene B.
- Replaced the positive render timestamp with `0`; `pnpm exec vitest run packages/core/__tests__/game-frame-budget-surface.spec.ts -t 'carries a positive GPU timestamp'` exited `1`: expected `gpuMs=6.25`, but the field was absent.
- Removed the rejected-timestamp catch; `pnpm exec vitest run packages/core/__tests__/game-frame-budget-surface.spec.ts -t 'keeps rendering and omits GPU time'` exited `1` with two unhandled `Error: timestamp unavailable` rejections.

Each mutation was restored before the green run.

Targets: Linux Node/Vitest through the real core renderer/game loop and frame-budget report surface.

## Phase 3 — built capabilities and bridge timeouts

Status: PASS for the targeted behavioral gate.

### Green runs

```text
pnpm exec vitest run scripts/__tests__/check-capability-docs.spec.ts packages/playtest/__tests__/bridgeClient.spec.ts
pnpm build
pnpm tsx scripts/check-capability-docs.ts --census
```

Exit codes: `0`, `0`, `0`.

Collected result: 2 files, 29 tests passed. The built-import gate verified 256 symbols across 31
import paths; the capability census verified 234 documented exports.

The bridge transport recording proves a 600-tick `advance` receives its calculated timeout while
`sample`, `ready`, and `describe` use the default timeout. The capability check imports built
package output and fails closed when a declared export-map target is absent.

### Negative controls

- Removed one export-map target and changed the checker to continue on the missing target; `pnpm exec vitest run scripts/__tests__/check-capability-docs.spec.ts -t 'export-map entry disappears'` exited `1`: it resolved with only `2` import paths and `1` symbol instead of rejecting.
- Omitted the third timeout argument from `advance`; `pnpm exec vitest run packages/playtest/__tests__/bridgeClient.spec.ts -t 'routes bulk advance and ordinary bridge calls through their intended timeouts'` exited `1`: expected `170000`, received `5000`.

Each mutation was restored before the green runs. During the built scan, the stale Three.js TSL
capability spelling `normal` was corrected to the shipped `normalView`, then the manifest and
reference were regenerated and rechecked.

Targets: Linux Node/Vitest, built workspace package output, and the root capability documentation
command.

## Phase 4 — observed contacts and entity drift

Status: PASS for the targeted behavioral gate.

### Green run

```text
pnpm exec vitest run packages/core/__tests__/playtest.spec.ts packages/playtest/__tests__/movement-evidence.spec.ts packages/core/__tests__/input.spec.ts packages/core/__tests__/probe-volume.spec.ts
```

Exit code: `0`

Collected result: 4 files, 90 tests passed.

The real playtest bridge resolves nested physics bodies, clears contacts on `goto`, bounds drains
at the production limit, and reports only registered capabilities. Movement evidence uses the
first and last observed entity windows and rejects a subject observed only once.

### Negative controls

- Replaced nested graph traversal with a shallow registry lookup; `pnpm exec vitest run packages/core/__tests__/playtest.spec.ts -t 'publishes registry tags and drained contacts through gameplay channels'` exited `1`: the expected `fox` contact was missing.
- Preserved contact history across `goto`; `pnpm exec vitest run packages/core/__tests__/playtest.spec.ts -t 'clears contact history on goto and bounds one oversized contact drain|should re-register only the active scene entities after goto'` exited `1`: old contacts and the oversized burst remained observable.
- Replaced missing movement-window handling with a zero/undefined fallback; `pnpm exec vitest run packages/playtest/__tests__/movement-evidence.spec.ts -t 'first observed step'` exited `1`: the required first observed position was missing rather than reporting a valid window.

Each mutation was restored before the green run.

Targets: Linux Node/Vitest through the real core playtest bridge/report builders and the playtest
movement evaluator. No device target was executed in this phase.

## Phase 5 — native WebGPU feature contracts

Status: PASS for the native source-contract gate; this record makes no unsupported platform claim.

### Green runs

```text
pnpm exec vitest run --config packages/runtime-native/vitest.config.ts --dir packages/runtime-native tests/physics-parity-verifier.test.mjs tests/conformance-runner.test.mjs tests/webgpu-bindings-contract.test.mjs -t 'native physics|floor contact|registry|windowed surface proof|backend and canvas contexts|normalizes|generated|controls|help'
cargo test --manifest-path packages/runtime-native/native/physics/Cargo.toml --lib --test parity -- --nocapture
```

Exit codes: `0`, `0`.

Collected result: native Vitest selected 3 files, 21 tests passed, 77 tests skipped by the filter;
Rust passed 13 unit tests and 2 parity tests. The requested/granted feature sets agree, including
`TextureAdapterSpecificFormatFeatures`, and all three initialization blocks retain the required
`hasTimestampQuery_` unpack contract. Rust feet-on-floor metrics were all zero or within the
declared tolerance.

### Negative controls

- Removed `TextureAdapterSpecificFormatFeatures` from the reporter; the native contract command exited `1`: the reported set had 7 features while the requested set had 8.
- Removed `hasTimestampQuery_` from the windowed initialization block; the same native contract command exited `1`: the required block regex no longer matched.

Each mutation was restored before the green runs.

Targets: Linux native contract test harness and Linux Rust physics library/parity harness. This is
not a desktop host-render execution, and no Android device/emulator or iOS target was executed
here.

## Phase 6 — behavioral core and fixture contracts

Status: PASS for the targeted behavioral gate.

### Green run

```text
pnpm exec vitest run packages/core/__tests__/game.spec.ts packages/core/__tests__/probe-volume.spec.ts packages/core/__tests__/input.spec.ts packages/core/__tests__/playtest.spec.ts packages/playtest/__tests__/runner.spec.ts
```

Exit code: `0`

Collected result: 5 files, 181 tests passed. The four source-text assertions were replaced by
runtime behavior, the real bridge reports no impossible `runtime.example` capability, and the
served fixture derives its protocol limits/capabilities from production values.

### Negative controls

- Inverted the render-metrics enable branch; `pnpm exec vitest run packages/core/__tests__/game.spec.ts -t 'renders the world behind an opaque CanvasLayer'` exited `1`: expected draw-call/triangle metrics were absent.
- Replaced the no-overlay metrics result with `{}`; the same command exited `1`: expected frame metrics were missing.
- Added `runtime.example` to production contributed capabilities; `pnpm exec vitest run packages/core/__tests__/playtest.spec.ts -t 'merges and deduplicates contributed capabilities'` exited `1`: the unexpected capability appeared in the report.
- Drifted the fixture `maxEventsPerDrain` from `1000` to `1001`; `pnpm exec vitest run packages/playtest/__tests__/runner.spec.ts -t 'served fixture reports the production protocol limits'` exited `1` because the deep-equality report failed.
- Made the gamepad test subject implement only an array `.find` path; `pnpm exec vitest run packages/core/__tests__/input.spec.ts -t 'uses the first connected gamepad'` exited `1` with `InputMap must scan gamepad slots without the array find method.`

Each mutation was restored before the green run.

Targets: Linux Node/Vitest through core runtime entry points, the served browser fixture, and the
runner report. No device target was executed in this phase.

## Phase 7 — template boot, windowed surfaces, and native physics

Status: PASS for the targeted surface and physics contracts; the broader desktop parity matrix
retains unrelated pre-existing failures documented below.

### Green runs

```text
pnpm exec vitest run --config packages/runtime-native/vitest.config.ts --dir packages/runtime-native tests/conformance-runner.test.mjs -t 'windowed surface proof'
```

Exit code: `0`

Collected result: 1 test passed. The validator accepts only at least two non-null view observations
and matching presents.

```text
pnpm exec vitest run packages/physics/__tests__/character.spec.ts -t '500ms render stall|feet|floor|grounded'
```

Exit code: `0`

Collected result: 5 tests passed. The fixed-step loop keeps five exact 1/60 simulation steps during
a 500 ms render stall.

```text
cargo test --manifest-path packages/runtime-native/native/physics/Cargo.toml --lib --test parity -- --nocapture
```

Exit code: `0`

Collected result: 13 Rust unit tests and 2 native parity tests passed. The feet-on-floor metrics
were all zero, including grounded mismatch, collider mismatch, and position/normal deltas.

```text
pnpm parity
```

Exit code: `1`

The desktop report was written at
`packages/runtime-native/artifacts/conformance/desktop/report.json` after the Linux desktop target
executed 93 rows: 82 passed, 8 failed, and 3 were blocked. The new
`windowed-surface-two-frames` row passed with `view=true`, 1,170 observed frames, 1,170 presents,
and exit code 0 for its native process. The web half reported 90 passed, 1 failed, and 2 blocked.
The Android and iOS reports each recorded 0 passed, 0 failed, and 93 blocked because no device or
simulator proof completed; no Android or iOS result is generalized here.

```text
SDL_AUDIODRIVER=dummy pnpm native:verify:desktop
```

The unqualified command first exited `1` because this headless host's PulseAudio connection was
refused and ALSA reported `Host is down`; the repository doctor confirmed the environment issue.
The final-HEAD rerun with SDL's dummy audio backend exited `0`.

Collected result: the native-smoke bundle verified, desktop audio/crash/lifecycle/null-handle/core
contracts passed, 300 desktop frames were presented, desktop physics actuation/query passed, 31 of
31 native contract targets passed, and the desktop loading playtest passed with 913,920 startup
loading pixels and 0 settled loading pixels.

```text
pnpm test:templates
```

The final-HEAD rerun exited `0`. A prior worker run exited `1` on the ordinary
`defense-survive-ten-waves` leak assertion; the rerun passed without changing its threshold. The
first complete post-rebase matrix also observed one transient browser-console error in
`action-rpg-touch-controls`; the isolated action-RPG rerun and the complete matrix rerun both
exited `0`, with no code change required. The shooter production-performance scenario exposed
stale ceilings after remote `main` changed its scene: measured `400` draw calls and `3,019`
triangles were re-pinned as `420` and `3,200`; its `33 ms` frame-time p95 ceiling was unchanged.
All six in-scope real-frame boot smokes passed after 60 frames, reached
`startup.phase=ready`, rendered a nonblank frame, and had zero console/network/runtime diagnostics:
`action-rpg`, `defense`, `minimal`, `racing`, `sailing`, and `shooter`. The command's ordinary
scenario matrix passed on the final run.

### Negative controls

- Perturbed the Rust floor contact position by `-0.1`; the parity command exited `101` with `feetOnFloorPositionMaxAxisDelta: 0.09007463`, above the `0.02` tolerance.
- Replaced the fixed-step callback delta with wall-clock elapsed time; the 500 ms render-stall command exited `1` because the observed deltas were not all `1/60`.
- Inverted the windowed validator's `view` check; `pnpm exec vitest run --config packages/runtime-native/vitest.config.ts --dir packages/runtime-native tests/conformance-runner.test.mjs -t 'windowed surface proof requires two non-null views'` exited `1` with two false `did not acquire a non-null view` errors for the healthy fixture.
- Raised the live template boot nonblank requirement from `0.0001` to `1.1`; the generated minimal-project smoke exited `1` with `TN_PLAYTEST_REGION_BLANK` (`observed 0.9904`, required `1.1`) after its real 60-frame run.
- Swapped the production X11 window argument from `reinterpret_cast<uint64_t>(window)` to `reinterpret_cast<uint64_t>(window) + 1`, rebuilt with `cmake --build packages/runtime-native/build/tn-linux --target mystral --parallel 2`, then ran `pnpm parity --target desktop --only-tests windowed-surface-two-frames`; it exited `1`, with the selected row failing at native exit `1`, `Invalid X Window`, zero `TN_SURFACE_FRAME` markers, and missing `TN_PRESENTS`. Restoring `reinterpret_cast<uint64_t>(window)` and rebuilding returned the selected row to `status=pass`, native exit `0`, `1,155` frames, `1,155` presents, and no validator errors; the bounded command itself exited `2` because its other 92 rows were intentionally blocked.
- Inserted `throw new Error("PRD-326 template boot red control")` into `packages/create-threenative/templates/minimal/src/game.ts`, then ran `TN_TEMPLATE_ONLY=minimal pnpm test:templates`; it exited `1`, with the real-frame smoke reporting `TN_PLAYTEST_CAPTURE_PROVENANCE_MISSING` and `frames: 0` (the ordinary scenarios also reported `TN_PLAYTEST_BRIDGE_MISSING`). Removing the throw restored the same smoke to `pass: true`, `frames: 60`, `startup.phase=ready`, `nonblankPixelRatio=0.9903472222222223`, and no diagnostics; the wrapper still exited `1` for the unrelated ordinary `play` scenario's pre-existing `maxTriangles` observation `4,737` versus `1,223`.

Each mutation was restored before the targeted green runs.

Targets: Linux Node/Vitest, Linux Rust physics parity, Linux desktop V8/Dawn native conformance,
and browser WebGPU template smokes executed. Android and iOS were not executed to a completed
target proof.
