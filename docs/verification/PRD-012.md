# PRD-012 verification — scaffold test runs playtest

Date: 2026-08-03

Status: PASS. The runner, templates, CI scaffold smoke, fresh scaffold test, and
fail-closed negative controls are verified. The fresh scaffold uses the same stable
WebGL2 fallback route as PRD-011 when no WebGPU adapter is available.

## Verified

| Check | Result |
| --- | --- |
| focused scaffold/runner suite | PASS — 4 files, 19 tests |
| `pnpm typecheck` | PASS — root, 8 workspace packages, and both examples |
| `pnpm lint` | PASS — 154 files |
| `pnpm test` | PASS — package builds and publint; 84 files, 410 tests |
| `pnpm budgets` | PASS — 8 packages, 2,097 framework LOC, 4 active PRD files |
| `pnpm sync:agents --check` | PASS — 9 CLAUDE mirrors |
| `git diff --check` | PASS |
| fresh packed scaffold `pnpm test` | PASS — build, browser runner, HUD score `0 → 1` |

The focused negative controls prove that a `pageerror`, missing runtime canvas,
missing HUD id, legacy TypeScript scenario, and empty assertion set fail closed. The
runner now samples the starter DOM HUD and the templates now run
`threenative-playtest --scenario playtests/play.playtest.json` after building.

Static migration checks also pass:

```text
find packages/create-threenative/templates -name '*.playtest.ts'   # no output
rg '"test": "vite build"' packages/create-threenative/templates   # no output
rg 'hud: \{\}' packages/playtest/src/runner                     # no output
```

Both templates declare Playwright and install Chromium from their `pretest` lifecycle
hook. The fresh scaffold test used an explicit private server URL and port, so it did
not attach to an unrelated process already listening on the default Vite port.

## Fresh scaffold evidence

The packed local workspace packages were installed into a newly generated starter.
The exact consumer command passed:

```text
pnpm --dir /tmp/tmp.TvFEQ2eDqC/starter test
```

The report contained no diagnostics or network errors. The supplied HUD observation
changed from `score0...` to `score1...`, and `hud.score.#root` passed. The only browser
messages were the expected WebGPU-unavailable and WebGL2-fallback warnings.

The runtime negative control was then run against the same temporary scaffold after
inserting `throw new Error("intentional PRD-012 runtime failure")` in `Play.enter`:

```text
pnpm test → exit 2
TN_PLAYTEST_RUNNER_FAILED: Cannot advance a stopped loop.
```

The command went red; it did not report a false green. The focused suite also records
red controls for page errors, `runtimeReady: false`, missing HUD ids, empty assertions,
and legacy TypeScript scenario loading.

CI now packs the local playtest package, passes it to the scaffold smoke, installs
Chromium, and runs the generated project's `pnpm test`.
