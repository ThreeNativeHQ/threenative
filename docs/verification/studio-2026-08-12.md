# ThreeNative Studio verification — 2026-08-12

**Result: Phases 1–3 pass.** A packed `@threenative/studio` ran inside a fresh starter
consumer, a user instruction sent from the browser caused bounded Codex to edit the real
TypeScript, Vite hot-reloaded it, and Studio's evidence panel observed three passing playtest
assertions. Git checkpoint and restore operated on the same ordinary project.

The nontrivial state-preservation proof remains the independent Phase 0 measurement in
[`studio-loop-2026-08-12.md`](../spikes/studio-loop-2026-08-12.md): 30 ticks moved
`1.000002` units before the edit and `1.999974` afterward, while `GameState.playerX` changed
by only `0.000003` across HMR. Its no-hot control lost the non-default position.

## Consumer proof

The package was built, packed, and installed into the isolated starter at
`/tmp/prd084-phase1.Cb2pra/game`; it was not workspace-linked. Studio started the project's
normal `pnpm dev` process and displayed it in the preview iframe.

From Studio's browser chat, the user sent:

```text
Edit exactly src/entities/Player.ts. Change the MOVE_SPEED constant from 4 to 6.
Do not modify any other line or file.
```

Observed results:

- Codex exited `0` in `17.454` seconds and changed only the requested constant.
- Vite logged the HMR update; Studio's transcript explicitly did not infer preview state from
  the agent result.
- The evidence panel reported `Observed 3 assertion result(s), exit 0.`
- A direct request to the same packaged server returned
  `{"assertionCount":3,"diagnosticCodes":[],"exitCode":0,"status":"passed"}`.
- Closing Studio left `pnpm dev`, the project's installed scripts, and `git log` working with
  no Studio project file or alternate format.

The final UI was inspected at 1600×1200. The game rendered, the process and agent indicators
were observed live, and the evidence and ownership controls were visible below the preview.

## Fail-closed and ownership controls

| Surface | Positive observation | Adversarial observation |
|---|---|---|
| Agent | Browser chat invoked bounded Codex and reported exit/time | An absent requested binary produces `AGENT ABSENT`, a reason, and disabled send |
| Preview | State becomes `PROCESS REACHED` only after the URL answers | Initial copy says render and hot-state preservation are not observed |
| Evidence | Existing CLI returned 3 assertions and exit `0` | Removing `playtest()` returned exit `2`, `TN_PLAYTEST_BRIDGE_MISSING`, and `not-observed` |
| Checkpoint | API committed `faf967a` (`Studio checkpoint speed four`) and observed a clean tree | Button is disabled without git, a message, or changes |
| Restore/export | Restore replaced speed `5` with checkpointed speed `4`; copy-directory has success and denied-clipboard outcomes | Restore requires a selected known commit and browser confirmation; no Share or fake Export control exists |

The disposable packaged-consumer driver had two harness defects corrected before use: its
proof wait accidentally retained Playwright's 30-second default, and keyboard events aimed at
Studio's outer window did not reach the game iframe. A third iframe-keyboard attempt also
produced zero movement, so that line of driver work stopped. It is not counted as a state
proof. Phase 0's direct game-page probe is the authoritative nontrivial state measurement;
the packaged run proves the Studio browser, bounded edit, HMR, evidence, and ownership
integration around that already-proved mechanism.

## Gates

```sh
pnpm exec biome check packages/studio/src packages/studio/__tests__
pnpm --filter @threenative/studio typecheck
pnpm exec vitest run packages/studio/__tests__/studio.spec.ts
pnpm --filter @threenative/studio test
```

All passed: 6 focused tests, TypeScript, build, pack, and publint. The tests include a real
temporary git repository and exact playtest exit classification.

The first root typecheck exposed that the root compiler follows the Studio test's import into
`app.tsx` but had no JSX transform configured. It failed with `TS6142`; adding the same
`react-jsx` setting already used by `packages/ui` to the root test compiler fixed the gate.

The deletability control moved `packages/studio` to a temporary directory under a restore
trap, then ran:

```sh
pnpm typecheck
pnpm test
```

Both passed without Studio: 100 Vitest files and 840 tests passed, plus the package build and
native parity chain. The package was restored automatically.

With Studio restored, the final repository gates passed: `pnpm typecheck`, `pnpm lint`, and
`pnpm test` completed with 101 Vitest files and 846 tests green. Lint emitted the repository's
179 warn-level diagnostics and no errors.

```sh
grep -rn "operationRegistry\|planHash\|applyPlan\|sceneFormat" packages/
grep -rn -i "studio" packages/core/src packages/physics/src packages/playtest/src packages/ui/src
pnpm budgets
```

Both greps had no hits. Budgets reported `7 framework packages`, `9,535/15,000 framework
LOC`, and Studio contributes 698 non-test source lines. The framework review trigger remains
uncrossed. The existing native-runtime trigger is unchanged because Studio has no native
source. The kill-switch comparison is 698 lines here versus the sibling Studio stack's
60,105 lines; no IR, model gateway, operation registry, project format, or revision store was
added.
