# PRD-100 verification evidence — 2026-08-13

Final evidence after review repair. Signal was killed because the Phase 0 census found one
distinct template with a hand-written event bridge, below the predeclared `>=3` bar. Groups
was also killed at zero qualifying queries. The delivered change is deferred `queueFree`; the
platformer remains the real playtest subject and its shipped `collect.playtest.json` scenario
remains unchanged.

## Phase 1 — Signal KILL

```text
Distinct templates with a hand-written event bridge: 1
Signal verdict: KILL; the predeclared >=3 bar was not met
```

The attempted Signal implementation, its tests, the `Area3D` field replacement, and the
automatic event recorder were removed before delivery. `Area3D.on()` and
`platformer/src/playtest-events.ts` remain the incumbent paths. No Signal negative control is
claimed.

## Phase 2 — `queueFree` and the end-of-frame sweep

The live callers are:

```text
packages/core/src/game.ts:494:        this.#entities?.sweep();
packages/core/src/entities.ts:58:  queueFree(target: string | object): void {
packages/core/src/entities.ts:65:  sweep(): void {
packages/create-threenative/templates/platformer/src/entities/Pickup.ts:43:      ctx.entities.queueFree(this);
packages/create-threenative/templates/platformer/src/entities/Patrol.ts:56:        ctx.entities.queueFree(this);
```

The scene incumbent check produced no output:

```text
rg -n 'splice|removeCollected|removeDefeated' packages/create-threenative/templates/platformer/src/scenes/Level.ts
```

`Level.ts` fell from 172 lines to 149 lines. The generated platformer fell from 1,486 LOC to
1,468 LOC.

Declared negative control:

```text
Temporary mutation: the frame-loop call to entities.sweep() was removed.
scenario_status=1
TN_PLAYTEST_TAG_COUNT_ASSERTION_FAILED: Tag 'coin' count 7 did not satisfy the expected count.
resource.GameState.coins: pass true
signal.collected: count 3, pass true
tags.coin: count 7, pass false
platformer-collect: pass false
```

Result: `sweep disabled — PASS; goes red on the shipped collect scenario.` The mutation was
restored, the core package was rebuilt, and the restored scenario passed.

## Phase 3 — Groups KILL

The Phase 0 census measured zero hand-written group queries outside the two per-type arrays
removed by Phase 2. The predeclared `>=3` bar was not met. No `Registry.group()` API or group
scenario was added.

## Phase 4 — Gates and platform evidence

```text
pnpm typecheck
exit 0

pnpm exec biome check --diagnostic-level=error <all in-scope files>
Checked 91 files in 48ms. No fixes applied.

pnpm lint
exit 1
./scripts/__tests__/native-cpu-profile.spec.ts:1 format
one clean-tree, out-of-scope formatter diagnostic; 178 existing warnings

pnpm test
Test Files  99 passed (99)
Tests       827 passed (827)
exit 0

pnpm --filter @threenative/runtime-native test
Test Files  42 passed (42)
Tests       246 passed | 31 skipped (277)
web parity: 24 passed; Rust parity: 1 passed, 0 failed

pnpm test:templates
minimal: scaffolded playtests passed.
starter: scaffolded playtests passed.
platformer: scaffolded playtests passed.

pnpm test:playtest
framework-movement: pass true
framework-camera: pass true

pnpm budgets
budgets ok: 6 framework packages, 4 example workspaces, 8604/15000 framework LOC,
68516/50000 native runtime LOC, 8 PRD files, largest template 1537 LOC

pnpm tsx scripts/count-loc.ts
suggested framework normalised baseline: 432 (current baseline 441)
platformer template LOC: 1468

pnpm native:build && pnpm native:verify:desktop
Verified native-smoke.js (4071565 bytes), one file with no imports
desktop core gate passed: 300 frames, 1280x720
```

The one root lint error is unrelated to this lane's files and was not edited. Root tests and
native desktop verification passed; no Android or iOS claim is made.

## Final composition check on local `main`

The lane was squashed onto a local `main` that already contained newer gameplay, Studio, and
native work. These checks prove the composed tree without attributing those pre-existing lines
to PRD-100:

```text
pnpm typecheck
exit 0

pnpm test:templates
minimal: scaffolded playtests passed.
starter: scaffolded playtests passed.
platformer: scaffolded playtests passed.

pnpm budgets
budgets trigger: native runtime LOC review trigger: 69910 lines (trigger 50000, +19910)
budgets ok: 7 framework packages, 4 example workspaces, 13746/15000 framework LOC,
69910/50000 native runtime LOC, 8 PRD files, largest template 2074 LOC

pnpm tsx scripts/count-loc.ts
platformer template LOC: 1562

pnpm native:build && pnpm native:verify:desktop
Verified native-smoke.js (4073138 bytes), one file with no imports
desktop core gate passed: 300 frames, 1280x720
desktop physics playtest proof passed: 14 assertions
desktop physics query proof passed
```

This final native proof ran on the composed local `main`; no Android or iOS claim is made.
