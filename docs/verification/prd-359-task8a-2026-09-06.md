# PRD-359 task 8a — discovery metadata — 2026-09-06

This is implementation evidence for the published ownership boundary. The row remains pending an
independent read-only review checkpoint.

## Caller and source

- `scripts/not-owned-capabilities.ts:37-40` keeps authoritative replication and client prediction
  game-owned while pointing an authenticated transport request at `@threenative/core/net`.
- `packages/core/capabilities.json:1908-1916` and
  `packages/create-threenative/capabilities.json:1908-1916` are generated from the existing
  `connect` export. The generated reference is at
  `packages/create-threenative/agent-docs/references/capability-reference.md:1544-1559`.
- `packages/engine-mcp/__tests__/search.spec.ts:53-85` is the named positive/negative regression.

## Red

Command:

```sh
pnpm exec vitest run packages/engine-mcp/__tests__/search.spec.ts
```

Exit `1`, `/tmp/prd359-red-capability-search.log`. Before the metadata and ownership update, two
assertions failed: the positive networking query did not expose `connect`, and the old blanket
guidance did not name authoritative replication. This is the captured pre-repair regression.

## Green

Commands:

```sh
pnpm capabilities:sync
pnpm exec vitest run packages/engine-mcp/__tests__/search.spec.ts scripts/__tests__/capability-recall.spec.ts
pnpm capabilities:check
```

All exited `0`.

- `pnpm capabilities:sync` generated 273 entries and 4 not-owned rows
  (`/tmp/prd359-green-capability-sync.log`).
- The focused suites passed 46/46 engine-MCP tests and 16/16 recall tests, 62/62 total
  (`/tmp/prd359-green-capability-recall-focused.log`).
- `pnpm capabilities:check` confirmed the generated manifest is fresh with 273 entries and 4
  not-owned rows (`/tmp/prd359-green-capabilities-check.log`).

The search regression requires `connect` from `@threenative/core/net` for transport, while the
replication/prediction queries return no capability and actionable game-owned guidance.
