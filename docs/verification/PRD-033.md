# PRD-033 verification

Baseline: `80557ed docs: release PRD-033 after Phase 1 gate`.

Current result: **implementation and supported Chromium/WebGPU consumer gates pass;
repository-wide release gates still inherit unrelated dirty proof-file failures**.

The platformer proof names `player.health`, but the pre-change `Character` registry
snapshot did not expose health. The smallest consumer-scoped addition is a `health` field
on `Character`, synchronized from the existing `Checkpoints.hearts` state in `Level`.

## Green source checks

`pnpm typecheck` passed. The final workspace output ended with:

```text
Scope: 7 of 8 workspace projects
packages/create-threenative typecheck: Done
packages/playtest typecheck: Done
packages/core typecheck: Done
packages/physics typecheck: Done
packages/ui typecheck: Done
examples/abyss-framework typecheck: Done
```

The repository-wide Biome gate is blocked by three pre-existing files outside this lane:

```text
./docs/verification/blind-exploration-2026-08-08-webgl/critic.json format
./docs/verification/blind-platformer-2026-08-08-webgl/critic.json format
./docs/verification/blind-topdown-action-2026-08-08-webgl/critic.json format
Found 3 errors.
```

The scoped check for the changed non-ignored files passed:

```text
pnpm exec biome check ...changed files...
Checked 8 files in 8ms. No fixes applied.
```

The repair-focused playtest/core gates passed without browser sockets:

```text
Test Files  5 passed (5)
Tests  43 passed (43)
```

The complete repository test gate passed after the manager-side e2e discovery caught and
fixed a circular import in the new observation-field availability check, and after the
read-only review repairs:

```text
Test Files  116 passed (116)
Tests  794 passed (794)
```

All 11 committed platformer scenario files load through the production JSON loader:

```text
validated 11 platformer scenarios
```

The final `pnpm budgets` gate passed:

```text
pnpm budgets
budgets ok: 7 packages, 3006 framework LOC, 9 PRD files, largest template 1102 LOC
```

An earlier worker attempt hit a transient sandbox IPC failure before the wrapper started;
the final manager run above passed.

```text
Error: listen EPERM: operation not permitted /tmp/tsx-1000/13.pipe
```

Direct `tsup` builds and `publint` checks for playtest, core, physics, UI, and
create-threenative completed as part of the full `pnpm test` gate:

```text
All good!
```

The scaffolded platformer typecheck and Vite build passed using the repository's existing
local dependencies. Vite reported only its existing large-chunk and dynamic-import
warnings.

## Read-only review repairs

The first fresh read-only review returned `VERDICT: REQUEST_CHANGES` with five defects. The
manager repaired each in the same lane:

- `collect.playtest.json` now samples a labeled hold step and expects four remaining coins
  when three have been collected, so the transition and tag checks are satisfiable.
- Pickup callbacks enqueue explicit `collected` events in `playtest-events.ts`; `main.ts`
  drains that queue instead of deriving signals from the score resource.
- Labeled `movement.reachesPositionWithin` and `contacts` assertions now name missing
  `effectLogSeries` or `physicsDebugSeries` observations before evaluation.
- The Integration Ledger has concrete non-test callers and no unresolved cells.

The repair-focused tests and final repository gate above pass after these changes.

## Contract decisions and census

`runtime.audio` was resolved by registering it in `PLAYTEST_CAPABILITY_REGISTRY`. Core
already advertises and emits the audio runtime channel, so removing the advertisement
would have made a live channel falsely unknown.

The caller census is non-test and reaches the connect path:

```text
packages/playtest/src/capabilities.ts:62:export function unknownPlaytestCapabilities(capabilities: readonly string[]): string[] {
packages/playtest/src/runner/bridgeClient.ts:10:  unknownPlaytestCapabilities,
packages/playtest/src/runner/bridgeClient.ts:76:  const unknown = unknownPlaytestCapabilities(description.capabilities);
```

The runner field list is derived from the exported constant; this command returned no
matches and exit code 0:

```text
rg -n "observations: \\{" packages/playtest/src/runner/runner.ts
```

## Observed red controls

Each source-revert control below was applied only temporarily, run, and restored before
continuing. The excerpts are the observed failures.

### Phase 1: observation availability

Removing the `unavailableObservation` check made the diagnostic test fail:

```text
FAIL packages/playtest/__tests__/silent-drop.spec.ts > refuses a scenario whose observation this runner cannot produce
AssertionError: expected undefined to be an instance of PlaytestBridgeError
```

The failure identifies the old silent path; the restored implementation reports
`TN_PLAYTEST_OBSERVATION_UNAVAILABLE`, `path: effectLog`, and `movement.notFacing`, with no
patrol-yaw advice.

Removing the unknown-capability connect check made the unknown-capability test fail:

```text
FAIL packages/playtest/__tests__/silent-drop.spec.ts > rejects a bridge advertising an unregistered capability
AssertionError: expected undefined to be an instance of PlaytestBridgeError
```

Removing `runtime.audio` from the registry reproduced the original discrepancy:

```text
Error: runtime.audio was rejected as unknown: runtime.audio
```

Restoring the old `throughoutFrames` fallback made the evidence test fail closed in the
wrong direction:

```text
FAIL ... > throughoutFrames fails without a captured frame series
AssertionError: expected true to be false
```

Removing one supported field from the exported runner list made the supported-kind test
fail with the observation named:

```text
Error: Assertion 'resources' requires observation 'resources', but this runner does not produce it.
```

### Phase 2: labeled evidence

Removing load-time label validation made the missing-label test fail:

```text
FAIL ... > scenario loading rejects an atSteps label that was never sampled
AssertionError: promise resolved ... instead of rejecting
```

Removing the series-length equality check made the short-series test fail:

```text
FAIL ... > throughoutSteps fails when fewer labeled samples were captured
AssertionError: expected true to be false
```

The transient-reset evaluator control kept the final score green and rejected the labeled
transition:

```text
Error: {"finalValue":{"id":"resource.GameState.coins","pass":true},"transition":{"id":"resource.GameState.coins.atSteps","pass":false},"diagnostics":[{"code":"TN_PLAYTEST_RESOURCE_TRANSITION_ASSERTION_FAILED"}]}
```

### Phase 3: components

Removing component forwarding from core made the component test fail:

```text
FAIL ... > exposes registry fields as components only when fields exist
AssertionError: expected [ 'camera.observe', … ] to include 'runtime.components'
```

The capability revert control against `damage.playtest.json` reports the required channel:

```text
Error: {"capability":"runtime.components","code":"TN_PLAYTEST_CAPABILITY_MISSING","message":"Scenario requires missing capability 'runtime.components'."}
```

The damage-handler control held diagnostics and the final resource green while the health
component failed:

```text
Error: {"component":{"id":"component.player.health.value","pass":false},"resource":{"id":"resource.GameState.topSpeed","pass":true},"diagnostics":[{"code":"TN_PLAYTEST_COMPONENT_ASSERTION_FAILED","observedRuntimePath":"observations.json/components/player/health"}]}
```

### Phase 4: signals

Removing the `drained` guard made the `maxCount: 0` test fail:

```text
FAIL ... > maxCount zero fails when the event channel was never drained
AssertionError: expected true to be false
```

Removing the bridge's `runtime.events` capability made the bridge test fail:

```text
FAIL ... > component and event providers advertise only the observations they supply
AssertionError: expected [ 'camera.observe', … ] to include 'runtime.events'
```

The whole-callback removal control fails before evaluation with the intended capability
diagnostic:

```text
Error: {"capability":"runtime.events","code":"TN_PLAYTEST_CAPABILITY_MISSING","message":"Scenario requires missing capability 'runtime.events'."}
```

The third-emission control reports two observed events against the required three:

```text
Error: {"signal":{"details":{"count":2,"entity":"player","minCount":3,"name":"collected"},"id":"signal.collected","pass":false},"diagnostics":[{"code":"TN_PLAYTEST_SIGNAL_NOT_OBSERVED"}]}
```

## Baseline control

The archived previous commit reproduced the pre-change reds before any current source was
used:

```text
Error: baseline throughoutFrames gate passed without a frame series
Error: baseline registry rejected runtime.audio: runtime.audio
Error: baseline loader accepted an atSteps label with no matching step
```

These are the three baseline behaviors this PRD changes: a single frame satisfying a
throughout assertion, a live audio capability rejected as unknown, and an undefined step
label accepted at load time.

## Browser gate

The real scaffolded platformer consumer now passes on headed Brave 150.1.92.143 with
WebGPU under Xvfb. The scaffold was created from the checked-in platformer template with
the lane's four locally packed packages, and the production CLI ran all 11 committed
platformer scenarios:

```text
jump 18, coyote 122, buffer 100, dash 44, patrol 100, collect 57,
damage 70, stomp-rise 74, stomp 95, oneway 104, respawn 290 fixed frames
11/11 pass; every scenario reported zero assertion errors and zero diagnostics errors
```

The first exploratory run used a temporary, uncommitted `executablePath` hook in the runner
to launch the supported Brave binary; the hook was restored before the lane diff was
reviewed. The final 11/11 evidence used the unmodified production runner with Playwright's
managed Chromium under the same Xvfb/WebGPU recipe:

```sh
xvfb-run -a -s '-screen 0 1600x900x24' \
  node packages/playtest/dist/runner/cli.js \
  --scenario playtests/collect.playtest.json \
  --project /tmp/prd033-platformer.oVYJdw \
  --url http://127.0.0.1:4190 \
  --browser-recipe webgpu --headed --timeout 120000
```

The checked-in `damage.playtest.json`, `stomp.playtest.json`, and
`stomp-rise.playtest.json` needed consumer-timing repairs exposed by this run. Damage now
holds long enough to contact the patrol; stomp approaches the patrol before jumping; and
stomp-rise records contact before the post-contact jump. The same 11/11 run passed after
those exact scenario changes.

An earlier managed-sandbox attempt without the working Xvfb display exited before page
creation:

```text
browserType.launch: Target page, context or browser has been closed
[pid=32][err] ... crashpad ... setsockopt: Operation not permitted (1)
[pid=32] <process did exit: exitCode=null, signal=SIGTRAP
```

Adding `--no-sandbox --disable-setuid-sandbox --no-zygote --single-process` reproduced the
same `SIGTRAP`. This historical failure is superseded by the current Xvfb run above. The
prescribed `pnpm test:playtest` command also passes now: `framework-movement` and
`framework-camera`, both with zero diagnostics errors.

CI still generates a platformer scaffold and runs its committed `test` suite under the
PRD-020 WebGPU/Xvfb recipe.

## Browser negative controls

The same supported runner observed the gameplay controls fail closed in a disposable copy
of the scaffold, with the source restored after each control:

| Control | Observed red while the unaffected proof stayed green |
|---|---|
| Remove `checkpoints.hurt(character, fromX)` from `Level.ts` | `damage` returned `pass:false`; `GameState.topSpeed` passed, `player.health` failed (`before=3 after=3`), diagnostics passed |
| Remove the `events` callback from `playtest({ events })` | `collect` stopped at connect with `TN_PLAYTEST_CAPABILITY_MISSING` for `runtime.events` |

A temporary one-tick score-reset mutation logged its reset but the runner still sampled the
old resource value, so it was **not** counted as evidence. The evaluator-level transient
reset control recorded earlier remains the accepted red control; the browser mutation was
restored and discarded rather than overstated.
