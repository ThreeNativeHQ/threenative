# PRD-215 recurring debt gates — verification

Date: 2026-08-24
Lane: lane-1
Source: `docs/PRDs/PRD-215-recurring-debt-classes-fail-a-gate.md`

## Setup and focused green

| Command | Result |
| --- | --- |
| `pnpm install --frozen-lockfile` | PASS, exit 0; repository dependencies bootstrapped. |
| `pnpm build` | PASS, exit 0; capability and package builds completed. Existing bundler size/dynamic-import warnings remained advisory. |
| `pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts scripts/__tests__/check-version-pins.spec.ts scripts/__tests__/check-core-boundary.spec.ts scripts/__tests__/check-native-shims.spec.ts scripts/__tests__/check-quality.spec.ts scripts/__tests__/budgets.spec.ts` | PASS, 6 files and 54 tests. |

## Negative controls

Each mutation was applied only for its red run and restored immediately afterward.

| Lever | Mutation and observed red |
| --- | --- |
| L1 | Added a two-package literal array to `scripts/profile-starter.ts`; `pnpm budgets` exited 1 with `TN_WORKSPACE_PACKAGE_LITERAL_ENUMERATION: derive package names with workspace-packages.ts; offending files: scripts/profile-starter.ts`. |
| L2 | Changed the starter template core pin to `9.9.9`; `pnpm budgets` exited 1 with `TN_VERSION_PINS_DRIFTED` naming `template starter`, `9.9.9`, and expected workspace version `0.3.0`. |
| L3 | Added an unwaived `biome-ignore` under `packages/core/src`; `pnpm quality` exited 1 with `quality report: 71 findings (1 new, 0 grew, 70 inherited, 0 waived)` and `quality gate failed: 1 suppression-class finding`. Added `// quality-allow: PRD-215 negative-control waiver`; the same command exited 0 with `0 new` and `1 waived`. |
| L4 | Appended banned token `System` to `packages/core/src/entities.ts`; `pnpm budgets` exited 1 with `TN_CORE_BOUNDARY_FAILED`, naming both `80 lines` and `banned entity-registry token 'System'`. |
| L5 | Added a `localStorage` read in `packages/core/src/entities.ts` and removed its manifest entry; `pnpm budgets` exited 1 with `TN_NATIVE_SHIM_MISSING`, naming `packages/core/src/entities.ts:76`, `localStorage`, and the required shim/allowlist remedy. |
| L6 | Retyped the census row from 53 to 59 and total from 80,035 to 80,041; `pnpm budgets` exited 1 with `native census drift: shim-manifest.json recorded 59, measured 53 (difference exceeds tolerance 5)`. |

## Whole-change gates

| Command | Result |
| --- | --- |
| `pnpm typecheck` | PASS, exit 0; root, package, and example typechecks completed. |
| `pnpm lint` | PASS, exit 0; Biome reported 294 existing warning-level cognitive-complexity diagnostics and no errors. |
| `pnpm test` | PASS, exit 0 on the final host run; 197 test files and 1,868 tests passed, with `suite temporary directory count unchanged: 136`. |
| `pnpm budgets` | PASS, exit 0; 8 framework packages, 8 example workspaces, 18,187 framework LOC, 80,035 native LOC, all generated references in sync. The existing framework LOC review trigger was reported, not fatal. |
| `pnpm quality` | PASS, exit 0; 70 findings, 0 new, 0 grew, 70 inherited, 0 waived. |
| `pnpm sync:agents --check` | PASS, exit 0; 16 `CLAUDE.md` mirrors in sync. |
| `git diff --check` | PASS, exit 0. |

Two earlier host `pnpm test` attempts passed all 197/1,868 tests but were red in the shared cleanup wrapper while parallel lanes changed `/tmp/threenative-*`; another attempt correctly caught a stale Chromium process. Those are harness diagnostics, not code failures. A private-`/tmp` bwrap retry was not counted because its playtest orphan probe became unverified before signal teardown. The final host run above was performed after the process inventory was empty and was green.

The Android parity-ledger workflow step was wired but not executed locally because this lane has no Android emulator/device. No playtest scenario is required by the PRD: these levers change contributor-facing build gates only.

## Repair round 1 red/green and final gates

The required regression command was run before implementation changes:

```text
pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts scripts/__tests__/check-quality.spec.ts scripts/__tests__/check-core-boundary.spec.ts scripts/__tests__/check-native-shims.spec.ts
```

It was red with exit 1: 4 test files ran, 4 assertions failed, and 20 passed. The failures were the generated-shooter package enumeration, the waived/inherited same-signal quality mutation, the second core source boundary scan, and the unlisted `indexedDB` global.

After implementation, the exact command was green with exit 0: 4 test files passed and 25 tests passed, including the preserved DOM/type-only exclusion assertion.

The first full-suite attempt after widening L1 timed out only in the five-second checked-in-call-site test because it parsed every source file under concurrent load. The source-tree walk now reads every implementation candidate but AST-parses only files containing workspace-package tokens; the final chained gate below passed.

| Command | Result |
| --- | --- |
| `pnpm typecheck && pnpm lint && pnpm test` | PASS, exit 0; typecheck completed, Biome reported 294 advisory complexity warnings, and the full suite passed. |
| `pnpm test` | PASS, exit 0; 197 test files and 1,873 tests passed, with `suite temporary directory count unchanged: 136`. |
| scoped Biome check for changed files | PASS, exit 0; 7 existing complexity warnings, no errors. |
| `pnpm budgets` | PASS, exit 0; workspace derivation, native shims, core boundary, capability docs, budgets, generated references, and duplicate checks passed. Framework LOC review trigger remained advisory. |
| `pnpm quality` | PASS, exit 0; 70 findings, 0 new, 0 grew, 70 inherited, 0 waived. |
| `pnpm sync:agents --check` | PASS, exit 0; 16 `CLAUDE.md` mirrors in sync. |
| `git diff --check` | PASS, exit 0. |

## Closure round r3 — review-2 repairs

Implementation commit: `34e31ee3`.

Each of the five capped-review defects was mutated and run red before the fix, then run green
after the fix. The mutation was restored before the next gate.

| Defect | Targeted red evidence | Targeted green evidence |
| --- | --- | --- |
| L1 CLI `for...of` package derivation | `pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts -t "CLI's inline for-of package map"` failed: expected `create-threenative.ts`, received `[]`; the scaffold fixture also failed because `packageSources` omitted the new package. | `pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts packages/create-threenative/__tests__/scaffold.spec.ts` passed; the CLI now derives `@threenative/new-package` from `--new-package-package`, and the generated-shooter proof remains green. |
| L3 runtime-native quality coverage | `pnpm exec vitest run scripts/__tests__/check-quality.spec.ts -t "runtime-native suppression"` failed: the unwaived runtime-native suppression produced `[]`. | The same targeted test passed after `packages/runtime-native/src` was included in the source roots. |
| L3 reasoned waiver with inherited same-signal row | `pnpm exec vitest run scripts/__tests__/check-quality.spec.ts -t "inherited same-signal"` failed: the unchanged shifted row at line 4 was classified `new` instead of `inherited`. | The targeted test passed: the inserted reasoned waiver is `waived`, the unchanged row is `inherited`, and the unwaived mutation remains fatal. |
| L5 backend-specific native-shim evidence | `pnpm exec vitest run scripts/__tests__/check-native-shims.spec.ts -t "each manifest evidence backend"` failed: removing `QuickOnly` from QuickJS returned no findings while V8 still contained it. | The targeted test passed after each manifest evidence path was checked against its own backend source. |
| L5 local/property exclusions | With the checker’s local-symbol exclusion temporarily disabled, `pnpm exec vitest run scripts/__tests__/check-native-shims.spec.ts -t "preserves local"` failed with `TN_NATIVE_SHIM_MISSING ... dom-exclusions.ts:5 reads indexedDB`; the implementation was restored. | The test passed with unallowlisted local/property `indexedDB` fixtures, plus imported and DOM type-only exclusions. |

The combined affected suite passed with 4 files and 46 tests:

```text
pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts scripts/__tests__/check-quality.spec.ts scripts/__tests__/check-native-shims.spec.ts packages/create-threenative/__tests__/scaffold.spec.ts
PASS — 4 files, 46 tests
```

## Closure round r3 — final gates

| Command | Result |
| --- | --- |
| `pnpm typecheck` | PASS, exit 0; all workspace projects typechecked. |
| `pnpm lint` | PASS, exit 0; 293 advisory warning-level diagnostics, no errors. |
| `pnpm test` | PASS, exit 0 on the standalone run; 197 test files and 1,878 tests passed, with the temporary-directory count unchanged at 137. |
| `pnpm budgets` | PASS, exit 0; workspace derivation reported 8 packages, version pins reported 8 cross-checked packages, and native shims, core boundary, capability docs, census, generated references, and duplicate checks passed. |
| `pnpm quality` | PASS, exit 0; 70 findings, 0 new, 0 grew, 70 inherited, 0 waived. |
| `pnpm sync:agents --check` | PASS, exit 0; 16 `CLAUDE.md` mirrors in sync. |
| `git diff --check` | PASS, exit 0. |

The exact composite command `pnpm typecheck && pnpm lint && pnpm test` was also run during
closure. Typecheck, lint, and all 197/1,878 tests completed, but the checked-in full-suite
wrapper exited red because parallel lanes were changing the shared `/tmp/threenative-*` cleanup
set and leaving Chromium processes. This is recorded as a harness/environment result; the
standalone `pnpm test` run above was green. `pnpm budgets` was run separately and passed.

No playtest scenario was required: these repairs change contributor-facing gates only.

## Repair round 1 — review-1 defects

Baseline: `42dc650e14c2b35151e9c4d4d0063c25f708bef7`.

Each regression was added first and run against the baseline behavior before its implementation
repair. The focused red/green evidence is:

| Defect | Red, before repair | Green, after repair |
| --- | --- | --- |
| Playwright archive prefix | `pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts -t "Playwright pack lookup"` — exit 1; 1 failed, 5 skipped. The config did not contain a direct `startsWith(prefix)` lookup. | Same command — exit 0; 1 passed, 5 skipped. The existing derived-prefix archive test remains in the file. |
| CI scaffold package enumeration | `pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts -t "scaffold smoke package inventory"` — exit 1; 1 failed, 6 skipped. CI had no derived archive-spec stream. | Same command — exit 0; 1 passed, 6 skipped. The fixture add/rename produces the corresponding derived archive prefix while the L1 source scan remains green. |
| Quality waiver ordering | `pnpm exec vitest run scripts/__tests__/check-quality.spec.ts -t "line-order waiver"` — exit 1; 1 failed, 15 skipped. The line-2 biome suppression was `new` while the line-1 file-length finding was `waived`. | Same command — exit 0; 1 passed, 15 skipped. The next-line suppression is `waived`, file length is `inherited`, and the suppression is not fatal. Existing same-signal waiver tests remain covered. |
| Native shim evidence | `pnpm exec vitest run scripts/__tests__/check-native-shims.spec.ts -t "assignment evidence"` — exit 1; 1 failed, 5 skipped. A probe/comment-only runtime file produced no finding. | Same command — exit 0; 1 passed, 5 skipped. Probe/comment-only evidence fails closed; assignment and setter evidence remains accepted. |

The combined affected suite passed:

```text
pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts scripts/__tests__/check-quality.spec.ts scripts/__tests__/check-native-shims.spec.ts
PASS — 3 files, 29 tests
```

The new derivation command also passed and emitted all 8 workspace package/archive records:

```text
pnpm exec tsx scripts/workspace-packages.ts --archives
PASS — exit 0
```

## Repair round 1 — final gates

| Command | Result |
| --- | --- |
| `pnpm typecheck && pnpm lint && pnpm test` | PASS, exit 0; 197 test files and 1,882 tests passed, temporary-directory count unchanged at 137. Biome reported 294 existing advisory complexity warnings and no errors. |
| `pnpm budgets` | PASS, exit 0; workspace derivation reported 8 packages, native shim/core-boundary checks passed, and generated references were in sync. Framework LOC was 18,181/15,000 and remained the documented advisory review trigger. |
| `pnpm quality` | PASS, exit 0; 70 findings, 0 new, 0 grew, 70 inherited, 0 waived. |
| scoped `pnpm exec biome check` for changed source/test files | PASS, exit 0; 5 existing complexity warnings, no errors. |
| `pnpm sync:agents --check` | PASS, exit 0; 16 `CLAUDE.md` mirrors in sync. |
| `git diff --check` | PASS, exit 0. |

Harness note: the final full suite emitted existing Xvfb/xkb and temporary-fixture git-discovery
diagnostics, but the wrapper completed green and its temporary-directory invariant passed. No
playtest scenario was required because these repairs affect contributor-facing build gates only.

## Closure round r4 — capped-review repairs

Prepared baseline: bea780073a00feb6dc4f4819df581a2c870b70d9.

Each regression below was added and run against the prepared baseline before its repair. The
final focused suite then ran all five regressions together.

| Defect | Red evidence before repair | Green evidence after repair |
| --- | --- | --- |
| Workflow inventories were invisible to L1 | pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts -t 'YAML inventory\|checked-in workflow package inventories' — exit 1, 2 failures: the stale YAML inventory returned [], and the workflow did not contain the derivation command. | The final focused suite passed the stale-package rename and checked-in workflow tests; YAML workflow files are scanned without being treated as TypeScript. |
| Scoped CLI/engine-MCP names collided with legacy aliases | pnpm exec vitest run packages/create-threenative/__tests__/scaffold.spec.ts -t 'colliding names' — exit 1: --threenative-cli-package and --threenative-engine-mcp-package were parsed as the unscoped packages. | The final focused suite passed the scoped-name parser regression. Scoped package flags now derive as --threenative-<suffix>-package; legacy unscoped flags remain accepted. |
| Unknown internal-scope pins were treated as third-party | pnpm exec vitest run scripts/__tests__/check-version-pins.spec.ts -t 'unknown internal-scope' — exit 1: findings were empty for @threenative/croe. | The final focused suite passed the unknown-internal-scope regression; third-party pin comparison remains covered. |
| Physics source was omitted from native-shim scanning | pnpm exec vitest run scripts/__tests__/check-native-shims.spec.ts -t 'shared physics source' — exit 1: findings were empty for indexedDB in packages/physics/src. | The final focused suite passed with the finding naming packages/physics/src/browser-global.ts, indexedDB, and the shim/allowlist remedy. |
| C++ raw-string comments could fake shim evidence | pnpm exec vitest run scripts/__tests__/check-native-shims.spec.ts -t 'comments inside C\\+\\+ raw strings' — exit 1: a commented-only ReadableStream assignment produced no finding. | The final focused suite passed both the commented-only red control and the active raw-string assignment; comments are stripped before evidence matching. |

Final affected suite:

```text
pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts scripts/__tests__/check-version-pins.spec.ts scripts/__tests__/check-native-shims.spec.ts packages/create-threenative/__tests__/scaffold.spec.ts
PASS — 4 files, 44 tests
```

## Closure round r4 — final gates

| Command | Result |
| --- | --- |
| pnpm typecheck && pnpm lint && pnpm test | PASS, exit 0; 197 test files and 1,889 tests passed. Biome reported 294 advisory warnings and no errors. |
| pnpm budgets | PASS, exit 0; workspace derivation, version pins, native shims, core boundary, capability docs, generated references, and duplicate checks passed. Framework LOC review trigger remained advisory at 18,187/15,000. |
| pnpm quality | PASS, exit 0; 70 findings, 0 new, 0 grew, 70 inherited, 0 waived. |
| pnpm sync:agents --check | PASS, exit 0; 16 CLAUDE.md mirrors in sync. |
| git diff --check | PASS, exit 0. |

Fresh-lane setup used pnpm install --frozen-lockfile, followed by the required workspace builds
for missing local dist artifacts. Initial missing-artifact probes were setup results and were
not counted as gate evidence. No playtest scenario was required: these repairs affect
contributor-facing build gates only.

## Closure repair round 1 — review-1 defect repairs

Baseline: `135d351f2006a35af7204490088c92149c5ab129`.

Each requested regression was added before its implementation repair and run against the
baseline behavior:

| Defect | Red evidence before repair | Green evidence after repair |
| --- | --- | --- |
| Repeated literal package commands were invisible to the workflow inventory guard | `pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts -t 'detects repeated literal package commands in a workflow inventory'` — exit 1; 1 failed, 10 skipped. The line-252-style `pnpm --filter` inventory returned `[]` instead of `.github/workflows/native-platforms.yml`. | Same command — exit 0; 1 passed, 10 skipped. The guard detects repeated literal package commands while retaining the YAML rename regression and derived package stream. |
| Quoted JavaScript assignment was accepted as native-shim evidence | `pnpm exec vitest run scripts/__tests__/check-native-shims.spec.ts -t 'does not treat a quoted JavaScript assignment as shim evidence'` — exit 1; 1 failed, 8 skipped. A quoted `globalThis.ReadableStream = ReadableStream` string produced no missing-evidence finding. | Same command — exit 0; 1 passed, 8 skipped. Quoted literals are excluded before assignment evidence matching. |

The workflow scanner initially made the three checked-in inventories fail closed (`ci.yml`,
`native-platforms.yml`, and `native-release.yml`). Those repeated build/pack/archive/CLI blocks
were migrated to the derived `scripts/workspace-packages.ts --archives` stream, including
derived archive-prefix and CLI-argument selection; the scanner was not weakened or bypassed.
The native-shim repair also preserves active direct assignments, backend setter forms, and the
existing local/property/type-only exclusions. The focused preservation mutation covered
`setGlobalProperty`, `Object.defineProperty`, `Global()->Set`, and `JS_SetPropertyStr`.

Affected tests:

```text
pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts scripts/__tests__/check-native-shims.spec.ts
PASS — exit 0; 2 files, 21 tests

pnpm --filter @threenative/runtime-native exec vitest run --config vitest.config.ts tests/native-platform-workflow.test.mjs
PASS — exit 0; 1 file, 10 tests
```

## Closure repair round 1 — final gates

| Command | Result |
| --- | --- |
| `pnpm typecheck && pnpm lint && pnpm test` | PASS, exit 0; 197 test files and 1,892 tests passed, with the temporary-directory count unchanged at 137. Lint reported 295 advisory warning-level diagnostics and no errors. |
| `pnpm budgets` | PASS, exit 0; workspace derivation reported 8 packages, native shim/core-boundary/capability/reference/duplicate checks passed, and the framework LOC advisory was 18,187/15,000. |
| `pnpm quality` | PASS, exit 0; 70 findings, 0 new, 0 grew, 70 inherited, 0 waived. |
| `pnpm sync:agents --check` | PASS, exit 0; 16 `CLAUDE.md` mirrors in sync. |
| `git diff --check` | PASS, exit 0. |

Harness-only caveat: none. No playtest scenario or device lane was required because these
repairs affect contributor-facing build gates only. The first composite invocation exposed the
in-scope workflow test's stale assertion for the removed literal package command; that test was
updated to assert the derived loop, and the final composite invocation passed.

## Closure round r5 — capped-review repairs

Prepared baseline after cherry-picking the complete r4 range:
`141632b4e5b2004b670ccc214b6bca3aefd00ed6`.

Each requested regression was added before its implementation repair and run against the
cherry-picked baseline:

| Defect | Red evidence before repair | Green evidence after repair |
| --- | --- | --- |
| Chained workflow package commands were reduced to the final `--filter` package | `pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts scripts/__tests__/check-native-shims.spec.ts` — exit 1; the chained-command test expected `.github/workflows/native-platforms.yml` but received `[]`. | The same command — exit 0; the chained workflow test passes and the existing derived-stream/workflow tests remain green. |
| JavaScript regex literals could fake native-shim assignment evidence | The same command — exit 1; the regex-only test expected `manifest shim ReadableStream has no matching runtime installation` but received an empty finding list. | The same command — exit 0; regex-only evidence fails closed while quoted assignments, active setters, raw-string comments, and exclusions remain green. |

Red run result:

```text
Test Files  2 failed (2)
Tests       2 failed | 21 passed (23)
```

Green run result:

```text
Test Files  2 passed (2)
Tests       23 passed (23)
```

## Closure round r5 — final gates

| Command | Result |
| --- | --- |
| `pnpm install --frozen-lockfile` | PASS, exit 0. Fresh-install bin warnings were setup-only because package dist files were not built yet. |
| `pnpm build` | PASS, exit 0; restored the generated package declarations required by the workspace typecheck. |
| `pnpm typecheck && pnpm lint && pnpm test` | PASS, exit 0; 197 test files and 1,894 tests passed, temporary-directory count unchanged at 137. Biome reported 297 existing advisory warnings and no errors. |
| `pnpm budgets` | PASS, exit 0; workspace derivation, version pins, native shims, core boundary, capability docs, census, generated references, and duplicate checks passed. Summary: 8 framework packages, 8 example workspaces, 18,187 framework LOC, 80,042 native LOC. |
| `pnpm quality` | PASS, exit 0; 70 findings, 0 new, 0 grew, 70 inherited, 0 waived. |
| `git diff --check` | PASS, exit 0. |

The first post-install composite invocation stopped in setup before typecheck completed because
`@threenative/playtest/protocol` and `@threenative/assets` declarations were not built; it was
not counted as a code-gate result. After `pnpm build`, the exact composite command passed.
No playtest scenario was required: these repairs affect contributor-facing build gates only.

Post-review compatibility recheck after restoring newline whitespace handling in the workflow
matcher:

```text
pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts scripts/__tests__/check-native-shims.spec.ts
PASS — 2 files, 23 tests
pnpm budgets
PASS — workspace package derivation, native shim contract, and all chained budget checks
pnpm typecheck && pnpm lint && pnpm test
PASS — 197 test files, 1,894 tests; temporary-directory count unchanged at 137
```

## Repair handoff — lane-215-closure-r5 — 2026-08-24

Repair baseline: `6abd3d9ab767f6b998cafbd91e2b77a05f27c43b`.

The two focused regressions were added before the implementation repair. The exact required
command was run against that baseline and was RED, exit 1:

```text
pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts scripts/__tests__/check-native-shims.spec.ts
Test Files  2 failed (2)
Tests       2 failed | 23 passed (25)
```

The two RED observations were:

- `workspace package derivation > detects literal package commands split before the filter flag`:
  expected `[".github/workflows/native-platforms.yml"]`, received `[]` because the command's
  shell-continuation newline prevented both `--filter` package names from being collected.
- `native shim contract > masks regex expression statements after closing conditions without
  masking division`: expected `manifest shim ReadableStream has no matching runtime installation`,
  received an empty finding list because the post-`)` regex assignment remained visible.

The repair unfolds only backslash-newline shell continuations before the existing bounded workflow
matcher, and classifies `)`/`}` regex starts by their control-header/block context while retaining
division and object-literal division as non-regex cases. The output masking keeps source offsets
unchanged and the existing quoted-literal, comment, C++ raw-string, setter, and exclusion behavior.

The exact focused command was rerun GREEN, exit 0:

```text
pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts scripts/__tests__/check-native-shims.spec.ts
Test Files  2 passed (2)
Tests       25 passed (25)
```

Final gate evidence for this repair round:

| Command | Result |
| --- | --- |
| `pnpm typecheck && pnpm lint && pnpm test` | PASS, exit 0; 197 test files and 1,896 tests passed, temporary-directory count unchanged at 137. Biome reported 298 advisory complexity warnings and no errors. |
| `pnpm budgets` | PASS, exit 0; workspace derivation, version pins, native shims, core boundary, capability docs, census, generated references, and duplicate checks passed. Summary: 8 framework packages, 8 example workspaces, 18,187 framework LOC, 80,042 native LOC. |
| `pnpm quality` | PASS, exit 0; 70 findings, 0 new, 0 grew, 70 inherited, 0 waived. |
| `pnpm sync:agents --check` | PASS, exit 0; 16 `CLAUDE.md` mirrors in sync. |
| `git diff --check` | PASS, exit 0. |
| changed-file `pnpm exec biome check` | PASS, exit 0; existing complexity warnings only. |

No playtest or device lane was required: these repairs affect contributor-facing build gates only.

## Closure repair round r6 — review-2 defects — 2026-08-24

Repair baseline: `1099b67a3f19c15982974a5ac45cf2ca0e28bba9`.

The fresh worktree required setup before the focused gate could run:

| Command | Result |
| --- | --- |
| `pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts scripts/__tests__/check-native-shims.spec.ts` | SETUP, exit 254; Vitest was not installed (`Command "vitest" not found`). |
| `pnpm install --frozen-lockfile` | PASS, exit 0. |
| `pnpm build` | PASS, exit 0. |

The required focused red-green loop was then run against the same tree. Tests were written before
the implementation repair.

RED, exact command, exit 1:

```text
pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts scripts/__tests__/check-native-shims.spec.ts
Test Files  2 failed (2)
Tests       2 failed | 24 passed (26)
```

The folded-YAML test expected `[".github/workflows/native-platforms.yml"]` but received `[]`.
The earlier-regex shim test expected `manifest shim ReadableStream has no matching runtime installation`
but received an empty finding list.

The repair unfolds only YAML `run: >` folded scalar bodies and shell continuations before the
existing bounded package-command matcher, preserving command separators and excluding prose. The
regex masker records each identified literal's original range and passes those ranges to delimiter
matching, so earlier regex literals cannot corrupt later `)`/`}` discovery while source offsets stay
unchanged. The division and object-literal division controls are independent loop cases, so either
post-delimiter branch becoming unconditional masking fails directly.

GREEN, exact command, exit 0:

```text
pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts scripts/__tests__/check-native-shims.spec.ts
Test Files  2 passed (2)
Tests       26 passed (26)
```

Final gate evidence:

| Command | Result |
| --- | --- |
| `pnpm typecheck && pnpm lint && pnpm test` | PASS, exit 0; 197 test files and 1,897 tests passed, 37 native tests skipped, temporary-directory count unchanged at 137. Biome reported 298 advisory complexity warnings and no errors. |
| `pnpm budgets` | PASS, exit 0; workspace package derivation, version pins, native shims, core boundary, capability docs, census, generated references, and duplicate checks passed. Summary: 8 framework packages, 8 example workspaces, 18,187 framework LOC, 80,042 native LOC. |
| `pnpm quality` | PASS, exit 0; 70 findings, 0 new, 0 grew, 70 inherited, 0 waived. |
| `pnpm sync:agents --check` | PASS, exit 0; 16 `CLAUDE.md` mirrors in sync. |
| changed-file `pnpm exec biome check` | PASS, exit 0; 8 existing advisory complexity warnings only. |
| `git diff --check` | PASS, exit 0. |

Two earlier composite attempts were not code failures: one observed concurrent `/tmp/threenative-*`
cleanup drift, and one observed a transient Chromium orphan. After the unrelated test process ended
and the orphan disappeared, the exact composite command above passed with both harness invariants
clear. No playtest or device lane was required: these repairs affect contributor-facing build gates
only.

Commit: lane-215-closure-r6; final SHA is reported in the delivery handoff.

Changed paths in this round:

- `scripts/workspace-packages.ts`
- `scripts/check-native-shims.ts`
- `scripts/__tests__/workspace-packages.spec.ts`
- `scripts/__tests__/check-native-shims.spec.ts`
- `docs/verification/prd-215-2026-08-24.md`

## Repair round r7 — review-1 class-extends regex-boundary defect — 2026-08-24

Repair baseline: `b2c79168c8b8824ffc10d9001219a1a3a32eef04`.

The focused negative control was added before implementation for a regex literal inside a valid
class `extends` expression:
`class X extends registry[/;/] {} /globalThis.ReadableStream = ReadableStream/.test(source);`.

The exact required command was run against the baseline behavior and was RED, exit 1:

```text
pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts scripts/__tests__/check-native-shims.spec.ts
Test Files  1 failed | 1 passed (2)
Tests       1 failed | 29 passed (30)
```

The failing test was `native shim contract > does not treat a regex after a class with a regex
literal in extends as shim evidence`: expected `manifest shim ReadableStream has no matching
runtime installation`, received an empty finding list. The `/;/` literal was not neutralized
before the class-header boundary pattern, so the trailing fake assignment regex supplied shim
evidence.

The repair passes the already-tracked regex literal ranges into class-boundary recognition and
neutralizes only those ranges with same-offset whitespace before the non-greedy class-header
pattern runs. Quoted literals, earlier class-boundary controls, escaped regexes, folded YAML,
division, block/condition expressions, comments, C++ raw strings, setters, and exclusions remain
covered by the focused suite.

The exact focused command was rerun GREEN, exit 0:

```text
pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts scripts/__tests__/check-native-shims.spec.ts
Test Files  2 passed (2)
Tests       30 passed (30)
```

Final gate evidence for this repair round:

| Command | Result |
| --- | --- |
| `pnpm typecheck && pnpm lint && pnpm test` | PARTIAL, exit 1; typecheck passed, then plain `pnpm lint` stopped at Biome 1.9.4's default 20-diagnostic display cap after reporting 298 pre-existing warn-level findings, so the chained test command was not reached. No lint error was reported in the changed files. |
| `pnpm test` | PASS, exit 0; 197 test files and 1,901 tests passed, 37 skipped, with the temporary-directory count unchanged at 137. |
| `pnpm exec biome check . --max-diagnostics=none --reporter=summary` | PASS, exit 0; 298 existing warnings, no errors, and no formatting findings. |
| `pnpm exec biome check scripts/check-native-shims.ts scripts/__tests__/check-native-shims.spec.ts` | PASS, exit 0; 8 existing advisory complexity warnings only. |
| `pnpm budgets` | PASS, exit 0; workspace derivation, version pins, native shims, core boundary, capability docs, census, generated references, and duplicate checks passed. Summary: 8 framework packages, 8 example workspaces, 18,187 framework LOC, 80,042 native runtime LOC. |
| `pnpm quality` | PASS, exit 0; 70 findings, 0 new, 0 grew, 70 inherited, 0 waived. |
| `pnpm sync:agents --check` | PASS, exit 0; 16 `CLAUDE.md` mirrors in sync. |
| `git diff --check` | PASS, exit 0. |

No playtest or device lane was required: this repair affects contributor-facing build-gate parsing
only. The plain `pnpm lint` cap is a pre-existing repository command issue and was not changed in
this bounded repair.

Changed paths in this round:

- `scripts/check-native-shims.ts`
- `scripts/__tests__/check-native-shims.spec.ts`
- `docs/verification/prd-215-2026-08-24.md`

## Repair handoff — lane-215-closure-r7 class-extends boundary defect — 2026-08-24

Repair baseline: `a4cb17bb62aa2d145e3489b0c9ea0f8edd73aed0`.

The focused regression was added before implementation for a valid class `extends` expression
containing delimiter-looking text:
`class X extends registry[";"] {} /globalThis.ReadableStream = ReadableStream/.test(source);`.

The first attempt found the fresh worktree had no installed `vitest` binary (`Command "vitest" not
found`, exit 254). After the repository-specified `pnpm install --frozen-lockfile` completed with
pnpm 10.25.0 and Node v20.19.6, the exact required command was run against the unguarded baseline
and was RED, exit 1:

```text
pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts scripts/__tests__/check-native-shims.spec.ts
Test Files  1 failed | 1 passed (2)
Tests       1 failed | 28 passed (29)
```

The failing test was:

- `native shim contract > does not treat a regex after a class with delimiter-looking extends text as shim evidence`:
  expected `manifest shim ReadableStream has no matching runtime installation`, received an empty
finding list. The class detector's `[^{};]*` boundary pattern rejected the semicolon inside the
valid quoted computed property, so the following regex was not masked and supplied false shim
evidence.

The class-boundary repair masks quoted literal contents before applying the existing line-bounded
class-header check; regex-literal masking remains non-greedy and its division controls are
unchanged. The exact focused command was rerun GREEN, exit 0:

```text
pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts scripts/__tests__/check-native-shims.spec.ts
Test Files  2 passed (2)
Tests       29 passed (29)
```

Required final gates were rerun after the focused GREEN:

| Command | Result |
| --- | --- |
| `pnpm typecheck && pnpm lint && pnpm test` | PASS, exit 0; 197 test files and 1,900 tests passed, with the temporary-directory count unchanged at 137. Lint reported 298 existing advisory complexity warnings and no errors. The initial pre-build typecheck failure was setup-only; `pnpm build` supplied the missing workspace declarations before this passing rerun. |
| `pnpm budgets` | PASS, exit 0; workspace derivation, version pins, native shims, core boundary, capability docs, census, generated references, and duplicate checks passed. Framework LOC remained the documented advisory trigger at 18,187/15,000. |
| `pnpm quality` | PASS, exit 0; 70 findings, 0 new, 0 grew, 70 inherited, 0 waived. |
| `pnpm sync:agents --check` | PASS, exit 0; 16 `CLAUDE.md` mirrors in sync. |
| `git diff --check` | PASS, exit 0. |

No playtest or device lane was required: this repair affects contributor-facing build-gate parsing
only.

Changed paths in this round:

- `scripts/check-native-shims.ts`
- `scripts/__tests__/check-native-shims.spec.ts`
- `docs/verification/prd-215-2026-08-24.md`

## Repair handoff — lane-215-closure-r6 review-1 defects — 2026-08-24

Repair baseline: `82f488f70c5b5f7e655915419c7f084ab7afa7fc`.

Both focused regressions were added before implementation. The exact required command was run
against the baseline behavior and was RED, exit 1:

```text
pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts scripts/__tests__/check-native-shims.spec.ts
Test Files  2 failed (2)
Tests       2 failed | 26 passed (28)
```

The observed failures were:

- `workspace package derivation > detects literal package commands in a reversed-indicator folded YAML scalar`:
  expected `[".github/workflows/native-platforms.yml"]`, received `[]` for the valid `run: >-2`
  header.
- `native shim contract > does not treat a regex after a class declaration as shim evidence`:
  expected `manifest shim ReadableStream has no matching runtime installation`, received an empty
  finding list for `class X {} /globalThis.ReadableStream = ReadableStream/.test(source);`.

The smallest repairs were applied: the folded-scalar header recognizes both indicator orders while
remaining line-bounded, and the regex masker classifies a class declaration body as an expression
statement boundary. Existing post-`)`/post-`}` expression masking, division controls, quoted
strings, comments, C++ raw strings, setters, and exclusions remain covered by the focused suite.

The exact focused command was rerun GREEN, exit 0:

```text
pnpm exec vitest run scripts/__tests__/workspace-packages.spec.ts scripts/__tests__/check-native-shims.spec.ts
Test Files  2 passed (2)
Tests       28 passed (28)
```

Required final gates:

| Command | Result |
| --- | --- |
| `pnpm typecheck && pnpm lint && pnpm test` | PASS, exit 0; 197 test files and 1,899 tests passed, with the temporary-directory count unchanged at 137. Lint reported 298 existing advisory complexity warnings and no errors. |
| `pnpm budgets` | PASS, exit 0; workspace derivation, version pins, native shims, core boundary, capability docs, census, generated references, and duplicate checks passed. Framework LOC remained the documented advisory trigger at 18,187/15,000. |
| `pnpm quality` | PASS, exit 0; 70 findings, 0 new, 0 grew, 70 inherited, 0 waived. |
| `pnpm sync:agents --check` | PASS, exit 0; 16 `CLAUDE.md` mirrors in sync. |
| `git diff --check` | PASS, exit 0. |

No playtest or device lane was required: these repairs affect contributor-facing build gates only.

Changed paths in this round:

- `scripts/workspace-packages.ts`
- `scripts/check-native-shims.ts`
- `scripts/__tests__/workspace-packages.spec.ts`
- `scripts/__tests__/check-native-shims.spec.ts`
- `docs/verification/prd-215-2026-08-24.md`
