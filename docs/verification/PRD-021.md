# PRD-021 verification — phases 1–2

Date: 2026-08-06

Status: phases 1–2 implementation complete. Round 1 is now recorded as an incomplete live
ledger at `docs/verification/round-1-2026-08-06.md`; it does not claim a completed comparison.

## Round ledger schema

The self-improvement template now records brief/proof hashes for both arms and allows an
explicit `void` stop condition for a failed firewall. `scripts/round-ledger.ts` parses the
Markdown tables and validates:

- both arms per genre with matching brief and proof hashes;
- complete column verdicts, gap dispositions, gates, and firewall rows;
- bounded visual/proof measurements and explanatory notes for `unmeasured` evidence;
- no placeholders, blank observations, or non-void failed firewall.

Focused command: `pnpm exec vitest run scripts/__tests__/round-ledger.spec.ts`

Result: PASS — 1 file, 5 tests, including the placeholder-template and failed-firewall
negative controls.

## Deterministic resume command

`pnpm round:next` reads the highest-numbered `docs/verification/round-*.md` file and returns
exactly one next command plus one reason. Tests cover missing arm, proof, capture, judge,
pair, gap disposition, PRD, gates, close, and ambiguous missing-arm states.

Focused command: `pnpm exec vitest run scripts/__tests__/round-next.spec.ts`

Result: PASS — 1 file, 5 tests.

Live resume: `pnpm round:next`

Result: PASS — it prints `stop round 1` because the ledger records `Stop condition met:
blocked`. The vanilla arm remains pending; no completed comparison is claimed.

The first isolated framework candidate was archived at
`docs/benchmark/sweeps/platformer-2026-08-07`. Its independent headed proof failed 0/2 with
`TN_PLAYTEST_BRIDGE_MISSING`; a direct browser check found the cause at runtime:
`src/scenes/Play.ts` imported `createMaterials`, which the candidate's
`src/render/materials.ts` did not export. `pnpm round:next` now also treats malformed or failed
`proof.json` as unproved and returns `pnpm sweep:proof ...` instead of allowing capture to hide
the failure. A replacement archive booted and passed the jump scenario but failed its sustained
route at 1/2: movement stopped at 2.87 units and coin, goal, HUD, and respawn state did not
change. Three isolated builder contexts then made no source edits for that route. Under the
repository's three-failed-fixes rule, the doubtful assumption is delegated builder execution,
not a validated framework gap.

## Repository gates

| Gate | Result |
| --- | --- |
| `pnpm typecheck` | PASS |
| `pnpm lint` | PASS — Biome checked 210 files |
| `pnpm test` | PASS — 110 files, 630 tests |
| `pnpm budgets` | PASS — 7 packages, 2,648 framework LOC, 2 active PRDs |
| `git diff --check` | PASS |

## Remaining phases

The vanilla build, guarded captures, fresh blind critic, pair report, gap disposition, and round
comparison are intentionally not claimed. Resume only after the delegated-builder execution
blocker is resolved; the ledger is explicitly blocked rather than silently advancing.
