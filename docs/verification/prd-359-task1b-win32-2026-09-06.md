# Task 1b Windows header-order repair

Source under test: `packages/runtime-native/src/webtransport/webtransport.cpp`

- Base source SHA-256 (`356cbe9f`): `df259e88326f3d5fb62cf874bbab8e255d2c271518806ec3171e227f3c9315d3`
- Repaired source SHA-256: `886273aa19acff1052422d90e63fea92dd2f3b8dd4cd63b475200364b7c06437`
- Diff: six added lines. `_WIN32` now defines `NOMINMAX` before `quiche.h`; the later Winsock include guard remains unchanged.

## Prior red

PR head `f0dfb24838d661af06e8f6bca0f1b6504d9c2140` failed Windows desktop core, run [34012201946](https://github.com/ThreeNativeHQ/threenative/actions/runs/34012201946), job [101429846972](https://github.com/ThreeNativeHQ/threenative/actions/runs/34012201946/job/101429846972), exit 2. MSVC reported `C2589` at `webtransport.cpp` lines 983, 1022, 1023, 1374, 1865, and 2026 for `std::min`; the base Windows job passed.

Exact CI excerpt:

```text
FAILED: [code=2] CMakeFiles/mystral-runtime.dir/src/webtransport/webtransport.cpp.obj
webtransport.cpp(983): error C2589: '(' illegal token on right side of '::'
webtransport.cpp(1022): error C2589: '(' illegal token on right side of '::'
webtransport.cpp(1023): error C2589: '(' illegal token on right side of '::'
webtransport.cpp(1374): error C2589: '(' illegal token on right side of '::'
webtransport.cpp(1865): error C2589: '(' illegal token on right side of '::'
webtransport.cpp(2026): error C2589: '(' illegal token on right side of '::'
ninja: build stopped: subcommand failed.
Exit status 2
```

## Verification

| Command | Exit | Result |
|---|---:|---|
| `cmake --build packages/runtime-native/build/tn-linux --target threenative-webtransport-wire-test threenative-webtransport-surface-test --parallel 4` | 0 | V8 rebuilt and linked |
| `cmake --build packages/runtime-native/build/tn-linux-quickjs --target threenative-webtransport-wire-test threenative-webtransport-surface-test --parallel 4` | 0 | QuickJS rebuilt and linked |
| `ctest --test-dir packages/runtime-native/build/tn-linux -R 'threenative-webtransport-(wire\|surface)-test' --output-on-failure` | 0 | 2/2 passed |
| `ctest --test-dir packages/runtime-native/build/tn-linux-quickjs -R 'threenative-webtransport-(wire\|surface)-test' --output-on-failure` | 0 | 2/2 passed |
| `sh scripts/xvfb.sh env TN_REQUIRE_LIVE_WEBTRANSPORT_FIXTURE=1 pnpm --dir packages/runtime-native exec vitest run --config vitest.config.ts tests/webtransport/webtransport.test.ts` | 0 | 33/33 passed |

The same live command without Xvfb exited 1 at SDL startup (`No available video device`), so it is retained as prerequisite evidence in `task1b-win32/live-v8-vitest.log`.

## Preprocessor check

`clang-cl` is installed but no Windows SDK is available, so a full Windows compile was not possible locally. Using the actual `quiche.h` include and scratch Winsock headers:

- guarded include preprocessor: exit 0
- unguarded control: exit 1 (`Windows min macro is present without NOMINMAX`)

Scratch logs and outputs are under `artifacts/networking-359/task1b-win32/`. Windows CI remains pending; no local Windows green claim is made.

Fresh read-only review accepted the include boundary: earlier engine headers do not import Winsock, and NOMINMAX now precedes quiche.h. Windows CI must still prove the corrected MSVC build.

## Wire-test include boundary follow-up

Commit `cd8fb820` passed the Windows runtime compilation but failed the wire-test
translation unit in [job 101433502044](https://github.com/ThreeNativeHQ/threenative/actions/runs/34013603832/job/101433502044).
The test includes quiche directly before including the implementation translation
unit, so the production guard cannot protect that earlier include. The repair
adds the same six-line guard before the test's first quiche include.

```text
Prior CI: FAIL threenative-webtransport-wire-test
webtransport.cpp(989): error C2589: '(' illegal token on right side of '::'
webtransport_wire_test.cpp(290): error C2589
Local V8 wire target rebuild: exit 0
ctest -R threenative-webtransport-wire-test: 1/1 passed, exit 0
Actual wire-test first-include prefix, clang-cl preprocessing:
  previous prefix: exit 1, Windows macro leaked
  repaired prefix: exit 0
```

Logs: `/tmp/prd359-win-cd8fb820-failed.log`, `/tmp/prd359-win-wire-v8.log`,
and `artifacts/networking-359/task1b-win32/wire-{red,green}.log`.
The preprocessing check uses SDK stand-ins. Its first attempt lacked BaseTsd.h;
that prerequisite failure is not behavior-red evidence. It does not replace a
Windows compiler run. Corrected Windows CI remains pending.

The corrected wire target also rebuilt and passed 1/1 under QuickJS (exit 0;
`/tmp/prd359-win-wire-quickjs.log`). Native coverage subsequently completed with
exit 0 (`/tmp/prd359-ci-required-coverage.log`). Fresh read-only review accepted
the earliest-include guard while retaining the Windows CI limitation.
