# Night batch 2026-08-26 — Step 0 record (lane reconciliation + HEAD gates)

**Date:** 2026-08-27 (overnight). **Operator lane:** desktop host, primary checkout.

## The three unmerged lanes — all merged to main

| Lane | Commits | Resolution |
| --- | --- | --- |
| `linchpin/prd-203-template-loading-screens-stop-drifting` | 3 | Clean merge. Loading screens single-sourced into `template-assets/loading.ts`; restamp made repeatable. |
| `linchpin/prd-202-runner-lanes-share-one-implementation` | 2 | Semantic merge resolved by hand: branch layout (new `server.ts`/`shared.ts`/`camera.ts`) + main's post-base additions preserved (`settledTeardownValue` → `server.ts` beside `TEARDOWN_TIMED_OUT`; `sampleAfterTransition` stays in `steps.ts`; `HOST_PLAYTEST_OBSERVATION_FIELDS` import kept). All five conflicted files re-typed against final homes; `packages/playtest` `tsc` clean. |
| `linchpin/prd-219-android-proof-menu-flow` | 5 | **Contains real device evidence** (step 0.1's test): PRD-219 closed DELIVERED 2026-08-25 on `emulator-5554` + physical Pixel 8 `37251FDJH0037Z`, evidence `prd-219-android-menu-flow-2026-08-25.md`, and its steering-note refutation is scoped to the emulator. NOT STARTED status reconciled by accepting the branch: PRD now archived at `docs/PRDs/done/PRD-219-android-proof-of-the-menu-flow-starter.md`; the stale NOT-STARTED copy (`docs/PRDs/performance/`) and the dead 08-24 batch-folder copies removed. |

Scaffold parent hashes (`PRD_201_PARENT_SCAFFOLD_HASHES`) recomputed twice as template bytes
landed (restamp, then the shooter scenario move below); byte-stability test green both times.

## Red gates found at HEAD and their reds/greens

None of these were caused by the lanes' intent; each is its own commit on main.

1. **`scripts/__tests__/sync-agent-docs.spec.ts`** — the mirror walker skipped `.worktrees` but
   not `.claude`, so it read `.claude/worktrees/prd-222-resume/` (a dead lane) and demanded 14
   mirror edits there. Red paste in commit `51960a4f`; one line; 8/8 green; zero files written
   into the sibling worktree.
2. **`packages/create-threenative/__tests__/loading-screen.spec.ts`** — stamped template copies
   pre-dated canonical's compileAsync-prewarm demotion, and two of b6d3a9bf's test bodies
   asserted the pre-demotion contract. Restamp + two test bodies reconciled to the shipped
   contract (one unawaited prewarm; reveal never waits on it). 21/21 green.
3. **`check-doc-links`** — 29 broken links, pre-existing (archived batches moved to
   `done/`/`mobile/`/`performance/`), plus two invalidated by tonight's PRD-219 deletion.
   Retargeted mechanically; two links to deliberately deleted records relabelled. 797 links /
   678 files, exit 0.
4. **`tests/runtime-next-contract.test.mjs` "generated Android bundle provenance"** — stale
   artifact: the generated first-proof bundle pinned SHA `9d674f5d…` against game.ts whose
   committed hash is `0f990e94…` (both recent revisions hash to the current bytes; the artifact
   was built from an older working tree). Regenerated via
   `node packages/runtime-native/scripts/build-android-first-proof.mjs`; 28 passed / 1 skipped.
5. **`generated-shooter-input.spec.ts`** — deterministic red, 5 runs, identical frame
   (bright ratio 0.04470 < 0.05): the capture fired inside the post-ready pre-present window;
   the same run's final capture showed the fully lit world and `TN_STARTUP_WARMUP` completed
   clean with no device loss. Kept artifacts at `/tmp/generated-shooter-input-tNeR7x`
   (HUD rendered, world canvas black). Engine fix in `packages/playtest` commit: one
   compositor grace window (two rAFs, bounded 500 ms) then one recapture — a second blank is
   still final, so the guard stays fail-closed. 3/3 green; full suite green after.
   **Open engine question, on the record:** why the WebGPU canvas presents nothing for a window
   after `whenReady()` on a contended GPU is not yet attributed (candidates: late pipeline
   creation beyond `compileAsync`, compositor re-present latency). Filed here so it is not
   rediscovered as a flake.

## Gate state after Step 0

```
pnpm typecheck  exit 0
pnpm lint       exit 0 (437 pre-existing warnings, no errors)
pnpm test       exit 0 — 224 files, 2243 tests passed
```

No whole-run averages anywhere above; the only timing number quoted (2905 ms compile stall,
0.04470 ratio) is a single named observation, not a frame-window average.
