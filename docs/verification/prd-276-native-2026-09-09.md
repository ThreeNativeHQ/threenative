# PRD-276 native verification

Date: 2026-09-09

The native lane for the portable `InstancedBatch` export was closed with a freshly scaffolded
racing project. The generated project's `test:native` command built a desktop artifact from the
rebuilt local host and ran its committed native scenario through the desktop playtest mailbox.

## Command

```sh
THREENATIVE_RUNTIME_BINARY=$PWD/packages/runtime-native/build/tn-linux/mystral \
  pnpm exec tsx scripts/verify-one-template-desktop.ts racing
```

## Result

```text
racing: native playtests passed at /tmp/threenative-racing-desktop-7ay2j1/racing
exit 0
```

The report for `native-racing-instanced-batch` recorded:

| Observation | Result |
| --- | --- |
| target/runtime | `desktop` / `native` |
| frames | 70 |
| player movement | 6.3963455 m; minimum 0.5 m |
| `player.normaliseFactor` | 0.6632707; expected ≤ 0.99 |
| runtime diagnostics | empty |
| adapter | NVIDIA GeForce RTX 2080, Vulkan |
| capture | 1280×720 `after.png`, visibly nonblank |

The native scenario intentionally omits desktop transport observations that the runner rejects:
browser CDP network errors, visual metric evaluation, and runtime diagnostic observation. The
browser scenario remains the policy proof for those signals. Android and iOS were not run.

The host prerequisite also passed in the same lane: `pnpm native:build`, 403/403 native build
targets, exit 0.
