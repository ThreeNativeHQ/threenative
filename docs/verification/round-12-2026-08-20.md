# Improvement round ledger — round 12 — repaired template visual AB — 2026-08-20

Round: 12
Date: 2026-08-20
Framework commit: pre-repair `3677542707ac88c95f8197f33c878e192c9a87aa`; post-repair source is committed in this lane
Framework version: 0.1.0
Round kind: visual-only
Genres: template-visual
Budget: two template repairs, paired seven-template capture, duplicate-calibrated blind bundle, three read-only model raters
Stop condition met: none
Next action: keep the three carried gaps below in user space; the starter and minimal repair gaps are closed by this round's source, frame, score, and gates

This is a template-only before/after run, not a framework-versus-vanilla game pair. The
`Round kind: visual-only` declaration uses `before` and `after` condition arms, so the labels below
describe the captured conditions. No functional or authoring-cost conclusion is drawn from the
schema-only column row.

## Arms

| Genre | Arm | Archive | Brief SHA-256 | Proof SHA-256 | Proof passed/total | Instrument visual | User LOC | Source files | Reach rate |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| template-visual | before | docs/verification/visuals/ab-2026-08-20/before | abf9eb1ebfdd8ae86ab67682754826d6fe383b0e367e9b6f21854e6207e01aed | abf9eb1ebfdd8ae86ab67682754826d6fe383b0e367e9b6f21854e6207e01aed | 7/7 | 1 | n/a | 7 | n/a |
| template-visual | after | docs/verification/visuals/ab-2026-08-20/after | abf9eb1ebfdd8ae86ab67682754826d6fe383b0e367e9b6f21854e6207e01aed | abf9eb1ebfdd8ae86ab67682754826d6fe383b0e367e9b6f21854e6207e01aed | 7/7 | 1 | n/a | 7 | n/a |

The paired bundle is `docs/verification/visuals/ab-2026-08-20/blind/bundle.json`, with 16 samples:
seven before frames, seven after frames, and two duplicate pairs. The brief and proof hash above
is the SHA-256 of `docs/product/VISUAL-BASELINE.md`, also recorded as the bundle prompt hash.

Both conditions were captured headed under Xvfb with
`--ozone-platform=x11 --enable-unsafe-webgpu --disable-gpu-sandbox --ignore-gpu-blocklist
--enable-features=Vulkan`. The before capture ran from detached `HEAD` in
`/tmp/threenative-prd163-before`; the after capture ran from this lane's repaired source. Each
produced seven non-blank 1280x720 PNGs. The adapter probe with the full recipe reported
`architecture=turing`, `vendor=nvidia`.

## Column verdicts

| Genre | Functional | Visual | Cost | Verdict |
| --- | --- | --- | --- | --- |
| template-visual | tie | tie | tie | schema-only row; this AB has no functional or cost column |

## Gap list

| # | Genre | Column | What vanilla did better | Evidence | Smallest next action |
| --- | --- | --- | --- | --- | --- |
| 1 | template-visual | Visual and native parity | This round observed zero change for the platformer (2 → 2, Δ0), below MDE 1; its native HUD gap remains an owner decision | `score.json`: platformer 2 → 2 INDETERMINATE; `packages/create-threenative/templates/platformer/AGENTS.md` documents the native HUD loss | Decide whether a portable native HUD is needed; do not add a second web-only HUD |
| 2 | template-visual | Visual | Defense and racing remain at or below the measured resolution | `score.json`: defense 4 → 4 and racing 3 → 2 are INDETERMINATE | Re-measure with a new paired duplicate-calibrated bundle before changing either template |
| 3 | template-visual | Visual | This round observed zero change for the platformer (2 → 2, Δ0), below MDE 1, so the fog result remains unresolved from round 11 | `score.json`: platformer 2 → 2 is INDETERMINATE | Re-measure with a higher-resolution instrument before changing the fog |
| 4 | template-visual | Visual | The starter's old occluding torus-knot caused a resolvable loss | `score.json`: starter 2 → 4, Δ+2 WIN; before/after starter frames; source diff removes `sculptureMesh` from `starter/src/scenes/Play.ts` | Closed: keep the starter source and frame paired; reopen only on a new measured regression |
| 5 | template-visual | Visual and correctness | The old minimal frame advertised an unsupported `ITEMS` count | `score.json`: minimal 2 → 3, Δ+1 INDETERMINATE; after minimal frame shows SCORE/TIME and source no longer supplies a counter | Closed for correctness: keep the single HUD's optional counter unused until a real item count exists |

## Dispositions

| Gap # | Disposition | Measurement disposition | 20-line verdict | Named live caller | PRD | Reason if rejected |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | user space | INDETERMINATE | unresolved | `templates/platformer` | none | This round observed zero change (Δ0), below MDE 1; retain round 11's unresolved status, and the native HUD choice belongs to the template owner |
| 2 | user space | INDETERMINATE | unresolved | `templates/defense`, `templates/racing` | none | Both visual deltas are at or under MDE; no source change is justified by this bundle |
| 3 | user space | INDETERMINATE | unresolved | `templates/platformer` | none | This round observed zero change (Δ0), below MDE 1; retain round 11's unresolved status because the frame cannot distinguish a fog change from rater variance |
| 4 | user space | WIN | closed | `templates/starter` | none | Removing the named `sculptureMesh` caller exposes the proof asset and improves the paired score by two points |
| 5 | user space | INDETERMINATE | closed correctness; visual result unresolved | `templates/minimal` | none | The score is at MDE, but the source and frame now agree: the single HUD only advertises SCORE and TIME |

### Gap 1 native HUD fact

The platformer source has no `createHud` call while its React `App.tsx` renders the web HUD.
Therefore the portable template still has an owner-visible native HUD decision; this round does
not add a second web-only HUD or pretend that the gap is resolved.

Visual MDE: 1

The three raters were fresh read-only model raters, not human participants. Six duplicate
observations across the three raters measured MDE 1. This round makes no claim about the human
blind session required by `docs/product/VISUAL-BASELINE.md`.

## Visual deltas

| Template | Before | After | Δ | Verdict |
| --- | --- | --- | --- | --- |
| action-rpg | 4 | 4 | 0 | INDETERMINATE |
| defense | 4 | 4 | 0 | INDETERMINATE |
| minimal | 2 | 3 | +1 | INDETERMINATE |
| platformer | 2 | 2 | 0 | INDETERMINATE |
| racing | 3 | 2 | −1 | INDETERMINATE |
| shooter | 4 | 4 | 0 | INDETERMINATE |
| starter | 2 | 4 | +2 | WIN |

The aggregate is over the one resolvable row: mean after **4.00**, with **1 of 1** counted row at
the 4/5 floor. Six rows are excluded as `INDETERMINATE`; this is a model score, not a
seven-template human quality-floor result.

## Negative controls (observed red)

| Check | Deliberate failure | Observed result |
| --- | --- | --- |
| Adapter identity | Omit `--enable-features=Vulkan` from the headed WebGPU probe | Exit 1; `adapter.info={"architecture":"swiftshader","vendor":"google"}` and `TN_VISUAL_ADAPTER_FLAG_OMITTED` rejected the run |
| Duplicate calibration | Build the same seven-template bundle with `--duplicates 0 --raters 1`, then score it | Exit 2; `TN_VISUAL_AB_NO_DUPLICATE_PAIR` rejected scoring |
| Delta resolution | In a synthetic ledger, change `minimal` from `INDETERMINATE` to `WIN` while retaining Δ `+1` and MDE `1` (`\|delta\| = MDE`) | Exit 1; see the exact boundary-control command and output below |
| Gap coverage | In a synthetic copy, remove one of the five disposition rows | Exit 1; the validator rejected the ledger because every gap must have exactly one disposition |
| Blank-frame guard | Send an all-black 2×2 PNG to the capture guard before scoring | Exit 1; `TN_CAPTURE_BLANK` rejected the frame before scoring |

The exact boundary-control run was:

```text
$ FORCE_COLOR=0 pnpm exec tsx -e 'import { readFileSync } from "node:fs"; import { validateRoundLedger } from "./scripts/round-ledger.ts"; const source = readFileSync("docs/verification/round-12-2026-08-20.md", "utf8"); const synthetic = source.replace("| minimal | 2 | 3 | +1 | INDETERMINATE |", "| minimal | 2 | 3 | +1 | WIN |"); try { validateRoundLedger(synthetic, "synthetic-boundary.md"); console.log("unexpected pass"); process.exitCode = 0; } catch (error) { console.error(error instanceof Error ? error.message : String(error)); process.exitCode = 1; }'
synthetic-boundary.md: visual delta for 'minimal' is 1 against a measured MDE of 1; the instrument cannot resolve it, so it may only be recorded INDETERMINATE, not 'WIN'.
```

Exit status: `1`. The synthetic copy changed only `minimal`'s verdict from `INDETERMINATE` to `WIN`; its `before=2`, `after=3`, and `delta=+1` remained arithmetically valid, so the validator rejected the exact `|delta| = MDE = 1` claim.

## Gates

| Gate | Command | Result | Evidence |
| --- | --- | --- | --- |
| Typecheck | `pnpm typecheck` | pass | exit 0 |
| Lint | `pnpm lint` | pass | exit 0; any cognitive-complexity output is warning-level |
| Test | `pnpm test` | pass | exit 0 |
| Budgets | `pnpm budgets` | pass | exit 0 |
| Template playtests | `pnpm test:templates` | pass | exit 0; all scaffolded template scenarios passed |

## Firewall attestation

| Rule | Held? | Evidence |
| --- | --- | --- |
| Before and after captured in separate detached worktrees | yes | before at `/tmp/threenative-prd163-before`; after from this lane; each produced seven PNGs |
| Adapter identity gate held | yes | full recipe reported `turing / nvidia`; omitted-Vulkan control reported SwiftShader and exited 1 |
| Raters did not see the reveal before scoring | yes | verdict files were supplied only after the blind samples were inspected; reveal was read afterward |
| No framework package source or second web-only HUD was added | yes | only template source changed; scoped `packages/*/src` diff outside templates is empty |

## Notes

The durable visual evidence is under `docs/verification/visuals/ab-2026-08-20/`: before and after
frames, the 16-sample blind bundle, reveal, three rater files, and `score.json`. The no-verdict
bundle checkpoint exited 2 as designed before the rater files were supplied. The starter repair
removes only the visible torus-knot ornament named in the round-11 gap. The minimal repair keeps
one HUD and makes its counter label/value optional; the minimal scene supplies no counter, so the
frame no longer advertises `ITEMS` without gameplay evidence. No native or device result is
claimed.
