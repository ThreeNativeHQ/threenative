# Improvement round ledger — round 3 — 2026-08-09

Round: 3
Date: 2026-08-09
Framework commit: a67806c
Framework version: 0.1.0
Genres: open-world
Budget: one completed round, granted by the 2026-08-09 `$self-improve` invocation
Stop condition met: budget
Next action: None — retain the losing open-world result and its user-space dispositions

## Arms

One row per genre per arm. Both arms of a genre must share a brief hash and a proof hash.

| Genre | Arm | Archive | Brief SHA-256 | Proof SHA-256 | Proof passed/total | Instrument visual | User LOC | Source files | Reach rate |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| open-world | framework | docs/benchmark/sweeps/open-world-2026-08-09-3 | d504a7038965e37ccb305f2d2ce013ec6898e0bb053fac1db126ada1841ebd57 | b12cbe809282996f248177028b712560df24894556dc0ac3ef78ef7f0b0138e8 | 0/1 | 1 | 1031 | 20 | 0.55 |
| open-world | vanilla | docs/benchmark/sweeps/open-world-2026-08-09-4 | d504a7038965e37ccb305f2d2ce013ec6898e0bb053fac1db126ada1841ebd57 | b12cbe809282996f248177028b712560df24894556dc0ac3ef78ef7f0b0138e8 | 0/1 | 4 | 489 | 1 | n/a |

The vanilla proof cell is the post-round arm-neutral transport replay stored in
`docs/verification/open-world-round-3-vanilla-proof-replay.json`; the historical archive's
original invalid `proof.json` remains untouched.

## Column verdicts

Framework arm wins a column when it is at least as good as the vanilla arm. Reach is
recorded, never gated.

| Genre | Functional | Visual | Cost | Verdict |
| --- | --- | --- | --- | --- |
| open-world | tie | loss | loss | vanilla wins visual and authored cost; both arms validly fail 0/1 functional scenarios |

## Gap list

One row per column the vanilla arm won. No losses means one row saying `None`, so the
absence is an observation rather than an empty table.

| # | Genre | Column | What vanilla did better | Evidence | Smallest change that would close it |
| --- | --- | --- | --- | --- | --- |
| 1 | open-world | visual | Vanilla kept the player, trail, terrain, props, and landmarks readable; the framework capture showed only HUD over sky. | `docs/verification/blind-open-world-round-3/judge.json`; reveal maps sample-01 to vanilla and sample-02 to framework | Repair the framework arm's user-owned camera/traversal frame so the world remains visible after the sealed input. |
| 2 | open-world | cost | Vanilla authored 489 LOC; framework authored 497 LOC above its starter. | Two `pnpm sweep:measure` results; the repaired `pnpm sweep:pair` reports an eight-line framework loss. | Remove or simplify eight user-owned lines; no framework abstraction clears the 20-line rule. |

## Dispositions

Every gap row gets exactly one disposition, decided before any code is written.

| Gap # | Disposition | 20-line verdict | Named live caller | PRD | Reason if rejected |
| --- | --- | --- | --- | --- | --- |
| 1 | user space | Camera, look, and gameplay code remain user-owned regardless of fix size. | framework open-world `Play`/camera/render source | none | n/a — package ownership would violate the screenshot boundary. |
| 2 | rejected | The measured delta is 8 authored lines, below the strict >20-line threshold. | framework open-world arm | none | No abstraction is justified for an eight-line user-source loss. |

## Deletions this round

`AGENTS.md` rule 2. Any export that no fresh uninformed build reached for is deleted in the
round that discovers it.

| Export | Rounds unreached | Deleted? | Evidence |
| --- | --- | --- | --- |
| 48 reached externally | 2 | no — kept after external-caller review | `docs/verification/deletions-2026-08-10.md` |
| 106 internal-only exports | 2 | yes — un-exported from package entrypoints by PRD-063 | `docs/verification/deletions-2026-08-10.md` |
| 8 public-by-contract exports | 2 | no — kept for the documented core/template contracts | `docs/verification/deletions-2026-08-10.md` |
| 5 dead exports | 2 | yes — deleted by PRD-063 | `docs/verification/deletions-2026-08-10.md` |

The five deleted exports are `applyScenarioOverrides`, `input`, `oneShotScenario`,
`parsePlaytestTarget`, and `parseViewport`. The full 167-row disposition table is in the
deletion evidence file; the 106 internal-only rows were un-exported but their module-local
implementations remain, while the eight public-by-contract rows preserve the documented core
and generated-template contracts.

## Gates

| Gate | Command | Result |
| --- | --- | --- |
| Typecheck | `pnpm typecheck` | pass |
| Lint | `pnpm lint` | pass |
| Test | `pnpm test` | pass |
| Budgets | `pnpm budgets` | pass |

## Firewall attestation

| Rule | Held? | Evidence |
| --- | --- | --- |
| Arms built in separate contexts | yes | fresh builders `/root/round3_framework_builder` and `/root/round3_vanilla_builder`, with the framework archive completed before the sandbox was recreated |
| Neither builder saw the sealed proofs | yes | each builder received only its generated sandbox brief/reference; sealed proofs ran after each archive |
| Judge was fresh, read-only, blind to arm | yes | fresh critic `/root/round3_blind_critic`; anonymous bundle `docs/verification/blind-open-world-round-3`; reveal stored outside it |
| Lead agent wrote no game code | yes | lead created only this round ledger and orchestration state |

A `no` on any row voids the round's comparison. Record it as void and rebuild; do not
publish the numbers with a caveat.

## Notes

- `open-world` is the only sealed genre not already represented in the Phase 1 four-genre
  comparison. It is therefore the highest-information next pair.
- The arm order is framework first, then vanilla, with an archive between them.
- Framework measurement: 1,031 total user LOC, 497 authored LOC above the starter, 20 source
  files, and 0.55 framework reach. The scaffold required a user-space repair because
  `create-threenative@0.1.0` was unavailable from the public registry; the supplied local
  framework tarballs still powered the game.
- The framework sealed proof failed 0/1: 162 console errors, 134.20 movement distance and
  134.76 path length against minimum 300, and `chunk.7` not projected visible. `chunk.0`
  correctly streamed out. This is retained as a finding; no sealed assertion was changed.
- Vanilla measurement: 489 LOC and one source file. The original sealed proof stopped before
  assertions because the arm-neutral bridge omitted `runtime.diagnostics`. The repaired
  transport replay reached all five sealed assertions and failed 0/1: 1,507 captured runtime
  errors, 178.56 movement distance and 179.75 path length against minimum 300, and `chunk.7`
  not projected visible. `chunk.0` correctly streamed out.
- Fair authored cost is 497 framework versus 489 vanilla LOC, an 8-line framework loss.
- A post-round replay ran both unchanged archives through the same repaired transport.
  `pnpm sweep:pair` then accepted the pair: framework 0/1, vanilla 0/1, and the same eight-line
  framework cost loss. Framework replay evidence is
  `docs/verification/open-world-round-3-framework-proof-replay.json`; vanilla replay evidence
  is `docs/verification/open-world-round-3-vanilla-proof-replay.json`.
- `pnpm sweep:capture` could not run after failed proofs. The available framework proof frame
  and the vanilla builder's headed-WebGPU frame both passed `assertFrameShowsSomething`; the
  manual blind bundle records this bounded fallback instead of claiming a standard capture.
- Blind visual judge: vanilla/sample-01 scored 4/5 and framework/sample-02 scored 1/5 at high
  confidence. Critic input is `docs/verification/open-world-round-3-critic.json`; validated
  output is `docs/verification/blind-open-world-round-3/judge.json`; external reveal is
  `docs/verification/open-world-round-3-reveal.json`.
- The repair assigns `runtime.diagnostics` to browser/device transports that already capture
  real runtime errors, removes core's never-written empty provider, and feeds those errors into
  the runtime diagnostic assertion. It does not fabricate an empty application channel.
- The native runtime budget review trigger remains active at 53,119 LOC. The hard budgets pass.
- Gate detail: lint checked 380 files; test covered package builds, publint, JavaScript tests,
  native parity tests, and root Vitest; budgets reported 6 framework packages and 5,815 of
  15,000 framework LOC.
