# PRD-359 task 8c — real-process MCP discovery — 2026-09-06

This is implementation evidence for spawned MCP search/detail and the cold packed launcher. The
row remains pending an independent read-only review checkpoint.

## Caller and source

- `packages/create-threenative/__tests__/scaffold-mcp.spec.ts:174-208` sends the concrete
  networking search and same-process `engine_capability_detail` request through one spawned MCP
  child for every template.
- `scripts/verify-golden-path.ts:376-480` applies the same networking search/detail contract to
  the packed `threenative-engine` launcher; `:526-543` imports `@threenative/core/net` from the
  installed package, and `:636-641` invokes both checks in the packed project.
- `scripts/__tests__/verify-golden-path.spec.ts:32-84` is the named removed-metadata negative
  control.

## Red

Command:

```sh
pnpm exec vitest run scripts/__tests__/verify-golden-path.spec.ts
```

Exit `1`, `/tmp/prd359-red-cold-mcp.log`. The newly added removed-metadata control failed at the
unimplemented exported assertion (`TypeError: assertEngineCapabilityDiscovery is not a function`).
After the assertion was implemented, the test's fake packed response returns `verdict: none` and
the control passes by rejecting with `missing portable networking transport metadata`.

## Green

Commands:

```sh
pnpm exec vitest run scripts/__tests__/verify-golden-path.spec.ts
pnpm exec vitest run packages/create-threenative/__tests__/scaffold-mcp.spec.ts
TN_GOLDEN_PATH_TEMPLATES=starter TN_PLAYTEST_ALLOW_SOFTWARE=1 TN_GOLDEN_PATH_SCENARIOS=1 pnpm verify:golden-path
```

The focused golden-path suite exited `0` with 23/23 tests
(`/tmp/prd359-green-cold-mcp-unit-final.log`); the spawned scaffold suite exited `0` with 4/4 tests
(`/tmp/prd359-green-scaffold-mcp-networking.log`). The final packed run exited `0` after packing
the `@threenative/core` tarball (`sha256:514e0bb4cc843b883f7992acc023044e81c06264594420dd215c93e35ce42083`)
and probing the installed `threenative-engine` twice, including the hidden-project-manifest
fallback (`/tmp/prd359-green-cold-golden-path-networking.log`).

The packed run printed `threenative-engine installed @threenative/core/net: connect` twice: once
with the generated project manifest and once after hiding it so the installed core bundle supplied
the manifest. The packed engine-MCP tarball was also recorded as
`sha256:b132222e3578e1339e3a310d674e08a95ae1636ab790c87ae7ff26832587a454`. This is a cold
temporary scaffold, not the required external networking game sandbox; it proves installed MCP
search/detail and the installed `@threenative/core/net` import only. No workspace symlink or source
import is used by that packed path, and no platform qualification is claimed.
