# PRD-218 final verification — scene-backed menu screen flow

Date: 2026-08-24

## Implementation proof

- Framework layer: `packages/core`, `packages/playtest`, `packages/create-threenative`, and the
  starter template. The menu recipe is generated starter source; carry, sampling, click delivery,
  tarball handling, and doctor preflight are framework plumbing.
- Focused regression suite after the review repair: 8 files passed, 201 tests passed.
- The cold-agent journey is recorded in
  `docs/verification/PRD-218-cold-agent-menu-settings-playtest-2026-08-24.md`: a fresh starter
  added Settings from generated guidance and passed menu → settings → play with carried
  `characterName` and `difficulty` assertions. The browser run used explicit `--allow-software`
  because this host exposed the SwiftShader adapter.
- The final doctor regression creates a real temporary `threenative.config.ts`, loads it through
  the same `loadConfig()` path used by builds, and proves a web-renderer project receives the
  desktop overlay probe. Native UI keeps the overlay check absent.

## Gate results

| Command | Result |
|---|---|
| `pnpm typecheck` | PASS, exit 0 |
| `pnpm lint` | PASS, exit 0; 307 existing complexity warnings |
| `pnpm test` | PASS, 214 files passed / 1 skipped; 2,099 tests passed / 3 skipped |
| `pnpm sync:agents --check` | PASS, 16 mirrors in sync |
| instruction-budget focused tests | PASS |
| `pnpm budgets` | FAIL, pre-existing native census drift in `tests/`, `scripts/`, `android/`, `native/`, and Root `CMakeLists.txt`; no census files changed |
| `pnpm test:templates` | FAIL after `action-rpg` passes: the six existing `defense` scenarios stop at `TN_PLAYTEST_BRIDGE_MISSING` before assertions; no defense files changed |

The two failing repository-wide gates are unrelated baseline drift and are retained as named
evidence rather than repaired inside PRD-218.

## Review and repair record

1. Review round 1 found native click silently advancing on native targets, an unconditional doctor
   overlay probe, non-plain carry values, and the missing cold-agent settings proof.
2. Repair commit `eef59dff` added fail-closed native click diagnostics, strict JSON carry checks,
   renderer-aware overlay behavior, and the cold-agent evidence.
3. Review round 2 found that the normal scaffold stores `ui.renderer` in TypeScript config, while
   doctor initially read only JSON/package configuration. Post-review fix commit `c0ae7b21` loads
   the shipped TypeScript config through `loadConfig()` and adds the real filesystem regression;
   focused tests, typecheck, lint, and the full suite are green afterward.
