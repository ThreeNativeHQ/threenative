# PRD-015 verification

Date: 2026-08-04

Status: implementation complete; repository and generated-consumer evidence is recorded
below. The package `publint` step and browser visual/runtime proof are environment-blocked.

## What shipped

- The starter scene calls the generated spring arm, rounded shapes, lighting, postprocess,
  gradient sky, pickup audio, and kill-plane respawn path.
- Starter and minimal players use `CharacterBody3D.moveAndSlide(dt)` with `body.velocity`,
  coyote time, and a jump buffer; `#verticalVelocity` is absent.
- Starter `Boot` awaits a texture before entering `Play`, and `Menu` exposes pause state
  with `aria-pressed`.
- The template contract test checks every render module and runtime export has a caller,
  rejects starter `BoxGeometry` use, and rejects the superseded vertical-velocity path.
- Starter look, coyote/buffer, and respawn scenarios are shipped and parse successfully.

## Green checks

| Check | Result |
| --- | --- |
| `pnpm typecheck` | PASS — root, workspace packages, and `examples/abyss-framework` |
| `pnpm lint` | PASS — Biome checked 176 files |
| Focused template/playtest suite | PASS — 3 files, 20 tests |
| `pnpm test` | BLOCKED — `@publint/pack` could not find its temporary tarball although `pnpm pack` exits 0 |
| `pnpm budgets` | PASS via `node --import tsx/esm scripts/check-budgets.ts` — 7 packages, 2,539 framework LOC, 8 PRD files |
| `pnpm sync:agents` | BLOCKED by `listen EPERM /tmp/tsx-1000/13.pipe`; equivalent direct runner passed with 10 mirrors in sync |
| `git diff --check` | PASS |
| Starter render dead-code rows | PASS — camera 1, shapes 3, lighting 1, materials 1, postprocessing 1, sky 1 |
| `rg '#verticalVelocity' templates` | PASS — no matches |
| Fresh scaffold tree | PASS — generated starter contains `sky.ts`, all three new scenarios, and updated player/scene files |
| Generated consumer typecheck | PASS — `tsc --noEmit -p tsconfig.json` |
| Generated consumer build | PASS — Vite transformed 43 modules and emitted `dist/` |
| Scenario parsing | PASS — look: 1 step; coyote: 3 steps; respawn: 2 steps |

The PRD's broad `rg 'new BoxGeometry\\(' packages/create-threenative/templates` command
still reports pre-existing geometry in the out-of-scope `minimal/src/scenes/Play.ts` and
`platformer/` template. The starter-only contract gate is green; those unrelated files were
not edited under the lane scope.

## Visual and runtime consumer gate

BLOCKED, not claimed green. The generated consumer built, but the declared browser capture
could not launch in this sandbox:

- Headless Chromium failed at `sandbox_host_linux.cc:41` with `Operation not permitted`.
- Headed Chromium under `xvfb-run -a -s "-screen 0 1600x900x24"` failed in crashpad with
  `Operation not permitted` and `SIGTRAP`, including the safe `--disable-gpu-sandbox`,
  `--disable-setuid-sandbox`, and `--no-zygote` variants.

No screenshot was captured beside `examples/REFERENCE.png`, so the human visual answer is
unrecorded rather than inferred. The `look`, `coyote`, and `respawn` browser assertions were
not reported as runtime passes for the same reason.

## Declared red controls observed

Each source mutation was restored immediately after the failing check.

| Control | Observed red result |
| --- | --- |
| Detach the spring-arm call | `template.spec.ts`: expected `createSpringArm(ctx.camera` was absent |
| Skip the `setupSky` call | `template.spec.ts`: expected `setupSky(ctx.scene` was absent |
| Add an unused render export | render-export test: caller was `false`, expected `true` |
| Replace a starter rounded mesh with `BoxGeometry` | starter geometry contract rejected `new BoxGeometry(` |
| Remove sky fog | sky test: expected `scene.fog = new Fog(bottom, 18, 80)` |
| Remove the incomplete-gradient guard | sky test: expected `options.bottom === undefined` |
| Set coyote time to `0` | movement contract rejected the required `COYOTE_TIME = 0.12` |
| Set jump buffer to `0` | movement contract rejected the required `JUMP_BUFFER = 0.14` |
| Remove the kill-plane respawn call | scene contract rejected `player.respawn()` |
| Mute pickup audio | scene contract rejected `this.#audio?.play(buffer)` |
| Drop the `Boot` texture `await` | load-order contract found no awaited texture load |
| Make the pause button a no-op | menu contract found no `game.pause()` call |
| Reintroduce `#verticalVelocity` | grep returned the injected line and exited nonzero under the zero-match gate |

Runtime coyote, audio, pause, and respawn mutation scenarios remain unrun because the browser
launcher is blocked above; their source-level fail-closed controls are the red evidence shown
here.
