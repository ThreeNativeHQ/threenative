# Improvement round ledger — round 8 — first fresh pair on the repaired instrument — 2026-08-15

Round: 8
Date: 2026-08-15
Framework commit: e67a0d2d
Framework version: 0.1.0
Genres: physics-puzzle
Budget: one paired physics-puzzle build, granted unattended
Stop condition met: none yet
Next action: Rebuild both arms against sealed proof `e5be692b` and re-run, because the naming defect this round exposed makes the current functional column unusable as a parity verdict

Round 7 was void: both its arms were re-archived snapshots rather than independent builds. This
round is the first with two genuinely fresh builders on the same sealed brief, so its cost column
is the first real one. Its functional column is not usable, for the reason in the notes.

## Arms

| Genre | Arm | Archive | Brief SHA-256 | Proof SHA-256 | Proof passed/total | Instrument visual | User LOC | Source files | Reach rate |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| physics-puzzle | framework | docs/benchmark/sweeps/physics-puzzle-2026-08-15-7 | d950471f7bfc7e68778711ff44438f55022b769b9ffa0d26776252c8414d5935 | 33c3acb029096205e3e04cc22afdd736575998b8a41ab3208888071ede654ab8 | 0/2 scenarios, 6/12 rows | unmeasured | 1815 | 22 | 0.455 |
| physics-puzzle | vanilla | docs/benchmark/sweeps/physics-puzzle-2026-08-15-6 | d950471f7bfc7e68778711ff44438f55022b769b9ffa0d26776252c8414d5935 | 33c3acb029096205e3e04cc22afdd736575998b8a41ab3208888071ede654ab8 | 0/2 scenarios, 8/12 rows | unmeasured | 1605 | 12 | 0.083 |

Both arms were scored against the proof they were sealed against, `33c3acb0`. The sealed replay
proof was changed later the same day under PRD-113 and now hashes `e5be692b`; that change is a
round-9 instrument and was deliberately not used to score builds that never saw it.

`Instrument visual` is `unmeasured`: `pnpm sweep:capture` was not run, because the machine was
running three other agents' workloads and every headed browser slot was contended.

## Column verdicts

| Genre | Functional | Visual | Cost | Verdict |
| --- | --- | --- | --- | --- |
| physics-puzzle | unmeasured | unmeasured | loss | unmeasured — cost is real and lost, functional is void on a defective instrument |

## Gap list

| # | Genre | Column | What vanilla did better | Evidence | Smallest change that would close it |
| --- | --- | --- | --- | --- | --- |
| 1 | physics-puzzle | Cost | Shipped the same brief in 1,605 authored lines across 12 files against the framework arm's 1,815 across 22 — 210 lines and 10 files fewer | `pnpm sweep:measure` on both archives | None proposed this round. A cost loss on one genre, on the first valid pair, is a signal to re-measure rather than to extract anything |

## Dispositions

| Gap # | Disposition | 20-line verdict | Named live caller | PRD | Reason if rejected |
| --- | --- | --- | --- | --- | --- |
| 1 | User space | n/a — no abstraction proposed | n/a | n/a | One genre, one pair. `CHARTER.md` puts the framework's floor at vanilla, and a single lost cost column is not yet the two-round kill-switch condition. Round 9 decides |

## Deletions this round

| Export | Rounds unreached | Deleted? | Evidence |
| --- | --- | --- | --- |
| applyImpulse | 3 | no — the deletion verdict needs a functional column, which this round does not have | framework arm reach rate 0.455; `pnpm round:deletions` not re-run because round 8 cannot supply a parity verdict |
| applyForce | 3 | no — same | same |

## Gates

| Gate | Command | Result |
| --- | --- | --- |
| Typecheck | `pnpm typecheck` | pass |
| Lint | `pnpm lint` | pass, 0 errors |
| Test | `pnpm test` | pass, 1,251 passed / 32 skipped |
| Budgets | `pnpm budgets` | pass, framework 15,077/15,000 and native 71,053/50,000 review triggers visible |

## Firewall attestation

| Rule | Held? | Evidence |
| --- | --- | --- |
| Arms built in separate contexts | yes | two subagents, `/home/joao/projects/threenative-r8-framework` and `/home/joao/projects/threenative-r8-vanilla` |
| Neither builder saw the sealed proofs | yes | verified by direct comparison — see the deviation note below |
| Judge was fresh, read-only, blind to arm | not run | `pnpm sweep:judge` not run; visual column is `unmeasured`, not guessed |
| Lead agent wrote no game code | yes | the lead spawned, collected and recorded only |

## Notes

**Recorded deviation: one arm saw the other's process line.** While both arms were building
concurrently, the vanilla builder ran `pgrep -af vite` to free a stuck port and the output printed
the framework arm's full command line, including a `probe.playtest.json` that arm had written with
a heredoc. The builder reported this unprompted, supplied the raw text on request, and corrected
its own earlier description of it from two resource paths to five.

Adjudicated as a deviation rather than a contamination, and the owner accepted that call:

- The leaked file is that arm's own run-time debugging probe, not a sealed proof. Compared
  directly, it shares no assertion, no step label and no route with either sealed scenario.
- Every vanilla design decision predates it, with named artifacts: `mission: won` with
  `player|goal|trigger` contacts, `replayHashA == replayHashB == 3214079977`, `ghostPasses: 2`.
- Zero edits under `src/` after it appeared.

The firewall attestation rows above are unaffected, since none of them is the rule that was
touched. Future rounds run the arms **sequentially**, and builders are now told not to enumerate
the machine — recorded in `.agents/skills/self-improve/SKILL.md`.

**Second deviation: neither builder could load `build-on-sandbox`.** The skill lives inside this
repository, which builders are forbidden to read, so both proceeded on the lead's prompt and the
sandbox `AGENTS.md`. The firewall held only because the prompt restated it. Also now recorded in
the skill.

**Why the functional column is void.** The sealed replay proof required
`state.replayPhase` to equal `"done"` and `state.replayMatch` to equal the string `"match"`, and
the brief publishes neither value. The vanilla arm produced `"complete"` and boolean `true` from a
correct replay whose two runs hashed identical, and lost both rows to the guess. That is
PRD-113's exact subject, evidenced in
`docs/verification/prd-113-sealed-proof-still-tests-naming-2026-08-15.md` and repaired in
`e67a0d2d`. A functional column scored on an instrument that measures vocabulary is not a parity
verdict, so it is recorded `unmeasured` rather than as a framework loss.

The cost column does not depend on that instrument and stands: **the framework arm cost 210 more
authored lines than plain Three.js for the same brief.** Round 7 also showed a cost loss, but
round 7 was void, so this is the first of a possible two. If round 9 repeats it on a working
instrument, `CHARTER.md`'s kill-switch condition is met and the correct response is to say so.

**Machine contention shaped this round.** Three unrelated agent workloads ran on this host
throughout — a jest suite at ten workers, a `fox-native` dev server holding port 4173, and a peer
session in the same tree. It cost the visual column, hung the golden-path gate twice, and is the
direct reason the arms ran concurrently in the first place.
