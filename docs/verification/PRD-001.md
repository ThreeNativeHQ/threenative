# PRD-001 verification evidence

This file records local verification for `docs/PRDs/PRD-001-monorepo-foundation.md`.

| Gate | Result | Negative control observed red? |
|---|---|---|
| catalog resolution | PASS — `pnpm install --frozen-lockfile` resolves all 4 workspace projects; `pnpm list --depth 0 -r three` shows `0.185.1` for `abyss-vanilla` and `@threenative/playtest` | PASS — literal `three: 0.185.1` fixture reports one catalog violation |
| package build + publint | PASS — `pnpm -r build` completes Vite, `@threenative/core`, and `@threenative/playtest`; both packages report `All good!` from publint | PASS — disposable core fixture without `src/index.ts` prints `Cannot find src/index.ts` and exits 1 |
| abyss-vanilla boot | PASS — `pnpm test:browser`: `1 passed (4.6s)`; canvas screenshot and luminance assertions pass | PASS — unfiltered Chromium runs failed on the observed Dawn `popErrorScope` / mapped-buffer diagnostics before the exact known backend messages were bounded |
| budgets: package cap | PASS — real tree reports 3 packages | PASS — 9-package fixture reports `workspace package cap exceeded: 9 packages (limit 8, +1)` |
| budgets: LOC cap | PASS — real tree reports 2 framework LOC | PASS — 15,001-line fixture reports `framework LOC cap exceeded: 15001 lines (limit 15000, +1)` |

## Regular gates

```text
pnpm typecheck        PASS — root check plus core and playtest typecheck
pnpm lint             PASS — Biome checked 17 files
pnpm test             PASS — 7 files, 23 tests
pnpm -r build         PASS — Vite, tsup, and publint
pnpm test:browser     PASS — 1 test in 4.6s
pnpm budgets          PASS — 3 packages, 2 framework LOC, 6 PRD files
node --import tsx --test ...  PASS — 13 tests, 0 failures
```

The root Biome pass excludes generated `test-results/**` and the standalone salvage
package `packages/playtest/**`; the latter remains covered by TypeScript, tsup,
publint, Vitest, and Node TAP tests.

## Integration proof

```text
$ rg -n 'check-budgets|pnpm test:browser|pnpm typecheck|pnpm lint|pnpm -r build|pnpm budgets' .github/workflows/ci.yml
34:      - run: pnpm typecheck
49:      - run: pnpm lint
66:      - run: pnpm test:browser
81:      - run: pnpm -r build
96:      - run: pnpm budgets

$ test ! -e index.html && test ! -e src && test ! -e vite.config.js
root runtime absent
$ for file in index.html src/main.js src/style.css vite.config.js; do git show 639cd29:$file | cmp - examples/abyss-vanilla/$file; done
frozen control byte comparisons passed
```
