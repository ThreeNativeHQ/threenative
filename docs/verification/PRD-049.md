# PRD-049 — physics parity verification

## Phase 0 — host measurement (2026-08-08)

**Result: measurement harness PASS; parity is intentionally not yet a passing claim.** The web
arm is Rapier `0.19.3`; the linked Rust arm is Rapier `0.30.0`. Both read scenario SHA-256
`c5f9c14ec977ee05e00c4662208fb5d8f1707ff5b64cb41b86bfbc6875330b4d` from
`physics-parity.scenario.json`.

This is host evidence only. It is not replay portability, Android evidence, `arm64` evidence,
physical-hardware evidence, or a release-mode performance comparison. The Rust timing below is
from a debug `cargo test` build and exists only to keep the row numeric.

### Proof subject

One seven-body world ran for 180 steps at `0.016666667` seconds per step. Both arms observed all
three scenario coverage controls:

| Control | Web | Rust |
| --- | ---: | ---: |
| character passed upward through the one-way layer | `true` | `true` |
| character reported grounded on moving platform id `2` | `true` | `true` |
| box-only area excluded character id `3` | `true` | `true` |

The same world also contained a fixed floor, a settling dynamic box, autostep and snap-to-ground,
and body id `6` removed while it overlapped area id `5`.

### Tolerance table

| Contract row | Tolerance | Web | Rust | Measured delta | Phase 0 |
| --- | --- | --- | --- | ---: | --- |
| resting position | `≤ 0.02` per axis | `[0.698946297, 0.398779213, 0.005187713]` | `[0.6989463, 0.3987792, 0.005187713]` | `0` max axis | inside |
| character displacement | `≤ 0.05` cumulative | `[5.185502052, 0.059996128, -0.000383814]` | `[5.185502, 0.059996128, -0.000383814]` | `0` max axis | inside |
| grounded / ground collider | exact | `true / 0` | `true / 0` | `0 / 0` mismatches | inside |
| area membership set | exact | `{1}` | `{1}` | `0` symmetric difference | inside |
| collision event set | exact, unordered | `{0-1-start, 1-2-start, 1-5-start}` | same | `0` symmetric difference | inside |
| rejected-input error class | exact | see below | see below | `6 / 6` raw outcome strings differ | outside / boundary incomplete |

The zero position deltas are the values printed after conversion to each harness's JSON number
representation. They do not mean cross-version snapshots are portable or bit-identical.

### Rejected-input observations

The Rust integration test reaches the shipping `Simulation` through its exported C ABI. It does
not link the C++/JavaScript host adapter, so a Rust `false` or `-1` is reported as `rejected`; it
cannot prove the JavaScript error class produced by that adapter.

| Probe | Web adapter | Rust ABI | Exact native JS error class |
| --- | --- | --- | --- |
| non-finite delta | `Error` | `rejected` | NOT REPRODUCIBLE ON THIS HOST HARNESS |
| `Float64Array` input | `Error` | NOT REPRODUCIBLE ON THIS HOST | NOT REPRODUCIBLE ON THIS HOST HARNESS |
| oversized kinematic count | `Error` | NOT REPRODUCIBLE ON THIS HOST (unsafe to over-read an FFI pointer) | NOT REPRODUCIBLE ON THIS HOST HARNESS |
| unknown kinematic id | `Error` | `rejected` | NOT REPRODUCIBLE ON THIS HOST HARNESS |
| unknown `removeBody` id | `accepted` | `rejected` | NOT REPRODUCIBLE ON THIS HOST HARNESS |
| undersized render buffer | `Error` | `rejected` | NOT REPRODUCIBLE ON THIS HOST HARNESS |

### D1–D8 inspection rows

| Row | Host measurement | Phase consequence |
| --- | --- | --- |
| D1 self-comparison | `1` real differential: versions differ and the linked Rust test count is `1` | Phase 1 still deletes the old web-over-web fake |
| D2 freshness coupling | Rust ABI and web both returned character state and `2` area members before a post-step visible read; native TypeScript cache coupling is **NOT REPRODUCIBLE ON THIS HOST HARNESS** | Phase 2 must cover the native adapter |
| D3 unknown removal | web accepted; Rust ABI rejected (`1` outcome mismatch); the C++ exception is **NOT REPRODUCIBLE ON THIS HOST HARNESS** | Phase 3 aligns the public outcomes |
| D4 teleport side effects | before: both `grounded=true`, collider `2`; after: web kept both while Rust returned `false / none` (`1` grounded mismatch) | Phase 3 runs |
| D5 collision derivation | event-set symmetric difference `0`; web `164,746 ns/step`, Rust debug `1,963,615 ns/step` | timing is not attributable to the O(n²) scan; Phase 4 needs its specified 10/100/300-body benchmark before optimisation |
| D6 quadratic buffers | at seven bodies: event buffer `784 B`, native area buffer `392 B`, total `1,176 B` | numeric allocation baseline for Phase 4 |
| D7 remove during overlap | stopped-event count web `0`, Rust `0`; delta `0` | not a parity bug on this subject; no event-emission change justified |
| D8 proof-only surface | non-test consumers outside its own definitions: `0`; TypeScript proof surface: `170` lines | Phase 5 deletion remains justified |

### Negative controls observed red

| Control | Observed result |
| --- | --- |
| changed fixture `steps` from `180` to `181` | both arms passed with new shared SHA `0568330694cb89845f4a7c5dc61c3c81da4e656a3e36c847af7c52b65d5b3fc4`; reverting restored `c5f9…0b4d` |
| forced expected Rust identity to web `0.19.3` | web identity test failed `1 / 3`: expected `0.19.3` not to equal `0.19.3` |
| removed the web observation artifact | Rust test failed exit `101`: `run the web parity arm before the Rust parity arm: No such file or directory` |
| deleted the `grounded` observation row | Rust test failed exit `101`: `missing field grounded`; the table cannot silently shorten |

### Commands and collection evidence

```text
pnpm --filter @threenative/runtime-native native:physics:parity
  web:  1 file, 3 tests passed
  Rust: running 1 test; 1 passed
```

The Rust library exposes both `staticlib` (shipping) and `rlib` (integration-test linkage). The
test imports `threenative_native_physics` normally; it does not include or recompile `src/lib.rs`.
`serde`, `serde_json`, and `sha2` are dev-only dependencies required to read the shared JSON and
verify its SHA without duplicating a parser.

The web measurement intentionally does not dispose its scenario world after reading character
collision state. Rapier `0.19.3` otherwise throws `attempted to take ownership of Rust value while
it was borrowed` from `world.free()`. The process exits immediately after writing the artifact;
all recorded simulation observations occur before this cleanup-only limitation.

## Phase 1 — real conformance gate (2026-08-08)

**Checkpoint result: correctly RED on measured D4.** `NativeHostOverWeb` and every delegated
web-over-web simulation assertion were deleted from `native-contract.spec.ts`. A focused census
for `NativeHostOverWeb|createWebPhysicsSimulation` in that file and the physics agent contract
returns zero hits.

The remaining TypeScript contract test proves only facts it can prove without a simulator fake:

- native and web exports resolve to the same four public node classes;
- an unsupported native heightfield throws before the raw host's `createBody` is called.

The linked Rust gate now asserts, in order:

1. scenario SHA and the distinct `0.19.3` / `0.30.0` identities;
2. resting position `≤ 0.02` per axis and character displacement `≤ 0.05` cumulative;
3. exact grounded/collider, area membership, collision-event membership, removal-event count,
   and scenario-coverage results;
4. exact character-state side effects after `setBodyTransform` (D4).

All assertions through item 3 passed. Item 4 produced the required red checkpoint:

```text
assertion `left == right` failed:
D4: setBodyTransform must have identical character-state side effects
  left: (false, None)       # Rust 0.30.0
 right: (true, Some(2))    # web 0.19.3
Exit status 101
```

Raw Rust rejection strings are still measurements only. They are not asserted as JavaScript
error-class parity; Phase 3 owns that adapter-level proof.

### Collection evidence

```text
pnpm exec vitest run packages/physics/__tests__/native-contract.spec.ts
  1 file passed; 2 tests passed

pnpm --filter @threenative/runtime-native native:physics:parity
  web:  1 file passed; 3 tests passed
  Rust: running 1 test; D4 assertion failed; exit 101

pnpm --filter @threenative/runtime-native test
  runtime-native: 16 files passed; 55 passed, 30 skipped
  parity web:     1 file passed; 3 tests passed
  parity Rust:    running 1 test; D4 assertion failed; exit 101
```

The package `test` script now invokes `native:physics:parity`, so the Rust integration test is
collected by the package gate rather than relying on an uncalled script. `pnpm sync:agents`
reported `11 mirrors, 1 written`; the only written mirror was `packages/physics/CLAUDE.md`.

## Phase 2 — observation freshness (2026-08-08)

**Result: freshness PASS; Phase 1's D4 checkpoint remains RED.** `PhysicsSimulation` now states
that character state and area intersections reflect the most recently completed step without a
visible-transform read.

The native adapter owns two independent dirty flags. A successful `createBody`,
`configureCharacter`, `removeBody`, or `step` invalidates both observations. The first subsequent
`readCharacterState` calls only `readCharacterStates`; the first subsequent `areaIntersections`
calls only `readAreaIntersections`. Repeated reads use the cache. `readVisibleTransforms` now does
only its named raw call.

### Required old-behaviour red

The native-adapter test was added before the implementation. After a step and before any visible
read, the old adapter produced:

```text
expected { members: [2], state: { grounded: true, groundCollider: 2 } }
received { members: [], state: undefined }
1 failed, 3 passed; exit 1
```

### Green and independence evidence

```text
pnpm exec vitest run packages/physics/__tests__/parity.spec.ts
  1 file passed; 4 tests passed
```

The native-adapter test asserts five independent refresh cycles across step, configure, create,
and remove. Both raw observation readers are called exactly five times; repeated reads do not add
calls; raw `readVisibleTransforms` is called zero times.

The linked Rust gate now also asserts the Phase 0 raw freshness row exactly: web and Rust both
report `statePresent=true` and `areaCount=2` before a post-step visible read. The run reached the
unchanged final D4 assertion, proving every earlier assertion passed:

```text
pnpm --filter @threenative/runtime-native native:physics:parity
  web:  1 file passed; 4 tests passed
  Rust: raw freshness assertion passed, then D4 failed
  left: (false, None)
 right: (true, Some(2))
  exit 101
```

Phase 2 does not change `setBodyTransform` behavior or validation. D4 and raw rejection-string
differences remain owned by Phase 3.

## Phase 3 — fail-closed symmetry (2026-08-08)

**Result: adapter validation and teleport-state parity PASS.** The web and native TypeScript
adapters now call the same three exported validators from `simulation.ts` before either backend
sees step input, a visible-transform buffer, or a collision-event buffer. The old inline helper
names and validation messages grep to no second implementation.

`requirePhysicsStepInput` rejects a non-positive or non-finite delta, a non-`Float32Array`
transform input, a negative, fractional, unsafe, or oversized record count, every non-finite
scalar in every declared record, an invalid id, and an unknown id. `requirePhysicsRenderBuffer`
enforces both `Float32Array` and capacity against the adapter's tracked body count.
`requirePhysicsEventBuffer` enforces `Uint32Array` before draining.

The native adapter control uses a raw ABI stub whose methods only record calls; it does not
delegate to a web simulation. Each rejected-input row runs against that adapter and a real Rapier
`0.19.3` web adapter, and asserts constructor identity (`Error === Error`), not message similarity.
A valid unknown `removeBody(999)` is accepted by both adapters. The C++ binding still rejects
missing, non-numeric, non-finite, negative, fractional, or non-exact ids, but ignores the Rust
ABI's `false` result for a valid unknown id.

Rust `set_body_transform` now retains the last completed step's `grounded` and
`ground_collider`. It still performs the existing translation and next-kinematic-translation
writes. This selects the web arm measured in Phase 0 as the contract and closes D4 without
changing the subsequent solver step.

### Required pre-fix reds

The adapter rows were added before the shared validators. The native wrapper forwarded every
malformed value to the recording stub, so the focused run failed all 18 rejection rows while the
web arm threw `Error`:

```text
pnpm exec vitest run packages/physics/__tests__/parity.spec.ts
  1 file failed; 18 failed, 5 passed
  expected native Error; received undefined
```

The linked differential's existing D4 checkpoint remained red through Phase 2:

```text
left:  (false, None)       # Rust 0.30.0
right: (true, Some(2))     # web 0.19.3
exit 101
```

### Post-fix gates

```text
pnpm exec vitest run packages/physics/__tests__/parity.spec.ts
  1 file passed; 23 tests passed

pnpm exec vitest run packages/physics/__tests__/native-contract.spec.ts
  1 file passed; 2 tests passed

pnpm --filter @threenative/physics typecheck
  exit 0

cargo fmt --manifest-path packages/runtime-native/native/physics/Cargo.toml -- --check
  exit 0

cargo check --manifest-path packages/runtime-native/native/physics/Cargo.toml
  finished dev profile; exit 0

cargo test --manifest-path packages/runtime-native/native/physics/Cargo.toml
  library: 5 passed; integration parity: 1 passed; doc tests: 0; exit 0

pnpm --filter @threenative/runtime-native native:physics:parity
  web adapter: 23 passed
  linked Rust parity: 1 passed
  teleportGroundedMismatch: 0
  groundedMismatch: 0
  groundColliderMismatch: 0
```

The linked Rust rejection strings remain raw ABI measurements and therefore still report six
string mismatches against JavaScript outcomes. Error-class parity is proven at the TypeScript
adapter boundary, where a game observes it; the differential continues to prove that its two
simulators are distinct Rapier `0.19.3` and `0.30.0` builds. This host result does not prove replay
portability, Android, `arm64`, physical hardware, or performance parity.

## Phase 4 — collision scan and reusable buffers (2026-08-09)

**Result: D5 retained from measurement; D6 fixed; D7 required no change.** A one-time release
benchmark used fixed boxes spaced four metres apart, zero gravity, 200 warmup steps, and 2,000
timed steps. The temporary ignored test called the shipping `tn_physics_step` ABI and was removed
after capture because this is host timing evidence, not a stable performance gate.

```text
cargo test --release --manifest-path packages/runtime-native/native/physics/Cargo.toml \
  --test parity phase_4_non_overlapping_step_benchmark -- --ignored --exact --nocapture

10 bodies:  total 2,725,252 ns / 2,000 = 1,362 ns/step
100 bodies: total 19,622,294 ns / 2,000 = 9,811 ns/step
300 bodies: total 129,475,075 ns / 2,000 = 64,737 ns/step (0.064737 ms)
```

The 300-body result is 0.39% of the 16.67 ms 60 fps frame ceiling. This does not justify adding
an event-handler implementation, so the measured O(n²) scan remains. It is a host release result;
Android and `arm64` per-frame cost remain open.

The eager quadratic allocations did not survive the kill-switch pass. Buffers now grow linearly
for the normal case and geometrically only after a fail-closed capacity response:

| Bodies | Old event + area allocation | New eager event + area allocation | Reduction |
| ---: | ---: | ---: | ---: |
| 7 | `784 + 392 = 1,176 B` | `256 + 128 = 384 B` | 67.3% |
| 300 | `1,440,000 + 720,000 = 2,160,000 B` | `16,384 + 4,096 = 20,480 B` | 99.1% |

The maximum quadratic size is now only a fail-closed retry ceiling, never an eager allocation.
The web adapter retains drained collision events when a buffer is short, matching the Rust ABI's
existing no-clear-on-short-buffer behavior; a retry therefore cannot lose or duplicate events.
The plugin's 20-overlapping-body negative control required multiple strictly increasing buffer
capacities before one exact drain succeeded. The native area control observed capacities
`[64, 128]`; disabling geometric retry makes that test throw instead of truncating.

Phase 0 measured the overlap-removal stopped-event count as `0` on both arms. D7 is therefore not
a parity bug for the proof subject, and no speculative removal event was added. The exact unordered
collision-event set remains asserted by the linked differential.

## Phase 5 — proof-only path deletion (2026-08-09)

**Result: D8 deleted; the native LOC trigger remains open.** Before deletion, the PRD's exact
caller-census command was run:

```sh
grep -rn "createPhysicsProof\|createProofSimulation\|tn_physics_proof" \
  --include='*.ts' --include='*.cpp' --include='*.rs' --include='*.mjs' . \
  | grep -v node_modules | grep -v '\.spec\.' | grep -v '/tests\?/'
```

It found no non-test caller. Every shipping-tree hit was a definition: the two TypeScript proof
factories, `NativePhysicsHost.createProofSimulation`, the C++ factory, and the five Rust proof ABI
functions. The remaining hits were generated declaration copies under `packages/physics/dist/`
and the untracked benchmark snapshot under
`docs/benchmark/sweeps/exploration-2026-08-09/framework-types/`; neither is a caller. The package
build cleaned the `dist/` copies. The benchmark snapshot was left untouched because it belongs to
concurrent benchmark work.

The proof source, contract, native wrapper, and proof-only test are deleted. The `./proof` export
and both tsup entries are gone. Rust no longer exposes `TnPhysicsProofOptions` or any
`tn_physics_proof_*` function; the C header and C++ host no longer declare or install them. With
the proof factory gone, `makeSimulationObject` no longer accepts an always-true `allowBodies`
switch and installs the general body methods directly. The load-bearing
`native:physics:measure` snapshot lane remains unchanged.

The Phase 5-owned native diff removes exactly **207 net native lines**: 142 from Rust, 24 from the
C header, and 41 net from the C++ binding. The budget command was also captured immediately before
and after deletion in a concurrently edited worktree:

```text
before: pnpm budgets — 5,944 framework LOC; 51,778 native LOC (+1,778 trigger)
after:  pnpm budgets — 5,770 framework LOC; 51,563 native LOC (+1,563 trigger)
```

The observed repository delta is -215 native lines; `git diff --numstat` assigns -207 to this
phase, while unrelated concurrent native edits account for the other -8. This truthfully replaces
the PRD's stale under-50,000 premise: deletion paid down the trigger but did **not** clear it.

### Gates

```text
pnpm --filter @threenative/physics typecheck                    PASS
pnpm --filter @threenative/physics build                        PASS — publint all good
pnpm exec vitest run packages/physics/__tests__                 PASS — 12 files, 83 tests
cargo fmt --manifest-path packages/runtime-native/native/physics/Cargo.toml -- --check
                                                               PASS
cargo check --manifest-path packages/runtime-native/native/physics/Cargo.toml
                                                               PASS
cargo test --manifest-path packages/runtime-native/native/physics/Cargo.toml
                                                               PASS — 3 unit, 1 parity, 0 doc
pnpm --filter @threenative/runtime-native native:physics:parity PASS — 24 web, 1 Rust
pnpm --filter @threenative/runtime-native test                  PASS — 55 passed, 30 skipped;
                                                                      linked parity passed
pnpm budgets                                                    PASS — hard invariants;
                                                                      +1,563 review trigger remains
```

The root chained attempt stopped during typecheck in concurrent Phase 6 work:
`examples/native-smoke/src/physics.ts:123` returned `void` from a plugin setup callback and line
171 read `version` from `PhysicsSimulation`. A later standalone `pnpm test` built all workspaces,
passed its first root collection (164 files, 1,248 tests) and the package gates, then was explicitly
interrupted during the duplicate final root collection when the concurrent owner requested that no
root gate continue while Phase 6 was editing. Package-local Phase 5 gates above are green.
