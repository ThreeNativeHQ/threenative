# Night batch 2026-08-26 → 27 — close-out, 2026-08-27

Closes the batch folder `docs/PRDs/night-batch-2026-08-26/` (deleted in the closing commit per
its own acceptance rule: every lane row closed, blocked-with-a-name, or parked by recorded
decision). Lane outcomes below; each closed PRD carries its own dated record.

## Lane outcomes

| Lane | Verdict | Durable record |
| --- | --- | --- |
| Step 0 | Done overnight: the three unmerged linchpin branches (PRD-203, PRD-202, PRD-219) merged to `main` 23:26–23:37, then loading screens re-stamped and doc links retargeted (`51960a4f`…`b50c4b93`) | [`night-batch-2026-08-26-step0-2026-08-27.md`](night-batch-2026-08-26-step0-2026-08-27.md) |
| A — PRD-224 | **PARTIAL, PRD stays live.** Phase 2's `GPURenderPassEncoder` per-class table landed (`47d1adb3`) and is contract-proven; phases 1/3/4 not started. Remaining scope parked to the PRD itself, moved to the PRD root | [`runtime-perf-state.md`](runtime-perf-state.md) |
| B — PRD-219 | **Closed.** The 08-25 DELIVERED verdict survived the step-0 merge: menu flow green + carry-mutation red re-proven on the merged tree, on `emulator-5554` | [`prd-219-menu-flow-merged-tree-2026-08-27.md`](prd-219-menu-flow-merged-tree-2026-08-27.md) |
| C — PRD-225 | **Closed (DELIVERED).** Probe answered: not reproducing at HEAD, 10/10 clean fresh-install cold launches on the emulator lane; the N-launch guard is standing tooling. Physical-Pixel rerun explicitly stays open (see scope limits) | [`prd-225-physics-callback-stability-2026-08-27.md`](prd-225-physics-callback-stability-2026-08-27.md) |
| D — Pixel 8 tails | **Blocked-with-a-name.** `adb connect 192.168.1.192:5555` → `Connection refused`, attempted at probe time ~00:45 and again 2026-08-27 morning. Items parked to the PRD-222 loop log's next actions: physical physics-guard rerun (#1), staging-v3 Tier-1 rerun (#2, already queued), Android loading-gate capture (#4, newly parked); PRD-224 phase 4 rides on the live PRD | [`runtime-perf-state.md`](runtime-perf-state.md), Next actions |

Archive moves in the closing commit: PRD-225 → `docs/PRDs/done/`; PRD-224 → `docs/PRDs/`
(PARTIAL PRDs do not archive); batch folder deleted. No external file linked into the folder
(grep at close time: zero inbound references).

## Gates at close

`pnpm typecheck && pnpm lint && pnpm test && pnpm budgets` on the closing tree, 2026-08-27
~07:30–07:45. **Overnight HEAD was red on three of the four** — all three left by the lanes'
commits, all repaired in the closing commit:

1. **typecheck red** — `loading-screen.spec.ts(398)` `Property 'mock' does not exist on type
   '() => Promise<void>'`, introduced by the re-stamp commit `6f709f06` (the spec assigns a
   `vi.fn` into a slot typed as a plain function, then reads `.mock` off the declared type).
   Repaired by hoisting the mock into a local and asserting on it directly; the assertion's
   meaning is unchanged. Green: `typecheck_exit=0`.
2. **test red** — `native-contract-lane.test.mjs` expected 22 declared contract targets and
   found 23: `47d1adb3` added `threenative-render-pass-class-table-test` without bumping the
   canary, and `executionContracts` had no row for it ("missing execution contracts:
   threenative-render-pass-class-table-test"). Repaired both (count + contract row with the
   executable's real pass line). Green: 551 passed / 34 skipped in runtime-native,
   `test_exit=0` at the root.
3. **budgets red** — native census drift (src/ 44 466 → 44 719 etc.): `47d1adb3` changed
   runtime-native without regenerating the census. `pnpm census` regenerated 5 cells
   (total 102 742 → 103 367). Green: `budgets_exit=0`.

Lint was green at overnight HEAD and stayed green: 1251 files checked, `lint_exit=0`.

## Notes for the next batch

- PRD-224's next lane should start with Phase 1's pricing gate: the predicted ≥2 ms frame win
  from the two landed conversions is still unmeasured, and Phase 1 also decides whether
  widening continues.
- The 5-of-9 physics-crash observation is refuted on the emulator lane only. Do not cite
  PRD-225 as closing the physical question; loop-log next action #1 owns the rerun.
