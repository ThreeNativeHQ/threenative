# G2 — conformance

**Milestone:** M3
**State:** 1 pass / 49 total (`1 pass, 48 planned`) on arrival.

The registry and same-source browser/native harness exist. The only executed row is the
basic upstream Three.js cube on Linux V8+Dawn. Future reports must state the result as a
fraction and must not summarize an empty or dry-run-only report as “the harness passes.”

Acceptance requires browser and native completion, finite visual metrics, clean GPU
validation output and explicit blocked/planned rows. The next run must move the executed
pass count above 1/49.
