[package]
name = "threenative-native-physics"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["staticlib"]

[dependencies]
bincode = "=1.3.3"
rapier3d = { version = "=0.30.0", features = ["serde-serialize"] }

[profile.release]
lto = "thin"
panic = "abort"
strip = "symbols"
