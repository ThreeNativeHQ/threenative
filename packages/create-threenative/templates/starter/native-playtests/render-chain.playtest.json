{
  "name": "native-render-chain",
  "target": "desktop",
  "schemaVersion": 1,
  "viewport": { "width": 1280, "height": 720 },
  "warmupFrames": 2,
  "steps": [{ "label": "ridge-refined", "waitTicks": 600 }],
  "artifacts": { "screenshots": "after" },
  "assert": {
    "components": [
      {
        "entity": "scenery.flora",
        "component": "floraPlants",
        "gte": 2,
        "allowTrivial": "The live flora stand reports its counts from the final attached arrays on native; bare ground carries no such entity."
      },
      {
        "entity": "scenery.flora",
        "component": "leafInstances",
        "gte": 10,
        "allowTrivial": "The live flora stand reports foliage instances from the final attached arrays on native."
      },
      {
        "entity": "scenery.flora",
        "component": "tipDisplacement",
        "gte": 0.01,
        "allowTrivial": "The live flora stand sways its tips under game-owned wind strength on native."
      },
      {
        "entity": "scenery.ridge",
        "component": "state",
        "equals": "refined",
        "allowTrivial": "A fast Worker may refine before the native baseline sample; the final state still proves the attached result is refined."
      },
      {
        "entity": "scenery.ridge",
        "component": "generation",
        "gte": 1,
        "allowTrivial": "A fast Worker may complete before the native baseline sample; generation one still proves the refinement dispatch ran."
      },
      {
        "entity": "scenery.ridge",
        "component": "topology",
        "path": "boundaryEdges",
        "equals": 0,
        "allowTrivial": "The final-array topology report is a held invariant for the attached native ridge."
      },
      {
        "entity": "scenery.ridge",
        "component": "topology",
        "path": "degenerateTriangles",
        "equals": 0,
        "allowTrivial": "The final-array topology report is a held invariant for the attached native ridge."
      },
      {
        "entity": "scenery.ridge",
        "component": "topology",
        "path": "windingConflicts",
        "equals": 0,
        "allowTrivial": "The final-array topology report is a held invariant for the attached native ridge."
      },
      {
        "entity": "scenery.ridge",
        "component": "topology",
        "path": "signedVolume",
        "gte": 1,
        "allowTrivial": "The final-array topology report is a held invariant for the attached native ridge."
      }
    ],
    "renderChain": { "tier": "high" }
  }
}
