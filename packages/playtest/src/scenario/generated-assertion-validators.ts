/* eslint-disable */
// Generated by scripts/generate-assertion-validators.ts from PLAYTEST_ASSERTION_REGISTRY.
// Do not hand-edit. Regenerate with: pnpm tsx scripts/generate-assertion-validators.ts

import { MIN_TRIVIALITY_REASON_LENGTH } from "./schema-base.js";

export type GeneratedAssertionFieldValidator = (value: unknown) => boolean;

function isRecord(value: unknown): value is Record<string, unknown> {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}

export const GENERATED_ASSERTION_FIELD_VALIDATORS: Readonly<Record<string, Readonly<Record<string, GeneratedAssertionFieldValidator>>>> = Object.freeze({
  "deviceMetrics": Object.freeze({
    "maxTemperatureRiseC": (value: unknown) => typeof value === "number" && Number.isFinite(value) && value >= 0,
    "maxThermalStatus": (value: unknown) => typeof value === "number" && Number.isInteger(value) && value >= 0 && value <= 6,
    "notThermallyConfounded": (value: unknown) => value === true,
  }),
  "framebufferCoverage": Object.freeze({
    "backdrop": (value: unknown) => Array.isArray(value),
    "grid": (value: unknown) => isRecord(value),
    "tolerance": (value: unknown) => typeof value === "number" && Number.isInteger(value) && value >= 0 && value <= 255,
    "window": (value: unknown) => isRecord(value),
  }),
  "reachability": Object.freeze({
    "artifact": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "entities": (value: unknown) => Array.isArray(value),
  }),
  "aerodynamics": Object.freeze({
    "entity": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "minForceSamples": (value: unknown) => typeof value === "number" && Number.isInteger(value) && value > 0,
    "controls": (value: unknown) => Array.isArray(value),
    "torques": (value: unknown) => Array.isArray(value),
  }),
  "visual": Object.freeze({
    "frameDiff": (value: unknown) => isRecord(value),
    "region": (value: unknown) => isRecord(value),
    "entityVisible": (value: unknown) => isRecord(value),
  }),
  "movement": Object.freeze({
    "entity": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "closesDistanceToPosition": (value: unknown) => isRecord(value),
    "facesMovementWithinDegrees": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "axis": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "minAxisDelta": (value: unknown) => isRecord(value),
    "minResolvedAxisDelta": (value: unknown) => isRecord(value),
    "maxTiltDegrees": (value: unknown) => typeof value === "number" && Number.isFinite(value) && value >= 0 && value <= 180,
    "minDistance": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "maxDistance": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "minVelocity": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "pathLength": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "notFacing": (value: unknown) => isRecord(value),
    "notFacingPosition": (value: unknown) => isRecord(value),
    "reachesPositionWithin": (value: unknown) => isRecord(value),
    "rotationChanged": (value: unknown) => typeof value === "boolean",
  }),
  "camera": Object.freeze({
    "entity": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "follows": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "within": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "targetInViewport": (value: unknown) => typeof value === "boolean",
  }),
  "components": Object.freeze({
    "entity": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "component": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "path": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "equals": (value: unknown) => true,
    "gte": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "lte": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "changed": (value: unknown) => typeof value === "boolean",
    "atSteps": (value: unknown) => Array.isArray(value),
    "allowTrivial": (value: unknown) => typeof value === "string" && value.replace(/\s/gu, "").length >= MIN_TRIVIALITY_REASON_LENGTH,
  }),
  "resources": Object.freeze({
    "id": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "path": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "equals": (value: unknown) => true,
    "gte": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "lte": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "textIncludes": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "changed": (value: unknown) => typeof value === "boolean",
    "throughoutSteps": (value: unknown) => typeof value === "boolean",
    "atSteps": (value: unknown) => Array.isArray(value),
    "allowTrivial": (value: unknown) => typeof value === "string" && value.replace(/\s/gu, "").length >= MIN_TRIVIALITY_REASON_LENGTH,
    "anyOf": (value: unknown) => Array.isArray(value),
  }),
  "tags": Object.freeze({
    "tag": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "count": (value: unknown) => typeof value === "number" && Number.isInteger(value) && value >= 0,
    "gte": (value: unknown) => typeof value === "number" && Number.isInteger(value) && value >= 0,
    "lte": (value: unknown) => typeof value === "number" && Number.isInteger(value) && value >= 0,
    "allowTrivial": (value: unknown) => typeof value === "string" && value.replace(/\s/gu, "").length >= MIN_TRIVIALITY_REASON_LENGTH,
  }),
  "signals": Object.freeze({
    "atStep": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "entity": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "maxCount": (value: unknown) => typeof value === "number" && Number.isInteger(value) && value >= 0,
    "minCount": (value: unknown) => typeof value === "number" && Number.isInteger(value) && value >= 0,
    "name": (value: unknown) => typeof value === "string" && value.trim() !== "",
  }),
  "states": Object.freeze({
    "entity": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "equals": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "allowTrivial": (value: unknown) => typeof value === "string" && value.replace(/\s/gu, "").length >= MIN_TRIVIALITY_REASON_LENGTH,
  }),
  "hud": Object.freeze({
    "id": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "path": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "equals": (value: unknown) => true,
    "gte": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "lte": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "textIncludes": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "changed": (value: unknown) => typeof value === "boolean",
    "allowTrivial": (value: unknown) => typeof value === "string" && value.replace(/\s/gu, "").length >= MIN_TRIVIALITY_REASON_LENGTH,
    "visible": (value: unknown) => typeof value === "boolean",
  }),
  "overlayNodes": Object.freeze({
    "overlayId": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "selector": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "attribute": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "equals": (value: unknown) => true,
    "textIncludes": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "visible": (value: unknown) => typeof value === "boolean",
  }),
  "diagnostics": Object.freeze({
    "noConsoleErrors": (value: unknown) => typeof value === "boolean",
    "noNetworkErrors": (value: unknown) => typeof value === "boolean",
    "noRuntimeDiagnostics": (value: unknown) => typeof value === "boolean",
    "consoleErrorsOptOutReason": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "networkErrorsOptOutReason": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "runtimeDiagnosticsOptOutReason": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "runtimeReady": (value: unknown) => typeof value === "boolean",
  }),
  "performance": Object.freeze({
    "maxFrameMsP95": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "minFps": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "maxPhaseMsP95": (value: unknown) => isRecord(value),
    "maxDrawCalls": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "maxTriangles": (value: unknown) => typeof value === "number" && Number.isFinite(value),
  }),
  "parity": Object.freeze({
    "minFpsRatio": (value: unknown) => typeof value === "number" && Number.isFinite(value) && value > 0,
    "minRenderParity": (value: unknown) => typeof value === "number" && Number.isFinite(value) && value > 0,
    "referenceReport": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "referenceSide": (value: unknown) => value === "browser" || value === "native",
    "reference": (value: unknown) => isRecord(value),
  }),
  "visibility": Object.freeze({
    "entity": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "minProjectedPixels": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "maxOffscreenRatio": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "present": (value: unknown) => typeof value === "boolean",
    "allowTrivial": (value: unknown) => typeof value === "string" && value.replace(/\s/gu, "").length >= MIN_TRIVIALITY_REASON_LENGTH,
  }),
  "world": Object.freeze({
    "seed": (value: unknown) => true,
    "runtime": (value: unknown) => isRecord(value),
  }),
  "contacts": Object.freeze({
    "atStep": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "entity": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "with": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "kind": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "minCount": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "maxCount": (value: unknown) => typeof value === "number" && Number.isInteger(value) && value >= 0,
    "requiredOn": (value: unknown) => Array.isArray(value),
  }),
  "settled": Object.freeze({
    "entity": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "atStep": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "minBodies": (value: unknown) => typeof value === "number" && Number.isInteger(value) && value > 0,
    "compareToStep": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "minMeanPoseDistance": (value: unknown) => typeof value === "number" && Number.isFinite(value) && value > 0,
    "requiredOn": (value: unknown) => Array.isArray(value),
    "allowTrivial": (value: unknown) => typeof value === "string" && value.replace(/\s/gu, "").length >= MIN_TRIVIALITY_REASON_LENGTH,
  }),
  "occluded": Object.freeze({
    "entity": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "target": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "allowTrivial": (value: unknown) => typeof value === "string" && value.replace(/\s/gu, "").length >= MIN_TRIVIALITY_REASON_LENGTH,
  }),
  "animation": Object.freeze({
    "entity": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "clip": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "entered": (value: unknown) => typeof value === "boolean",
    "advancedFrames": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "finished": (value: unknown) => typeof value === "boolean",
    "maxFootSlide": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "strideSynced": (value: unknown) => typeof value === "boolean",
    "allowTrivial": (value: unknown) => typeof value === "string" && value.replace(/\s/gu, "").length >= MIN_TRIVIALITY_REASON_LENGTH,
  }),
  "scene": Object.freeze({
    "cameraClearsScene": (value: unknown) => typeof value === "boolean",
    "fogClearsScene": (value: unknown) => typeof value === "boolean",
    "litMaterialsAreLit": (value: unknown) => typeof value === "boolean",
    "minVisibleLights": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "allowTrivial": (value: unknown) => typeof value === "string" && value.replace(/\s/gu, "").length >= MIN_TRIVIALITY_REASON_LENGTH,
  }),
  "sceneNodes": Object.freeze({
    "select": (value: unknown) => isRecord(value),
    "visible": (value: unknown) => typeof value === "boolean",
    "inFrustum": (value: unknown) => typeof value === "boolean",
    "texturesLoaded": (value: unknown) => typeof value === "boolean",
    "animated": (value: unknown) => typeof value === "boolean",
    "minCount": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "maxCount": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "minTriangles": (value: unknown) => typeof value === "number" && Number.isFinite(value),
  }),
  "causedBy": Object.freeze({
    "cause": (value: unknown) => isRecord(value),
    "effect": (value: unknown) => isRecord(value),
    "neverBefore": (value: unknown) => typeof value === "boolean",
    "withinTicks": (value: unknown) => typeof value === "number" && Number.isFinite(value),
  }),
  "startup": Object.freeze({
    "maxEnteredMs": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "maxCompileSettledMs": (value: unknown) => typeof value === "number" && Number.isFinite(value),
    "maxReadyMs": (value: unknown) => typeof value === "number" && Number.isFinite(value),
  }),
  "renderChain": Object.freeze({
    "tier": (value: unknown) => typeof value === "string" && value.trim() !== "",
    "stages": (value: unknown) => isRecord(value),
    "contributions": (value: unknown) => isRecord(value),
    "velocity": (value: unknown) => isRecord(value),
  }),
});
