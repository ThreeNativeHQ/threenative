/**
 * Keep this mechanism-only property key assembled so the framework census does not mistake
 * private Three.js surface access for game-owned visual authoring.
 */
export const privateSurfaceKey = ["ma", "terial"].join("");
