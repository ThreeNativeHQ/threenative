node_modules/
.pnpm-store/
.npmrc
dist/
dist-native/
.threenative/
coverage/
playwright-report/
test-results/
*.log
.DS_Store
artifacts
.playwright-mcp
# Godot's per-project import cache, regenerated by the opt-in engine load test export.
benchmark/godot-load-test/.godot/
hosting/.data/
docs/benchmark/sweeps/*/vendor/
# Third-party models a sweep was handed as source material. The FPS kit's first-person
# viewmodel records "user-provided; verify upstream pack terms before redistribution" and its
# enemy is CC-BY-4.0 carrying retargeted Mixamo clips, so neither is ours to redistribute from
# an MIT repository. The archive keeps them on disk; git does not carry them.
# Builder session transcripts. They are the archived evidence a cold run actually happened and
# they stay on disk in the sweep archive, but they are ~8 MB of gzipped JSONL each and carry
# absolute paths from the machine that ran them. Commit one deliberately if a result is
# disputed; do not carry every run's transcript in history by default.
docs/benchmark/sweeps/*/transcript.jsonl.gz
docs/benchmark/sweeps/*/assets/models/
docs/benchmark/sweeps/*/assets/imported/
docs/benchmark/sweeps/*/assets/generated/
.linchpin/
.gauntlet/

# Shared editor settings are committed — they keep VS Code's watcher and TypeScript server out
# of the 6+ GB of build output under packages/runtime-native. Everything else VS Code drops in
# here is per-machine and is not.
.vscode/*
!.vscode/settings.json

# Other agents' lanes. Never searched, never read; see AGENTS.md.
.worktrees

# Secrets. Never committed, never copied into an image.
.env
.env.*
!.env.example

# written by installing @threenative/core (workspace link writes it at the repo root too)
.mcp.json
