node_modules/
.pnpm-store/
.npmrc
dist/
dist-native/
.threenative/
coverage/
playwright-report/
test-results/
*.log
.DS_Store
artifacts
# Scratch CMake configure/build trees and negative-control sources the native lanes drop at the
# repository root while proving a gate reds. Anchored so a package's own `build/` still answers to
# the ignore rules in its own tree.
/build/
# Android NDK simpleperf downloads device binaries here while symbolizing a profile.
binary_cache/
.playwright-mcp
# Godot's per-project import cache, regenerated by the opt-in engine load test export.
benchmark/godot-load-test/.godot/
hosting/.data/
# A sweep archive is half evidence and half build output, and only the second half leaves git
# (PRD-323 Phase 4). The tree held 5,292 tracked files, of which 2,963 were generated arm `.ts`
# game sources — crossed by every repo-wide grep an agent runs, the same hazard the root
# `AGENTS.md` names for `.worktrees/` at five times the file count.
#
# **What stays is decided by what the suite reads, and the only instrument that answers that is a
# clean-tree run.** Two earlier attempts were wrong because they were checked on a machine that
# still had the files on disk. Ignoring the whole tree failed three suites in CI. Ignoring these
# four directories everywhere still failed two, because `sweep-delta.spec.ts` and
# `sweep-ledger.spec.ts` do not read the recorded measurement — they **recompute** it with
# `measureSandbox` and diff it against the committed record, which is the regression ratchet
# working as designed. `measure-sandbox.ts:306` needs the archive's `src/`, `:216` its frozen
# `starter-baseline/src/`, and `:102` the `framework-types/` declarations. `git ls-files` showing
# a tracked `proof.json` proves nothing about any of that.
#
# So the three measurable directories are **kept for the 13 archives a `docs/verification/sweep-*.md`
# ledger names** — 456 files, 374 `.ts`, 2.6 MB — and ignored for the other 94. Tracked `.ts` under
# `docs/` lands near 376 rather than 2,965: an 87% cut that keeps the ratchet honest. Making the
# specs compare a recorded manifest instead of re-measuring would reclaim the rest and weaken the
# ratchet, which is a worse trade.
#
# The verification is `git archive <sha> | tar -x` into an empty directory, symlink `node_modules`,
# and run `sweep-delta`, `sweep-ledger` and `capture` there. Nothing else counts.
#
# Every archive keeps its measurements either way: `sweep.json`, `proof.json`, `proof-artifacts/`,
# `playtests/`, `captures/`, `screenshots/`, `brief.md`, `reference.png`. The whole archive stays on
# disk, and every sweep command still resolves this tree through the filesystem, never through git.
#
# `**` rather than a bare directory, because git cannot re-include a path whose parent directory is
# excluded — the negations below would silently do nothing against `.../src/`.
docs/benchmark/sweeps/*/src/**
docs/benchmark/sweeps/*/starter-baseline/**
docs/benchmark/sweeps/*/framework-types/**
!docs/benchmark/sweeps/endless-runner-2026-08-05/src/**
!docs/benchmark/sweeps/endless-runner-2026-08-05/starter-baseline/**
!docs/benchmark/sweeps/endless-runner-2026-08-05/framework-types/**
!docs/benchmark/sweeps/endless-runner-2026-08-08-11/src/**
!docs/benchmark/sweeps/endless-runner-2026-08-08-11/starter-baseline/**
!docs/benchmark/sweeps/endless-runner-2026-08-08-11/framework-types/**
!docs/benchmark/sweeps/endless-runner-2026-08-08-6/src/**
!docs/benchmark/sweeps/endless-runner-2026-08-08-6/starter-baseline/**
!docs/benchmark/sweeps/endless-runner-2026-08-08-6/framework-types/**
!docs/benchmark/sweeps/exploration-2026-08-05/src/**
!docs/benchmark/sweeps/exploration-2026-08-05/starter-baseline/**
!docs/benchmark/sweeps/exploration-2026-08-05/framework-types/**
!docs/benchmark/sweeps/fps-2026-08-17/src/**
!docs/benchmark/sweeps/fps-2026-08-17/starter-baseline/**
!docs/benchmark/sweeps/fps-2026-08-17/framework-types/**
!docs/benchmark/sweeps/fps-2026-08-18/src/**
!docs/benchmark/sweeps/fps-2026-08-18/starter-baseline/**
!docs/benchmark/sweeps/fps-2026-08-18/framework-types/**
!docs/benchmark/sweeps/platformer-2026-08-05/src/**
!docs/benchmark/sweeps/platformer-2026-08-05/starter-baseline/**
!docs/benchmark/sweeps/platformer-2026-08-05/framework-types/**
!docs/benchmark/sweeps/platformer-2026-08-05-2/src/**
!docs/benchmark/sweeps/platformer-2026-08-05-2/starter-baseline/**
!docs/benchmark/sweeps/platformer-2026-08-05-2/framework-types/**
!docs/benchmark/sweeps/platformer-2026-08-06/src/**
!docs/benchmark/sweeps/platformer-2026-08-06/starter-baseline/**
!docs/benchmark/sweeps/platformer-2026-08-06/framework-types/**
!docs/benchmark/sweeps/platformer-2026-08-16/src/**
!docs/benchmark/sweeps/platformer-2026-08-16/starter-baseline/**
!docs/benchmark/sweeps/platformer-2026-08-16/framework-types/**
!docs/benchmark/sweeps/platformer-2026-08-16-2/src/**
!docs/benchmark/sweeps/platformer-2026-08-16-2/starter-baseline/**
!docs/benchmark/sweeps/platformer-2026-08-16-2/framework-types/**
!docs/benchmark/sweeps/topdown-action-2026-08-05/src/**
!docs/benchmark/sweeps/topdown-action-2026-08-05/starter-baseline/**
!docs/benchmark/sweeps/topdown-action-2026-08-05/framework-types/**
!docs/benchmark/sweeps/topdown-action-2026-08-05-3/src/**
!docs/benchmark/sweeps/topdown-action-2026-08-05-3/starter-baseline/**
!docs/benchmark/sweeps/topdown-action-2026-08-05-3/framework-types/**
docs/benchmark/sweeps/*/public/
docs/benchmark/sweeps/*/vite.config.ts
docs/benchmark/sweeps/*/tailwind.config.ts
docs/benchmark/sweeps/*/threenative.config.ts
docs/benchmark/sweeps/*/package.json
docs/benchmark/sweeps/*/tsconfig.json
docs/benchmark/sweeps/*/index.html
docs/benchmark/sweeps/*/pnpm-lock.yaml
# Third-party models a sweep was handed as source material. The FPS kit's first-person viewmodel
# records "user-provided; verify upstream pack terms before redistribution" and its enemy is
# CC-BY-4.0 carrying retargeted Mixamo clips, so neither is ours to redistribute from an MIT
# repository. The archive keeps them on disk; git does not carry them.
# Generated sweep instruction files are not benchmark evidence; their proof records remain tracked.
docs/benchmark/sweeps/*/AGENTS.md
docs/benchmark/sweeps/*/CLAUDE.md
docs/benchmark/sweeps/*/vendor/
docs/benchmark/sweeps/*/assets/models/
docs/benchmark/sweeps/*/assets/imported/
docs/benchmark/sweeps/*/assets/generated/
# Builder session transcripts: the archived evidence a cold run happened, but ~8 MB of gzipped
# JSONL each carrying absolute paths from the machine that ran them. Commit one deliberately with
# `git add -f` if a result is disputed; do not carry every run's in history by default.
docs/benchmark/sweeps/*/transcript.jsonl.gz
.linchpin/
.gauntlet/

# Shared editor settings are committed — they keep VS Code's watcher and TypeScript server out
# of the 6+ GB of build output under packages/runtime-native. Everything else VS Code drops in
# here is per-machine and is not.
.vscode/*
!.vscode/settings.json

# Other agents' lanes. Never searched, never read; see AGENTS.md.
.worktrees
# The other half of the same rule (PRD-357 F1). The global worktree convention puts Claude Code's
# lanes in `<repo-root>/.claude/worktrees/`, and until 2026-09-04 the only thing keeping them out
# of a search was line 12 of `.git/info/exclude` — per-clone, untracked, hand-written on one
# machine. Every other clone's ripgrep crossed 629,652 files and stopped being the fast path.
# No trailing slash, matching `.worktrees` above, so `git check-ignore .claude/worktrees` resolves
# from this file even when the directory does not exist yet.
.claude/worktrees

# Secrets. Never committed, never copied into an image.
.env
.env.*
!.env.example

# written by installing @threenative/core (workspace link writes it at the repo root too)
.mcp.json
# ...except the template copies, which are shipped source. The pattern above is unanchored, so it
# swallowed the config of every template added after it: the file generated fine, `git status` said
# nothing, and the scaffold shipped without it. That surfaced as four scaffold suites failing on
#   Error: Scaffold produced no .mcp.json at '.../puzzle/.mcp.json'
# for templates whose config was present on the author's disk the whole time.
!packages/create-threenative/templates/*/.mcp.json
/.codex/

# generated from packages/engine-mcp/dist during the core package build
packages/assets/dist/blender-gpl/

# Sandbox mini-game captures, left at the root for inspection. Not part of the repo.
screenshot-*.png

# `pnpm pack` output. Several gates pack the workspace into `packages/*.tgz` to install a
# scaffold the way a stranger's machine would; three of them were committed by accident in #37.
*.tgz

# The site's Playwright output. The prerendered artifact under site/dist is already ignored.
site/test-results/

# Python bytecode caches. `packages/blender-mcp/gpl/recipes` is executed by Blender, which
# writes a `__pycache__` beside the recipes on every run; one .pyc was committed by accident.
__pycache__/
*.pyc

.afk/
